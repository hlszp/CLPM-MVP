# CLPM 功能设计说明（FDS）总册

日期：2026-06-16
版本：v1

## 1. 文档目的

本文件定义正式产品首版的功能边界、模块职责、关键状态、核心交互与跨模块关系，是前后端开发、测试和交付设计的统一功能说明。

## 2. 模块一览

| 模块 | 目标 | 关键对象 |
|---|---|---|
| 样本验证 | 证明样本可信且可用于后续 KPI/诊断/闭环 | `SampleBatch` |
| 回路台账 | 固化回路身份、点位映射和统计口径 | `LoopLedger` |
| 绩效评估 | 输出可追溯的 KPI 和低效排序 | `KpiResult` |
| 诊断中心 | 解释低性能原因并形成证据链 | `DiagnosisFinding`、`EvidenceWindow` |
| 闭环治理 | 形成审核、实施、复评、回退链路 | `ReviewRecord`、`ImplementationRecord`、`Reevaluation` |
| 证据报告 | 汇总为可汇报、可审计的证据包与导出物 | `EvidencePackage` |
| 可信整定 | 提供单回路整定建议、风险和回退说明 | `TuningCase` |
| 系统管理 | 管理数据源、质量规则、安全边界、角色权限 | `DataSourceConfig`、`QualityRuleSet` |

## 3. 跨模块主链

```text
SampleBatch -> LoopLedger -> KpiResult -> DiagnosisFinding -> Review / Implementation / Reevaluation -> EvidencePackage
```

## 4. 模块功能摘要

### 4.1 样本验证

| 页面/功能 | 说明 |
|---|---|
| 样本批次 | 展示样本范围、来源、时间窗、状态、风险 |
| 数据导入 | 支持 historian 导出、CSV、模拟数据；不直接接 DCS 写口 |
| 就绪校验 | 展示映射率、好值率、事件可用性、质量规则、partial 原因 |
| 样本仪表 | 汇总样本规模、场景分布、P0 主链数据概况 |
| 数据雷达 | 用可评估/可诊断/可整定/需现场核实/数据不足/不可判定六类状态表达可信度 |
| 样本冻结 | 冻结版本、manifest hash、冻结后不可漂移说明 |

### 4.2 回路台账

| 页面/功能 | 说明 |
|---|---|
| 回路清单 | 展示回路、装置、类型、状态、风险、评分、下一步动作 |
| 点位映射 | 展示 PV/SP/OP/MODE/quality 映射矩阵与缺失项 |
| 台账校核 | 展示人工修正记录、冻结前阻断项、现场核实项 |
| 排除管理 | 展示排除原因、有效窗口、审批状态、影响口径 |
| 版本管理 | 展示 ledger、mapping、formula、threshold、quality rule、mode mapping、rule version |

### 4.3 绩效评估

| 页面/功能 | 说明 |
|---|---|
| 指标总览 | 展示 P0 KPI 和基础口径来源 |
| 低效排行 | 仅对可评估/可诊断/可整定/需现场核实对象排序；数据不足/不可判定不按真实 0 分处理 |
| 指标溯源 | 展示公式、阈值、输入数据和版本追溯 |

### 4.4 诊断中心

| 页面/功能 | 说明 |
|---|---|
| 诊断清单 | 至少覆盖 PID 疑似、阀门/仪表疑似、数据/工况问题三类诊断 |
| 单回路证据链 | 根据 `loopId` 展示不同趋势、规则命中、事件线、建议动作和可信度 |

### 4.5 闭环治理

| 页面/功能 | 说明 |
|---|---|
| 建议审核 | 支持通过 / 驳回 / 需补证据三类状态表达 |
| 多方审核 | 展示工艺、仪表、安全意见及其冲突 |
| 实施记录 | 强调系统不执行参数写入，只记录授权人员人工实施 |
| 风险回退 | 展示原参数、回退条件、观察要求 |
| 观察复评 | 展示前后 KPI、报警、操作频次等对比；未满足条件时保持 partial |

### 4.6 证据报告

| 页面/功能 | 说明 |
|---|---|
| 证据包 | 展示 manifest、版本引用、included refs、status、risk_summary、结论和溯源 |
| 样本报告 | 展示样本范围、场景分布、可信度、不能证明事项、安全边界 |
| 导出中心 | 展示 PDF/JSON/Excel 导出结构与 partial 边界 |

### 4.7 可信整定

| 页面/功能 | 说明 |
|---|---|
| 整定样例 | 仅一条 P0 样例；含 current / suggested / fallback / confidence / risk / boundary |

## 5. 共通状态规则

| 状态 | 说明 |
|---|---|
| success | 数据完整、可继续主链操作 |
| partial | 证据不全、仅桌面评审或模拟版可用 |
| unavailable | 输入不足，不能计算 |
| inconclusive | 输入存在但不足以形成可信结论 |
| error | 系统或配置错误 |

## 6. 正式开发对接要求

| 方向 | 要求 |
|---|---|
| 前端 | 以模块为单位实现，不允许把主链全堆在单一超长文件 |
| 后端 | 每个主链模块有独立接口与状态模型 |
| 测试 | 现有 smoke 路径保留为正式 E2E 验收基线 |
| 导出 | 任何导出都必须受 `package_status` 与 `validity_status` 约束 |
