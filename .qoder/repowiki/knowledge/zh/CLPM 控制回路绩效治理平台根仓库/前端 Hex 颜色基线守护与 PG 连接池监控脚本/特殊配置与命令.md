`check-hex-whitelist.mjs`：首次运行需先 `node scripts/check-hex-whitelist.mjs --update-baseline` 生成 `hex-baseline.json`，之后 CI 中直接运行检查；`monitor_db_connections.py`：默认通过 `uv run python scripts/monitor_db_connections.py` 启动，支持 `--interval`、`--duration`、`--api-base http://localhost:7101`、`--dsn postgresql://...` 切换直连 PG 诊断模式。