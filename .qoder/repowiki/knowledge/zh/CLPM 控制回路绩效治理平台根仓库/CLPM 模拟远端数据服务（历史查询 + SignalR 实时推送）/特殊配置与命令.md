运行方式：`cd mock_data_server && python -m uvicorn main:app --host 0.0.0.0 --port 8100 --reload` 或直接 `python main.py`。需设置环境变量 `TDENGINE_HOST/PORT/USER/PASSWORD/DB`、`MOCK_DATA_SERVER_PORT`、`REALTIME_INTERVAL`、`QUALITY_BAD_RATIO` 控制仿真行为。