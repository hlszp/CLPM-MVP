根级 `make help` 列出全部可用目标；`make dev-all` 一键拉起 `deploy/docker/docker-compose.dev.yml` 基础设施并启动后端与前端；生产部署统一走 `./deploy/deploy.sh`（自动执行 alembic upgrade head、备份、镜像构建与健康检查）；`make migrate` / `make seed` 封装数据库迁移与种子数据加载；`make build-docker` 基于 `docker-compose.prod.yml` 构建 backend/frontend 镜像。