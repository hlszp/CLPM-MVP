运行前需确保后端 API 已在 localhost:7101 启动；`pnpm test` 会由 Playwright 自动 `cd ../frontend/apps/web-antd && pnpm dev` 拉起前端。视觉基线采集命令：`node e2e/scripts/capture-visual-baseline.mjs` 写入 baseline，`--out current` 写入 current 目录用于对比；支持环境变量 `CLPM_WEB_BASE`、`CLPM_API_BASE`、`CLPM_E2E_USER`、`CLPM_E2E_PASS` 覆盖目标地址与账号，`--dark` 切换暗色模式采集。