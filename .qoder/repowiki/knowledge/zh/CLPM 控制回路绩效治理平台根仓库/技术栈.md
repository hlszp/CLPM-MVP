Docker + Docker Compose（含 profiles 隔离 tdengine/monitoring）作为跨子模块的统一编排层；Makefile 作为跨语言（Python uv / Node pnpm）的统一命令入口；PostgreSQL 16 + TDengine 3.3 + Redis 7 作为共享数据总线；Celery + Celery Beat 作为后端异步任务与定时调度统一框架。