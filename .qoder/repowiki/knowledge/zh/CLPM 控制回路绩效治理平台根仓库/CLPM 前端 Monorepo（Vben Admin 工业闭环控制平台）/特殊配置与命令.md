根目录使用 pnpm 安装后自动执行 postinstall 生成 stub 与 lefthook 钩子；`pnpm dev` 通过 turbo-run 启动所有开发服务；`pnpm build` 通过 Turbo 并行构建全部产物；`pnpm check` 串联 circular、dep、type、cspell 检查；`pnpm test:unit` / `test:e2e` 分别运行 Vitest 与 Playwright。