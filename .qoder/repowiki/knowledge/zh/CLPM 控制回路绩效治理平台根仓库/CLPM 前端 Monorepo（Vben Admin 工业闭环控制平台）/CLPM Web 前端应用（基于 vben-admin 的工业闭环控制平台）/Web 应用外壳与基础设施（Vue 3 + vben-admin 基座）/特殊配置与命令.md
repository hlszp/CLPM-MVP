应用启动依赖环境变量：`VITE_APP_NAMESPACE`、`VITE_APP_VERSION`、`VITE_APP_TITLE`、`VITE_ROUTER_HISTORY`（hash 或 web）、`VITE_BASE`、`VITE_SENTRY_DSN`（可选）。主题模式在 `main.ts` 中通过 localStorage 键 `${namespace}-preferences-theme` 持久化，修改 `preferences.ts` 后需清空浏览器缓存方可生效。