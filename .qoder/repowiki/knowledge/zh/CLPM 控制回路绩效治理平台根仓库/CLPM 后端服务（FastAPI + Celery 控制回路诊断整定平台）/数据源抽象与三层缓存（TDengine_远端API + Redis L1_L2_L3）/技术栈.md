TDengine（taosrest 宽表查询 + 连接池）、PostgreSQL（SQLAlchemy ORM，UUID/JSON/CheckConstraint/Partial Unique Index）、Redis（异步客户端，Pipeline/SCAN/SETEX）、httpx（带超时与 Bearer Token 的 AsyncClient）、zstandard（level=3 压缩，线程局部单例）