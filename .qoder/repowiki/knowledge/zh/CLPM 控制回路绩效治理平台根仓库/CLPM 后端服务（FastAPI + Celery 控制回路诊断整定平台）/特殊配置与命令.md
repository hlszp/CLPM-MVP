非生产模式下 `uvicorn app.main:create_app` 会自动 fork 子进程启动 Celery Beat 与 Worker（含看门狗自愈），日志输出至 `logs/celery-beat.log`、`logs/celery-worker.log`；生产环境通过 docker-compose 独立 celery-beat/celery-worker 容器运行，跳过内嵌启动。数据库迁移通过 `alembic upgrade head` 执行，`scripts/` 下提供数据导入、回算、仿真、基准测试等运维脚本。