- 设计文档按 00-BASELINE → 01-PRD → 02-FDS → 03-ADS → 04-DDS → 05-IDS → 06-UIUX → 07-DEPLOYMENT → 08-TECHSTACK → 09-DEVPLAN → 10-API 的固定编号顺序组织，体现从基线到实现的层级关系。
- 实现契约类文档（如 implementation-contract.md）采用“版本修订摘要 + 正文章节”结构，每个版本以 vN.N 修订摘要记录变更范围、新增 API、状态机调整与路由迁移。
- IA 与路由变更通过“新增路由表 + legacy redirect 表”成对登记，确保旧书签/E2E 兼容不被直接删除。
- 架构图统一使用 Mermaid `.mmd` 文件命名 `clpm-*.mmd`，并在文件名中体现主题（full-picture、roles-views、version-roadmap 等）。
- 原型与线框图以 HTML 形式与 Markdown 设计文档并列存放，便于在浏览器中直接打开验证布局与交互。
- 过程文档以日期或 Phase 编号前缀命名（如 `*-2026-07-28.md`、`phase3-*`），并按主题分目录（superpowers/plans、superpowers/specs）管理 AI 辅助生成的规划与规格。