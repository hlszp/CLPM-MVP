Vue 3 `<script setup>` + TypeScript；UI 基于 ant-design-vue（Table/Tree/Drawer/Modal/Segmented/RangePicker 等）与 Vben 框架的 `@vben/common-ui` Page 组件；图表使用 ECharts（workbench-radar6、score-trend-chart、workbench-process-trend）；时间处理使用 dayjs；WebSocket 实时推送通过 `useLoopRealtime` 封装并降级为 30s 轮询。