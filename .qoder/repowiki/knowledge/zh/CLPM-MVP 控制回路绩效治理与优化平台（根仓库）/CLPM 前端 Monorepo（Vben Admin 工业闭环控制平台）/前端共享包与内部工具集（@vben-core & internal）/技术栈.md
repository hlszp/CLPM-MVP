Vue 3 + TypeScript；状态管理使用 `@tanstack/vue-store`；Vite 构建通过自定义 `vite-config` 插件体系（inject-app-loading、importmap、nitro-mock、tailwind-reference、vxe-table 等）；代码质量采用 eslint + oxlint + stylelint + commitlint + oxfmt 多工具统一配置；偏好设置通过 `StorageManager` 持久化到 localStorage。