生产环境启动时 `config.Settings.validate_security()` 会强制校验 JWT_SECRET_KEY ≥32 位、POSTGRES_PASSWORD/TDENGINE_PASSWORD/REDIS_PASSWORD 非空且不为开发默认值、AAS_SECURITY_MODE 不得为 None；TDengine 通过端口 +11 的 REST 端口访问；Tailscale 网络模式切换需宿主机 sudoers 免密权限（deploy/sudoers.d/clpm-tailscale），容器内静默跳过。