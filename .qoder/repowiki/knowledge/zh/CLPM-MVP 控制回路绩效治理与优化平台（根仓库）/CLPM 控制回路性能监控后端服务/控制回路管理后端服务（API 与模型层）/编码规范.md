- 每个业务域以独立文件定义 FastAPI APIRouter，并在 main.py 中集中 include_router 完成路由装配
- 数据访问统一基于 SQLAlchemy 2.0 声明式模型，CRUD 端点通过 session 事务封装增删改查
- 可配置的阈值、权重、映射等维度以独立的 endpoint 文件按领域拆分，保持单一职责
- 计算逻辑以无状态函数或类实现并注册到统一注册表，端点仅负责参数校验与结果序列化