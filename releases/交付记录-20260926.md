# 本版本交付记录（2026-09-26）

| 项 | 值 |
|---|---|
| 交付版本 | `20260926-145041` |
| 构建 commit | **`d8c0014a`**（宽表退役 + 诊断修复 + 缺口语义 + 位号级质量体检） |
| 文档归档 commit | **`c2ce7789`**（本次交付说明与 manifest 入库） |
| GitHub Release | https://github.com/hlszp/CLPM-MVP/releases/tag/v20260926-d8c0014a |
| 目标环境 | Windows VM + Docker Desktop（Linux 容器）+ `docker-compose.prod.yml` |
| 镜像平台 | **linux/amd64**（本机 arm64 交叉构建，可直接用于 x86_64 虚拟机） |

## 一、Release 附件清单（7 个，均可免登录下载）

| 附件 | 大小 | sha256 | 用途 |
|---|---|---|---|
| `clpm-delivery-20260926-145041.tar.gz` | 1020.0 MB | `3a4c542d909a11f1093f44af5ecfa3fcd1a90453398d1175071e0b7becc5aa79` | **生产用这个**（离线全镜像：自有 + postgres/redis/tdengine + 监控） |
| `clpm-delivery-20260926-145041.tar.gz.sha256` | <1 KB | — | 交付包校验值 |
| `clpm-mvp-d8c0014a-20260926-141019.tar.gz` | 67.5 MB | 见下 | 源码包（备选，需目标机 build、需联网） |
| `clpm-mvp-d8c0014a-20260926-141019.zip` | 69.7 MB | 见下 | 同上（zip 形态） |
| `clpm-mvp-d8c0014a-20260926-141019.sha256` | <1 KB | — | 源码包校验值 |
| `部署移交说明-20260926.md` | 5 KB | — | 部署团队执行依据 |
| `manifest.json` | 1.4 KB | — | 包清单（已校准为标准 JSON） |

源码包校验值（取自 `clpm-mvp-d8c0014a-20260926-141019.sha256`）：

```
966bba05893a3aac4da06b09eed64fa2c8457ade252fc01527cc217b3c4b5be1  releases/clpm-mvp-d8c0014a-20260926-141019.tar.gz
aa74cf6033a5b202e3387c6ffffdc176a4adfafb734b8cad43d6be731be2483b  releases/clpm-mvp-d8c0014a-20260926-141019.zip
```

## 二、仓库记录

- 代码与文档：`github/main`（`c2ce7789`）；
- 镜像与交付包：**不进 git**（`.gitignore` 忽略 `releases/*.tar.gz|zip|sha256`，且 GitHub 仓库单文件上限 100MB）→ 走 Release 附件（单附件上限 2GB）；
- 构建参数：`deploy/package.sh --include-monitoring`（含 Prometheus / Grafana / node-exporter 镜像）。

## 三、部署要点（详见《部署移交说明-20260926.md》）

1. 校验 sha256 → 解压 → 填 `.env.prod`（数据库口令 / JWT 密钥 / AAS·SignalR 地址与凭证）；
2. `docker load` + `./deploy.sh`（含备份与自动回滚）；覆盖升级时 `up -d --force-recreate backend celery-worker celery-beat frontend`；
3. 部署后三项验证：落库形态 `point + consistent`；日志见 `PointHistoryWriter 已启动`；点表增长且**宽表不再增长属预期**；
4. **生产删宽表：必须先 `taosdump` 备份并确认导出目录非空再 `DROP`**（开发环境漏此步曾致数据不可恢复）；
5. 本版不要启用 `kpi.min_observed_ratio`、不要急于重算历史 KPI。

## 四、验收证据（本版本）

- 后端：`pytest` 5024 passed（0 failed）· `ruff check` / `format` 通过 · 契约对账退出码 0；
- 前端：`eslint` 0 error · `vitest` 605 passed · `check:type` 通过 · hex 棘轮无新增债务（1191 处 / 95 文件，基线 1224）；
- 真实联调：位号级体检接口 HTTP 实测（`total=222`、非法参数 400）+ 浏览器五条断言（默认近 24h / 阈值可调 / 含 HELD 开关 / 过滤 / 分页）。
