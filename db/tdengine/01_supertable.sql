-- =============================================================================
-- 01_supertable.sql — 宽表超级表（已退役，2026-09-25）
-- =============================================================================
-- 本文件原定义宽表超级表 st_loop_data（7 列 OPC 原始秒级数据 + PV 质量码）
-- 及子表实例化示例。
--
-- 退役过程：
--   2026-09-09  历史导入停止写宽表（data_import 恒写测点点表）
--   2026-09-25  实时写入路径删除（PointHistoryWriter 成为唯一落库形态）
--   2026-09-25  读取路径删除（趋势/评估/诊断统一读测点点表）
--
-- 现行写入与读取的唯一形态是测点点表超级表 st_point_data_v1，
-- 定义见同目录 02_point_history.sql。
--
-- 本文件保留为空占位（不执行任何 DDL）：
--   1. docker-compose 仍挂载本文件到 TDengine entrypoint 初始化目录，
--      删除文件会导致挂载报错；
--   2. 生产库中的既有 st_loop_data 数据未删除（数据处置属 S3 阶段，
--      需单独授权 + 归档），此处仅停止新建。
-- =============================================================================
