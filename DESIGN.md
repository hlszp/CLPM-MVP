# CLPM Design Baseline

日期：2026-06-18
状态：approved-baseline
适用范围：`docs/设计文档/prototype/` 高保真原型重构第一阶段

## 1. 目标

本文件定义 CLPM 原型系统的内部设计输出，作为第一阶段重构的 1:1 还原标准。

目标不是继续产出“可点击的概念演示”，而是构建一个可用于用户测试、需求评审、业务流程验证的高保真原型系统。页面必须同时满足以下要求：

- 视觉风格统一，符合工业治理平台语气。
- 核心流程可交互、可流转、可回退。
- 所有动作有后果，所有状态可追踪。
- 页面优先服务任务，不优先服务说明文案。

## 2. 产品语气

CLPM 是“控制回路绩效治理与优化闭环平台”，不是通用 SaaS 控制台，也不是概念展示网站。

界面语气要求：

- 稳重、审计化、任务驱动。
- 信息密度高，但层级清晰。
- 优先展示状态、风险、下一步动作。
- 文案以业务动作和结论为中心，避免营销化表达。
- 不用大面积装饰性渐变、漂浮图形、SaaS 式功能卡片瀑布。

## 3. 设计原则

- 不允许“空点击”。任何主按钮或主链接都必须触发状态变化、上下文变化或路由变化。
- 不允许“解释压过任务”。首屏必须先回答用户“现在该做什么”，再解释为什么。
- 不允许“所有页面一个样”。工程师、审核、Sponsor 页面应有不同的信息重心。
- 不允许“卡片代替工作台”。列表页以表格、筛选、侧栏详情、时间线、对比视图为主。
- 不允许“视觉假高保真”。高保真不等于精致卡片，而是像真实生产系统一样可操作、可判断、可回看。

## 4. 角色与权限模拟

第一阶段不做真实登录鉴权，但必须模拟菜单权限路由。

- 采用顶部角色切换器模拟角色视角。
- 角色切换会影响菜单可见项、默认首页、主 CTA 和页面操作权限。
- 角色切换不刷新页面数据，但会重新计算当前页面的可见区域和禁用态。

推荐角色集：

- 工程师
- 专家审核
- Sponsor
- 授权实施人员
- 管理员

如果某阶段只启用部分角色，未启用角色仍保留在设计规范中，便于后续扩展。

## 5. 信息架构要求

第一阶段深做 6 个核心页面：

- 工程师首页工作台
- 样本导入与就绪校验
- 低效排行与指标总览
- 单回路证据中心
- 审核/实施/复评联动页
- Sponsor 汇报与证据包

其余页面在第一阶段可以保留结构，但必须遵守统一壳层、统一状态栏、统一布局规则，不允许出现风格漂移。

### 5.1 菜单结构声明

菜单结构是设计基线的一部分，不只是代码实现细节。

每个菜单项都必须显性声明以下信息：

- `menu_id`，代码字段映射为 `id`
- `label`
- `path`
- `parent_id`，代码字段映射为 `parentId`
- `page_level`，代码字段映射为 `pageLevel`
- `stage`
- `version`
- `depth`
- `roles`
- `default_entry`，代码字段映射为 `defaultEntry`
- `is_deep_page`，代码字段映射为 `isDeepPage`

`docs/设计文档/prototype/src/routes/menuConfig.ts` 是菜单结构的唯一事实来源，设计文档、类型声明和实现配置必须保持一致，不得派生第二份独立菜单真相。

## 6. 视觉系统

### 6.1 色彩

沿用当前原型的中性底色与风险色语义，但收紧使用方式：

- 页面底色：浅灰白，保证长时间阅读舒适。
- 面板底色：纯白或弱区分浅色，不堆叠多层阴影。
- 强调色：单一蓝色，仅用于主 CTA、当前态、链接和焦点态。
- 风险色：
  - 正常：青绿
  - 信息：蓝
  - 警告：琥珀
  - 危险：红
  - 中性：灰

规则：

- 风险色优先用于标签、边线、状态点，不大面积铺底。
- Sponsor 视图中的 `partial / completed` 必须有明显状态印章，不靠段落描述。
- 所有颜色必须从 token 引用，不允许页面散落写死 hex。

### 6.2 字体

- 继续使用中文友好的无衬线系统。
- 全站以中文为主，不混入英文标题风格词。
- 数字、版本号、包 ID、规则 ID 使用等宽字体。

排版要求：

- 页面主标题：24px / 700
- 分区标题：18px / 700
- 正文：14px 至 16px，按页面密度选择
- 辅助说明：12px 至 13px

规则：

- 页面的层级由大小、粗细、间距共同表达，不只靠颜色。
- 不允许 Sponsor 页面出现中英混排标题，例如 `Sponsor verdict`。

### 6.3 间距与密度

- 采用 4px/8px spacing scale。
- 一级区块上下间距大于区块内元素间距。
- 列表和工作台页面优先高密度紧凑布局，不做营销站式大留白。

### 6.4 圆角与阴影

- 控件圆角偏克制。
- 面板圆角可保留中等值。
- 阴影只做层级分离，不做悬浮装饰。

## 7. 布局规则

### 7.1 全局壳层

全局壳层必须包含：

- 左侧主导航
- 顶部状态栏
- 页面主标题区
- 页面主体区
- 当前角色/样本/包状态的全局摘要

### 7.2 页面结构模式

第一阶段只允许三类页面结构：

- 工作台页：左侧列表 + 中央主工作区 + 右侧动作/上下文区
- 数据表页：顶部筛选 + 中央表格 + 右侧详情抽屉或侧栏
- 审计页：顶部结论摘要 + 中部证据矩阵 + 底部风险与不可证明事项

### 7.3 响应式

响应式不是简单堆叠。

- Desktop：保留三栏/双栏结构
- Tablet：保留两栏，次级区折叠
- Mobile：切换为步骤式或抽屉式，不允许整页压成细长卡片流

所有可点击元素必须满足 44px 触达标准。

## 8. 组件标准

### 8.1 表格

列表页必须以表格为主，不允许用卡片代替批量信息浏览。

表格必须支持：

- 筛选
- 排序
- 批量选择
- 空状态
- Partial 状态提示
- 行级快捷动作

### 8.2 状态标签

统一标签体系：

- quality_status
- diagnosis_status
- workflow_status
- package_status
- risk_level

标签必须有：

- 颜色
- 文本
- 图标或语义前缀

不能只靠颜色区分。

### 8.3 筛选栏

所有列表页统一采用顶部筛选栏，包括：

- 关键词搜索
- 类型过滤
- 风险过滤
- 状态过滤
- 排序入口
- 批量操作入口

### 8.4 时间线

审核/实施/复评页必须使用纵向时间线表达流程历史。

时间线节点至少包含：

- 节点名称
- 状态
- 执行人
- 执行时间
- 节点结论

### 8.5 对比组件

复评页、整定样例页、Sponsor 页必须支持前后对比组件，统一展示：

- before
- after
- delta
- 风险说明

### 8.6 空状态与 Partial 状态

- 空状态必须告诉用户“当前为什么没有数据、可以做什么”。
- Partial 状态必须明确“缺什么、还能继续做什么、下一步去哪”。
- 数据不足不是错误。

## 9. 状态机要求

原型必须采用统一状态机，不允许每页自行解释业务状态。

核心对象：

- SampleBatch
- Loop
- ReviewDecision
- ImplementationRecord
- ReevaluationResult
- EvidencePackage

关键状态：

- SampleBatch：`draft -> validating -> ready/partial -> frozen`
- Loop：`eligible -> diagnosing -> review_pending -> approved/rejected/needs_evidence -> implemented -> reevaluated`
- EvidencePackage：`partial / completed`

规则：

- `EvidencePackage` 状态由整体流程自动派生，不能手工切换。
- 任一关键回路为 `needs_evidence` 或缺复评时，包状态必须为 `partial`。
- 页面间必须共享状态源，不能各页硬编码各自版本的“真相”。

## 10. 文案规范

- 所有页面用中文。
- 句子优先回答“是什么、为什么重要、下一步做什么”。
- 少解释系统理念，多解释当前业务结论。
- 禁止用“模拟版”“当前页面用于说明”作为主视觉内容。
- 如果需要说明是原型，应集中在系统级提示区，不应反复打断用户任务。

## 11. 第一阶段验收

高保真第一阶段验收必须同时满足：

- 6 个核心页面全部可运行。
- 角色切换器可驱动菜单与页面可见态变化。
- 至少 1 条顺畅闭环链路可完整跑通。
- 至少 1 条驳回回退链路可完整跑通。
- 至少 1 条 `partial` 状态链路可完整跑通。
- 所有主按钮都有真实结果。
- 所有列表页支持筛选、排序和批量选择。
- `npm run dev` 与 `npm run build` 通过。
- 原型可被独立部署运行，支持用户测试和需求评审。

## 12. 非目标

第一阶段不做：

- 真实后端
- 真实数据库
- 真实登录鉴权
- 真实文件导出
- 真实工业数据接入

但这些能力的交互入口、状态变化和边界说明必须在原型中被逼真模拟。
