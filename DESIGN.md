# CLPM Design Baseline

日期：2026-08-24（对齐 CLPM-MVP 现行口径；历史对齐对象为 PRD v6.2、UI/UX v6.3 与重构后实现契约 v2.11）
状态：active-baseline
适用范围：CLPM-MVP 前端与原型体验对齐

## 0. 权威来源声明

本文件是重构后 CLPM 前端与原型研发的**设计基线约束**，规定视觉、布局、组件、文案、验收等横切规则。

页面结构、菜单、状态机、数据字段的**事实来源**为：

| 类型 | 唯一来源 | 版本 |
|---|---|---|
| **MVP 设计与 IA（现行）** | `docs/MVP设计/`（IA 以 `00-信息架构.md` 为准） | 2026-08-24 |
| 产品需求 | `docs/设计文档/01-PRD/PRD.md` | v6.2 |
| 重构后实现契约 | `docs/设计文档/00-BASELINE/implementation-contract.md` | v2.11 |
| 功能规范 | `docs/设计文档/02-FDS/FDS.md` | v6.0 |
| 应用架构 | `docs/设计文档/03-ADS/ADS.md` | v6.0 |
| 数据模型 | `docs/设计文档/04-DDS/DDS.md` | v6.0 |
| API 接口 | `docs/设计文档/05-IDS/IDS.md` | v6.0 |
| UI/UX 规范 | `docs/设计文档/06-UIUX/ui-ux-design-guidelines.md` | v6.3 |
| 当前生产前端路由 | `frontend/apps/web-antd/src/router/routes/modules/` | 实现事实来源 |

CLPM-MVP 派生后（2026-08-20 起），页面结构、菜单与模块口径以 `docs/MVP设计/` 为现行事实来源；上表 v6.2 系列文档降为历史基线（见 `docs/历史基线/AGENTS-v6.2-archive.md`）。当本文件与实现契约冲突时，以 `docs/设计文档/00-BASELINE/implementation-contract.md` 为准；当业务范围与安全边界冲突时，以 PRD 为准。本文件保留横切设计约束。

## 1. 目标

本文件定义 CLPM 原型系统的内部设计输出，作为原型研发的 1:1 还原标准。

目标不是继续产出"可点击的概念演示"，而是构建一个可用于用户测试、需求评审、业务流程验证的高保真原型系统。页面必须同时满足以下要求：

- 视觉风格统一，符合工业治理平台语气。
- 核心流程可交互、可流转、可回退。
- 所有动作有后果，所有状态可追踪。
- 页面优先服务任务，不优先服务说明文案。

## 2. 产品语气

CLPM 是"产品化、工具化的控制回路绩效治理与优化闭环平台"，不是通用 SaaS 控制台，也不是概念展示网站。

界面语气要求：

- 稳重、审计化、任务驱动。
- 信息密度高，但层级清晰。
- 优先展示状态、风险、下一步动作。
- 文案以业务动作和结论为中心，避免营销化表达。
- 不用大面积装饰性渐变、漂浮图形、SaaS 式功能卡片瀑布。

## 3. 设计原则

- 不允许"空点击"。任何主按钮或主链接都必须触发状态变化、上下文变化或路由变化。
- 不允许"解释压过任务"。首屏必须先回答用户"现在该做什么"，再解释为什么。
- 不允许"所有页面一个样"。不同角色的页面应有不同的信息重心。
- 不允许"卡片代替工作台"。列表页以表格、筛选、侧栏详情、时间线、对比视图为主。
- 不允许"视觉假高保真"。高保真不等于精致卡片，而是像真实生产系统一样可操作、可判断、可回看。
- **产品化原则**：所有配置项必须支持用户自助组态（管理员/工程师），减少开发团队介入；配置类操作必须有确认对话框与变更留痕。

## 4. 角色与权限模拟

第一阶段不做真实登录鉴权，但必须模拟菜单权限路由。

- 采用顶部角色切换器模拟角色视角。
- 角色切换会影响菜单可见项、默认首页、主 CTA 和页面操作权限。
- 角色切换不刷新页面数据，但会重新计算当前页面的可见区域和禁用态。

角色集（对齐 PRD v6.1 §3 与实现契约 v2.0 §5，共 5 角色）：

- 系统管理员（ADMIN）
- 仪控工程师（IC_ENGINEER）
- 工艺设备工程师（PE_ENGINEER）
- 外部专家（EXPERT）
- Sponsor（SPONSOR）

如果某阶段只启用部分角色，未启用角色仍保留在设计规范中，便于后续扩展。角色与功能权限矩阵详见实现契约 v2.0 §5 与 FDS v6.0 §3 RBAC 矩阵。

## 5. 信息架构要求

### 5.1 模块结构

对齐 CLPM-MVP 现行口径（AGENTS.md 与 `docs/MVP设计/00-信息架构.md`），采用 **8 个一级模块** 结构（监控/评估/诊断/整定/处置/报告/配置/系统），左侧导航按闭环顺序排列（order 1~8）。相对 v6.2 的 6 模块结构（监控/评估/诊断/整定/配置/系统）：

- **处置**：新建一级模块（08 处置方案 v2.0 双实体：建议实体 + 处置工单，2026-08-20）。
- **报告**（原名“统计报告”，2026-08-24 改名）：升为一级菜单（order=6，配置→7、系统→8），含管理总览/绩效/诊断/处置/收益/订阅配置 6 子页，成熟度 S1/S2/S3 自适应骨架 + 旧路径 redirect。
- **模块热插拔**（2026-08-23 落地）：后端 `app/core/modules.py` 注册表 + 前端 `use-modules` composable + 系统-模块管理页 + `beat_registry.py` 条件调度，诊断/整定/处置支持按客户阶段弹性启用禁用。

IA 细节（二级菜单、角色权限、隐藏项与 redirect）一律以 `docs/MVP设计/00-信息架构.md` 为准，本节不再派生第二份菜单真相。各业务模块仍遵循"配置→运行→分析"三态自包含原则与工程/操作分离原则。

### 5.2 页面清单

完整页面清单以 `docs/设计文档/00-BASELINE/implementation-contract.md` §2 为准。旧 UI/UX 25 页面清单只作为历史设计来源，不再作为当前菜单验收项。

### 5.3 菜单结构声明

菜单结构是设计基线的一部分，不只是代码实现细节。

每个菜单项都必须显性声明以下信息：

- `menu_id`，代码字段映射为 `id`
- `label`
- `path`
- `parent_id`，代码字段映射为 `parentId`
- `page_level`，代码字段映射为 `pageLevel`
- `stage`
- `version`
- `depth`
- `roles`
- `default_entry`，代码字段映射为 `defaultEntry`
- `is_deep_page`，代码字段映射为 `isDeepPage`

`docs/MVP设计/00-信息架构.md` 与 `frontend/apps/web-antd/src/router/routes/modules/` 是当前菜单结构与路由结构的事实来源；`docs/设计文档/00-BASELINE/implementation-contract.md` 降为 v6.2 历史基线。原型 `menuConfig.ts` 仅作为历史原型入口，不再单独派生第二份真相。

## 6. 视觉系统

### 6.1 色彩

沿用当前原型的中性底色与风险色语义，但收紧使用方式：

- 页面底色：浅灰白，保证长时间阅读舒适。
- 面板底色：纯白或弱区分浅色，不堆叠多层阴影。
- 强调色：单一蓝色，仅用于主 CTA、当前态、链接和焦点态。
- 风险色：
  - 正常：青绿
  - 信息：蓝
  - 警告：琥珀
  - 危险：红
  - 中性：灰

规则：

- 风险色优先用于标签、边线、状态点，不大面积铺底。
- 所有颜色必须从 token 引用，不允许页面散落写死 hex。
- PV 质量码渲染规则：Good=正常色实线、Bad=灰色虚线断点、Uncertain=琥珀色点划线（对齐 UI/UX v6.1 与实现契约 v2.0）。

### 6.2 字体

- 继续使用中文友好的无衬线系统。
- 全站以中文为主，不混入英文标题风格词。
- 数字、版本号、回路 ID、规则 ID 使用等宽字体。

排版要求：

- 页面主标题：24px / 700
- 分区标题：18px / 700
- 正文：14px 至 16px，按页面密度选择
- 辅助说明：12px 至 13px

规则：

- 页面的层级由大小、粗细、间距共同表达，不只靠颜色。

### 6.3 间距与密度

- 采用 4px/8px spacing scale。
- 一级区块上下间距大于区块内元素间距。
- 列表和工作台页面优先高密度紧凑布局，不做营销站式大留白。

### 6.4 圆角与阴影

- 控件圆角偏克制。
- 面板圆角可保留中等值。
- 阴影只做层级分离，不做悬浮装饰。

## 7. 布局规则

### 7.1 全局壳层

全局壳层必须包含：

- 左侧主导航
- 顶部状态栏
- 页面主标题区
- 页面主体区
- 当前角色/工厂层级/时间窗口的全局摘要

### 7.2 页面结构模式

允许的页面结构模式（对齐 UI/UX v6.1 与实现契约 v2.0）：

- **工作台页**：左侧列表 + 中央主工作区 + 右侧动作/上下文区
- **数据表页**：顶部筛选 + 中央表格 + 右侧详情抽屉或侧栏
- **详情页**：顶部摘要 + 中部分区内容 + 底部时间线/对比
- **配置页**：左侧分类导航 + 中央表单 + 右侧预览/说明
- **总览页**：顶部筛选条 + KPI 卡片行 + 中部列表 + 底部趋势图（性能总览首页专用三行布局）
- **监控页**：顶部筛选 + 实时列表 + 行内迷你趋势 + 抽屉详情

### 7.3 响应式

响应式不是简单堆叠。

- Desktop：保留三栏/双栏结构
- Tablet：保留两栏，次级区折叠
- Mobile：切换为步骤式或抽屉式，不允许整页压成细长卡片流

所有可点击元素必须满足 44px 触达标准。

## 8. 组件标准

### 8.1 表格

列表页必须以表格为主，不允许用卡片代替批量信息浏览。

表格必须支持：

- 筛选
- 排序
- 批量选择
- 空状态
- 行级快捷动作

### 8.2 状态标签

统一标签体系（对齐实现契约 v2.0 §6 与 DDS v6.0 字段）：

- `loop_status`（回路状态：READY/PARTIAL/INACTIVE）
- `pv_quality`（PV 质量码：GOOD/BAD/UNCERTAIN）
- `kpi_status`（KPI 状态：SUCCESS/INCONCLUSIVE/PARTIAL）
- `action_status`（异常跟踪状态：PENDING/IN_PROGRESS/IMPLEMENTED/IGNORED）
- `diag_label`（诊断标签：stiction/oscillation/saturation/tuning_needed/normal）
- `tuning_status`（整定状态：DRAFT/RUNNING/COMPLETED/ROLLED_BACK）
- `risk_level`（风险等级：low/medium/high）

标签必须有：

- 颜色
- 文本
- 图标或语义前缀

不能只靠颜色区分。

### 8.3 筛选栏

所有列表页统一采用顶部筛选栏，包括：

- 关键词搜索
- 工厂层级过滤（厂/装置/单元）
- 状态过滤
- 排序入口
- 批量操作入口

### 8.4 时间线

异常跟踪详情、整定记录页必须使用纵向时间线表达流程历史。

时间线节点至少包含：

- 节点名称
- 状态
- 执行人
- 执行时间
- 节点结论

### 8.5 对比组件

整定效果对比页必须支持前后对比组件，统一展示：

- before
- after
- delta
- 风险说明

### 8.6 空状态与数据不足状态

- 空状态必须告诉用户"当前为什么没有数据、可以做什么"。
- 数据不足（PV 质量码为 BAD/UNCERTAIN 占比高）必须明确"缺什么、还能继续做什么、下一步去哪"。
- 数据不足不是错误，KPI 状态应标记为 `INCONCLUSIVE` 或 `PARTIAL`。

### 8.7 Tag 关联选择器（新增）

回路台账页与 Tag 关联管理页必须提供 Tag 关联选择器组件（对齐 UI/UX v6.1 与实现契约 v2.0）：

- 左侧 AAS Tag 列表（从 `tag_registry` 只读读取）
- 右侧 7 个槽位（PV/SP/OP/MODE/PID_P/PID_I/PID_D）
- 支持拖拽或下拉关联
- PV/SP/OP/MODE 必填，PID_P/PID_I/PID_D 选填
- 已关联的 Tag 在左侧列表标记占用状态

### 8.8 配置确认对话框（新增）

所有配置类操作（指标配置、引擎规则、诊断配置、Tag 关联）必须弹出确认对话框：

- 显示变更摘要（新增/修改/删除项）
- 显示影响范围（受影响回路数、下次引擎运行时间等）
- 二次确认按钮
- 变更留痕写入 `sys_audit_log`

## 9. 状态机要求

原型必须采用统一状态机，不允许每页自行解释业务状态。

核心对象与状态（对齐实现契约 v2.0 §6、DDS v6.0 与 UI/UX v6.1）：

| 对象 | 关键状态 |
|---|---|
| Loop（回路） | `READY` / `PARTIAL` / `INACTIVE` |
| Action Tracker（异常跟踪） | `PENDING` → `IN_PROGRESS` → `IMPLEMENTED` / `IGNORED` |
| KPI 快照 | `SUCCESS` / `INCONCLUSIVE` / `PARTIAL` |
| 诊断结果 | `stiction` / `oscillation` / `saturation` / `tuning_needed` / `normal` |
| 整定记录 | `DRAFT` → `RUNNING` → `COMPLETED` / `ROLLED_BACK` |
| PV 质量码 | `GOOD` / `BAD` / `UNCERTAIN` |

规则：

- KPI 状态由引擎自动派生，不能手工切换。
- 任一关键回路 PV 质量码为 `BAD` 或 `UNCERTAIN` 占比超阈值时，该回路 KPI 状态必须为 `PARTIAL` 或 `INCONCLUSIVE`。
- Action Tracker 状态机由诊断引擎或用户手动触发，状态流转必须留痕。
- 页面间必须共享状态源，不能各页硬编码各自版本的"真相"。
- 历史文档中的 `RESOLVED` 统一视为旧命名；当前代码与后续文档使用 `IMPLEMENTED`。
- 历史文档中的 `ACTIVE`/`PAUSED`/`DECOMMISSIONED` 统一视为旧命名；当前使用 `READY`/`PARTIAL`/`INACTIVE`。

## 10. 文案规范

- 所有页面用中文。
- 句子优先回答"是什么、为什么重要、下一步做什么"。
- 少解释系统理念，多解释当前业务结论。
- 禁止用"模拟版""当前页面用于说明"作为主视觉内容。
- 如果需要说明是原型，应集中在系统级提示区，不应反复打断用户任务。

## 11. 验收标准

原型与正式前端验收必须同时满足（对齐实现契约 v2.0 与 UI/UX v6.1）：

- Phase 1 范围内的当前实现契约页面全部可运行。
- 角色切换器可驱动菜单与页面可见态变化。
- 至少 1 条顺畅闭环链路可完整跑通：回路创建 → Tag 关联 → 性能评估 → 诊断 → 异常跟踪 → 解决。
- 至少 1 条驳回回退链路可完整跑通：异常跟踪 `IGNORED` 或整定 `ROLLED_BACK`。
- 至少 1 条 `PARTIAL` KPI 链路可完整跑通：PV 质量码异常 → KPI `PARTIAL` → 用户查看波形 → 创建跟踪。
- 所有主按钮都有真实结果。
- 所有列表页支持筛选、排序和批量选择。
- 所有配置类操作有确认对话框与变更留痕。
- `npm run dev` 与 `npm run build` 通过。
- 原型可被独立部署运行，支持用户测试和需求评审。

## 12. 非目标

Phase 1 不做：

- 自动下写 DCS 参数
- 真实 DCS 参数变更闭环
- 多节点审批流工单系统
- Phase 2 规模化算法服务拆分

Phase 1 可保留整定辨识、推荐、仿真的实验/辅助接口，但必须只输出建议、证据、风险和回退方案，不得表达为自动实施能力。

## 13. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-06-18 | v1.0 | 初版基线，定义 6 核心页面、视觉系统、组件标准 |
| 2026-06-21 | v2.0 | 对齐 v3.0/v4.0 基线：移除 SampleBatch/EvidencePackage/ReviewDecision/ImplementationRecord/ReevaluationResult 旧模型；改用 Loop/ActionTracker/DiagnosisResult/TuningRecord 新模型；6 模块+门户结构；AAS Tag 关联模型；产品化配置原则；新增 Tag 关联选择器与配置确认对话框组件；验收标准对齐 Phase 1 |
| 2026-06-25 | v2.1 | 追认重构后实现契约 v1.0：以当前 Vue 前端 IA、路由、权限、状态机、KPI 与阶段口径为设计基线；旧 25 页面清单降为历史来源。 |
| 2026-07-06 | v3.0 | 对齐 UIUX v6.1 与实现契约 v2.0：所有版本号引用统一升级为 v6.0；角色集补充英文枚举（ADMIN/IC_ENGINEER/PE_ENGINEER/EXPERT/SPONSOR）；状态机枚举统一为大写（5 类状态机：Loop/Action Tracker/KPI 快照/整定记录/PV 质量码）；标注 RESOLVED 与 ACTIVE/PAUSED/DECOMMISSIONED 为旧命名；状态标签、PV 质量码渲染规则、页面结构模式与验收标准全部对齐 v6.0。 |
| 2026-07-22 | v3.0 | D5 口径同步：PRD 引用统一为 v6.1、UI/UX 引用统一为 v6.1、实现契约引用统一为 v2.0（历史 v2.1 摘要已并入 v2.0）；DESIGN 自身版本保持 v3.0 不变。 |
| 2026-08-24 | v3.1 | 对齐 CLPM-MVP 现行口径：一级模块结构由 6 模块更新为 8 模块（新增处置、统计报告升一级菜单 order=6），声明模块热插拔机制；IA 与菜单事实来源切换至 `docs/MVP设计/00-信息架构.md`，v6.2 系列文档引用降为历史基线；active-baseline 日期更新为 2026-08-24。 |
