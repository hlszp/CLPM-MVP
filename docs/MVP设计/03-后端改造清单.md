# 03 - 后端改造清单

## 1. 已屏蔽的 API endpoints（main.py 注释了路由注册）

以下文件的路由注册已在 `app/main.py` 中注释，API 请求返回 404。后续应彻底删除文件。

| 文件 | 大小 | 说明 |
|---|---|---|
| `endpoints/diagnosis.py` | 59KB | 诊断全套接口（tags_router / router / timeseries_router / tracker_router） |
| `endpoints/tuning.py` | 23KB | 整定全套接口（模型辨识/PID/仿真/任务管理） |
| `endpoints/aas.py` | 12KB | AAS/OPC UA tag 同步 |
| `endpoints/llm_config.py` | 14KB | LLM 配置（诊断 AI 解读依赖） |
| `endpoints/diagnosis_trigger_config.py` | 4KB | 诊断触发阈值配置 |

## 2. 保留 endpoints 中的交叉引用（需改造）

### 2.1 `endpoints/dashboard.py`（系统概览）⚠️核心

**引用问题**：导入并使用了 `DiagnosisResult` 和 `ActionTracker` 模型。

| 行号 | 引用内容 | 处理方式 |
|---|---|---|
| L20 | `from app.models.diagnosis import DiagnosisResult` | 删除导入 |
| L802 | `diagnosisDistribution: 诊断建议类型分布` | 删除该字段 |
| L999-1096 | `tracker_count` 统计 ActionTracker（PENDING/IN_PROGRESS/VERIFYING） | 返回 0 或删除统计逻辑 |
| L1032-1040 | `select(func.count(...)).where(ActionTracker.action_status.in_(...))` | 删除 |
| L1080-1087 | tracker_loops 查询 ActionTracker.loop_id | 删除 |
| L1094-1096 | `"trackerCount": tracker_count` / `"pendingCount": tracker_count` | 返回 0 |
| L1118-1142 | `diagnosis_distribution` 统计 DiagnosisResult.diag_label | 删除 |
| L1308 | `"pendingTrackerCount": tracker_count` | 返回 0 |
| L1317 | `"diagnosisDistribution": diagnosis_distribution` | 返回空字典 |

### 2.2 `endpoints/monitor.py`（关注队列）

**引用问题**：`_VALID_SOURCES` 包含 TRACKER 和 VERIFICATION 来源。

| 行号 | 引用内容 | 处理方式 |
|---|---|---|
| L7 | 注释"关注队列聚合五类来源（ALERT/DEGRADATION/DATA_QUALITY/TRACKER/VERIFICATION）" | 改为三类 |
| L11 | 注释"评估/诊断/整定摘要/Tracker 时间线/生命周期/nextAction" | 删除诊断/整定/Tracker 描述 |
| L34 | `_VALID_SOURCES = frozenset(("ALERT", "DEGRADATION", "DATA_QUALITY", "TRACKER", "VERIFICATION"))` | 移除 TRACKER/VERIFICATION |
| L45 | `description="来源筛选...TRACKER/VERIFICATION"` | 移除 TRACKER/VERIFICATION |
| L60 | `"""统一关注队列——聚合预警/评分恶化/数据质量/Tracker/验证超期"""` | 移除 Tracker/验证超期 |

### 2.3 `endpoints/ai_insight.py`（AI 洞察）

**引用问题**：4 场景含 diagnosis 和 tuning。

| 行号 | 引用内容 | 处理方式 |
|---|---|---|
| L3-6 | 注释"4 场景：diagnosis/tuning/performance/workbench" | 改为 2 场景 |
| L4 | `- diagnosis：回路诊断解读` | 删除 |
| L6 | `- tuning：回路整定建议` | 删除 |
| L12 | `旧端点 POST /diagnosis/{loopId}/interpret` | 删除兼容说明 |
| L60-61 | `diagnosis / performance：loopId` / `tuning：taskId` | 删除 diagnosis/tuning |

### 2.4 `endpoints/configs.py`（配置）

**引用问题**：含诊断配置接口 `GET/PUT /configs/diagnosis`。

| 行号 | 引用内容 | 处理方式 |
|---|---|---|
| L10-11 | `GET /api/v1/configs/diagnosis` / `PUT /api/v1/configs/diagnosis` 注释 | 删除 |
| L31 | `from app.models.diagnosis import DiagnosisConfig` | 删除导入 |
| L36-39 | `DiagnosisConfigBatchResponse/Request/Item/UpdateItem` 类型导入 | 删除 |
| L148-149 | `_diagnosis_to_response_dict` 函数 | 删除 |
| L215-218 | `_apply_diagnosis_update` 函数 | 删除 |
| L429-456 | `GET /configs/diagnosis` / `PUT /configs/diagnosis` 接口 | 删除 |

### 2.5 `endpoints/tasks.py`（任务管理）

**引用问题**：引用 `task_tracker` 服务（通用任务跟踪，非诊断专用）。

| 说明 | 处理方式 |
|---|---|
| `task_tracker` 是通用任务跟踪服务，被评估任务使用 | **保留**，不改动 |
| 需确认是否有诊断/整定任务类型的创建逻辑 | 排查并删除诊断/整定任务类型 |

### 2.6 `endpoints/reports.py`（报表）

**引用问题**：grep 未发现诊断/整定直接引用。

| 说明 | 处理方式 |
|---|---|
| 无直接引用 | **保留**，需确认是否有诊断报告生成逻辑 |

## 3. 需删除的 Services 文件

### 3.1 诊断服务（8 个文件）

| 文件 | 说明 |
|---|---|
| `services/diagnosis.py` | 诊断核心服务 |
| `services/diagnosis_rule.py` | 诊断专家规则 |
| `services/diagnosis_recommendation.py` | 诊断推荐建议 |
| `services/diagnosis_interpretation.py` | 诊断结果解释 |
| `services/diagnosis_approval.py` | 诊断审批 |
| `services/diagnosis_report.py` | 诊断报告生成 |
| `services/diagnosis_threshold.py` | 诊断阈值 |
| `services/diagnosis_trigger_config.py` | 诊断触发条件配置 |

### 3.2 整定服务（5 个文件 + 1 个目录）

| 文件 | 说明 |
|---|---|
| `services/tuning.py` | 整定核心服务 |
| `services/tuning_algorithms.py` | 整定算法 |
| `services/tuning_progress.py` | 整定进度管理 |
| `services/tuning_knowledge.py` | 整定知识库 |
| `services/tuning_identification/` | 过程对象辨识算法栈（目录） |

### 3.3 Tracker 服务（1 个文件）⚠️需确认

| 文件 | 说明 | 处理方式 |
|---|---|---|
| `services/tracker.py` | Action Tracker 服务（诊断闭环跟踪） | **需确认**：dashboard.py 和 monitor.py 引用，删除前需先清理这些引用 |

### 3.4 AI 洞察场景策略（2 个文件）

| 文件 | 说明 |
|---|---|
| `services/ai_insight/scenes/diagnosis.py` | 诊断场景策略 |
| `services/ai_insight/scenes/tuning.py` | 整定场景策略 |

### 3.5 阀门诊断 ⚠️需确认

| 文件 | 说明 | 处理方式 |
|---|---|---|
| `services/metric_calculator/valve_diagnosis.py` | 阀门诊断指标计算 | **需确认**：是否被 KPI 计算调用，若是则需保留或用空实现替代 |

## 4. 需删除的 Models 文件

| 文件 | 说明 | 被引用位置 |
|---|---|---|
| `models/diagnosis.py` | 诊断 ORM 模型（DiagnosisResult/DiagnosisConfig 等） | dashboard.py / configs.py |
| `models/tuning.py` | 整定 ORM 模型 | tuning.py endpoint |
| `models/tuning_knowledge.py` | 整定知识库 ORM 模型 | tuning.py endpoint |
| `models/tracker.py` | Action Tracker ORM 模型 | dashboard.py / monitor.py |

**注意**：删除 models 文件前，必须先清理所有引用（dashboard.py / monitor.py / configs.py 等）。数据库表保留不删（不做 schema 迁移）。

## 5. 已移除的 Celery 任务（celery_app.py 已从 include 移除）

| 文件 | 大小 | 说明 |
|---|---|---|
| `tasks/diagnosis_engine.py` | 174KB | 诊断引擎（自动诊断+手动诊断） |
| `tasks/tuning.py` | 25KB | 整定任务（模型辨识/PID/仿真） |
| `tasks/tracker_verification.py` | 12KB | 诊断效果验证任务 |
| `tasks/aas_sync.py` | 3KB | AAS/OPC UA 同步任务 |

### Celery Beat 定时任务

需确认 `celery_app.py` 的 `beat_schedule` 中是否还有诊断/整定相关的定时任务条目，如有则删除。

### Lifespan 预载

`main.py` 的 lifespan 已跳过：
- 诊断触发条件预载（`preload_diagnosis_trigger`）
- 诊断专家规则预载（`preload_rules`）

## 6. 保留的 Celery 任务（不动）

| 文件 | 说明 |
|---|---|
| `tasks/kpi_calc.py` | KPI 指标计算（12 个指标计算器） |
| `tasks/report_generator.py` | 报表生成（含 PDF 导出） |
| `tasks/alert_patrol.py` | 智能预警巡检 |
| `tasks/audit_archive.py` | 审计归档 |
| `tasks/data_integrity_check.py` | 数据完整性检查 |
| `tasks/data_link_monitor.py` | 数据链路监控 |
| `tasks/dead_letter.py` | 死信队列处理 |
| `tasks/arma.py` | ARMA 算法（需确认是否仅用于整定） |

## 7. 后端改造执行顺序

1. **先清理保留 endpoints 的交叉引用**（dashboard.py / monitor.py / ai_insight.py / configs.py）
2. **再删除 services 文件**（诊断/整定/tracker 服务）
3. **然后删除 models 文件**（确认无引用后）
4. **最后删除 endpoints 文件**（diagnosis.py / tuning.py / aas.py / llm_config.py / diagnosis_trigger_config.py）
5. **清理 celery_app.py** 的 beat_schedule 和 import
6. **清理 main.py** 的注释代码块
7. **运行 ruff check + pytest** 验证无残留引用
