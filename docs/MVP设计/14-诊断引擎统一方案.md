# 14-诊断引擎统一方案（A-03 迁 diagnosis_run + 旧引擎退役）

> 地位：方案 A 的书面实施方案（2026-08-26 立项，2026-08-27 **已实施完毕**：A1~A4 四阶段全部落地，D1~D4 裁决见 §3）。
> 背景：工作台"有据可查"梳理（13 号矩阵）暴露诊断模块双引擎口径分裂——工作台 A-03 统计读旧引擎表（`diagnosis_tag`/`diagnosis_result`，写入方已停用，数据为静态快照），明细页读 v2 引擎表（`diagnosis_run`，有活跃 Beat）。决策：A-03 聚合迁 v2 数据源，旧引擎退役归档。
> 可行性依据：2026-08-26 只读调查报告（本文 §2 摘要其结论，全部含文件行号）。
> 纪律：按 `docs/过程文档/staged-implementation-workflow-2026-08-24.md` 分阶段推进；不删除诊断专属文件（AGENTS.md 红线，退役=解除注册+注释，非删除）。

## 1. 目标与成功标准

- 工作台诊断 tab（A-03 六块）+ 总览（plants.alarm_count/roots）全部以 `diagnosis_run` 为唯一诊断数据源
- 诊断统计数字与 `/diagnosis/records` 明细同源可对账（13 号矩阵 §7 抽验指标"确诊异常数"可对上）
- 旧引擎（`tasks/diagnosis_engine.py` 写入链路 + 旧表读方）退役归档，无活跃写入/读取
- 后端 pytest/ruff/alembic、前端 lint/typecheck 全过

## 2. 可行性核对结论（调查摘要）

| A-03 数据块 | v2 覆盖度 | 关键差异 |
|---|---|---|
| fitness_gates | 完全无关（kpi_snapshot_hourly） | 零影响 |
| open_tags Top6 | 可迁 | 粒度：标签→run；severity 枚举失配（旧4档 INFO/WARN/ERROR/CRITICAL → v2 3档 HIGH/MEDIUM/LOW）；SLA 倒计时无 v2 对应（且旧表该字段本就无运行时写入，纯种子演示态）；"未处置"需 review_status + loop_action_item 合成 |
| concl_timeline | 可迁 | disposition 四态可由 review_status/converted_order_id/ignore_reason 合成；条数骤减（标签流→run 流） |
| rule_stats | 部分可迁 | hits 可用 symptom_tags JSONB 聚合（与旧 tag_code 同域）；resolved_rate 无 v2 对应（**人工决策点 D2**） |
| pareto / rootcause_top | 可迁 | 按 primary_category / symptom_tags 聚合；值域中文自由文本→8 类枚举（可复用 diagnosis_v2._CATEGORY_LABELS 中文映射）；顺带消除旧 MV 按 loop_id JOIN 重复计数瑕疵 |
| 总览 alarm_count/roots | 可迁 | 存量态(ACTIVE)→流量态(近窗口检出)（**人工决策点 D3**） |

**前置条件**：① `/algorithms/diagnosis/analyze`（旧引擎唯一活跃写入口，endpoints/algorithms.py L274）需收口；② `seed_workbench_demo.py` 补 diagnosis_run 种子（否则迁后统计全空）；③ severity/值域映射层。

**易漏点**：`workbench_tuning.py` L521 `_query_diag_src_map`（读 diagnosis_tag，待整定队列"建议来源"）需同步迁；`event_bus.tag_id` FK→diagnosis_tag（models/event_bus.py L89，DROP 表前必须先处理约束，建议保留旧表仅归档不 DROP）。

## 3. 人工决策点（阻塞实施）

| # | 决策 | 选项 | 裁决（2026-08-26，用户确认） |
|---|---|---|---|
| D1 | SLA 倒计时列 | a) 明示下线（v2 无 SLA 概念，旧字段本就是演示态）；b) 为 v2 立新 SLA 机制 | **a**（SLA 归处置域，诊断域下线该列） |
| D2 | rule_stats（规则命中×解决率） | a) 重定义（hits=symptom_tags 检出数，resolved_rate=复核确认率）；b) 区块下线替换 | **a**（复核确认率，数据现成 review_status） |
| D3 | 总览 alarm_count 口径 | a) 流量态：近 24h 诊断检出异常回路数；b) 近 7d 未复核检出数 | **a**（与工作台窗口口径一致） |
| D4 | 旧表处置 | a) 保留表+数据，退役读方（event_bus FK 安全）；b) DROP | **a**（不 DROP，解除活跃读写即退役） |

## 4. 实施阶段（骨架）

### 阶段 A1：种子与语义映射层（先行）
- seed_workbench_demo.py 补 diagnosis_run 种子：多回路×多窗口×多 primary_category/severity/review_status/symptom_tags/fusion_results 组合，幂等可重跑
- 后端映射层：severity 3→4 档映射（HIGH→CRITICAL? 见实施时定，MEDIUM→WARN/ERROR 按置信度细分）、category 8 类中文标签复用 `_CATEGORY_LABELS`
- 核验：种子跑完后 /workbench/diagnosis 六块非空（dev 库）

### 阶段 A2：A-03 六块聚合迁 v2
- workbench_diagnosis.py 各 `_query_*`/`shape_*` 改读 diagnosis_run（open_tags/concl_timeline/rule_stats 按 D2/pareto/rootcause_top），D1/D3 裁决落实
- mv_diagnosis_pareto 重建：新 alembic 迁移 DROP+CREATE（基于 diagnosis_run），refresh_workbench_mv 任务零改动
- test_workbench_diagnosis.py mock 同步改写
- 核验：A-03 与 /diagnosis/runs 同窗口对账一致

### 阶段 A3：总览与残留读方迁移
- workbench_overview.py alarm_count/roots 迁 v2（D3 口径）；EventTimeline.vue severity 颜色映射改 3 档（或后端映射 4 档不动前端）
- workbench_tuning.py `_query_diag_src_map` 迁 diagnosis_run
- 核验：总览→诊断下钻链路对账

### 阶段 A4：旧引擎退役收口
- endpoints/algorithms.py `/algorithms/diagnosis/analyze` 改为调 v2 编排器（或注释退役并登记）
- 旧读方死代码确认（tracker/report_generator 等已死，登记即可）；test_production_bootstrap.py 表清单更新
- AGENTS.md 已知残留 + MVP设计 README + 13 号矩阵同步更新；根因：双引擎分裂根因记录
- 核验：grep 全仓无活跃 diagnosis_tag/diagnosis_result 运行时读写（除种子/归档/测试）

## 5. 用户可感知的数字变化（已落地，2026-08-27 A4 收口时逐条核验）

- 确诊异常/结论时间线条数：标签流→run 流，数量级下降（语义更准）——**已落地**（A2，对账 24h/7d/30d = 5/23/27 与 /diagnosis/runs 全一致）
- 严重度颜色/排序：4 档→3 档（或映射保 4 档）——**已落地**（A1 compat 层 HIGH→CRITICAL/MEDIUM→WARN/LOW→INFO 映射回旧四档颜色域，前端零改动）
- Pareto/根因类目：中文自由文本→8 类规范枚举（中文标签展示）——**已落地**（A2，中文+代码双域，`_CATEGORY_LABELS`）
- 总览装置"告警"数：存量→近 24h 流量——**已落地**（A3，D3=a 近 24h 流量态，对账 5=5）
- SLA 倒计时列（D1=a）——**已落地**（A2，诊断域下线该列，前端列删）
- 切换时历史跳变：旧表历史不再计入，v2 从种子+新 run 开始积累——**已落地**（A1 种子 12 条 + v2 Beat 持续积累）

## 6. 明确不做

- 不删除 diagnosis_engine.py / diagnosis.py / tracker.py 等诊断专属文件（红线：解除注册+归档注释）
- 不 DROP 旧表（D4=a）
- 不在本方案内为 v2 新建 SLA 机制（若 D1=b 另立项）
- 不改诊断 v2 引擎本体（算子/编排/复核闭环不动）

## 7. 修订记录（阶段状态锚点）

| 日期 | 阶段 | 状态 | 摘要 |
|---|---|---|---|
| 2026-08-26 | 设计 | 定稿 | D1~D4 裁决完毕（全 a），A1 启动 |
| 2026-08-26 | A1 | 完成 | 种子 12 条 diagnosis_run（11 回路×6 类×3 档×复核双态×双触发，`00000000-0000-0001-` 前缀幂等，/diagnosis/runs total=27 核验过）；compat 层 `diagnosis_v2_compat.py`（severity HIGH→CRITICAL/MEDIUM→WARN/LOW→INFO + category 中文标签）；ruff/pytest 29 过。注意：PG 会话时区 +8，A2 聚合窗口须用参数化 naive UTC，勿裸用 now() |
| 2026-08-26 | A2 | 完成 | workbench_diagnosis 五块迁 diagnosis_run（open_tags 未处置=排除关联 action_item 达任意终态的 run；concl 四态合成 CONVERTED>ACK_REVIEWED>IGNORED>UNADDRESSED；rule_stats=症状 JSONB 聚合+复核确认率；pareto 中文+代码双域）；MV 重建迁移 e3f4a5b6c7d8；SLA 字段下线（前端列删）；onlyActive 重定义；对账 24h/7d/30d = 5/23/27 与 /diagnosis/runs 全一致；pytest 4440 过、alembic check 过 |
| 2026-08-26 | A3 | 完成 | overview alarm_count 迁近 24h 流量态（对账 5=5，asyncpg UUID 断点修复 unit_id::text；assessment 复用同查询连带统一）；roots 迁 symptom_tags 聚合（中文+四档+active_count=未复核 run 数，EventTimeline 零改动）；tuning _query_diag_src_map 迁 v2（建议来源中文类别逐回路对上）；pytest 4446 过 |
| 2026-08-27 | A4 | 完成 | `/algorithms/diagnosis/analyze` 退役（前端/e2e 全仓 grep 无调用方 → 注释路由解除注册+归档注释，替代入口 POST /diagnosis/run；test_api_algorithms 改写为 404 退役断言）。退役核验：全仓 grep 确认 diagnosis_tag/diagnosis_result 无活跃运行时读写——剩余引用全部为死代码链（旧引擎/旧端点/services.tracker/diagnosis_report/diagnosis_recommendation/ai_insight 死场景/report_generator 死任务，均登记不删）、注释归档（workbench 三 service 口径说明等，含 4 处过时注释修正）、种子脚本（seed_workbench_demo/data_simulator/cleanup）、FK 与 ORM/DDL/迁移（D4=a 保留）；test_production_bootstrap 表清单断言保持（D4=a 注释登记）。13 号矩阵升 v1.0、README/AGENTS 同步。门禁：ruff/format/pytest 全过；curl /workbench/diagnosis 与 /workbench/overview 抽验正常 |
