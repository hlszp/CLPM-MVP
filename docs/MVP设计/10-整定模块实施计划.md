# 10 - 整定模块实施任务清单

| 项目 | 内容 |
|---|---|
| 版本 | v1.0 |
| 日期 | 2026-08-18 |
| 状态 | 待执行 |
| 设计依据 | `09-整定模块设计方案.md`（v1.0，已评审） |
| 分支 | `feat/tuning-module`（从 main 拉出，门禁全绿后 `--no-ff` 合并） |
| 执行顺序 | **Phase 1 后端解禁 → Phase 2 新端点 → Phase 3 前端三页 → Phase 4 嵌入联动** |

**总目标**：恢复整定为一级模块，跑通「辨识 → 全算法矩阵 → 仿真对比 → 保存方案 → 处置挂接 → 效果验证」闭环。

**架构**：后端解除屏蔽复用 Phase 2 算法栈（零重写），仅新增 2 个端点 + 处置回写 2 处；前端全新实现 3 页 + 1 个共享验证组件。

**Tech Stack**：FastAPI + SQLAlchemy(async) + Celery + TDengine（本地时序）/ Vue 3 + TS + Ant Design Vue + vue-vben-admin / ECharts。

**门禁（每 Phase 收尾必跑）**：

```bash
cd backend && uv run ruff check . && uv run ruff format --check . && uv run pytest -q && uv run alembic check
cd frontend && pnpm run check:type
```

---

## T0 前置：工作区清理（执行前必做）

当前工作区有 45 个前端文件的未提交格式化改动（oxfmt 结果，与整定无关）。

- [ ] 确认改动来源后单独提交：

```bash
git add -A frontend/
git commit -m "style(frontend): apply oxfmt to existing sources"
```

- [ ] 从 main 拉分支：`git checkout -b feat/tuning-module`

---

# Phase 1 后端解禁（T1~T3）

## T1 解禁 API 路由注册

**Files:**
- Modify: `backend/app/main.py`（第 76-77 行 import 区 + 第 981-982 行路由注册区）

- [ ] **Step 1**: 解除 import 注释

```python
# 第 76-77 行
#   # MVP 精简：已屏蔽整定模块 → 不注册 tuning
#   # tuning,
# 改为：
    tags,
    tuning,  # 整定模块（09 设计方案恢复为一级模块）
    users,
```

- [ ] **Step 2**: 解除路由注册注释

```python
# 第 981-982 行
#   # MVP 精简：已屏蔽整定模块 → 不挂载 tuning.router
#   # v1_router.include_router(tuning.router)
# 改为：
    # 整定模块（09 设计方案恢复）
    v1_router.include_router(tuning.router)
```

- [ ] **Step 3**: 启动后端验证端点可见

```bash
cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 17101 &
curl -s http://localhost:17101/api/v1/tuning/methods -H "Authorization: Bearer <token>" | head -c 300
# 预期：返回 5 种整定算法元数据（IMC/LAMBDA/ZN/COHEN_COON/SIMC）
# 同时确认 OpenAPI：curl -s http://localhost:17101/openapi.json | grep -c '/tuning/' 预期 ≥ 16
```

- [ ] **Step 4**: Commit

```bash
git add backend/app/main.py
git commit -m "feat(tuning): 解禁整定模块 API 路由注册"
```

## T2 解禁 Celery 任务注册

**Files:**
- Modify: `backend/app/tasks/celery_app.py`（第 42-43 行 include 列表 + 第 147 行尾部显式 import）

- [ ] **Step 1**: include 列表恢复

```python
# 第 42-43 行
#   # MVP 精简：已屏蔽整定模块 → 不注册 tuning
#   # "app.tasks.tuning",
# 改为：
        "app.tasks.tuning",  # 整定模块（09 设计方案恢复：历史辨识异步任务）
```

- [ ] **Step 2**: 尾部显式 import 恢复（第 147 行附近，Beat 进程需显式导入）

```python
#   # import app.tasks.tuning  # noqa: E402, F401
# 改为：
import app.tasks.tuning  # noqa: E402, F401
```

- [ ] **Step 3**: 重启后端（lifespan 会拉起新 Worker/Beat），验证任务注册

```bash
ps aux | grep -E 'celery.*CLPM-MVP' | grep -v grep   # 确认 worker/beat 已随宿主拉起
# Worker 日志或 redis 中确认 app.tasks.tuning 内任务已注册（identify 异步任务名以 tasks/tuning.py 中 @celery_app.task 名为准）
```

- [ ] **Step 4**: Commit

```bash
git add backend/app/tasks/celery_app.py
git commit -m "feat(tuning): 解禁整定 Celery 任务注册"
```

## T3 原有测试盘点与全量门禁

**Files:**
- Test: `backend/tests/test_tuning.py` / `test_tuning_fixes.py` / `test_tuning_knowledge.py` / `test_tuning_phase2.py`

- [ ] **Step 1**: 跑原测试（当前基线：48 passed / 114 skipped——skipped 多为路由未注册时的 API 用例，解禁后应转为执行）

```bash
cd backend && uv run pytest tests/test_tuning.py tests/test_tuning_fixes.py tests/test_tuning_knowledge.py tests/test_tuning_phase2.py -q
```

- [ ] **Step 2**: 逐个处理由 skipped 转为执行后的失败用例（MVP 种子数据/配置差异导致），修复或按现状适配断言；禁止直接删除用例
- [ ] **Step 3**: 全量门禁

```bash
cd backend && uv run ruff check . && uv run ruff format --check . && uv run pytest -q && uv run alembic check
# 预期：ruff 无输出、pytest 全绿、alembic check 退出码 0（无 schema 漂移——本模块无新表）
```

- [ ] **Step 4**: Commit

```bash
git add backend/tests/
git commit -m "test(tuning): 适配解禁后原有用例"
```

**Phase 1 出口**：`/tuning/*` 全部端点可调；历史辨识异步任务可经 Celery 执行；门禁全绿。

---

# Phase 2 新端点（T4~T5）

## T4 全算法矩阵端点 `POST /tuning/tune/matrix`

**Files:**
- Modify: `backend/app/schemas/tuning.py`（新增 `TuneMatrixRequest`，复用 `TuneResult`）
- Modify: `backend/app/api/v1/endpoints/tuning.py`（在 `tune_pid_endpoint` 后新增端点）
- Test: `backend/tests/test_tuning_fixes.py`（追加用例，或新建 `test_tuning_matrix.py`）

- [ ] **Step 1**: Schema（`schemas/tuning.py`，紧随 `TuneRequest` 定义后）

```python
class TuneMatrixRequest(CamelModel):
    """POST /tuning/tune/matrix 请求体（全算法矩阵：TuneRequest 去掉 algorithm 单值）。"""

    modelType: ModelType = Field(..., description="模型类型: FOPDT/SOPDT/IPDT")
    modelParams: ModelParams
    algorithmParams: dict[str, Any] | None = Field(
        None, description="算法参数（如 lambda 比例系数），5 算法共用"
    )
    currentPid: PidParams | None = Field(None, description="当前 PID 参数（用于对比）")
    loopId: str | None = Field(None, description="回路 ID（可选，用于记录）")
    sourceRecordId: str | None = Field(None, description="模型辨识记录 ID")
    modelSource: ModelSource | None = None
    riskConfirmed: bool | None = None
```

- [ ] **Step 2**: 端点实现（`endpoints/tuning.py`，放在 `/tune` 端点之后；单行失败不阻断、不写审计——审计仍由单行 `/tune` 与最终保存方案承担）

```python
@router.post("/tune/matrix", response_model=ApiResponse[dict])
async def tune_matrix_endpoint(
    body: TuneMatrixRequest,
    db: AsyncSession = Depends(get_db),
    user: SysUser = Depends(require_roles("ADMIN", "IC_ENGINEER", "EXPERT")),
) -> dict:
    """全算法矩阵整定（09 设计方案 §4.2）：5 算法一次全算，单行失败不阻断。"""
    source_context = await authorize_tuning_model(
        db=db,
        requested_model_type=body.modelType,
        requested_model_params=body.modelParams.model_dump(exclude_none=True),
        loop_id=body.loopId,
        source_record_id=body.sourceRecordId,
        model_source=body.modelSource,
        risk_confirmed=body.riskConfirmed,
    )
    rows: list[dict[str, Any]] = []
    for algo in ("IMC", "LAMBDA", "ZN", "COHEN_COON", "SIMC"):
        try:
            result = await tune_pid(
                model_type=source_context.model_type,
                model_params=source_context.model_params,
                algorithm=algo,
                algorithm_params=body.algorithmParams,
                current_pid=body.currentPid.model_dump() if body.currentPid else None,
                loop_id=source_context.loop_id,
                source_context=source_context,
            )
            rows.append({"algorithm": algo, "ok": True, "result": result})
        except BizError as exc:
            rows.append({"algorithm": algo, "ok": False, "error": exc.message})
    return success(data={"rows": rows})
```

- [ ] **Step 3**: 先写失败测试再实现（TDD）

```python
# tests/test_tuning_matrix.py
def test_tune_matrix_returns_five_rows(...):       # 合法 FOPDT 模型 → 5 行，ok 全 True
def test_tune_matrix_row_failure_not_blocking(...): # 构造某算法失败入参 → 该行 ok=False 且其余行正常
def test_tune_matrix_permission(...):               # SPONSOR 角色 → 403
```

运行：`uv run pytest tests/test_tuning_matrix.py -q` → 3 用例全绿。

- [ ] **Step 4**: Commit

```bash
git add backend/app/schemas/tuning.py backend/app/api/v1/endpoints/tuning.py backend/tests/test_tuning_matrix.py
git commit -m "feat(tuning): 新增全算法矩阵端点 POST /tuning/tune/matrix"
```

## T5 效果验证数据端点 `GET /tuning/verification/data`

**Files:**
- Modify: `backend/app/api/v1/endpoints/tuning.py`
- 复用: `app/services/waveform.py::get_waveform`（前后窗各拉一次）；KPI 快照 helper 复用 `endpoints/handling.py` 的 `_latest_snapshot_in_window` / `_kpi_summary`（**将其下移为共享**：挪入 `app/services/kpi_snapshot.py` 新文件，handling.py 改为 import——避免跨端点私有函数引用）
- Test: `backend/tests/test_tuning_verification.py`（新建）

- [ ] **Step 1**: 提取共享 KPI 快照 helper

```python
# backend/app/services/kpi_snapshot.py（新建）
#   async def latest_snapshot_in_window(db, loop_id, start, end) -> KpiSnapshotHourly | None
#   def kpi_summary(snap) -> dict | None   # 评分+六率+可信度+tsStart/tsEnd
# handling.py 中 _latest_snapshot_in_window/_kpi_summary/_pull_kpi_windows 改为薄封装 import；
# 跑 handling 既有单测确认无回归：uv run pytest tests/ -k handling -q
```

- [ ] **Step 2**: 端点实现

```python
@router.get("/verification/data", response_model=ApiResponse[dict])
async def verification_data_endpoint(
    loopId: str = Query(...),
    pointTime: str = Query(..., description="对比时点 ISO 8601（naive UTC）"),
    windowHours: int = Query(..., description="窗口小时数：1/2/24"),
    db: AsyncSession = Depends(get_db),
    user: SysUser = Depends(get_current_user),
) -> dict:
    """效果验证前后窗曲线数据（09 设计方案 §4.5）：实时拉取不落库。"""
    if windowHours not in (1, 2, 24):
        raise BizError(code="ERR_PARAM", message="windowHours 仅支持 1/2/24", status_code=400)
    point = _parse_iso_datetime(pointTime)  # 复用 waveform 模块解析（naive UTC）
    delta = timedelta(hours=windowHours)
    before = await get_waveform(db, loopId, start_time=_iso(point - delta), end_time=_iso(point))
    after = await get_waveform(db, loopId, start_time=_iso(point), end_time=_iso(point + delta))
    kpi_before = kpi_summary(await latest_snapshot_in_window(db, loopId, point - delta, point))
    kpi_after = kpi_summary(await latest_snapshot_in_window(db, loopId, point, point + delta))
    return success(data={
        "pointTime": _iso(point), "windowHours": windowHours,
        "before": before, "after": after,           # 各含 timestamps/sp/pv/op（LTTB 降采样）
        "kpiBefore": kpi_before, "kpiAfter": kpi_after,
        "afterTruncated": (point + delta) > datetime.now(UTC).replace(tzinfo=None),  # 后窗超当前时刻标记
    })
```

- [ ] **Step 3**: 测试（TDD 先行）

```python
# tests/test_tuning_verification.py
def test_verification_window_split(...):    # pointTime 前后窗边界正确、字段齐全
def test_verification_invalid_window(...):  # windowHours=3 → 400 ERR_PARAM
def test_verification_loop_not_found(...):  # 未知 loopId → 404（get_waveform 既有行为）
def test_verification_kpi_null_side(...):   # 窗口无快照侧 kpiBefore/After 为 null
```

- [ ] **Step 4**: Commit

```bash
git add backend/app/services/kpi_snapshot.py backend/app/api/v1/endpoints/tuning.py backend/app/api/v1/endpoints/handling.py backend/tests/test_tuning_verification.py
git commit -m "feat(tuning): 新增效果验证数据端点 GET /tuning/verification/data"
```

**Phase 2 出口**：矩阵端点返回 5 行算法结果；验证端点返回前后窗曲线+KPI 摘要；`pytest -q` 全绿。

---

# Phase 3 前端三页（T6~T10）

## T6 路由菜单 + API 客户端

**Files:**
- Modify: `frontend/apps/web-antd/src/router/routes/modules/tuning.ts`（空数组改为 3 路由）
- Create: `frontend/apps/web-antd/src/api/tuning.ts`

- [ ] **Step 1**: 路由（对齐 `diagnosis.ts` 模式；`order: 5` 位于处置之后，authority 与设计 §7 一致）

```typescript
const routes: RouteRecordRaw[] = [
  {
    name: 'Tuning',
    path: '/tuning',
    redirect: '/tuning/workbench',
    meta: {
      authority: ['ADMIN', 'EXPERT', 'IC_ENGINEER', 'PE_ENGINEER', 'SPONSOR'],
      icon: 'lucide:sliders-horizontal',
      order: 5,
      title: '整定',
    },
    children: [
      { name: 'TuningWorkbench', path: '/tuning/workbench',
        component: () => import('#/views/tuning/workbench.vue'),
        meta: { authority: ['ADMIN', 'IC_ENGINEER', 'EXPERT'], icon: 'lucide:sliders-horizontal', title: '整定工作台' } },
      { name: 'TuningRecords', path: '/tuning/records',
        component: () => import('#/views/tuning/records.vue'),
        meta: { authority: ['ADMIN', 'EXPERT', 'IC_ENGINEER', 'PE_ENGINEER', 'SPONSOR'], icon: 'lucide:history', title: '整定记录' } },
      { name: 'TuningVerification', path: '/tuning/verification',
        component: () => import('#/views/tuning/verification.vue'),
        meta: { authority: ['ADMIN', 'EXPERT', 'IC_ENGINEER', 'PE_ENGINEER', 'SPONSOR'], icon: 'lucide:git-compare-arrows', title: '效果验证' } },
    ],
  },
];
export default routes;
```

- [ ] **Step 2**: API 客户端 `api/tuning.ts`（复用项目 request 封装，camelCase 入参）：

```typescript
// 方法清单（对应后端端点）：
// getMethods()                GET    /tuning/methods
// identifyStep(body)          POST   /tuning/identify
// identifyHistory(body)       POST   /tuning/identify/history     → { taskId }
// getTaskStatus(taskId)       GET    /tuning/tasks/{taskId}/status
// tuneMatrix(body)            POST   /tuning/tune/matrix
// tuneSingle(body)            POST   /tuning/tune                 （行内参数微调重算）
// comparePids(body)           POST   /tuning/compare
// saveTuningTask(body)        POST   /tuning/tasks
// getTuningTasks(params)      GET    /tuning/tasks
// getTuningTaskDetail(id)     GET    /tuning/tasks/{id}
// getTuningHistory()          GET    /tuning/history
// getVerificationData(params) GET    /tuning/verification/data
```

- [ ] **Step 3**: `pnpm run check:type` 通过；登录后菜单出现「整定」且 3 个子路由可达（页面先放空壳）
- [ ] **Step 4**: Commit `feat(tuning-web): 整定模块路由菜单与 API 客户端`

## T7 整定工作台（单页 4 锚点）

**Files:**
- Create: `frontend/apps/web-antd/src/views/tuning/workbench.vue`
- Create: `frontend/apps/web-antd/src/views/tuning/components/identify-section.vue`（锚点①）
- Create: `frontend/apps/web-antd/src/views/tuning/components/matrix-section.vue`（锚点②）
- Create: `frontend/apps/web-antd/src/views/tuning/components/simulate-section.vue`（锚点③）
- Create: `frontend/apps/web-antd/src/views/tuning/components/confirm-section.vue`（锚点④）
- Create: `frontend/apps/web-antd/src/views/tuning/composables/use-tuning-workbench.ts`（跨锚点共享状态：loopId/辨识结果/勾选组/仿真结果）

- [ ] **Step 1**: 页头：ClpmPageToolbar + 回路选择器；URL `?loopId=xx&from=diagnosis` 进入时预填并显示来源 Alert
- [ ] **Step 2**: 锚点① 辨识区：时间窗 + 路径 tabs（历史=默认/阶跃）→ 历史路径走异步任务（identifyHistory → 轮询 getTaskStatus，细粒度进度条，复用 `use-workbench-task-runner` 模式）；结果卡展示模型类型/K/τ/θ/拟合度/可信度徽标；**D/E 级警示且锚点③④置灰**（Poka-Yoke）
- [ ] **Step 3**: 锚点② 矩阵区：调 `tuneMatrix` 渲染 5 行（算法/P/I/D/特性备注/复选框，最多勾选 2 组——超出禁选并提示）；首行固定当前 PID 对照（数据来自既有 `GET /loops/{id}` 的 `runtimeParams.pidP/pidI/pidD`，缺失显示"未配置"并禁用锚点④）；行内展开算法参数微调（λ_ratio/τc_ratio）→ 单行调 `tuneSingle` 重算
- [ ] **Step 4**: 锚点③ 仿真区：勾选组 + 当前 PID 组提交 `comparePids`；ECharts 叠加 3 色曲线（当前=灰/推荐1=蓝/推荐2=橙）；指标表（上升时间/超调量/调节时间/ITAE + improvement 列）
- [ ] **Step 5**: 锚点④ 确认区：radio 选定最终 1 组 → 「保存方案」调 `saveTuningTask` → 成功提示 + 「创建处置项」入口（跳 `/handling?create=tuning&loopId=xx&tuningRecordId=xx`）
- [ ] **Step 6**: 锚点顺序解锁（前序未完成后续置灰）；`pnpm run check:type` 通过；全流程人工走通一次
- [ ] **Step 7**: Commit `feat(tuning-web): 整定工作台单页 4 锚点`

## T8 整定记录页

**Files:**
- Create: `frontend/apps/web-antd/src/views/tuning/records.vue`
- Create: `frontend/apps/web-antd/src/views/tuning/components/record-detail-drawer.vue`

- [ ] **Step 1**: 顶部统计卡（`getTuningHistory`）：总任务数/已实施（APPLIED+VERIFIED）/平均拟合度/近 7 天新增
- [ ] **Step 2**: 表格（`getTuningTasks` 分页）：回路位号 | 模型类型 | 算法 | 推荐 P/I/D | 拟合度（≥80 绿/≥60 黄/<60 红）| 状态 tag（SIMULATED=蓝/APPLIED=青/VERIFIED=绿）| 创建人/时间 | 操作（详情/去验证）
- [ ] **Step 3**: 详情抽屉：完整模型参数 + 仿真快照图（`simulationResult` 落库 JSON 直接画 ECharts）+ 关联处置项反查（`GET /handling/items?tuningRecordId=`——若无该筛选参数则按 loopId+actionType=TUNING 过滤，标注为已知近似）
- [ ] **Step 4**: 「去验证」跳 `/tuning/verification?loopId=xx&recordId=xx`
- [ ] **Step 5**: Commit `feat(tuning-web): 整定记录页`

## T9 效果验证页 + 共享组件

**Files:**
- Create: `frontend/apps/web-antd/src/components/clpm/tuning-verify-compare.vue`（**共享组件**，props: `loopId`, `pointTime?`, `windowHours?`；内部调 `getVerificationData`）
- Create: `frontend/apps/web-antd/src/views/tuning/verification.vue`

- [ ] **Step 1**: 共享组件 `tuning-verify-compare.vue`：前后窗趋势对比（上下分区同轴 ECharts，上前窗/下后窗，Y 轴对齐）+ PV/OP X-Y 轨迹图（OP 为 x、PV 为 y，前窗=灰/后窗=蓝）+ KPI 摘要条（评分+六率涨跌箭头，无快照侧显示"数据不足"）；`afterTruncated=true` 时后窗标注"数据截至当前时刻"
- [ ] **Step 2**: 独立页 `verification.vue`：查询区（回路选择 + 时点日期时间 + 窗口 1h/2h/24h 三档 segmented + 「对比」按钮）；URL 带 `recordId` 时先查记录反查处置 `submitted_at` 带出时点（决策 #5：带出+可改）
- [ ] **Step 3**: `pnpm run check:type` 通过；真实回路（如 90PIC51212A_PIDA）走通前后窗对比
- [ ] **Step 4**: Commit `feat(tuning-web): 效果验证页与共享对比组件`

## T10 诊断联动入口

**Files:**
- Modify: `frontend/apps/web-antd/src/views/diagnosis/components/diagnosis-detail-modal.vue`（处置建议 Tab）

- [ ] **Step 1**: `category=TUNING` 的建议行操作列加「去整定」：跳 `/tuning/workbench?loopId={loopId}&from=diagnosis`
- [ ] **Step 2**: Commit `feat(tuning-web): 诊断 TUNING 类建议联动去整定`

**Phase 3 出口**：菜单第 5 位「整定」3 页可用；辨识→矩阵→仿真→保存全流程走通；`check:type` ✅。

---

# Phase 4 嵌入联动（T11~T13）

## T11 处置状态回写

**Files:**
- Modify: `backend/app/api/v1/endpoints/handling.py`（`submit_handling` 第 589 行 / `verify_handling` 第 615 行）
- Test: `backend/tests/` 处置既有测试文件追加用例

- [ ] **Step 1**: 先写失败测试

```python
def test_submit_tuning_writes_back_applied(...):   # TUNING 项 + tuning_record_id → submit 后 tuning_record.status=="APPLIED"
def test_verify_effective_writes_back_verified(...): # verify EFFECTIVE → "VERIFIED"；INEFFECTIVE → "SIMULATED"
def test_non_tuning_type_no_writeback(...):        # 非 TUNING 类型不回写
```

- [ ] **Step 2**: 实现（两处流转内各加回写块）

```python
# submit_handling 内状态置 VERIFYING 后：
if item.action_type == "TUNING" and item.tuning_record_id:
    rec = await db.get(TuningRecord, item.tuning_record_id)
    if rec is not None:
        rec.status = "APPLIED"

# verify_handling 内：
if item.action_type == "TUNING" and item.tuning_record_id:
    rec = await db.get(TuningRecord, item.tuning_record_id)
    if rec is not None:
        rec.status = "VERIFIED" if body.verifyResult == "EFFECTIVE" else "SIMULATED"
```

- [ ] **Step 3**: `uv run pytest tests/ -k "handling or tuning" -q` 全绿
- [ ] **Step 4**: Commit `feat(tuning): 处置流转回写整定记录状态`

## T12 处置 VERIFYING 嵌入验证组件

**Files:**
- Modify: `frontend/apps/web-antd/src/views/handling/components/handling-detail-drawer.vue`

- [ ] **Step 1**: VERIFYING 状态操作区 KPI 对比卡上方新增「曲线对比」折叠块，嵌入共享组件：

```vue
<TuningVerifyCompare
  v-if="item.status === 'VERIFYING'"
  :loop-id="item.loopId"
  :point-time="item.submittedAt"
  :window-hours="24"
/>
```

- [ ] **Step 2**: 走查处置 VERIFYING 场景（曲线对比与 KPI 卡同屏）
- [ ] **Step 3**: Commit `feat(handling-web): 验证环节嵌入整定效果曲线对比`

## T13 收尾：全量门禁 + 走查 + 文档同步

- [ ] **Step 1**: 全量门禁

```bash
cd backend && uv run ruff check . && uv run ruff format --check . && uv run pytest -q && uv run alembic check
cd frontend && pnpm run check:type && pnpm run format
```

- [ ] **Step 2**: 五角色冒烟（重点：EXPERT 可操作整定、SPONSOR 只读 403 路径）；亮色截图走查 3 页
- [ ] **Step 3**: 文档同步：`09-整定模块设计方案.md` 状态改为"已实施"；`docs/MVP设计/README.md` 索引更新；AGENTS.md 当前基线表增补整定模块行
- [ ] **Step 4**: 合并

```bash
git checkout main && git merge --no-ff feat/tuning-module
git push github main
```

---

## 任务依赖与风险

| # | 依赖/风险 | 说明 |
|---|---|---|
| 1 | T3 原测试适配量未知 | 114 个 skipped 转执行后可能暴露 MVP 环境差异，预留修复余量；禁止删用例 |
| 2 | T5 依赖 T11 前的 handling helper 提取 | `kpi_snapshot.py` 提取需同步跑 handling 回归，避免影响处置模块 |
| 3 | T7 当前 PID 依赖回路 7 tag 关联完整 | PID_P/I/D 未配置的回路：对照行显示"未配置"、锚点④禁用（设计 §9 风险 #2 已定口径） |
| 4 | 历史辨识大窗口耗时 | 30 天窗可能分钟级，工作台必须异步进度可见（复用 task runner 模式），原端点支持 cancel |
| 5 | DCS PID 口径转换 | `pid_conversion.py` 覆盖型号需在 T7 联调时核查（设计 §9 风险 #3） |
