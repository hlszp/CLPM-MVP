# 重构优化方案 × 代码实际 对照评估报告

| 项目 | 内容 |
|---|---|
| 评估日期 | 2026-08-12 |
| 评估方式 | **只读静态核查**（未修改代码仓库任何文件） |
| 代码仓库 | `/Users/zhangping/DEV/CLPM` |
| 核查基线 | 分支 `feat/ia-rectification-2026-08-10` @ `d741569b`——即 IA 复盘文档自身声明的事实基线（实现契约 v2.11 + 该提交） |
| 评估对象 | 《IA v2.11 第一性原理复盘》§6 的 FP-P0-01~08 闭环断点声明；《IA 驱动 UIUX 重构方案 v1.1》的三项实施前置声明 |
| 总体结论 | **方案与代码实际高度吻合：8 项断点声明全部属实，3 项前置声明全部属实**；另发现 1 处分支管理风险与 1 处疑似重写回归（见 §3、§4） |

---

## 1. 逐项核查结果（FP-P0-01 ~ FP-P0-08）

| ID | 文档声明 | 判定 | 代码证据（文件:行号） |
|---|---|---|---|
| FP-P0-01 | 工作台推荐下一步 9 类动作中 5 类 no-op | ✅ 属实 | `frontend/apps/web-antd/src/views/loop/workbench.vue:394-420`：`CREATE_TRACKER` / `FIX_TAG_CONFIG` / `IMPORT_DATA` / `RECORD_IMPLEMENTATION` / `VERIFY_EFFECT` 五个 case 直接 `break`（含 `CONTINUE_MONITORING` 共 6 个空分支）；仅 `RUN_ASSESSMENT` / `RUN_DIAGNOSIS` / `RUN_TUNING` 打开弹窗 |
| FP-P0-02 | 闭环时间线 viewDetail/verify 事件父组件未绑定 | ✅ 属实 | 子组件 `components/monitor/workbench-tracker-timeline.vue:117,122` 确实 `emit('viewDetail')` / `emit('verify')`；父组件 `workbench.vue:1631-1638` 使用 `<WorkbenchTrackerTimeline>` 时只绑了 `:tracker` 和 `:unavailable`，**无任何事件监听**，事件直接丢失 |
| FP-P0-03 | 三类坏链接 | ✅ 属实 | ① `views/loop/manage.vue:1450` 跳转 `/loop/aas`，但 `router/routes/modules/loop.ts` 仅定义 `/loop`、`/loop/workbench`、`/loop/detail/:id`，**无此路由**（`views/loop/aas.vue` 文件存在但无路由可达）；② `views/task/detail.vue:156` 回跳 `/tasks`，但 `task.ts` 仅定义 `/tasks/:taskId`，**无 `/tasks` 列表路由**；③ `views/tuning/workbench.vue:487` 的 returnTo 用 query 形式 `/diagnosis/detail?loopId=`，而 `diagnosis.ts:103` 路由是路径参数 `/diagnosis/detail/:loopId`，**形式不匹配** |
| FP-P0-04 | 诊断任务/关注队列不消费 loopId；plantNodeId 误当 loopId | ✅ 属实 | ① `views/diagnosis/tasks.vue` 全文 **0 处 `useRoute`**，传入的 `?loopId=` 完全被忽略；② `views/monitor/attention.vue:506-507` 只消费 `route.query.source` / `eventId`，不消费 `loopId`；③ `attention.vue:483` 跳转预警记录时 `loopId: query.plantNodeId \|\| undefined`——把装置节点筛选值塞进 loopId 参数，误传实锤 |
| FP-P0-05 | Config 父路由仅 ADMIN，IC/PE 子页查看权死配置 | ✅ 属实 | `router/routes/modules/config.ts:24` 父级 `authority: ['ADMIN']`，而子路由 `/config/tag`(47)、`/config/loop`(58)、`/config/factory`(70)、`/config/ledger`(80)、`/config/datasource`(87) 均声明 `['ADMIN','IC_ENGINEER','PE_ENGINEER']`；过滤实现 `packages/@core/base/shared/src/utils/tree.ts:59-69` 的 `filterTree` **节点不过滤通过就不递归子节点**——父级被 ADMIN 限制后，IC/PE 子页永无机会被评估，确为死配置 |
| FP-P0-06 | LoopLink 与诊断任务操作缺角色感知 | ✅ 属实（基线分支上） | ① `components/clpm/loop-link.vue` 全文无任何 role/authority/permission 逻辑；② `views/diagnosis/tasks.vue` 在当前基线分支上 **0 处** v-permission/v-access/AccessControl（见 §4 回归说明） |
| FP-P0-07 | Tracker 前端 VERIFYING 与旧 IMPLEMENTED 混用，MOC 表单只在旧状态显示 | ✅ 属实 | `views/diagnosis/tracker.vue:190,193` 状态下拉同时提供"验证中 VERIFYING"和"已实施 IMPLEMENTED"；`:400` `showMocFields` 仅当 `status === 'IMPLEMENTED'`；`:481-482` MOC 字段也只在 IMPLEMENTED 时提交。后端 `backend/app/services/tracker.py:37-67` canonical 状态机为 `PENDING→IN_PROGRESS→VERIFYING→CLOSED/REOPENED`（IMPLEMENTED 仅为历史兼容映射），`:115` 标记 VERIFYING 强制实施 PID（Poka-Yoke）——前端选 VERIFYING 时 MOC 表单不出现，与后端强制校验错位 |
| FP-P0-08 | 默认首页存在多套定义 | ✅ 属实 | ① 后端 `backend/app/services/auth.py:101-108` `ROLE_DEFAULT_HOME` 全角色 `/dashboard`；② 前端 `frontend/apps/web-antd/src/store/auth.ts:27-34` `ROLE_DEFAULT_HOME`：EXPERT→`/diagnosis`、SPONSOR→`/metric`、其余 `/dashboard`；③ `backend/app/schemas/auth.py:115` 与 `frontend/apps/web-antd/src/api/core/user.ts:14` 另有字段级默认值 `/dashboard`。前端 auth.ts:19-20 注释自证："后端归属另一波次整改，前端先行落地本表"——多套定义且 EXPERT/SPONSOR 内容分叉。注：三个落点路由均真实存在（`/dashboard`→redirect、`/metric`、`/diagnosis`），问题是**事实源不唯一**而非 404 |

## 2. 方案 v1.1 三项实施前置声明核查

| 前置声明 | 判定 | 代码证据 |
|---|---|---|
| 闭环案例需 `trackerId-first` API（当前没有） | ✅ 属实 | `backend/app/api/v1/endpoints/diagnosis.py:1059-1220`：Tracker 全部端点按 **loopId** 寻址（`PATCH /tracker/{loop_id}/status`、`GET /tracker/{loop_id}/timeline`、`POST /tracker/{loop_id}/export`、verification-config、effectiveness），无 trackerId 维度 |
| 需先补 `interventionType` + 按类型校验（当前没有） | ✅ 属实 | 全后端 + 全前端 **0 处** `interventionType` / `intervention_type`（grep 无命中）；Tracker 的 MOC/PID 校验对全部案例一刀切，与方案描述一致 |
| `useLoopContext` 无生产消费者，应废止 | ✅ 属实 | 除定义文件 `composables/use-loop-context.ts` 与测试外，**无任何生产代码引用** |

## 3. 分支拓扑风险（方案之外的新发现）

核查期间确认仓库分支处于**分叉且活跃变动**状态（本次只读未干预）：

- `main` @ `90e30733`（2026-08-12 07:29）= 整改前基点 `50e0fdfb`（08-10）+ 1 个样式提交，**不包含** 27 个 IA P2/P3 整改提交、契约 v2.11 及两份 IA 决策文档；
- 上述 27+ 个提交在 `feat/ia-rectification-2026-08-10`（顶端 `d741569b`，即文档事实基线）；
- `backup/ia-rectification-20260812`（顶端 `a32eef3e` "回退前备份 IA Phase2/3 未提交成果"）另含 IA 复盘文档提交 `f42642bb` 和一份未提交成果的备份；
- 核查会话期间 HEAD 从 main 被切换到 `feat/ia-rectification-2026-08-10`——**仓库正被其他会话活跃操作**。

**含义**：若从 main 开工做 Phase 0 止血，会丢掉 27 个已完成的整改提交；若从 feature 分支开工，需知它与 main 已分叉。整改动手前必须先由人决定主干归并策略。

## 4. 疑似重写回归（方案之外的新发现）

`views/diagnosis/tasks.vue` 在 main（`90e30733`）上有 **4 处 `v-permission`** 角色指令，在 feature 分支的 P2-16-B2 Tab 化重写（该文件两提交间变更 1383 行）后变为 **0 处**——与契约"可见即有权"原则相悖。无法从静态代码判断是有意移除（改由其他层控制）还是重写遗漏，建议人工确认；若为遗漏，应并入 FP-P0-06 一并修复。

## 5. 结论与建议

1. **前序方案的事实基础可信**：IA 复盘的 8 项断点、方案 v1.1 的 3 项前置声明，在其声明的基线提交 `d741569b` 上全部复现，无夸大、无过时。方案可以按"先修事实、再做标杆"的顺序执行，不需要先返工核对。
2. **动手前的真正门槛是分支归并**（§3），而不是方案本身。建议先明确：feature 分支 27 个整改提交是否、何时、以何种方式合回 main。
3. **建议把 §4 的 tasks.vue 权限指令问题加入 Phase 0 止血清单**，与 FP-P0-06 同批处理。
4. 本报告所有证据均为 `file:line` 静态引用，未做运行态复现（点击无反馈、403 等动态行为建议按方案 §14 协议在运行时逐一验证）。

---

*附：核查覆盖文件清单——workbench.vue、workbench-tracker-timeline.vue、workbench-next-action.vue、attention.vue、diagnosis/tasks.vue、diagnosis/tracker.vue、loop-link.vue、task/detail.vue、tuning/workbench.vue、loop/manage.vue、router/routes/modules/{loop,diagnosis,task,config,monitor}.ts、store/auth.ts、api/core/user.ts、composables/use-loop-context.ts、packages/utils generate-routes-frontend.ts、@core tree.ts、backend services/{tracker,auth}.py、schemas/auth.py、api/v1/endpoints/diagnosis.py。*
