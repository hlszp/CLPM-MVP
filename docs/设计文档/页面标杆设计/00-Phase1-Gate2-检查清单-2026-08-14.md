# Phase 1 共享载体任务包 + Gate G2 自查检查表

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 文档日期 | 2026-08-14 |
| 文档性质 | 实施前置工作文档，含三部分：① 四页标杆验收清单勾选表 ② Phase 1 共享载体任务包 ③ Gate G2 八项条件自查检查表 |
| 事实基线 | 实现契约 v2.11 ｜ PRD v6.2 ｜ UI/UX v6.3 ｜ 代码 main @ `a8f96740`（2026-08-14 核查） |
| 上游文档 | [总纲](./00-总纲-IA-UX-UI系统性重构最佳实践方案-2026-08-12.md) §4.2（Phase 1 共享载体）/ §4.3（Gate G2）/ §11（页面设计通用规则）｜ [前置核查报告](./00-前置-FP-P0代码只读核查报告-2026-08-12.md) ｜ 四页标杆设计文档 |
| 核查方式 | 五路并行子代理只读核查：01 工作台 19 项 / 02 回路列表 11 项 / 03 关注队列 11 项 / 04 系统概览 12 项 / Phase 1 共享载体现状调研 |
| 安全边界 | 平台只读 DCS、不下写参数；只输出建议、证据、风险、回退与人工实施留痕 |

---

## 目录

- [第一部分：四页标杆验收清单勾选表 + 待办项](#第一部分四页标杆验收清单勾选表--待办项)
  - [1.0 概述与文档对齐汇总](#10-概述与文档对齐汇总)
  - [1.1 01 回路工作台](#11-01-回路工作台v110代码已实施-phase-1)
  - [1.2 02 回路列表](#12-02-回路列表v15待评审代码未启动)
  - [1.3 03 关注队列](#13-03-关注队列v12待评审代码已对标优化)
  - [1.4 04 系统概览](#14-04-系统概览v31待评审代码已对标重写)
- [第二部分：Phase 1 共享载体任务包](#第二部分phase-1-共享载体任务包)
  - [2.1 调研结论](#21-调研结论决定任务包形态)
  - [2.2 任务包 P1-A：typed OperationalContext](#22-任务包-p1-atyped-operational-context先做是其他三资产的基础)
  - [2.3 任务包 P1-B：Decision Dock](#23-任务包-p1-bdecision-dock决策坞)
  - [2.4 任务包 P1-C：Evidence Canvas](#24-任务包-p1-cevidence-canvas证据画布)
  - [2.5 任务包 P1-D：State 组件族](#25-任务包-p1-dstate-组件族六态一等公民)
  - [2.6 任务包 P1-E：迁移与门禁](#26-任务包-p1-e迁移与门禁收口)
  - [2.7 总工期与依赖](#27-phase-1-总工期与依赖)
- [第三部分：Gate G2 八项条件自查检查表](#第三部分gate-g2-八项条件自查检查表)
  - [3.1 八项条件总览](#31-八项条件总览)
  - [3.2 G2-1：18 项强制门禁 + Q-13~Q-16 语义门全过](#32-g2-118-项强制门禁--q-13q-16-语义门全过)
  - [3.3 G2-2：品质分 ≥85 且无 blocker](#33-g2-2品质分-85-且无-blocker)
  - [3.4 G2-3：四角色任务测试无 403/无不可执行 CTA](#34-g2-3四角色任务测试无-403无不可执行-cta)
  - [3.5 G2-4：3 秒识别测试 4/5](#35-g2-43-秒识别测试-45)
  - [3.6 G2-5：四分辨率亮色 + 暗色代表状态配对](#36-g2-5136614401920768-亮色通过--暗色代表状态配对)
  - [3.7 G2-6：首屏与 BFF p95 达标](#37-g2-6首屏与-bff-p95-达标)
  - [3.8 G2-7：产品负责人明确选择 S1/S2/S3](#38-g2-7产品负责人明确选择-s1s2s3)
  - [3.9 G2-8：临时数据与 mock 清零](#39-g2-8临时数据与-mock-清零)
  - [3.10 Gate G2 评审流程建议](#310-gate-g2-评审流程建议)
  - [3.11 G2 当前阻断项汇总](#311-g2-当前阻断项汇总基于第一部分核查)
- [后续建议](#后续建议)

---

# 第一部分：四页标杆验收清单勾选表 + 待办项

## 1.0 概述与文档对齐汇总

本部分基于代码 main @ `a8f96740` 的只读核查，逐条对照四页标杆设计文档 §5（或 §6）的验收清单，判定每项"已落地 ✅ / 部分落地 🟡 / 未落地 ❌"，并按优先级（P0/P1/P2/P3）汇总待办项。

### 文档对齐汇总表

| 页面 | 文档头状态建议 | 验收清单勾选 | 关键风险 |
|---|---|---|---|
| 01 回路工作台 | "已定稿 v1.10" → "Phase 1 已实施 / Phase 2 待启动" | 3✅ / 12🟡 / 3❌ | 设计文档自身 §7/§8/§10/§13 多处自相矛盾（卡数/横道项数/缩略图），需先定稿 |
| 02 回路列表 | 维持"修订版待评审 v1.5" | 1🟡 / 10❌（基本未启动） | v1.5 必须先人评审拍板；后端 E-1/E-2/E-3 三项扩展是前置 |
| 03 关注队列 | "修订版待评审 v1.2" → "已对标落地（待评审收口）" | 5✅ / 5🟡 / 1❌ | attention p95≈610ms 超 500ms 目标，是唯一阻断项 |
| 04 系统概览 | "重构版待评审 v3.1" → "已对标落地（待评审收口）" | 4✅ / 6🟡 / 2❌ | 下钻链路三处断裂（11 项），是"用户操作闭环未通"的典型例证 |

### 落地状态统计

- 已落地 ✅：3 + 0 + 5 + 4 = **12 项**
- 部分落地 🟡：12 + 1 + 5 + 6 = **24 项**
- 未落地 ❌：3 + 10 + 1 + 2 = **16 项**
- **总计**：52 项验收点，整体完成度约 23% 已落地 + 46% 部分落地

---

## 1.1 01 回路工作台（v1.10，代码已实施 Phase 1）

**文档头状态建议**：从"已定稿 v1.10"→ "**Phase 1 已实施 / Phase 2 待启动**"

**现状文件**：`frontend/apps/web-antd/src/views/loop/workbench.vue`（3579 行，commit `552e3821` 已合入 main）

### 19 项验收清单逐条核查

| 序 | 验收项 | 落地 | 代码证据 / 待办 |
|---|---|---|---|
| 1 | 主画布为过程趋势且占主区域 ≥45% | 🟡 | CSS Grid `1fr` 行 + R4 占主区，理论 ≈48%；**待办**：1366×768 截图实测确认 |
| 2 | R2 状态条中文等级+动态阈值+空中性灰+生命周期内联 | ✅ | `useScoreColor(summaryScore, gradingThresholds)` + `getGradingThresholdsApi()` onMounted 加载；空值→`NEUTRAL`；生命周期 5 段紧凑态 `.wb-r2__lifecycle` |
| 3 | KPI 历史图告警线取定级阈值（默认 60 降级）无硬编码 | ✅ | `alarmLine = gradingThresholds.value.find(t => t.level===4)?.minScore ?? 60`；KPI 图 `markLine.data[0].yAxis = props.alarmLine` |
| 4 | R4 双模式切换+共享时间窗+双轴+折线棒图混合+图例自解释+五档 | 🟡 | Segmented 共享 `timeWindow`，混合图与图例已实现；**待办**：①`TREND_WINDOW_MAP` 仅 8h/24h/72h，168h/custom 过程趋势返回 null（loop.ts:34 后端不支持 last_7_days）②KPI 模式 Y 轴无单位标注 |
| 5 | 左脊柱：树到装置/单元级+回路列表独立+搜索双支持+沉浸折叠 28px | ✅ | `PlantNodeApi.NodeType = 'AREA'\|'FACTORY'\|'UNIT'`；`buildPlantTree` 递归；回路列表独立区；`filteredLoopList` 三字段搜索；`sidebarCollapsed` + 28px 细条 |
| 6 | §2.5 溯源清单 20 项逐项落地 | 🟡 | 6 个端点全部存在；**待办**：①实时点值设计要 `summary.runtime`，实际用 `selectedLoop.currentValues`（WS 推送，功能等价但溯源清单不符）②过程趋势自定义起止未支持（同序号 4） |
| 7 | 布局结构：时间窗画布头第一行+图例第二行+决策栏不通高+底部四卡全宽 | 🟡 | 时间窗/图例位置正确；决策栏仅跨 2 行；**待办**：实际 5 卡（多了验证卡），设计 §6 说"四卡"§2.2 说"四区"自相矛盾——需统一口径 |
| 8 | 信息密度：五档+8 项横道+诊断 2×2 网格+整定缩略图+雷达 7 读数 | 🟡 | 时间窗五档+自定义分钟精度 ✅；雷达 7 读数 ✅；**待办**：①横道实际 7 项（设计 §8 要 8 项、§13 要"其余 6 项"，设计自相矛盾）②诊断扩展 6 项横道归一化 0-100，设计要"2×2 网格 + 原始量纲"③整定缩略图设计 §8 与 §10 自相矛盾，代码取"已删除"口径 |
| 9 | 实时值与负向语义：图例行 SP/PV/OP/MODE+等宽+负向横道+底部降高 20px | 🟡 | 图例行实时点值 + `tabular-nums` 等宽；`WorkbenchMetricBars :negative="true"` 负向语义；**待办**：①数据源偏差（同序号 6）②"底部降高 20px"无对比基准 |
| 10 | 数据诚实：结构化+规则模板标注+LLM 不渲染不留白+无静态结论+缩略图删除 | 🟡 | 诊断/整定卡全结构化；LLM 仅抽屉门控；**待办**：诊断卡无"规则模板建议一行（标注来源）"——CSS 类 `.wb-r5__diag-reasoning` 存在但模板未渲染 |
| 11 | 整定卡布局：6 项结构化指标分行均分撑满 | 🟡 | `.wb-r5__tune-rows` flex:1 均分 ✅；**待办**：实际 7 行（多"状态"行），设计要 6 项——需合并或修订设计 |
| 12 | 时间窗联动三层语义：A 类随窗+B 类固定快照+窗口可见+C 类不联动+无闪烁 | 🟡 | A/B/C 三层语义已实现，无闪烁；**待办**：事件标记 `computedEventMarks` 未按时间窗过滤（设计 §2.6 要求"只显示落在窗口内的标记"） |
| 13 | R5 证据四区：六轴雷达中心评分+等级+横道 6 项+1366 不裁切 | 🟡 | 六轴雷达中心评分+等级 ✅；**待办**：横道 7 项（含综合评分）vs 设计 6 项；5 卡 vs 设计 4 区；1366 不裁切需实测 |
| 14 | 8 个 fixture F1-F8 全部通过 | ❌ | **待办**：e2e/ 无 fixture 测试，`workbench-mock.ts` 存在但 `USE_MOCK=false` 且未作为 fixture 驱动；需新建 `e2e/tests/workbench-fixtures.spec.ts` |
| 15 | 9 类 nextAction 全部有真实行为或带原因禁用态（FP-P0-01） | 🟡 | `handleNextAction` switch/case 9 类全通，无空 break；禁用态显示 `disabledReason`；**待办**：无 E2E 守护测试 |
| 16 | 时间线 viewDetail/verify 事件有绑定、有测试（FP-P0-02） | 🟡 | `handleTrackerViewDetail`/`handleTrackerVerify` 已定义并绑定；**待办**：无单测/E2E |
| 17 | S1~S5 走查剧本 E2E 全绿 + S6 真人 3 秒测试 ≥4/5 | ❌ | **待办**：无 S1-S5 E2E 文件，无 S6 真人测试记录 |
| 18 | 四角色"可见即有权"抽查无 403 | 🟡 | `e2e/tests/roles-smoke.spec.ts` 覆盖 ADMIN/IC/PE/EXPERT 不 403；**待办**：SPONSOR 整页 403 与设计"应可见只读"口径偏差——需对齐权限矩阵 |
| 19 | 页面品质评分 ≥90 且无 blocker | ❌ | **待办**：无品质评分记录，需人工走查 |

### 01 待办项汇总（按优先级）

- **P0（阻断验收）**：
  1. fixture F1-F8 测试缺失（序号 14）——新建 e2e 测试文件，按 fixture 特征构造数据驱动断言
  2. S1-S5 走查剧本 E2E + S6 真人测试缺失（序号 17）——新建 `e2e/tests/workbench-walkthrough.spec.ts` 覆盖 S1-S5；S6 需组织真人测试留档
  3. 页面品质评分缺失（序号 19）——走查产出 ≥90 分证据

- **P1（功能缺口，影响数据诚实/闭环）**：
  4. 诊断卡规则模板建议文案未接入（序号 10）——CSS 类 `.wb-r5__diag-reasoning` 存在但模板未渲染
  5. 9 类 nextAction + viewDetail/verify 无 E2E/单测（序号 15、16）——FP-P0-01/02 回归风险无保障
  6. SPONSOR 权限口径偏差（序号 18）——设计要求可见只读，实际整页 403
  7. 事件标记未按时间窗过滤（序号 12）——`computedEventMarks` 取全部标记

- **P2（信息密度/布局对齐）**：
  8. 168h/custom 过程趋势未支持（序号 4、6）——`TREND_WINDOW_MAP` 只映射 8h/24h/72h，与设计 §2.4 矛盾
  9. 指标横道 7 项 vs 设计 8 项（序号 8、13）——设计自身不一致，需对齐
  10. 诊断扩展指标非 2×2 网格且非原始量纲（序号 8）——需改布局+保留原始量纲
  11. 整定卡 7 行 vs 设计 6 行（序号 11）——多了"状态"行
  12. 底部 5 卡 vs 设计 4 卡（序号 7、13）——验证卡额外存在，设计自相矛盾
  13. KPI 模式 Y 轴无单位（序号 4）——设计要求"含单位"

- **P3（数据源/实测项）**：
  14. 实时点值数据源偏差（序号 6、9）——设计要 `summary.runtime`，实际用 WS 推送
  15. 主画布占比 ≥45% 需截图实测（序号 1）——CSS 结构理论 ≈48%
  16. 1366 宽度不裁切需实测（序号 13）——5 卡 flex:1 均分，200px 高度偏紧

### 01 设计文档自身矛盾点（需先定稿）

| 矛盾点 | §8 表述 | §10/§13 表述 | 建议口径 |
|---|---|---|---|
| 卡数 | "四卡" | "四区"含验证 | 统一为"四区（评估/诊断/整定/验证）"，验证卡计入 |
| 横道项数 | "8 项主要指标" | "其余 6 项"（不含综合评分） | 8 项含综合评分，分两行：综合评分独占 + 其余 7 项 |
| 整定缩略图 | "含近 24h PV 趋势缩略图" | "已删除" | 取"已删除"（数据诚实优先） |
| 诊断扩展指标 | "2×2 网格（饱和率/粘滞系数/稳定时间/振荡率，原始量纲）" | 实际 6 项横道归一化 | 修订为"6 项横道（含坏值率/振荡率/行程指数），负向语义" |

---

## 1.2 02 回路列表（v1.5 待评审，**代码未启动**）

**文档头状态建议**：维持"**修订版待评审 v1.5**"——评审通过后授权实施

### 重要路径错配纠正

**02 标杆设计 §0 自述现状文件为 `components/monitor/loop-fleet-view.vue`（741 行，内嵌于 `loop/workbench.vue` 的 table 模式）**，**不是 `views/loop/manage.vue`**。

- `manage.vue` 实为**回路配置页**（`LoopManage`，路由 `/config/loop`，定义于 `router/routes/modules/config.ts:59`），是结构性的回路 CRUD/批量/导入导出页
- `manage.vue` 在 v6.1 反而**移除了评分列**（行 334 注释），与本页"评分升序默认列"的核心诉求相反
- 当前 `/monitor/loops` 路由根本不存在（前端仅 `workbench.vue:600` 留了一处 `router.push({ path: '/monitor/loops' ... })` 的死链）

### 11 项验收清单逐条核查

| 序 | 验收项 | 落地 | 代码证据 / 待办 |
|---|---|---|---|
| 1 | `/monitor/loops` canonical + `/loop/monitor` 重定向 + 工作台 table 守卫移除 | ❌ | `/monitor/loops` 路由不存在；`/loop/monitor` 重定向到 `/monitor/loop-workbench?view=table`；`monitor.ts:78-87` 仍带 EXPERT 守卫无 SPONSOR |
| 2 | 默认评分升序+E-1/E-2/E-3 扩展+降级口径标注 | ❌ | `loop-fleet-view.vue` 无 sort；后端 `/loops/monitor` 无 sort 参数 |
| 3 | 卡片视图 ≤100 可用+>100 禁用+原因+无 sparkline | ❌ | 后端 `view=card` 参数前端未接通 |
| 4 | 等级标签五档中文+动态阈值+空评分中性灰 | ❌ | loop-fleet-view.vue 未对接 grading-thresholds |
| 5 | R2.5 等级速览卡（v1.1/v1.3） | ❌ | 无等级速览卡区块 |
| 6 | SP/PV/OP/MODE 默认列+等宽+PV 偏离显红+快照时间标注 | ❌ | loop-fleet-view.vue 列结构与 v1.4 13 列清单不符 |
| 7 | 默认 13 列（v1.4 清单）+回路等级独立列+性能评分带日Δ | ❌ | 列结构未对标 |
| 8 | 性能评分列对齐（v1.4/v1.5） | ❌ | 无评分列对齐规则 |
| 9 | 操作列"工作台 ↗"（v1.4）三入口一致 | ❌ | 当前 `workbench.vue:600` 有 `router.push('/monitor/loops')` 死链（404） |
| 10 | 六态全实现（empty 区分两种文案）+ 1000 回路 p95 | 🟡 | loop-fleet-view.vue 实现部分空态，未区分两种 empty 文案 |
| 11 | SPONSOR/EXPERT 可见无工程动作+导出限 ADMIN/IC/PE+无 no-op+无时间窗+组态默认收起 | ❌ | EXPERT/SPONSOR 守卫未撤销 |

### 02 待办项汇总

- **前置**：v1.5 标杆设计人评审拍板（执行纪律要求）

- **前端 P0**：
  1. 新建 `/monitor/loops` 路由+独立回路列表组件（从 `loop-fleet-view.vue` 抽取迁移）
  2. `/loop/monitor` 重定向改指 `/monitor/loops`
  3. 撤销 workbench table 模式+EXPERT/SPONSOR 守卫
  4. 修 `workbench.vue:600` 死链

- **后端 P0**：
  1. E-1 `aggregate` 聚合返回（五档计数/平均分/恶化数/MODE 分布）
  2. E-3 等级/状态筛选参数（`grade` / `loopStatus` / `kpiStatus`）
  3. E-2 服务端排序参数（`sort` = `score_asc` / `score_delta_worsened` / `tag_name`）

- **压测 P0**：
  1. 1000 回路 fixture（3 装置 × 12 单元 × 1000 回路）
  2. 首屏 p95 压测

### 关键澄清

核查发现后端 `/loops/monitor` 的 `pageSize` 上限实为 `le=100`（不是任务描述中的 500），与标杆 §2.2 R5 "pageSize≤100 已有"一致。需在 IDS/契约文档中显式固化"单来源上限 100"以避免后续误传为 500。

---

## 1.3 03 关注队列（v1.2 待评审，代码已对标优化）

**文档头状态建议**：从"修订版待评审 v1.2"→ "**已对标落地（v1.2 待评审收口）**"

**现状文件**：`frontend/apps/web-antd/src/views/monitor/attention.vue`（1169 行）

### 11 项验收清单逐条核查

| 序 | 验收项 | 落地 | 代码证据 / 待办 |
|---|---|---|---|
| 1 | 路由保持+六态全实现（empty-green/empty-filter 严格区分） | 🟡 | 路由 ✅；empty-green/empty-filter/loading ✅；**待办**：error/denied/stale 三态未实现 |
| 2 | R2/R2.5/来源 chip 全部来自 aggregates 单一聚合源 | ✅ | 前端零计算，直接用 `res.aggregates` |
| 3 | 分诊表 v1.2 八列+按回路合并+行内展开+紧急红底/高组左红条 | ✅ | 八列定义齐全；`_group_items_by_loop` 服务端合并；紧急/高组样式 ✅ |
| 4 | 计数双口径+服务端 G3 合并 | ✅ | 双口径显性；G3 已修复（`monitor_attention.py:852-964`） |
| 5 | 无列排序+帮助面板四节与 docstring 逐字一致 | 🟡 | 无排序 ✅；**待办**：帮助面板中文简化表述与 docstring 非逐字一致 |
| 6 | 操作列 primaryAction+写动作折叠+二次确认+SPONSOR 只读+PE/EXPERT 禁用显因 | 🟡 | 大部分已实现；**待办**：ACKNOWLEDGE/MARK_FALSE_POSITIVE 无二次确认；主表行工作台按钮未视觉强化 |
| 7 | 下钻携带 from+上下文恢复+深链接生效 | 🟡 | from 携带 ✅；深链接 ✅；**待办**：page 未写入 URL，无显式 keepAlive |
| 8 | 截断语义三处显性+500 上限文案兜底 | 🟡 | 截断提示条/分页条/500 兜底 ✅；**待办**：R2.5 右侧缺"已加载/共"显性截断 |
| 9 | 装置·单元列依赖 G1+plantNodeId 递归依赖 G2 | ✅ | **G1/G2 已修复**（`_build_node_name_map` + `collect_descendant_node_ids`） |
| 10 | summary/rankReasons 全部后端规则模板，无 LLM | ✅ | 后端纯规则模板；前端无 AI 组件导入 |
| 11 | 1000 回路/10000 项 p95 ≤500ms 压测通过 | ❌ | **待办**：当前 p95≈610ms，超目标 20%；需 SQL 优化/Redis 缓存/线程池改造后复测 |

### G1/G2/G3 后端缺口专项核查

| 缺口 | 状态 | 证据 |
|---|---|---|
| **G1** 五聚合器 join 单元表填 unit_name | ✅ 已修复 | 3 个聚合器函数均通过 `_build_node_name_map`（`monitor_attention.py:233-286`）预构建 loop_id→(area_name, unit_name) 映射并注入 |
| **G2** plantNodeId 递归查询 | ✅ 已修复 | `list_attention`（`:994-1016`）按节点类型分流：FACTORY/AREA → `collect_descendant_node_ids`（`plant_node_tree.py:20`，WITH RECURSIVE CTE） |
| **G3** 服务端按回路合并 | ✅ 已修复 | `_group_items_by_loop`（`:852-964`）：分组主键=loop_id；组优先级=组内最高；分页按组切片 |

### 03 待办项汇总（按优先级）

- **前端 P0**：R2.5 右侧补"已加载 X 回路组 · Y 项 / 共 X 回路组 · Y 项"截断语义

- **前端 P1**：
  1. ACKNOWLEDGE/MARK_FALSE_POSITIVE 补二次确认
  2. 返回恢复页码显式保障（page 写入 URL 或显式 keepAlive）

- **前端 P2**：
  1. 补全六态空态（error/denied/stale）
  2. 帮助面板文案与 docstring 对齐
  3. 主表行工作台按钮视觉强化

- **后端 P0**：attention 端点 p95 降至 ≤500ms
  - 建议项：SQL UNION ALL+窗口函数下推排序/分页、Redis 缓存 TTL=30s、realtime_subscriber 写入移至独立线程池

- **压测 P0**：E-3 性能契约复测后更新 `perf-report-MW-P5-04.md`

---

## 1.4 04 系统概览（v3.1 待评审，代码已对标重写）

**文档头状态建议**：从"重构版待评审 v3.1"→ "**已对标落地（v3.1 待评审收口）**"

**现状文件**：`frontend/apps/web-antd/src/views/dashboard/workbench.vue`（990 行，commit `5fe1de8b`）

### 12 项验收清单逐条核查

| 序 | 验收项 | 落地 | 代码证据 / 待办 |
|---|---|---|---|
| 1 | 一屏 1440×900 不滚动+四横区结构完整 | 🟡 | 四横区结构 ✅；**待办**：高度未严格按 44/90/460/250 分配；无 1440×900 实测验证 |
| 2 | 锚点层=R2 不随树节点变化+联动层=R3/R4 随树聚焦 | ✅ | `loadAnchorData` 仅 watch(timeWindow)；`loadScopeData` watch(scope) |
| 3 | 左栏工厂模型树与 plant-node-tree 同风格 | 🟡 | 搜索/展开折叠/类型图标/尾部评分+回路数 ✅；**待办**：未用 antd Tree 组件，手写 `<div v-for>`，与同风格要求不严格成立 |
| 4 | R2 评分两位小数等宽不加粗+五档标签+三率+参评/INCONCLUSIVE+实时自控率口径标注 | ✅ | 全部实现，实时自控率标注 `(实时口径)` |
| 5 | 中栏趋势：时间轴+数值轴+五档阈值参考线走配置+粒度标注+E-T1 降级 | 🟡 | 轴齐全+粒度标注+E-T1 降级标注 ✅；**待办**：阈值参考线硬编码 `v === 60`，未走 `/configs/grading-thresholds` |
| 6 | 右栏 Top10 跟随范围+每行"工作台↗"+关键事项 chips 进 03 | ✅ | Top10 跟随+工作台↗+chips 跳转 03 ✅ |
| 7 | R4 帕累托三级范围+柱三率折线评分+图例 | 🟡 | 范围语义+柱折+图例 ✅；**待办**：①单元范围设计要"升序"代码统一降序（设计 §116 与 §173 自相矛盾需澄清）②柱图颜色硬编码 |
| 8 | R4 扇形图本级+下一级+无快照灰色环+五档计数 E-1 | 🟡 | 本级+下一级+灰色环+降级标注 ✅；**待办**：donutSVG 只画评分单色环，E-1 后端就绪但前端未消费 |
| 9 | 时间窗五档全域同口径联动+页头时点与口径标注随动 | ✅ | 五档联动+时点+口径标注 ✅ |
| 10 | 无例外列表/待办门户/入口卡+链路健康指示灯+无 LLM | 🟡 | 无例外/入口卡/LLM ✅；**待办**：①链路指示灯硬编码 `bg-emerald-500` 恒绿，未对接 `GET /datasource/health` ②展开面板文案硬编码 |
| 11 | 下钻三出口携带 from=overview+01 面包屑"← 系统概览" | ❌ | **严重缺口**：①`/monitor/loops` 路由不存在（goToLoopList 会 404）②01 workbench 不读 `route.query.from`，不显示面包屑 ③03 attention 不读 `plantNodeId`，范围上下文丢失 |
| 12 | 六态全实现+分区容错+1000 回路 p95+5 接口并行 | ❌ | **待办**：①无统一加载/错误态 UI ②`loadAnchorData` 用 Promise.all 整体失败 ③缺 `/nodes/overview` 第 5 接口调用 ④无压测报告 |

### 后端扩展项核查

| 扩展项 | 状态 | 证据 |
|---|---|---|
| **E-T1** 快速/准确趋势端点 | ❌ 未落地 | `node_performance.py:760-771` `get_node_trend` 的 `series` 列表只含 5 项（score/auto_loop_ratio/realtime_auto_rate/steady_rate/effective_auto_rate），无 fast_rate/accuracy_rate。模型字段已存在（line 43-44），仅需加 2 行 `series()` 调用 |
| **E-1** 节点 overview statusDistribution | 🟡 后端已实现但口径偏差 | `node_performance.py:861-933` 返回 `statusDistribution`，但**按节点 status 计数**（SUCCESS/INCONCLUSIVE），**非设计 §79 要求的"五档回路占比"**；前端未消费 |

### 04 待办项汇总（按优先级）

- **前端 P0**：
  1. 修 `/monitor/loops` 死链（goToLoopList 404）——需补 02 回路列表路由或改为已存在路径
  2. 01 workbench 补 `route.query.from` 消费+"← 系统概览"面包屑
  3. 03 attention 补 `route.query.plantNodeId` 读取

- **前端 P1**：
  1. R3 趋势阈值参考线对接 grading-thresholds
  2. R1 链路指示灯对接 `/datasource/health`
  3. 六态完整+`loadAnchorData` 改 `Promise.allSettled`
  4. R2 五档迷你条走 `/nodes/overview` statusDistribution

- **前端 P2**：
  1. R4 扇形图重写为五档占比环
  2. R4 帕累托单元范围排序方向澄清
  3. 柱图颜色走配置
  4. 左栏树改 antd Tree 组件

- **后端 P1**：
  1. E-T1 `get_node_trend` 加 fast_rate/accuracy_rate 两行
  2. E-1 `get_nodes_overview` statusDistribution 改为按回路评分五档计数
  3. `get_nodes_overview` 支持 plantId 过滤

- **压测 P1**：
  1. 1000 回路/12 单元 fixture
  2. 首屏 5 接口并行 p95 报告
  3. 1440×900 一屏不滚动实测

---

# 第二部分：Phase 1 共享载体任务包

## 2.1 调研结论（决定任务包形态）

总纲 [§4.2](./00-总纲-IA-UX-UI系统性重构最佳实践方案-2026-08-12.md) 明确要求：Phase 2 标杆前必须先做 Phase 1 共享载体，使"后续页面只允许组装、不允许自造"。当前实施直接跳到标杆实施，未沉淀共享载体——这是总纲路线的偏离，必须补做。

| 资产 | 现状 | 复用基础 | 必须新建 |
|---|---|---|---|
| **typed OperationalContext** | 零实现 | 后端 `workbench_summary` 已具备 scope/evidence/work 三组字段雏形；`use-monitor-context.ts`（监控模块内 10 字段 URL 真相源）可作 query 同步层雏形 | TS 类型定义 + 全模块 composable + Provider 容器 |
| **Decision Dock** | 零实现 | 后端 `_build_next_action` 已输出 actionType/label/reason/enabled/disabledReason/target；`ClpmObjectSummaryBar` 是形态雏形 | 统一决策栏组件 + 接入 nextAction 数据 |
| **Evidence Canvas** | 零实现 | `ClpmStructuredDiagnosisReport` + `ClpmDispositionTimeline` 是单业务雏形 | EvidenceCard + EvidenceCanvas 通用容器 |
| **State 组件族** | 4/6 态 | `ClpmStateOverlay`（loading/empty/error/success）+ `ClpmEmptyState`（5 场景） | 补 partial/stale/no-permission 三态 + 固定 fixture 机制 |

### 关键发现

跨模块上下文散落严重——**16 个 views 文件各自 `route.query.xxx` 读取**，`use-loop-context.ts` 已废止无消费者，`use-monitor-context.ts` 仅监控模块内生效。这是 Phase 1 必须先补的核心资产。

### 现有资产清单（可直接复用）

**后端数据层**：
1. `workbench_summary.py` 服务：scope/evidence/work 三组字段已齐备
2. `WorkbenchSummary` Pydantic schema：11 个类型化 schema 可作为 TS 类型化信封的对齐蓝本
3. `_build_next_action` 函数：8 步优先级决策 + 角色过滤，是 Decision Dock 数据层事实来源

**前端 composables（20 个文件）**：
- `use-monitor-context.ts`：URL 真相源 + 10 字段 + `update/reset/navigateWithMonitorContext` API
- `use-clpm-roles.ts`：5 角色枚举 + `canAccessTuning/canEditAdvancedParams` 权限判断
- `use-section-visibility.ts`：IntersectionObserver + onceVisible 语义
- `use-latest-request.ts`：代次保护（epoch + targetId）
- `use-polling.ts` + `use-async-pdf-export.ts`：通用轮询 + 任务跟踪
- `use-clpm-preferences.ts`：localStorage 持久化
- `use-industrial-status.ts` + `use-clpm-theme.ts`：6 语义色 token 映射

**前端组件（33 个文件）**：
- `ClpmObjectSummaryBar`：**最接近 DecisionDock 雏形**
- `ClpmStructuredDiagnosisReport` + `ClpmDispositionTimeline`：证据承载的单业务雏形
- `ClpmStateOverlay`：4 态统一组件，可扩展为六态
- `ClpmEmptyState`：5 预设场景空态
- `ClpmLoopLink`：跨模块跳转 carrier（硬编码 6 个目标路由，需参数化）
- `ClpmPageToolbar` + `use-page-toolbar.ts`：声明式工具栏 + 权限灰显
- `ClpmAiInsight`：4 场景 AI 洞察通用组件
- `ClpmConfidenceBadge` / `ClpmSeverityBadge` / `ClpmDataHealthBadges`：徽标组件

**前端指令与工具**：
- `directives/permission.ts`：v-permission 指令 + Comment 节点占位替换
- `components/clpm/index.ts`：统一导出 33 个组件 + `CLPM_TABLE_GUIDE` 常量

### 必须从零新建的资产

1. **`OperationalContext` TypeScript 类型**：五组类型化信封
2. **`Decision` / `Evidence` / `State` 类型定义**
3. **`DecisionDock` 组件**：参数化为配置驱动 + 接入 nextAction 数据
4. **`EvidenceCanvas` + `EvidenceCard` 组件**
5. **`StateFaces` 六态组件族**：扩展 ClpmStateOverlay 到完整六态
6. **`OperationalContextProvider` 容器组件**：provide/inject 注入子树
7. **`__fixtures__/` 目录与共享 mock-data 模块**：State 组件族要求"固定 fixture 可复现"
8. **统一 `OperationalContext` composable**：替代已废止的 `use-loop-context.ts` + 泛化 `use-monitor-context.ts`
9. **`from` / `backTo` / `section` 等 navigation 字段的后端补齐**
10. **四资产的单测基线**

---

## 2.2 任务包 P1-A：typed OperationalContext（先做，是其他三资产的基础）

**目标**：把后端 `WorkbenchSummary` 扁平字段收敛为 `{scope, evidence, work, time, navigation}` 五组类型化信封，全模块统一读写，替代 16 处散落的 `route.query.xxx`。

### 步骤 P1-A-1：定义 TypeScript 类型信封（半天）

- 新建 `frontend/apps/web-antd/src/composables/types/operational-context.ts`
- 五组类型定义（对齐后端 `WorkbenchSummary` schema）：

```ts
interface OperationalContext {
  scope: OperationalScope      // loopId/tagName/description/unitName/loopType/controlType/loopStatus/isActive/importanceLevel
  evidence: OperationalEvidence // runtime/dataFreshness/dataHealth/scoreTrend/assessment/diagnosis/tuning/trackerTimeline/activeAttention
  work: OperationalWork         // lifecycle/nextAction
  time: OperationalTime         // 评估/诊断/整定/Tracker/EffectCompare 各 resultAt + 时间窗
  navigation: OperationalNavigation // from/backTo/section/anchor + nextAction.target
}
```

- **验收**：`pnpm run check:type` 通过；类型字段与后端 schema 一一对应（建一份对照表）

### 步骤 P1-A-2：新建全模块 composable（1 天）

- 新建 `composables/use-operational-context.ts`
- 替代 `use-loop-context.ts`（已废止）+ 泛化 `use-monitor-context.ts`（仅监控模块）
- API：

```ts
const { context, scope, evidence, work, time, navigation,
        loadByLoopId, loadFromRoute, navigateWith, patchScope } = useOperationalContext()
```

- **关键能力**：
  1. URL 真相源（loopId/taskId/eventId/trackerId/section/from/plantNodeId 七字段）
  2. 业务判断按 ID 从服务端重载（不信 URL 里的展示字段，对应总纲 P5 单一事实源）
  3. 跨模块跳转携带上下文
- **验收**：
  1. `use-loop-context.ts` 删除并替换
  2. `use-monitor-context.ts` 改为 `useOperationalContext` 的监控模块特化
  3. 所有 16 处散落 `route.query.xxx` 替换为 `useOperationalContext()` 读取

### 步骤 P1-A-3：Provider 容器组件（半天）

- 新建 `components/clpm/operational-context-provider.vue`
- 通过 `provide/inject` 注入子树，避免 prop drilling
- 用法：`<OperationalContextProvider :loop-id="route.query.loopId">` 包裹整页
- **验收**：01/03/04 三页改造为 Provider 模式；子组件通过 `inject(OperationalContextKey)` 获取信封

### 步骤 P1-A-4：单测 + fixture（半天）

- 新建 `__tests__/operational-context.test.ts`：覆盖正常/异常/partial 三态
- 新建 `__tests__/__fixtures__/operational-context.ts`：固定 fixture 工厂
  - `createMockOperationalContext()`
  - `createMockOperationalContextWithPartial()`
  - `createMockOperationalContextWithStale()`
- **验收**：单测全绿；fixture 可被后续 State 组件族与 Evidence Canvas 复用

### P1-A 出口门禁

类型定义 + composable + Provider + 单测 + fixture 全部完成；01/03/04 三页改造为 Provider 模式；16 处散落 `route.query.xxx` 清零（grep 验证）

---

## 2.3 任务包 P1-B：Decision Dock（决策坞）

**目标**：每页唯一下一步 + 完整证据入口；9 类 nextAction 全部可执行；禁用态必显示原因。

### 步骤 P1-B-1：定义 Decision 类型（半天）

- 在 `operational-context.ts` 中扩展：

```ts
interface Decision {
  primaryAction: NextAction      // 唯一主按钮
  secondaryActions: NextAction[] // 次操作（折叠在 ⋯ 菜单）
  evidenceLinks: EvidenceLink[]  // 完整证据深链（不在 Dock 内展示详情）
}
```

### 步骤 P1-B-2：DecisionDock 组件（1 天）

- 新建 `components/clpm/decision-dock.vue`
- 复用 `ClpmObjectSummaryBar` 形态（loading 骨架 + 主指标 + tags + items + actions）
- **关键能力**：
  1. 主按钮强化（`type="primary"`）
  2. 次操作 ⋯ 折叠
  3. 禁用态显示 `disabledReason`（不许 no-op，FP-P0-01 红线）
  4. 权限灰显（接入 `useClpmRoles`）
  5. loading 骨架对齐 `ClpmObjectSummaryBar`
- **验收**：9 类 nextAction 全部有真实行为或带原因禁用态（含 E2E 守护）；SPONSOR 不出现工程动作按钮；EXPERT 只读+升级

### 步骤 P1-B-3：01 工作台决策栏改造（半天）

- 替换 01 workbench.vue 的 `.wb-decision` 为 `<DecisionDock>`
- 接入 `work.nextAction` 数据
- **验收**：01 验收清单序号 15（9 类 nextAction）从 🟡 → ✅

### 步骤 P1-B-4：单测 + E2E（半天）

- `__tests__/decision-dock.test.ts`：覆盖 9 类 nextAction 渲染 + 禁用态显示原因
- E2E：`e2e/tests/workbench-next-action.spec.ts` 覆盖 9 类动作点击行为
- **验收**：FP-P0-01 回归有自动化守护

### P1-B 出口门禁

DecisionDock 组件 + 01 改造 + 9 类 nextAction E2E 全绿

---

## 2.4 任务包 P1-C：Evidence Canvas（证据画布）

**目标**：结论必带时间、来源、可信度、阈值/基线；完整证据留在职能页，工作台是编排器不是容器。

### 步骤 P1-C-1：定义 Evidence 类型（半天）

- 在 `operational-context.ts` 中扩展：

```ts
interface Evidence {
  title: string
  conclusion: string           // 结论
  timestamp: string            // 证据时间
  source: EvidenceSource       // API 端点 + 计算口径
  confidence: ConfidenceLevel  // A/B/C/D/E
  threshold?: Threshold        // 阈值/基线（如有）
  deepLink: DeepLink           // 完整证据深链
}
```

### 步骤 P1-C-2：EvidenceCard 组件（1 天）

- 新建 `components/clpm/evidence-card.vue`
- 复用 `ClpmStructuredDiagnosisReport` + `ClpmDispositionTimeline` 单业务雏形
- **关键能力**：
  1. 标题 + 结论 + 时间 + 可信度徽标（`ClpmConfidenceBadge`）+ 严重度徽标（`ClpmSeverityBadge`）
  2. 阈值/基线标线
  3. "查看完整证据 ↗"深链
  4. loading/empty/partial 三态（接入 P1-D State 组件族）
  5. 展开/折叠详情
- **验收**：A2 文字三级来源原则落地——结构化字段/规则模板短句/LLM 叙述（未接入不渲染不留白）

### 步骤 P1-C-3：EvidenceCanvas 容器（1 天）

- 新建 `components/clpm/evidence-canvas.vue`
- 多源证据卡片网格（评估/诊断/整定/验证四卡）+ 排序 + 折叠
- 接入 P1-A OperationalContext.evidence 五组数据
- **验收**：01 R5 证据四区改造为 EvidenceCanvas；03 关注队列详情抽屉改造为 EvidenceCanvas

### 步骤 P1-C-4：单测 + fixture（半天）

- `__tests__/evidence-card.test.ts` + `__tests__/evidence-canvas.test.ts`
- fixture：`createMockEvidence()` / `createMockEvidenceWithLowConfidence()` / `createMockEvidenceWithMissingSource()`
- **验收**：4 卡在 1366 宽度下均不裁切（01 验收序号 13）

### P1-C 出口门禁

EvidenceCard + EvidenceCanvas + 01/03 改造 + 单测全绿

---

## 2.5 任务包 P1-D：State 组件族（六态一等公民）

**目标**：loading/empty/partial/stale/error/无权限六态全设计，不得全白或布局塌陷；固定 fixture 可复现。

### 步骤 P1-D-1：扩展 ClpmStateOverlay 到六态（1 天）

- 当前 `ClpmStateOverlay` 仅 4 态（loading/empty/error/success）
- 补三态：
  - **partial**：显式标注缺哪块（接入 `OperationalContext.evidence.unavailableSections`）
  - **stale**：数据陈旧提示（接入 `dataFreshness.status === 'DELAYED'`）
  - **no-permission**：友好"无权限"占位（替代 v-permission Comment 节点隐藏）
- **关键能力**：
  1. 每态有独立 fixture（P1-A-4 已建）
  2. 不布局塌陷（保持页面骨架）
  3. 可重试按钮（error/partial）
- **验收**：六态全实现；23 处 v-permission 散落可选用 `<StateFace state="no-permission">` 替代

### 步骤 P1-D-2：StateFace 统一入口（半天）

- 新建 `components/clpm/state-face.vue`
- 统一 API：`<StateFace state="loading|empty|partial|stale|error|no-permission" :fixture="mockFixture">`
- 内部委托给扩展后的 `ClpmStateOverlay`
- **验收**：六态 fixture 可复现（grep 验证 fixture 工厂被 State 组件族与 Storybook 共用）

### 步骤 P1-D-3：替换 24 处 antd 原语散落（1 天）

- grep `a-empty|a-result|a-spin` 在 `views/` 下命中 24 个文件 39 处
- 替换为 `<StateFace>` 统一入口
- **验收**：grep `a-empty|<Empty|a-result|<Result` 在 `views/` 下清零（除特殊情况标注原因）

### 步骤 P1-D-4：单测 + fixture 验收（半天）

- `__tests__/state-face.test.ts`：六态渲染 + fixture 复现
- **验收**：01 验收序号 1（六态全设计）从 🟡 → ✅；03 验收序号 1（六态）从 🟡 → ✅

### P1-D 出口门禁

StateFace 六态 + 24 处散落替换 + 单测全绿

---

## 2.6 任务包 P1-E：迁移与门禁（收口）

### 步骤 P1-E-1：01/03/04 三页迁移到共享载体（2 天）

- 01 workbench：DecisionDock + EvidenceCanvas + StateFace + OperationalContextProvider
- 03 attention：EvidenceCanvas（详情抽屉）+ StateFace + OperationalContextProvider
- 04 dashboard workbench：DecisionDock（如适用）+ StateFace + OperationalContextProvider
- **验收**：三页验收清单中"组件统一"相关条目从 🟡 → ✅

### 步骤 P1-E-2：契约文档升级（半天）

- 实现契约升 v2.12：新增 OperationalContext/Decision/Evidence/State 四资产章节
- UI/UX v6.3 补 §X 共享载体使用规范
- **验收**：契约与代码一致；漂移自动检查 = 0

### 步骤 P1-E-3：门禁全绿（半天）

- `pnpm run check:type` ✅
- `pnpm run vitest` ✅（新增 4 个测试文件）
- `cd backend && uv run pytest -q` ✅（后端零改动，但 workbench_summary schema 已就绪）
- `cd backend && uv run ruff check . && uv run ruff format --check .` ✅
- `cd backend && uv run alembic check` ✅
- E2E：`pnpm exec playwright test` ✅（新增 workbench-next-action.spec.ts）

### P1-E 出口门禁

三页迁移完成 + 契约 v2.12 + 全部门禁全绿

---

## 2.7 Phase 1 总工期与依赖

```
P1-A (2.5 天) ──┬─→ P1-B (2.5 天) ──┐
                ├─→ P1-C (3 天)   ──┼─→ P1-E (3 天) = 11 天
                └─→ P1-D (3 天)   ──┘
```

- **P1-A 是其他三资产的基础**（类型信封先行），必须最先完成
- P1-B/P1-C/P1-D 可并行（三人分工）
- P1-E 收口依赖前三者完成

**Phase 1 完成后**：后续页面"只允许组装、不允许自造"（总纲 §4.2 红线），Phase 4 传播时每页只需写 Page Contract + 组装四资产，不再各自实现决策栏/证据卡/状态面。

---

# 第三部分：Gate G2 八项条件自查检查表

> 总纲 [§4.3](./00-总纲-IA-UX-UI系统性重构最佳实践方案-2026-08-12.md) 明确：**Gate G2 不过不传播**（红线）。本检查表把八项条件具体化为可逐项核对的子项，每项标注：核查方法、当前状态（基于第一部分核查结果）、阻断门槛。

## 3.1 八项条件总览

| # | 条件 | 当前状态 | 阻断等级 |
|---|---|---|---|
| G2-1 | 18 项强制门禁 + Q-13~Q-16 语义门全过 | ❌ 多项未过 | 阻断 |
| G2-2 | 品质分 ≥85 且无 blocker | ❌ 无评分记录 | 阻断 |
| G2-3 | 四角色任务测试无 403/无不可执行 CTA | 🟡 SPONSOR 403 偏差 | 阻断 |
| G2-4 | 3 秒识别测试 4/5 | ❌ 无测试记录 | 阻断 |
| G2-5 | 1366/1440/1920/768 亮色通过 + 暗色代表状态配对 | ❌ 无走查记录 | 阻断 |
| G2-6 | 首屏与 BFF p95 达标 | ❌ 多项无压测 | 阻断 |
| G2-7 | 产品负责人明确选择 S1/S2/S3 | ❌ 待决 | 阻断 |
| G2-8 | 临时数据与 mock 清零 | 🟡 待核查 | 阻断 |

---

## 3.2 G2-1：18 项强制门禁 + Q-13~Q-16 语义门全过

**核查方法**：逐项对照 UI/UX v6.3 §14 + 方案 v1.1 §12.3，每项打勾或标注未过原因。

### 18 项强制门禁（UI/UX v6.3 §14）

| 子项 | 检查内容 | 04 系统概览 | 03 关注队列 | 01 回路工作台 |
|---|---|---|---|---|
| 1 | 首屏三要素可见（状况+趋势+重点） | ☐ | ☐ | ☐ |
| 2 | 单视觉锚点（主画布 ≥45%） | ☐ 待实测 | ☐ N/A | ☐ 待实测 |
| 3 | 单高强调主按钮 | ☐ | ☐ | ☐ |
| 4 | 数值带基线/可信度 | ☐ | ☐ | ☐ |
| 5 | 空白 ≤30% | ☐ | ☐ | ☐ |
| 6 | 事实不重复三次 | ☐ | ☐ | ☐ |
| 7 | 单风险标记/行 | ☐ | ☐ | ☐ |
| 8 | ≤1 点击到工作台 | ☐ | ☐ | ☐ |
| 9 | 状态不塌陷（六态） | ☐ ❌ | ☐ ❌ | ☐ ❌ |
| 10 | 1366 无横滚/裁切 | ☐ 待实测 | ☐ 待实测 | ☐ 待实测 |
| 11 | light/dark 语义一致 | ☐ | ☐ | ☐ |
| 12 | 名称-内容一致 | ☐ | ☐ | ☐ |
| 13 | TopN 语义透明 | ☐ | ☐ | ☐ |
| 14 | 术语快照一致 | ☐ | ☐ | ☐ |
| 15 | 主任务有持久结果 | ☐ | ☐ | ☐ |
| 16 | 阈值禁硬编码 | ☐ ❌ 阈值硬编码 60 | ☐ | ☐ |
| 17 | 文字三级来源 | ☐ | ☐ ✅ | ☐ ❌ 规则模板未接入 |
| 18 | 中文五档定级 | ☐ ✅ | ☐ | ☐ ✅ |

### Q-13~Q-16 语义门（方案 v1.1 §12.3）

| 子项 | 检查内容 | 当前状态 |
|---|---|---|
| Q-13 | 名称-内容一致（伪命名一票否决） | ☐ 待逐页核对 |
| Q-14 | TopN 语义透明（已加载/全部/截断） | ☐ 03 已实现，04 待核对 |
| Q-15 | 术语快照一致 | ☐ 待核对 |
| Q-16 | 主任务有持久结果 | ☐ 待核对 |

### G2-1 出口

18×3 + 4 = 58 个子项全部打勾或标注降级方案；阻断项（9/16/17）必须修复

---

## 3.3 G2-2：品质分 ≥85 且无 blocker

**核查方法**：按方案 v1.1 §12.2 六维度评分（100 分制），标杆 ≥90 且无 blocker。

### 六维度评分表（每页一份）

| 维度 | 权重 | 04 系统概览 | 03 关注队列 | 01 回路工作台 |
|---|---|---|---|---|
| 任务与 IA | 25 | __/25 | __/25 | __/25 |
| 证据与可信 | 20 | __/20 | __/20 | __/20 |
| 动作与闭环 | 20 | __/20 | __/20 | __/20 |
| 构图与层级 | 15 | __/15 | __/15 | __/15 |
| 工业品质一致性 | 10 | __/10 | __/10 | __/10 |
| 响应式与可访问 | 10 | __/10 | __/10 | __/10 |
| **总分** | 100 | __/100 | __/100 | __/100 |

### Blocker 清单（任一存在即不过 G2）

| 检查项 | 当前状态 |
|---|---|
| 是否有 no-op CTA（FP-P0-01 残留） | ☐ 01 已修复，待 E2E 守护 |
| 是否有悬空入口（404） | ☐ ❌ 04 下钻 `/monitor/loops` 死链 |
| 是否有 403 不该出现的角色 | ☐ ❌ 01 SPONSOR 整页 403 |
| 是否有硬编码阈值 | ☐ ❌ 04 阈值 60 硬编码 |
| 是否有 LLM 文案未门控 | ☐ 待核查 |
| 是否有 mock 数据残留 | ☐ 待核查（G2-8 联动） |
| 是否有 partial 不标注 | ☐ 待核查 |

### G2-2 出口

三页总分均 ≥90（标杆线）；blocker 清单全空

---

## 3.4 G2-3：四角色任务测试无 403/无不可执行 CTA

**核查方法**：四角色（ADMIN/IC/PE/EXPERT/SPONSOR）× 三页 × 典型任务，记录是否 403 / 是否有不可执行 CTA。

### 角色任务矩阵

| 角色 | 04 系统概览 | 03 关注队列 | 01 回路工作台 |
|---|---|---|---|
| ADMIN | ☐ 任务:查看全厂KPI | ☐ 任务:分诊+处置 | ☐ 任务:全功能 |
| IC_ENGINEER | ☐ 任务:班前巡检 | ☐ 任务:确认+处置 | ☐ 任务:全功能 |
| PE_ENGINEER | ☐ 任务:补充工艺证据 | ☐ 任务:确认（无整定） | ☐ 任务:无整定动作 |
| EXPERT | ☐ 任务:只读+升级 | ☐ 任务:只读+禁用显因 | ☐ 任务:只读+升级 |
| SPONSOR | ☐ 任务:看治理成效 | ☐ 任务:只读 | ☐ ❌ **整页 403**（应只读） |

### 不可执行 CTA 检查（FP-P0-01 红线）

| 页面 | 9 类 nextAction | 是否有 no-op | 禁用态是否显示原因 |
|---|---|---|---|
| 01 工作台 | ☐ 9 类全通 | ☐ 无 no-op | ☐ 显示 disabledReason |
| 03 关注队列 | ☐ primaryAction | ☐ 无 no-op | ☐ PE/EXPERT 禁用显因 |
| 04 系统概览 | ☐ 下钻三出口 | ☐ ❌ `/monitor/loops` 404 | ☐ N/A |

### G2-3 出口

5 角色 × 3 页 = 15 个任务格子全部打勾；无 403（除设计明确拒绝）；无 no-op CTA

---

## 3.5 G2-4：3 秒识别测试 4/5

**核查方法**：5 名目标用户（建议覆盖 IC/PE/EXPERT/SPONSOR/ADMIN），展示页面截图 3 秒后遮住，问三个问题：①这是哪条回路/哪个范围？②它现在什么状态？③你该点哪里？

### 测试记录表（每页一份）

| 用户 | 角色 | 04 系统概览（3 问题） | 03 关注队列 | 01 回路工作台 |
|---|---|---|---|---|
| 用户 1 | IC | ☐☐☐ /3 | ☐☐☐ /3 | ☐☐☐ /3 |
| 用户 2 | PE | ☐☐☐ /3 | ☐☐☐ /3 | ☐☐☐ /3 |
| 用户 3 | EXPERT | ☐☐☐ /3 | ☐☐☐ /3 | ☐☐☐ /3 |
| 用户 4 | SPONSOR | ☐☐☐ /3 | ☐☐☐ /3 | ☐☐☐ /3 |
| 用户 5 | ADMIN | ☐☐☐ /3 | ☐☐☐ /3 | ☐☐☐ /3 |
| **达标线** | — | ≥4/5 用户答对 ≥2 题 | ≥4/5 | ≥4/5 |

### G2-4 出口

三页均 ≥4/5 用户通过；测试记录留档（截图 + 用户回答 + 通过判定）

---

## 3.6 G2-5：1366/1440/1920/768 亮色通过 + 暗色代表状态配对

**核查方法**：四分辨率 × 亮色逐页截图；暗色至少覆盖代表状态（正常/异常/空态/加载）。

### 亮色四分辨率矩阵（每页一张截图）

| 分辨率 | 04 系统概览 | 03 关注队列 | 01 回路工作台 |
|---|---|---|---|
| 1366×768 | ☐ 无横滚/裁切 | ☐ | ☐ 待实测 |
| 1440×900 | ☐ 一屏不滚动 | ☐ | ☐ |
| 1920×1080 | ☐ 无大段留白 | ☐ | ☐ |
| 768×1024（iPad） | ☐ 可用 | ☐ | ☐ |

### 暗色代表状态配对（每页 4 张截图）

| 状态 | 04 系统概览 | 03 关注队列 | 01 回路工作台 |
|---|---|---|---|
| 正常（有数据） | ☐ | ☐ | ☐ |
| 异常（error/partial） | ☐ | ☐ | ☐ |
| 空态（empty-green/empty-filter） | ☐ | ☐ | ☐ |
| 加载（loading 骨架） | ☐ | ☐ | ☐ |

### G2-5 出口

12 张亮色截图 + 12 张暗色截图 = 24 张全部通过；WCAG AA 对比度 100% 达标

---

## 3.7 G2-6：首屏与 BFF p95 达标

**核查方法**：参照 `perf-report-MW-P5-04.md` 模板，压测各页首屏接口 + BFF 聚合接口。

### 前端首屏 p95（1000 回路 / 10000 项 fixture）

| 页面 | 接口数 | p95 目标 | 当前 | 状态 |
|---|---|---|---|---|
| 04 系统概览 | 5（aggregate/auto-rate-rt/nodes overview/trend/ranking+attention） | ≤500ms | 无报告 | ❌ 待压测 |
| 03 关注队列 | 1（attention aggregates） | ≤500ms | 610ms | ❌ 超 20% |
| 01 回路工作台 | 6（summary/kpi-history/trend/snapshots/loops/plant-tree） | ≤500ms | 无报告 | ❌ 待压测 |
| 02 回路列表 | 1（/loops/monitor + aggregate） | ≤500ms | 无报告 | ❌ 待压测 |

### BFF 聚合接口 p95

| 接口 | p95 目标 | 当前 | 状态 |
|---|---|---|---|
| `GET /loops/{id}/workbench-summary` | ≤200ms | 53ms（MW-P5-04） | ✅ |
| `GET /monitor/attention` | ≤500ms | 610ms | ❌ |
| `GET /dashboard/board/aggregate` | ≤200ms | 无报告 | ❌ 待压测 |
| `GET /performance/nodes/overview` | ≤200ms | 无报告 | ❌ 待压测 |
| `GET /loops/monitor` | ≤200ms | 无报告 | ❌ 待压测 |

### G2-6 出口

4 页首屏 p95 + 5 BFF 接口 p95 全部达标；压测报告留档（参照 perf-report-MW-P5-04.md 模板）

---

## 3.8 G2-7：产品负责人明确选择 S1/S2/S3

**核查方法**：产品负责人在 Gate G2 评审会上明确选择传播等级，写入决策记录。

### 三选项说明（总纲 §4.3）

| 等级 | 含义 | 适用场景 | 成本 |
|---|---|---|---|
| S0 | 仅保留标杆（方向存疑） | Gate G2 不过 | 0 |
| S1 | 只传播基础载体（结构有效、成本受限） | 资源紧张 | 低 |
| **S2（推荐默认）** | 传播到 12~18 个核心页 | 平衡 | 中 |
| S3 | 全量传播（40+ 页） | 需版本预算与回归资源 | 高 |

### 决策记录模板

```
Gate G2 评审决议
日期：____
产品负责人：____
选择等级：S0 / S1 / S2 / S3
理由：____
传播页面清单（如 S1/S2/S3）：____
回归资源承诺：____
```

### G2-7 出口

决策记录留档；传播页面清单（如选 S2，列出 12~18 个核心页）

---

## 3.9 G2-8：临时数据与 mock 清零

**核查方法**：grep + 代码审查，确认无 `USE_MOCK`、无硬编码 fixture、无 TODO 标记的临时数据。

### 清零检查清单

| 检查项 | 核查方法 | 当前状态 |
|---|---|---|
| `USE_MOCK` 标志 | grep `USE_MOCK` 在 src/ | ☐ 01 workbench `USE_MOCK=false`（已关闭，但 mock 文件残留 `workbench-mock.ts`） |
| 硬编码 fixture 数据 | grep `mockData\|MOCK_\|fakeData\|testData` 在 src/ | ☐ 待核查 |
| TODO 临时数据 | grep `TODO\|FIXME\|临时\|temp` 在 src/ | ☐ 待核查 |
| 硬编码阈值数字 | grep `\b60\b\|\b80\b\|\b95\b` 在配置文件外 | ☐ ❌ 04 阈值 60 硬编码 |
| 硬编码颜色 | grep `#2563eb\|#7c3aed\|#1a7f4b` 在 src/ | ☐ ❌ 04 帕累托柱图颜色硬编码 |
| 硬编码状态 | grep `bg-emerald-500` 在 src/ | ☐ ❌ 04 链路指示灯恒绿 |
| 硬编码文案 | grep `实时订阅 已停机\|Tailscale 可用` 在 src/ | ☐ ❌ 04 展开面板文案硬编码 |
| 旧组件残留 | grep `useLoopContext` 在 src/（已废止） | ☐ P1-A 完成后清零 |
| 旧路由残留 | grep `/loop/monitor/legacy` 在 src/ | ☐ 02 实施时清理 |

### G2-8 出口

上述 9 项全部清零或标注降级方案；grep 验证无残留

---

## 3.10 Gate G2 评审流程建议

### 评审前准备（1 周）

1. 完成第一部分"文档对齐"——四页验收清单勾选完毕，待办项标注阻断/非阻断
2. 完成 G2-1（18 项门禁 + Q-13~Q-16）逐项打勾
3. 完成 G2-2（品质分评分）三页总分计算
4. 完成 G2-3（角色任务矩阵）15 格子打勾
5. 完成 G2-5（24 张截图）亮色 + 暗色
6. 完成 G2-6（压测报告）4 页 + 5 接口
7. 完成 G2-8（清零检查）9 项核查
8. 组织 G2-4（3 秒识别测试）5 用户 × 3 页

### 评审会（半天）

1. 逐项过八项条件，标注 ✅/❌
2. 阻断项讨论：是否可在传播前修复
3. 产品负责人选择 S1/S2/S3（G2-7）
4. 写入决策记录

### 评审后

- **G2 通过**：启动 Phase 4 传播（按选定等级）
- **G2 不通过**：回退标杆构图并复盘（S0），不传播（总纲红线）

---

## 3.11 G2 当前阻断项汇总（基于第一部分核查）

| 阻断项 | 影响条件 | 修复任务 | 工期 |
|---|---|---|---|
| 04 下钻 `/monitor/loops` 死链 | G2-1（项 8）、G2-2（blocker）、G2-3 | 02 实施或改路径 | 02 实施时 |
| 01 SPONSOR 整页 403 | G2-3 | 权限矩阵对齐 | 半天 |
| 04 阈值 60 硬编码 | G2-1（项 16）、G2-8 | 对接 `/configs/grading-thresholds` | 半天 |
| 04 链路指示灯硬编码 | G2-8 | 对接 `/datasource/health` | 半天 |
| 01 规则模板建议文案未接入 | G2-1（项 17） | 接入诊断规则配置 | 半天 |
| 03 attention p95≈610ms | G2-6 | SQL 优化/Redis 缓存/线程池 | 1-2 天 |
| 三页六态未完整 | G2-1（项 9） | P1-D State 组件族 | 3 天 |
| 无压测报告 | G2-6 | 4 页 + 5 接口压测 | 2 天 |
| 无 3 秒识别测试 | G2-4 | 组织 5 用户测试 | 1 天 |
| 无品质评分 | G2-2 | 六维度评分 | 半天 |
| 无暗色走查 | G2-5 | 12 张暗色截图 | 半天 |
| S1/S2/S3 待决 | G2-7 | 产品负责人决策 | 评审会 |

**预估修复工期**：约 10-12 个工作日（含 Phase 1 共享载体 P1-D State 组件族）

---

# 后续建议

## 优先级排序

1. **处理 01 设计文档自身矛盾**（卡数/横道项数/缩略图）——这是阻断 Phase 2 实施的前置
2. **修复 04 下钻链路三处断裂**（半天）——这是"用户操作闭环"最痛的断点
3. **启动 Phase 1 共享载体 P1-A**（2.5 天）——是其他三资产的基础，越早开工越好
4. **02 回路列表 v1.5 评审拍板**——评审通过后授权实施
5. **03 attention p95 性能优化**——唯一阻断 03 收口的硬指标
6. **Gate G2 评审准备**——按 §3.10 流程逐项推进

## 文档同步要求

本文件完成后，建议同步更新：

| 文件 | 更新内容 |
|---|---|
| `README.md`（页面标杆设计目录） | 在阅读顺序表新增本文档为第 5 项（实施前置工作文档） |
| `AGENTS.md` 当前基线表 | 页面标杆设计行追加"Phase 1 共享载体任务包 + Gate G2 自查检查表" |
| 四页标杆设计文档头 | 按 §1.0 汇总表更新文档头状态（01/03/04 改为"已对标落地"等） |
| 四页标杆设计 §5/§6 验收清单 | 逐条勾选已落地条目为 `[x]`，未落地条目单列 |

## 引用文档

- [总纲 §4.2 Phase 1 共享载体](./00-总纲-IA-UX-UI系统性重构最佳实践方案-2026-08-12.md)
- [总纲 §4.3 Gate G2 八项条件](./00-总纲-IA-UX-UI系统性重构最佳实践方案-2026-08-12.md)
- [总纲 §11 页面设计通用规则](./00-总纲-IA-UX-UI系统性重构最佳实践方案-2026-08-12.md)
- [前置核查报告 FP-P0-01~08](./00-前置-FP-P0代码只读核查报告-2026-08-12.md)
- [01 回路工作台标杆设计](./01-回路工作台/01-回路工作台标杆设计-2026-08-12.md)
- [02 回路列表标杆设计](./02-回路列表/02-回路列表标杆设计-2026-08-12.md)
- [03 关注队列标杆设计](./03-关注队列/03-关注队列标杆设计-2026-08-12.md)
- [04 系统概览标杆设计](./04-系统概览/04-系统概览标杆设计-2026-08-12.md)
- 性能报告参考：`docs/过程文档/perf-report-MW-P5-04.md`

---

*文档结束*
