# 关注队列标杆设计（页型 D：队列分诊）

| 项目 | 内容 |
|---|---|
| 文档日期 | 2026-08-12（v1.0 初版） |
| 文档性质 | 第二步·关键页面重构方案 ③：标杆页设计三件套（一页纸契约 + 目标布局 + 走查剧本） |
| 事实基线 | 分支 `feat/ia-rectification-2026-08-10` @ `d741569b`（只读核查，未改动代码） |
| 上游决策 | [IA 体检与工业产品视角改进建议](../00-决策-IA体检与工业产品视角改进建议-2026-08-12.md) §3（页型 D 队列分诊）、§4（例外队列"回答今天干什么"）；通用规则见 [总纲 §11](../00-总纲-IA-UX-UI系统性重构最佳实践方案-2026-08-12.md)；加载契约：TopN/已加载/全部/截断语义透明（Q-14）、1000 回路/10000 开放项 p95 ≤500ms |
| 现状文件 | `frontend/apps/web-antd/src/views/monitor/attention.vue`（895 行）、`backend/app/api/v1/endpoints/monitor.py:41`（`/monitor/attention`）、`backend/app/services/monitor_attention.py`（856 行聚合服务） |
| 关联标杆 | [01-回路工作台](../01-回路工作台/01-回路工作台标杆设计-2026-08-12.md)（下钻目标，页型 C）、[02-回路列表](../02-回路列表/02-回路列表标杆设计-2026-08-12.md)（页型 B，回答"谁最差"；本页回答"今天先干什么"） |

## 0. 现状诊断（已具雏形，缺的是"分诊台"形态）

后端聚合服务是全套标杆里最成熟的：五来源统一队列、优先级规则透明、排序可解释（rankReasons）、动作按角色服务端生成、不新增业务表——**方向正确，予以保留**。问题集中在前端形态与两个后端缺口：

1. **页面是"通用表格"不是"分诊台"**：895 行 attention.vue 用 antd Table 平铺 8 列（优先级/来源/回路/摘要/状态/发生时间/排序原因/操作），无范围摘要、无优先级速览、无左脊柱导航；班长早会"3 分钟分诊"场景下，需要自己脑内聚合"几条 URGENT、几条超期"。
2. **排序原因藏在 200px 列里**：rankReasons 是本系统"可信 AI"的最佳实践（排序可解释），但当前挤在一列灰色小字里，应该升格为一级信息。
3. **G1 后端缺口：`unitName` 恒为 null**——五个聚合器（monitor_attention.py:383/519/571/648/690）全部 `unit_name=None`，页面无法显示"装置·单元"，千级回路下分诊失去空间定位。
4. **G2 后端缺口：`plantNodeId` 非递归**——`LoopLedger.unit_id == plant_node_id` 精确匹配（monitor_attention.py:739），选装置级节点会漏掉挂在单元下的回路，与 02 列表的递归语义（`get_loop_type_stats` 递归）不一致。
5. **截断语义不显性**：每来源上限 500 条（MW-P5-04 `_MAX_ITEMS_PER_SOURCE`），内存聚合+内存分页；Q-14 要求 TopN/已加载/全部/截断语义透明，当前页面无此表达。
6. **主动作不够"主"**：操作列 160px 平铺按钮，服务端 primaryAction 没有被视觉强化；R-F 要求"唯一主动作"。

---

## 1. 一页纸契约（Page Contract）

| 项 | 内容 |
|---|---|
| 页面名称 / 路由 | 关注队列 `/monitor/attention`（保持 canonical，不新增路由） |
| 页型 | D 队列分诊：例外/案例集合 → 分诊列表 → 受理/处置 |
| 主对象 | **关注项集合（五来源统一队列）× 范围 × 筛选**（不是预警列表、不是工单列表——是"今日例外"的统一分诊台） |
| 主任务 | 3 分钟回答三个问题：今天有几条例外 → 哪条必须先处理（URGENT 未确认/验证超期）→ 为什么它排在这（rankReasons）→ 点一个动作 |
| 主用户 | IC 工程师（分诊+处置）、EXPERT（会诊）、ADMIN（全量）；SPONSOR 只读 |
| 主动作 | **服务端 `primaryAction`（唯一主动作，R-F）**：非 Sponsor 默认"进入工作台"；ALERT 加确认/处置/误报，TRACKER/VERIFICATION 加查看工单；禁用必显因 |
| 信息所有权 | 本页只放"分诊所需最小信息"：优先级/来源/回路/装置·单元/摘要/状态/时间/为什么/动作；证据、趋势、处置闭环一律留给 01 工作台与详情抽屉 |
| 上下文 | 深链接 `?eventId=`（自动开详情）/`?source=`/`?loopId=`/`?trackerId=`；下钻工作台携带 `from=/monitor/attention` + `section/eventId/trackerId`；返回时完整恢复筛选与页码 |
| 空态 | 六态全实现；**"范围内无关注项"是绿色好状态**（"今日无例外"），与"筛选无结果"严格区分 |
| 时间窗 | **无时间窗选择器**（R-C 正确应用）；时间为"发生时间"字段（相对时间 + 绝对时间 tooltip），超期由后端规则判定（验证周期 24h，VERIFICATION_PERIOD_HOURS，对齐 diagnosis.py 口径） |
| 持久结果 | 确认/处置/标记误报复用现有 alert API，产生审计事件；本页不新建状态机 |
| 禁止项 | ① 不新增业务表（聚合现有五表）；② 不用 LLM 生成描述（summary/rankReasons 均为后端规则模板，R-A 文字第②级）；③ 不提供列排序（排序即分诊观点，规则透明展示）；④ 不出现 no-op CTA；⑤ 不在本页画趋势图（那是工作台的）；⑥ 不让前端做权限推断（只渲染服务端返回的 actions） |

---

## 2. 目标布局（真实数据 fixture）

fixture：常减压装置/常压塔单元范围，**26 条关注项**（URGENT 3 / HIGH 6 / MEDIUM 11 / LOW 6；OPEN 14 / VERIFYING 4 / IN_PROGRESS 3 / ACKNOWLEDGED 3 / SUPPRESSED 2），五来源齐全，与 01/02 同一回路族（FIC-2209 卡涩、TIC-2215 振荡、PIC-2203 饱和、LIC-2207 手动干预、LIC-101 振荡）。

### 2.1 骨架（与 01/02 同构）

```
R1 页头      标题「关注队列」+ 范围面包屑 + 数据时点 + 帮助
R2 摘要条    开放 14 · URGENT 3 · 验证超期 2 · 数据质量 4（aggregates 同源）
R2.5 速览卡  优先级单行紧凑卡（全部/URGENT/HIGH/MEDIUM/LOW，点击即筛选）
             右侧右对齐：来源分布 chip ×5 + 截断语义（Q-14）
R3 工具条    来源多选 chip ×5 · 状态筛选 · 搜索（位号/标题）· 密度 · 列配置
R4 主画布    分诊表（URGENT 行红底 / HIGH 左红条）+ 左脊柱树（关注项计数徽章）
R5 分页条    TopN/当前页/总数/截断语义透明（Q-14）
```

### 2.2 区域规格与数据来源

| 区域 | 内容 | 数据来源 |
|---|---|---|
| R1 页头 | 标题"关注队列"；范围面包屑；数据时点"聚合于 10:07"（本页为实时聚合查询，无 KPI 快照口径——与 02 的 B 类快照标注不同，须显式说明）；帮助（五来源定义 + 优先级规则 + 排序规则，全部透明可解释） | 接口响应时间戳 |
| R2 摘要条 | 开放 14（status=OPEN）· URGENT 3 · 验证超期 2（VERIFICATION 且超 24h）· 数据质量 4 | 响应 `aggregates`（bySource/byPriority/byStatus）**单一聚合源**，禁止前端另算（同 02 的 E-1 原则） |
| R2.5 优先级速览卡 | 单行紧凑卡：全部 26 / URGENT 3 / HIGH 6 / MEDIUM 11 / LOW 6，点击即筛选（对齐 02 等级速览卡交互）；右侧右对齐：五来源分布 chip（活跃预警 8 · 评分恶化 6 · 数据质量 4 · 待处置工单 5 · 验证超期 3，点击=来源筛选）+ 截断语义"已加载 26 / 共 26 项" | 同 R2 `aggregates`；截断语义见 §2.3-5 |
| R3 工具条 | 五来源多选 chip（服务端 `source[]` 参数）；状态多选（OPEN/ACKNOWLEDGED/SUPPRESSED/IN_PROGRESS/VERIFYING）；搜索框（位号/标题，`keyword`）；密度三档、列配置（现有机制保留）；**无排序选择器** | 现有 query 参数全量保留 |
| R4 分诊表 | **默认列（9 列）**：优先级（URGENT 红底行/HIGH 左红条，四档色阶徽章）｜来源（五类中文标签+工业色板，沿用 P3-10 降饱和配色）｜回路位号（mono 加粗，点击=深链接筛选）｜装置·单元（**依赖 G1 修复**；修复前列隐藏并说明）｜摘要（规则模板文案，评分Δ/严重度内联）｜状态（五态徽章）｜发生时间（相对时间，tooltip 绝对时间）｜**为什么排在这**（rankReasons chips 升格为一级信息，最多 2 条 + 更多 tooltip）｜操作（**primaryAction 主按钮强化** + 更多 ⋯ 折叠次要动作） | `GET /monitor/attention` 全字段已有；动作=服务端 `primaryAction`/`actions` 直渲染 |
| 左脊柱 | 与 01/02 同款树（装置/单元级），**徽章语义=开放关注项数（URGENT 数红色优先显示）**，区别于 02 的"异常回路数/最低分"徽章；搜索双支持；底部沉浸折叠 | 树：现有 monitor context；徽章：**待扩展 E-1**（见 §2.5） |
| R5 分页条 | `第 1 页 · 20/页 ｜ 已加载 20 / 共 26 项 · 服务端分诊排序`；超出来源上限时显式："已达单来源 500 条聚合上限，请细化筛选"（Q-14 截断语义透明） | 响应 total/page/pageSize + 截断标志（**待扩展 E-2**） |

### 2.3 关键设计决策

1. **排序即分诊观点，不提供列排序**：服务端固定排序——优先级（URGENT→LOW）→ 同级阶段（未确认→超期→处理中/验证中→已确认→已抑制）→ 时间倒序（monitor_attention.py:169 `_sort_key`）。这不是技术限制，是产品观点："先干什么"应该由透明规则回答，而不是让用户自己点表头。排序规则完整写进帮助面板，`rankReasons` 列让每一行自证"我为什么在这"。
2. **rankReasons 升格为一级信息**：这是全系统"可信 AI"的样板——每条例外出示排序理由（"严重预警未确认 · 重复触发 3 次"）。从 200px 灰字列升级为独立列 chips 展示，是 R-A 数据诚实原则在"算法决策"上的落地。
3. **动作分层（R-F）**：主按钮=服务端 `primaryAction`（视觉强化，唯一）；确认/处置/误报收进 ⋯ 菜单，处置与误报需二次确认 Modal + 备注（复用现有交互）；PE/EXPERT 的写动作禁用并显示"当前角色无确认权限"（服务端 disabledReason 直渲染，前端不做权限推断）；SPONSOR 只读（查看详情/返回运行总览）。
4. **下钻比 02 更深**：02 行下钻带 `loopId` 进工作台；本页下钻额外携带 `section=overview&eventId=&trackerId=`（服务端已生成，monitor_attention.py:212-219），工作台直接定位到相关证据区——队列页的"点"比对象表的"点"上下文更具体。
5. **截断语义透明（Q-14）**：三处显性表达——R2.5 右侧"已加载 X / 共 Y 项"；R5 分页条同上；任一来源达 500 条上限时显示截断提示条（**依赖 E-2 截断标志**），引导细化筛选而非沉默丢数据。
6. **空态分级**：loading / error（重试）/ **empty-green（范围内无关注项："今日无例外，回路运行正常"——这是系统的正面信号，不是缺失）** / empty-filter（筛选无结果，一键清除）/ denied / stale。
7. **与 02/未来 A 页的边界**：02 回答"谁最差"（全量对象扫视），本页回答"今天先干什么"（例外分诊）；同一回路可能同时出现在两页，语义不同不冲突。未来系统概览（页型 A）右联"今日例外 Top-N"取自本页前 N 条投影（IA 体检 §3），本页是例外的唯一权威来源。

### 2.4 页面指标数据来源与计算端口清单（A1 溯源表）

| # | 页面元素 | 展示内容 | API 端点 | 后端计算 | 数据表/字段 |
|---|---|---|---|---|---|
| 1 | R4 队列行数据 | 26 条五来源关注项 | `GET /monitor/attention`（monitor.py:41，登录即可读；动作按 `user.role` 生成） | `list_attention`（monitor_attention.py:716）内存聚合+排序+分页 | `alert_event` / `kpi_snapshot_hourly` / `loop_integrity_snapshot` / `loop_confidence_latest` / `action_tracker` |
| 2 | 优先级徽章 | URGENT 红 / HIGH 橙 / MEDIUM 蓝 / LOW 灰 | 同 ① `priority` | 透明规则：URGENT=CRITICAL 活跃预警或 CRITICAL 工单；HIGH=ERROR 预警/验证超期/完整性 CRITICAL/scoreDelta≤-10；MEDIUM=WARN/开放 Tracker/完整性 WARNING/-10<Δ≤-5；LOW=INFO/-5<Δ≤-2/可信度 D/E（monitor_attention.py:13-17 模块 docstring，须与帮助面板逐字一致） | 五表源字段 |
| 3 | 来源标签 | 活跃预警/评分恶化/数据质量/待处置工单/验证超期 | 同 ① `source` | 五来源聚合器（ALERT=ACTIVE/ACKNOWLEDGED/SUPPRESSED 预警；DEGRADATION=dayTrend=WORSENED 且 Δ≤-2；DATA_QUALITY=完整性 WARNING/CRITICAL 或可信度 D/E；TRACKER=PENDING/IN_PROGRESS 工单；VERIFICATION=VERIFYING 超 24h） | 同上 |
| 4 | 状态徽章 | 待处理/已确认/已抑制/处理中/验证中 | 同 ① `status`/`sourceStatus` | 状态映射：ALERT_STATUS_MAP / TRACKER_STATUS_MAP（monitor_attention.py:52/67） | 源表状态字段 |
| 5 | 摘要列 | "规则 R-OSC-01 触发，严重度 ERROR" | 同 ① `title`/`summary` | **后端规则模板生成（R-A 文字第②级），非 LLM** | 源表字段插值 |
| 6 | 为什么排在这 | rankReasons chips | 同 ① `rankReasons[]` | 聚合器按规则追加（"严重预警未确认"/"重复触发 N 次"/"评分日降 5.1"/"验证超期 26 小时"…） | 派生 |
| 7 | 发生时间 | 相对时间 + tooltip 绝对时间 | 同 ① `occurredAt`/`updatedAt` | 源表时间戳；超期判定=VERIFICATION 且 updatedAt 超 24h | 源表时间字段 |
| 8 | 装置·单元列 | 常减压·常压塔 | 同 ① `unitName` | **G1 缺口：当前恒 null**；修复=聚合器 join 单元表（LoopLedger.unit_id → 单元名），小改动 | `loop_ledger.unit_id` → 单元表 |
| 9 | R2 摘要/R2.5 速览卡/来源 chip | 计数 | 同 ① `aggregates`（bySource/byPriority/byStatus，monitor_attention.py:808） | 单一聚合源，禁止前端另算 | 派生 |
| 10 | 操作列 | primaryAction 主按钮 + ⋯ | 同 ① `primaryAction`/`actions` | `_build_actions`（monitor_attention.py:200）按角色生成；SPONSOR 无 OPEN_WORKBENCH/写动作 | — |
| 11 | 确认/处置/误报 | 写动作 | 复用 `acknowledgeEventApi`/`resolveEventApi`/`markFalsePositiveApi`（alert API） | 现有预警状态机，产生审计事件 | `alert_event` |
| 12 | 左脊柱树+徽章 | 装置/单元 + 开放关注项数 | 树：现有 monitor context；徽章：**待扩展 E-1**（节点级关注项计数聚合，或复用 `kpi_node_snapshot_*` 扩展） | 待扩展 | — |
| 13 | 截断标志 | "已达单来源 500 条上限" | **待扩展 E-2**：响应增加 `truncated: {ALERT: true, …}`；当前 `_MAX_ITEMS_PER_SOURCE=500` 静默截断 | 待扩展 | — |
| 14 | 深链接 | ?eventId= 开详情 / ?source= 筛选 | 前端路由参数 + 同 ① `loopId`/source 参数 | — | — |

### 2.5 待扩展项汇总（实施排期决策点）

| # | 缺口 | 现状 | 影响与降级方案 |
|---|---|---|---|
| **G1** | `unitName` 恒 null（五聚合器 `unit_name=None`） | 响应字段存在但无值 | 装置·单元列无法显示；**修复=聚合器 join 单元表（小改动，建议随本页实施一并做）**；修复前列隐藏，不显示空列 |
| **G2** | `plantNodeId` 非递归（`unit_id ==` 精确匹配） | 选装置级节点漏回路 | 与 02 递归语义不一致；**修复=递归子节点解析（与 `get_loop_type_stats` 同款），建议同批做**；修复前左脊柱只允许选单元级 |
| E-1 | 左脊柱节点级关注项计数徽章 | 无此聚合 | 降级：徽章暂用 02 同款异常回路数（语义不同须标注），或隐藏徽章 |
| E-2 | 截断标志 `truncated` | 500/来源静默截断 | 降级：分页条固定文案"单来源聚合上限 500 条"（Q-14 最低限度透明）；建议同批做（聚合器已有计数，加一个返回字段） |
| E-3 | 内存聚合性能契约验证 | 内存聚合+内存分页，10k 项未压测 | 1000 回路/10000 开放项 p95 ≤500ms 是既定验收门（MW-P5-04）；不达标再评估物化/缓存，**不预先优化** |

> G1/G2 是"后端差一点、前端没法补"的硬缺口，与 01 标杆 FP-P0 的教训同类；E-1/E-2/E-3 均为小改或验证项，建议全部随本页实施同批做。

---

## 3. fixture（走查与验收的数据基线）

常减压装置/常压塔单元，26 条关注项（与 01/02 同一回路族）：

| 优先级 | 条数 | 典型项 |
|---|---|---|
| URGENT | 3 | FIC-2209 CRITICAL 卡涩预警未确认（重复触发 3 次）；TIC-2215 CRITICAL 振荡工单；PIC-2203 CRITICAL 完整性 |
| HIGH | 6 | LIC-101 验证超期 26h；TIC-2215 评分日降 12.4；ERROR 预警 ×2；ERROR 工单 ×2 |
| MEDIUM | 11 | WARN 预警、开放 Tracker、完整性 WARNING、-10<Δ≤-5 恶化 |
| LOW | 6 | INFO 预警、-5<Δ≤-2 恶化、可信度 D/E |

状态分布：OPEN 14 / VERIFYING 4 / IN_PROGRESS 3 / ACKNOWLEDGED 3 / SUPPRESSED 2。

---

## 4. 走查剧本（验收依据）

**S1 班长早会分诊（主场景）**
- **Given** 常压塔单元 26 条关注项，IC 工程师登录
- **When** 打开 `/monitor/attention`
- **Then** ① 3 秒内看到：开放 14 · URGENT 3 · 验证超期 2；② 首屏前三行即 3 条 URGENT（红底），FIC-2209 在最上（未确认阶段优先）；③ 每行"为什么排在这"自证（"严重预警未确认 · 重复触发 3 次"）；④ 点 FIC-2209 主按钮"进入工作台"→ 01 工作台打开且携带 `eventId`，面包屑"← 关注队列"；⑤ 返回 → 筛选/页码完整恢复

**S2 一键确认**
- **Given** FIC-2209 CRITICAL 预警（OPEN）
- **When** ⋯ 菜单点"确认"
- **Then** ① 复用 acknowledgeEventApi 成功；② 行状态变"已确认"，重新排序落到 ACKNOWLEDGED 阶段；③ R2 开放数 14→13（aggregates 联动）

**S3 验证超期专项**
- **Given** 来源 chip 点"验证超期"
- **When** 筛选 VERIFICATION
- **Then** ① 列表 3 条，LIC-101"验证超期 26 小时"（HIGH）；② 速览卡计数同步为筛选集合口径；③ 点"查看工单"→ 工单详情

**S4 SPONSOR 只读**
- **Then** ① 无"进入工作台"主按钮（服务端不返回 OPEN_WORKBENCH）；② 仅"查看详情/返回运行总览"；③ 无确认/处置/误报入口；④ 摘要与速览卡可读

**S5 PE/EXPERT 禁用显因**
- **Then** 确认/处置/误报按钮禁用，tooltip 显示"当前角色无确认权限"（服务端 disabledReason 原文）

**S6 深链接**
- **When** `/monitor/attention?eventId=xxx`
- **Then** 自动打开该事件详情抽屉；`?source=ALERT` 自动按活跃预警筛选

**S7 空态分级**
- **Then** ① 切换到一个无关注项的单元 → 绿色空态"今日无例外，回路运行正常"（非缺失感）；② 加筛选无结果 → "筛选无结果 · 清除筛选"

**S8 性能契约（E-3）**
- **Given** 1000 回路/10000 开放项压测 fixture
- **Then** 接口 p95 ≤500ms；首屏 p95 达标；DOM 节点数受控；截断提示按 Q-14 显式

---

## 5. 验收清单

- [ ] 路由保持 `/monitor/attention`；六态全实现（empty-green 与 empty-filter 严格区分）
- [ ] R2 摘要/R2.5 速览卡/来源 chip 全部来自响应 `aggregates` 单一聚合源，前端不另算
- [ ] 分诊表 9 默认列；URGENT 红底行/HIGH 左红条；rankReasons 独立列 chips 展示（最多 2 条+更多 tooltip）
- [ ] 无列排序选择器；帮助面板含五来源定义+优先级规则+排序规则（与 monitor_attention.py docstring 逐字一致）
- [ ] 操作列：primaryAction 唯一主按钮强化；写动作 ⋯ 折叠+二次确认+备注；SPONSOR 只读；PE/EXPERT 禁用显因（服务端 disabledReason 直渲染）
- [ ] 下钻携带 `from=/monitor/attention` + `loopId/section/eventId/trackerId`；返回恢复筛选页码；深链接 eventId/source 生效
- [ ] 截断语义三处显性（R2.5/R5/截断提示条）；单来源 500 上限文案兜底（E-2 扩展前）
- [ ] 装置·单元列依赖 G1 修复；修复前隐藏不显示空列；plantNodeId 递归依赖 G2 修复
- [ ] summary/rankReasons 全部为后端规则模板（R-A 文字第②级），页面无任何 LLM 生成文案
- [ ] 1000 回路/10000 开放项 p95 ≤500ms 压测通过（E-3）

---

## 6. 线框图与验证截图

- 可交互线框图：[03-关注队列线框图 HTML](./assets/03-关注队列线框图-2026-08-12.html)（浏览器直接打开；优先级速览卡/来源 chip 可点筛选、左脊柱"减压塔单元"演示绿色空态、操作列含跳转演示）
- 验证截图：`assets/03-关注队列线框图-分诊视图.png`（默认态）、`assets/03-关注队列线框图-优先级筛选.png`（URGENT 筛选+截断语义）、`assets/03-关注队列线框图-绿色空态.png`（S7 empty-green）
