# CLPM 集成测试用例清单（S8）

> **版本**：v1.0
> **创建日期**：2026-06-22
> **覆盖范围**：S8-TEST-001 ~ S8-TEST-006 全部 6 个任务
> **设计依据**：D01 PRD、D02 FDS、D05 IDS v3.2、D06 UI/UX v4.1、D07 部署运维设计、D09 关键算法设计说明
> **测试环境**：Orbstack 虚拟化（PostgreSQL 16 + TDengine 3.3.6.0 + Redis 7）

---

## 目录

- [1. 测试总览](#1-测试总览)
- [2. 测试环境与前置条件](#2-测试环境与前置条件)
- [3. S8-TEST-001 后端单元测试](#3-s8-test-001-后端单元测试)
- [4. S8-TEST-002 前端单元测试](#4-s8-test-002-前端单元测试)
- [5. S8-TEST-003 E2E 端到端测试](#5-s8-test-003-e2e-端到端测试)
- [6. S8-TEST-004 性能压测](#6-s8-test-004-性能压测)
- [7. S8-TEST-005 安全扫描](#7-s8-test-005-安全扫描)
- [8. S8-TEST-006 部署与发布](#8-s8-test-006-部署与发布)
- [9. 测试用例汇总统计](#9-测试用例汇总统计)

---

## 1. 测试总览

### 1.1 测试范围

| 任务 ID | 任务名称 | 测试类型 | 用例数 | 优先级 |
|---------|----------|----------|--------|--------|
| S8-TEST-001 | 后端单元测试（pytest） | 单元测试 | 42 | P0 |
| S8-TEST-002 | 前端单元测试（Vitest） | 单元测试 | 28 | P1 |
| S8-TEST-003 | E2E 端到端测试（Playwright） | 端到端 | 22 | P1 |
| S8-TEST-004 | 性能压测（Locust） | 性能 | 12 | P1 |
| S8-TEST-005 | 安全扫描与渗透测试 | 安全 | 18 | P0 |
| S8-TEST-006 | 部署与发布验证 | DevOps | 14 | P0 |
| **合计** | | | **136** | |

### 1.2 角色权限矩阵（测试基准）

| 角色 | 用户名 | 权限范围 | 默认首页 |
|------|--------|----------|----------|
| ADMIN | admin | `["*"]` 全部权限 | /dashboard |
| IC_ENGINEER | ic_engineer | loop/metric/diagnosis/tuning/portal | /dashboard |
| PE_ENGINEER | pe_engineer | loop:view/metric:view/diagnosis:view/portal/tracker | /dashboard |
| SPONSOR | sponsor | portal:view/metric:view/diagnosis:view | /dashboard |
| EXPERT | expert | portal:view/metric:view/diagnosis:view/tracker:review | /dashboard |

> 5 个种子用户密码统一为 `admin123`

### 1.3 API 端点统计

| 模块 | 端点数 | 公开 | 已认证 | 角色受限 |
|------|--------|------|--------|----------|
| 健康检查 | 1 | 1 | 0 | 0 |
| 认证 | 6 | 2 | 3 | 1 |
| 工厂节点 | 4 | 0 | 1 | 3 |
| 回路台账 | 9 | 0 | 5 | 4 |
| AAS | 5 | 0 | 1 | 4 |
| 性能评估 | 8 | 0 | 8 | 0 |
| 工作台 | 1 | 0 | 1 | 0 |
| 诊断中心 | 8 | 0 | 8 | 0 |
| 用户管理 | 5 | 0 | 0 | 5 |
| 审计日志 | 1 | 0 | 0 | 1 |
| 报表配置 | 5 | 0 | 0 | 5 |
| 回路整定 | 8 | 0 | 4 | 4 |
| **合计** | **61** | **3** | **31** | **27** |

---

## 2. 测试环境与前置条件

### 2.1 基础设施

| 服务 | 容器 | 端口 | 健康检查 |
|------|------|------|----------|
| PostgreSQL 16 | clpm-postgres | 5432 | `SELECT 1` |
| TDengine 3.3.6.0 | clpm-tdengine | 6030 | `SHOW DATABASES` 含 clpm_ts |
| Redis 7 | clpm-redis | 6379 | `PING` → PONG |

### 2.2 应用服务

| 服务 | 端口 | 启动命令 |
|------|------|----------|
| 后端 API | 8001 | `cd backend && uv run uvicorn app.main:app --port 8001 --reload` |
| 前端 Dev | 5666 | `cd frontend && pnpm run dev:antd` |
| Celery Worker | — | `cd backend && uv run celery -A app.celery_app worker -l info` |
| Celery Beat | — | `cd backend && uv run celery -A app.celery_app beat -l info` |

### 2.3 测试数据

- 种子数据：`db/postgresql/02_seed_data.sql`（5 用户 + 3 回路 + 18 AAS Tag + 6 KPI 配置 + 8 诊断配置 + 1 整定记录）
- TDengine 数据：需预先注入测试波形数据（至少 3 回路 × 24 小时 × 1 秒采样）

### 2.4 通用前置条件

- 所有容器健康运行
- 数据库已执行 schema + seed
- Alembic 迁移在 head（`a1b2c3d4e5f6`）
- 后端服务可访问 `/health` 返回 200
- 前端服务可访问 `http://localhost:5666` 返回 200

---

## 3. S8-TEST-001 后端单元测试

> **目标**：补充后端单元测试，覆盖率 ≥85%
> **框架**：pytest + pytest-asyncio + unittest.mock
> **现有基线**：267 测试通过（13 文件）
> **测试模式**：TestClient (sync) + mock_current_user + mock_db fixture

### 3.1 认证模块（补充）

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-AUTH-001 | 登录成功 | 种子用户存在 | POST /auth/login {username:"admin", password:"admin123"} | 200，返回 accessToken + refreshToken + 用户信息 | P0 |
| UT-AUTH-002 | 登录失败-密码错误 | — | POST /auth/login {username:"admin", password:"wrong"} | 400，code=ERR_INVALID_CREDENTIALS | P0 |
| UT-AUTH-003 | 登录失败-用户不存在 | — | POST /auth/login {username:"nouser", password:"x"} | 400，code=ERR_INVALID_CREDENTIALS | P0 |
| UT-AUTH-004 | 登录失败锁定-5次 | — | 连续 5 次错误密码登录 | 第 5 次返回 423，code=ERR_ACCOUNT_LOCKED | P0 |
| UT-AUTH-005 | 登录失败锁定-15分钟解锁 | 账户已锁定 | 等待 15 分钟后正确密码登录 | 200 登录成功 | P1 |
| UT-AUTH-006 | 刷新令牌成功 | 持有有效 refreshToken | POST /auth/refresh {refreshToken} | 200，返回新 accessToken | P0 |
| UT-AUTH-007 | 刷新令牌失败-过期 | refreshToken 已过期 | POST /auth/refresh {expiredToken} | 401，code=ERR_TOKEN_EXPIRED | P0 |
| UT-AUTH-008 | 登出成功 | 已登录 | POST /auth/logout (Bearer token) | 200，token jti 加入 Redis 黑名单 | P0 |
| UT-AUTH-009 | 登出后令牌失效 | 已登出 | 使用登出前的 token 请求 /auth/me | 401，code=ERR_TOKEN_INVALID | P0 |
| UT-AUTH-010 | 获取当前用户 | 已登录 | GET /auth/me | 200，返回 username/role/permissions/defaultHome | P0 |
| UT-AUTH-011 | 修改密码成功 | 已登录 | PUT /auth/password {oldPassword, newPassword} | 200，旧 token 全部失效 | P0 |
| UT-AUTH-012 | 修改密码失败-旧密码错误 | 已登录 | PUT /auth/password {wrongOld, new} | 400，code=ERR_INVALID_CREDENTIALS | P0 |
| UT-AUTH-013 | 伪造 JWT 被拒 | — | 使用伪造签名的 JWT 请求 | 401，code=ERR_TOKEN_INVALID | P0 |
| UT-AUTH-014 | 过期 JWT 被拒 | — | 使用过期 JWT 请求 | 401，code=ERR_TOKEN_EXPIRED | P0 |

### 3.2 回路管理模块（补充）

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-LOOP-001 | 创建回路成功 | ADMIN 登录 | POST /loops {tagName, unitId, description} | 201，返回 loopId，status=INCONCLUSIVE | P0 |
| UT-LOOP-002 | 创建回路-权限拒绝 | SPONSOR 登录 | POST /loops | 403，code=ERR_PERMISSION_DENIED | P0 |
| UT-LOOP-003 | 回路列表分页 | — | GET /loops?page=1&pageSize=10 | 200，返回 items/total/page/pageSize | P0 |
| UT-LOOP-004 | 回路列表-关键词搜索 | — | GET /loops?keyword=L001 | 200，仅返回含 L001 的回路 | P1 |
| UT-LOOP-005 | 回路详情 | 回路存在 | GET /loops/{loopId} | 200，返回 basicInfo/tagMapping/runtimeParams/aasSyncStatus | P0 |
| UT-LOOP-006 | 更新回路-评分权重 | ADMIN 登录 | PUT /loops/{id} {scoreWeights} | 200，权重总和校验=100 | P0 |
| UT-LOOP-007 | 更新回路-权重总和≠100 | ADMIN 登录 | PUT /loops/{id} {scoreWeights 总和=90} | 400，code=ERR_INVALID_WEIGHTS | P0 |
| UT-LOOP-008 | 删除回路-无关联Tag | ADMIN 登录 | DELETE /loops/{id} | 200，返回 deleted=true | P0 |
| UT-LOOP-009 | 删除回路-有关联Tag | 回路有 Tag 关联 | DELETE /loops/{id} | 400，code=ERR_LOOP_HAS_TAGS | P0 |
| UT-LOOP-010 | Tag关联-完整映射 | ADMIN 登录 | PUT /loops/{id}/tags {pv,sp,op,mode,...} | 200，status 推导为 Ready | P0 |
| UT-LOOP-011 | Tag关联-缺PV | ADMIN 登录 | PUT /loops/{id}/tags {sp,op,mode}（无 pv） | 400，code=ERR_TAG_REQUIRED | P0 |
| UT-LOOP-012 | 回路监控列表 | — | GET /loops/monitor | 200，返回 currentValues(PV/SP/OP/MODE/quality) | P0 |
| UT-LOOP-013 | 回路监控详情-趋势 | 回路存在 | GET /loops/{id}/monitor?trendWindow=last_24_hours | 200，返回 trend(pv/sp/op 数组) + kpiSummary | P0 |

### 3.3 性能评估模块（补充）

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-PERF-001 | 指标配置列表 | — | GET /performance/metrics | 200，返回 6 项 KPI 配置 | P0 |
| UT-PERF-002 | 更新指标配置-权重合法 | ADMIN 登录 | PUT /performance/metrics/{id} {weight} | 200，6 项权重总和仍=100 | P0 |
| UT-PERF-003 | 更新指标配置-权限拒绝 | IC_ENGINEER 登录 | PUT /performance/metrics/{id} | 403 | P0 |
| UT-PERF-004 | 全局看板-缓存命中 | Redis 有缓存 | GET /performance/board | 200，响应头含 X-Cache-Hit | P1 |
| UT-PERF-005 | 全局看板-缓存未命中 | Redis 无缓存 | GET /performance/board | 200，查询 DB 并写入 Redis | P1 |
| UT-PERF-006 | 低效回路排行 | — | GET /performance/ranking?limit=10 | 200，按 score 升序排列 | P0 |
| UT-PERF-007 | 统计报表 | — | GET /performance/analytics?startTime=...&endTime=... | 200，返回趋势+分布数据 | P0 |
| UT-PERF-008 | 导出CSV | — | POST /performance/analytics/export | 200，Content-Type=text/csv | P1 |

### 3.4 诊断中心模块（补充）

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-DIAG-001 | 诊断列表-标签筛选 | — | GET /diagnosis/list?diagnosisLabel=OSCILLATION | 200，仅返回振荡标签 | P0 |
| UT-DIAG-002 | 诊断详情-8类标签 | 回路有诊断结果 | GET /diagnosis/{loopId} | 200，返回 diagnosisLabels 数组 + evidenceChain | P0 |
| UT-DIAG-003 | 波形查询-正常 | 回路有 TDengine 数据 | GET /timeseries/{id}/waveform?startTime=...&endTime=... | 200，返回 timestamps/pv/sp/op/mode/pvQuality | P0 |
| UT-DIAG-004 | 波形查询-超30天 | 时间范围>30天 | GET /timeseries/{id}/waveform?startTime=...(40天前) | 400，code=ERR_TS_001 | P0 |
| UT-DIAG-005 | 波形查询-LTTB降采样 | 数据点>1000 | GET /timeseries/{id}/waveform?downsample=true | 200，数据点≤1000 | P1 |
| UT-DIAG-006 | Tracker状态更新 | IC_ENGINEER 登录 | PATCH /tracker/{id}/status {status:RESOLVED} | 200，自动生成 A/B 对比 | P0 |
| UT-DIAG-007 | Tracker状态更新-权限拒绝 | SPONSOR 登录 | PATCH /tracker/{id}/status | 403 | P0 |
| UT-DIAG-008 | 诊断统计报表 | — | GET /diagnosis/analytics?startTime=...&endTime=... | 200，返回 labelDistribution + efficiencyTrend | P0 |

### 3.5 系统管理模块（补充）

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-SYS-001 | 用户列表-ADMIN | ADMIN 登录 | GET /users?page=1 | 200，返回用户列表 | P0 |
| UT-SYS-002 | 用户列表-权限拒绝 | IC_ENGINEER 登录 | GET /users | 403 | P0 |
| UT-SYS-003 | 创建用户-用户名重复 | ADMIN 登录 | POST /users {username:"admin"} | 400，code=ERR_USER_DUPLICATE | P0 |
| UT-SYS-004 | 禁用用户-软删除 | ADMIN 登录 | DELETE /users/{id} | 200，is_active=false | P0 |
| UT-SYS-005 | 审计日志查询 | ADMIN 登录 | GET /audit-logs?page=1 | 200，返回操作日志列表 | P0 |
| UT-SYS-006 | 报表配置CRUD | ADMIN 登录 | POST /reports/configs → GET → PUT → generate | 全流程 200 | P0 |
| UT-SYS-007 | 报表任务轮询 | 已触发生成 | GET /reports/tasks/{taskId} 每 2 秒轮询 | 状态从 PENDING→RUNNING→COMPLETED | P1 |

### 3.6 回路整定模块（补充）

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-TUNE-001 | 模型辨识-FOPDT | 回路有波形数据 | POST /tuning/identify {loopId, modelType:"FOPDT"} | 200，返回 K/tau/theta + fittingScore | P0 |
| UT-TUNE-002 | 模型辨识-数据不足 | 数据点<10 | POST /tuning/identify | 400，code=ERR_TUNING_DATA_INSUFFICIENT | P0 |
| UT-TUNE-003 | PID整定-IMC | 已有模型参数 | POST /tuning/tune {modelType, modelParams, algorithm:"IMC"} | 200，返回 recommendedPid(kp/ti/td) | P0 |
| UT-TUNE-004 | PID整定-无效算法 | — | POST /tuning/tune {algorithm:"INVALID"} | 400，code=ERR_INVALID_ALGORITHM | P0 |
| UT-TUNE-005 | 闭环仿真 | 已有 current+recommended PID | POST /tuning/simulate | 200，返回 timestamps + currentResponse + recommendedResponse + metrics | P0 |
| UT-TUNE-006 | 整定任务保存 | ADMIN 登录 | POST /tuning/tasks | 200，返回 taskId | P0 |
| UT-TUNE-007 | 整定历史统计 | — | GET /tuning/history | 200，返回 totalTasks/byAlgorithm/byStatus | P0 |

### 3.7 异常分支与边界条件

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-ERR-001 | 无Token访问受保护端点 | — | GET /users（无 Authorization） | 401，code=ERR_TOKEN_MISSING | P0 |
| UT-ERR-002 | 格式错误Token | — | Authorization: Bearer invalid_format | 401，code=ERR_TOKEN_INVALID | P0 |
| UT-ERR-003 | 资源不存在-404 | — | GET /loops/00000000-0000-0000-0000-999999999999 | 404，code=ERR_LOOP_NOT_FOUND | P0 |
| UT-ERR-004 | 请求体校验失败 | — | POST /auth/login（缺 password 字段） | 422，Pydantic 校验错误 | P0 |
| UT-ERR-005 | 分页参数越界 | — | GET /loops?page=0 | 422，page 必须 ≥1 | P1 |
| UT-ERR-006 | UUID格式错误 | — | GET /loops/not-a-uuid | 422 或 404 | P1 |

---

## 4. S8-TEST-002 前端单元测试

> **目标**：前端组件测试覆盖率 ≥75%
> **框架**：Vitest + @vue/test-utils + jsdom
> **现有基线**：无任何前端测试（需从零搭建）

### 4.1 Pinia Store 测试

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-STORE-001 | userStore-login 成功 | mock API 返回 token | 调用 userStore.login(username, password) | state.accessToken 已设置，userInfo 已填充 | P0 |
| UT-STORE-002 | userStore-logout | 已登录状态 | 调用 userStore.logout() | state 清空，路由跳转 /auth/login | P0 |
| UT-STORE-003 | userStore-refreshToken | accessToken 过期 | 调用 userStore.refreshToken() | accessToken 已更新 | P0 |
| UT-STORE-004 | userStore-getUserInfo | mock API | 调用 getUserInfo() | 返回 role/permissions/defaultHome | P0 |
| UT-STORE-005 | permissionStore-generateRoutes | ADMIN 角色 | 调用 generateRoutes(roles) | 返回全部路由，无过滤 | P0 |
| UT-STORE-006 | permissionStore-generateRoutes-SPONSOR | SPONSOR 角色 | 调用 generateRoutes(roles) | 仅返回有权限的路由 | P0 |
| UT-STORE-007 | permissionStore-hasPermission 通配符 | 权限=["*"] | hasPermission("loop:view") | 返回 true | P1 |
| UT-STORE-008 | permissionStore-hasPermission 精确匹配 | 权限=["loop:view"] | hasPermission("loop:edit") | 返回 false | P1 |

### 4.2 Axios 拦截器测试

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-HTTP-001 | 请求拦截-Token注入 | 已登录 | 发起请求 | Header 含 Authorization: Bearer {token} | P0 |
| UT-HTTP-002 | 请求拦截-无Token | 未登录 | 发起请求 | 无 Authorization Header | P0 |
| UT-HTTP-003 | 响应拦截-200成功 | API 返回 200 | 处理响应 | 返回 response.data | P0 |
| UT-HTTP-004 | 响应拦截-401触发刷新 | accessToken 过期 | API 返回 401 | 自动调用 refreshToken，重试原请求 | P0 |
| UT-HTTP-005 | 响应拦截-刷新失败跳登录 | refreshToken 也过期 | 刷新返回 401 | 清空 store，跳转 /auth/login | P0 |
| UT-HTTP-006 | 响应拦截-400业务错误 | API 返回 400 + code | 处理响应 | message.error 显示错误，reject Promise | P0 |
| UT-HTTP-007 | 响应拦截-403权限拒绝 | API 返回 403 | 处理响应 | message.error "权限不足" | P0 |
| UT-HTTP-008 | 响应拦截-网络错误 | 请求超时/断网 | 处理响应 | message.error "网络异常" | P1 |

### 4.3 工具函数测试

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-UTIL-001 | formatTime-ISO字符串 | — | formatTime("2026-06-22T10:00:00Z") | 返回本地化时间字符串 | P1 |
| UT-UTIL-002 | formatTime-空值 | — | formatTime("") | 返回 "—" | P1 |
| UT-UTIL-003 | confidenceColor-高置信度 | — | confidenceColor(0.9) | 返回 "#52c41a" | P1 |
| UT-UTIL-004 | confidenceColor-低置信度 | — | confidenceColor(0.3) | 返回 "#ff4d4f" | P1 |
| UT-UTIL-005 | flattenNodes-树扁平化 | 工厂节点树 | flattenNodes(treeData) | 返回扁平数组，含所有层级 | P1 |
| UT-UTIL-006 | labelName-标签映射 | — | labelName("OSCILLATION") | 返回 "振荡" | P1 |

### 4.4 关键交互逻辑测试

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| UT-INTERACT-001 | 登录表单-空用户名 | — | 提交空表单 | 显示校验错误 "请输入用户名" | P0 |
| UT-INTERACT-002 | 登录表单-空密码 | — | 仅填用户名提交 | 显示校验错误 "请输入密码" | P0 |
| UT-INTERACT-003 | v-permission指令-有权限 | 用户权限含 loop:view | 渲染 v-permission="'loop:view'" | 元素可见 | P1 |
| UT-INTERACT-004 | v-permission指令-无权限 | 用户权限不含 loop:edit | 渲染 v-permission="'loop:edit'" | 元素隐藏 | P1 |
| UT-INTERACT-005 | v-permission-通配符 | 用户权限=["*"] | 渲染任意 v-permission | 元素可见 | P1 |
| UT-INTERACT-006 | 表格分页-页码切换 | 表格已加载 | 点击第 2 页 | 触发 loadList，page=2 | P1 |

---

## 5. S8-TEST-003 E2E 端到端测试

> **目标**：覆盖 6 大业务全流程 + 5 类角色权限验证
> **框架**：Playwright + @playwright/test
> **测试策略**：真实后端 + 真实数据库（非 mock）

### 5.1 登录流程

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| E2E-LOGIN-001 | ADMIN 登录成功 | 后端运行 | 1. 访问 /auth/login 2. 输入 admin/admin123 3. 点击登录 | 跳转 /dashboard，显示工作台 | P0 |
| E2E-LOGIN-002 | 登录失败-错误密码 | — | 输入 admin/wrong → 登录 | 显示错误提示，停留在登录页 | P0 |
| E2E-LOGIN-003 | 登出 | 已登录 | 点击用户菜单 → 登出 | 跳转 /auth/login，token 清除 | P0 |
| E2E-LOGIN-004 | Token 过期自动跳转 | token 已过期 | 访问任意受保护页面 | 自动跳转 /auth/login | P1 |

### 5.2 回路管理全流程

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| E2E-LOOP-001 | 创建回路 | ADMIN 登录 | 1. /loop/ledger 2. 点击新建 3. 填写 tagName/unitId 4. 提交 | 列表出现新回路，status=INCONCLUSIVE | P0 |
| E2E-LOOP-002 | Tag 关联 | 回路已创建 | 1. /loop/tag-mapping 2. 选择回路 3. 关联 PV/SP/OP/MODE 4. 保存 | status 推导为 Ready | P0 |
| E2E-LOOP-003 | 回路监控 | 回路有 Tag | 1. /loop/monitor 2. 查看列表 | 显示 PV/SP/OP/MODE 实时值 + 评分 | P0 |
| E2E-LOOP-004 | 回路详情 | 回路存在 | 点击回路 → 详情页 | 显示 basicInfo + tagMapping + runtimeParams + 趋势图 | P0 |

### 5.3 性能评估全流程

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| E2E-PERF-001 | 指标配置 | ADMIN 登录 | 1. /metric/config 2. 修改权重 3. 保存 | 6 项权重总和=100，保存成功 | P0 |
| E2E-PERF-002 | 全局看板 | — | 访问 /metric/dashboard | 显示 KPI 卡片 + 趋势图 + 低效回路 | P0 |
| E2E-PERF-003 | 低效排行 | — | 访问 /metric/ranking | 显示按评分升序的回路列表 | P0 |

### 5.4 诊断中心全流程

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| E2E-DIAG-001 | 诊断列表 | — | 1. /diagnosis/list 2. 筛选标签 | 显示诊断列表，标签颜色区分 | P0 |
| E2E-DIAG-002 | 波形查看 | 回路有诊断 | 1. /diagnosis/waveform 2. 选择回路 3. 查询 | ECharts 显示 PV/SP/OP 趋势线 | P0 |
| E2E-DIAG-003 | Tracker 处理 | IC_ENGINEER 登录 | 1. /diagnosis/tracker 2. 选择记录 3. 更新状态为 RESOLVED | 状态更新成功，生成 A/B 对比 | P0 |

### 5.5 系统管理全流程

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| E2E-SYS-001 | 用户 CRUD | ADMIN 登录 | 1. /system/users 2. 新建用户 3. 编辑 4. 禁用 | 全流程成功 | P0 |
| E2E-SYS-002 | 审计日志 | ADMIN 登录 | 访问 /system/audit | 显示操作日志列表 | P0 |

### 5.6 多角色权限验证

| 用例 ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| E2E-ROLE-001 | SPONSOR 菜单限制 | sponsor 登录 | 查看左侧菜单 | 仅可见 工作台/性能评估/诊断中心，无回路管理/整定/系统管理 | P0 |
| E2E-ROLE-002 | SPONSOR 直接访问受限页 | sponsor 登录 | 手动输入 /loop/ledger | 跳转 403 或重定向 | P0 |
| E2E-ROLE-003 | PE_ENGINEER 无整定权限 | pe_engineer 登录 | 查看菜单 | 无"回路整定"菜单项 | P0 |
| E2E-ROLE-004 | EXPERT 有整定权限 | expert 登录 | 访问 /tuning/workbench | 正常显示整定工作台 | P0 |
| E2E-ROLE-005 | IC_ENGINEER 全业务权限 | ic_engineer 登录 | 查看菜单 | 可见 回路/性能/诊断/整定/系统(部分) | P0 |

---

## 6. S8-TEST-004 性能压测

> **目标**：验证非功能需求指标
> **框架**：Locust + Python 脚本
> **验收标准**：API P95 < 500ms / 前端首屏 < 3s / TDengine 1万点 < 200ms / Redis 命中率 > 90%

### 6.1 API 响应时间

| 用例 ID | 用例名称 | 并发用户 | 持续时间 | 验收指标 | 优先级 |
|---------|----------|----------|----------|----------|--------|
| PERF-API-001 | 登录接口 | 50 | 2 分钟 | P95 < 200ms，错误率 0% | P0 |
| PERF-API-002 | 回路列表查询 | 100 | 3 分钟 | P95 < 300ms | P0 |
| PERF-API-003 | 回路监控列表 | 100 | 3 分钟 | P95 < 500ms | P0 |
| PERF-API-004 | 诊断列表 | 80 | 3 分钟 | P95 < 400ms | P0 |
| PERF-API-005 | 波形查询-24小时 | 50 | 2 分钟 | P95 < 500ms（LTTB 降采样后） | P0 |
| PERF-API-006 | 工作台聚合 API | 100 | 3 分钟 | P95 < 500ms（缓存命中 < 100ms） | P0 |

### 6.2 数据库性能

| 用例 ID | 用例名称 | 数据量 | 验收指标 | 优先级 |
|---------|----------|--------|----------|--------|
| PERF-DB-001 | TDengine 查询 1 万点 | 10,000 数据点 | < 200ms | P0 |
| PERF-DB-002 | TDengine 查询 24 小时波形 | ~86,400 点 | LTTB 降采样后 < 500ms | P0 |
| PERF-DB-003 | PostgreSQL 回路列表 | 1200 回路 | < 100ms | P1 |

### 6.3 前端性能

| 用例 ID | 用例名称 | 验收指标 | 优先级 |
|---------|----------|----------|--------|
| PERF-FE-001 | 首屏加载时间 | < 3 秒（Lighthouse） | P0 |
| PERF-FE-002 | 工作台 ECharts 渲染 | 6 图表 < 2 秒 | P1 |

### 6.4 缓存与并发

| 用例 ID | 用例名称 | 验收指标 | 优先级 |
|---------|----------|----------|--------|
| PERF-CACHE-001 | Redis 缓存命中率 | > 90%（工作台/看板） | P0 |
| PERF-CONC-001 | 1200 回路 KPI 计算 | 1 小时内完成（Celery Beat） | P0 |

---

## 7. S8-TEST-005 安全扫描

> **目标**：JWT 安全 + SQL 注入 + XSS/CSRF + 权限越权 + 只读边界 + 依赖漏洞
> **优先级**：P0（安全一票否决）

### 7.1 JWT 安全性

| 用例 ID | 用例名称 | 测试方法 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| SEC-JWT-001 | 伪造签名 Token | 使用错误密钥签发 JWT | 401 拒绝 | P0 |
| SEC-JWT-002 | 过期 Token | 使用过期 accessToken | 401 拒绝 | P0 |
| SEC-JWT-003 | 黑名单 Token | 登出后重用原 Token | 401 拒绝（jti 在 Redis 黑名单） | P0 |
| SEC-JWT-004 | refreshToken 冒充 accessToken | 用 refreshToken 访问 API | 401 拒绝（token_type 校验） | P0 |
| SEC-JWT-005 | 算法混淆攻击 | HS256 → none 算法 | 401 拒绝 | P0 |

### 7.2 注入攻击

| 用例 ID | 用例名称 | 测试方法 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| SEC-INJ-001 | SQL 注入-登录 | username: `' OR '1'='1` | 登录失败，无数据泄露 | P0 |
| SEC-INJ-002 | SQL 注入-查询参数 | keyword: `'; DROP TABLE loops;--` | 参数化查询，无注入 | P0 |
| SEC-INJ-003 | XSS-回路描述 | description: `<script>alert(1)</script>` | 存储后前端渲染转义，不执行 | P0 |
| SEC-INJ-004 | CSRF-跨站请求 | 构造跨站 POST | 被 CORS 策略拦截 | P0 |

### 7.3 权限越权

| 用例 ID | 用例名称 | 测试方法 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| SEC-AUTH-001 | 垂直越权-SPONSOR 访问用户管理 | SPONSOR token → GET /users | 403 拒绝 | P0 |
| SEC-AUTH-002 | 垂直越权-PE_ENGINEER 访问整定 | PE token → POST /tuning/identify | 403 拒绝 | P0 |
| SEC-AUTH-003 | 水平越权-IC_ENGINEER 访问他人回路 | IC token → GET /loops/{他人回路ID} | 403 或数据范围限制 | P0 |
| SEC-AUTH-004 | 禁用用户访问 | is_active=false 用户 token | 403，code=ERR_ACCOUNT_DISABLED | P0 |

### 7.4 只读边界验证

| 用例 ID | 用例名称 | 测试方法 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| SEC-RO-001 | AAS 代码无 Write 调用 | grep 代码库 `asyncua.*write\|write_value\|method_call` | 仅 Read 调用，无 Write | P0 |
| SEC-RO-002 | TDengine 仅查询 | grep 代码库 `INSERT\|UPDATE\|DELETE` on TDengine SQL | 无写操作 | P0 |

### 7.5 依赖漏洞

| 用例 ID | 用例名称 | 测试方法 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| SEC-DEP-001 | 后端依赖扫描 | `uv run pip audit` | 无高危漏洞 | P0 |
| SEC-DEP-002 | 前端依赖扫描 | `pnpm audit --high` | 无高危漏洞 | P0 |
| SEC-DEP-003 | 密钥硬编码扫描 | grep 代码库 `password\|secret\|key` | 无硬编码密钥（除 .env） | P0 |

---

## 8. S8-TEST-006 部署与发布

> **目标**：Docker 镜像构建 + docker-compose 生产配置 + 数据库迁移 + Nginx + 健康检查 + 回滚
> **设计依据**：D07 部署运维设计 v1.0

### 8.1 Docker 镜像构建

| 用例 ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| DEPLOY-IMG-001 | 后端镜像构建 | `docker build -f Dockerfile.backend -t clpm-backend:latest .` | 构建成功，镜像大小 < 500MB | P0 |
| DEPLOY-IMG-002 | 前端镜像构建 | `pnpm run build:antd && docker build -f Dockerfile.frontend -t clpm-frontend:latest .` | 构建成功，Nginx 静态文件 | P0 |
| DEPLOY-IMG-003 | 镜像启动验证 | `docker run -d clpm-backend` + `curl /health` | 返回 200 | P0 |

### 8.2 docker-compose 生产配置

| 用例 ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| DEPLOY-COMP-001 | compose 启动全部服务 | `docker-compose -f docker-compose.prod.yml up -d` | 5 服务全部 healthy（backend/frontend/postgres/tdengine/redis） | P0 |
| DEPLOY-COMP-002 | 环境变量注入 | 检查 .env.prod 加载 | 配置正确注入容器 | P0 |
| DEPLOY-COMP-003 | 数据卷持久化 | 重启容器后检查数据 | PostgreSQL/TDengine 数据不丢失 | P0 |

### 8.3 数据库迁移

| 用例 ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| DEPLOY-DB-001 | 空库迁移 | `alembic upgrade head` | 14 张表 + 索引 + 约束创建成功 | P0 |
| DEPLOY-DB-002 | 种子数据导入 | `psql -f 02_seed_data.sql` | 5 用户 + 3 回路 + 配置数据导入成功 | P0 |
| DEPLOY-DB-003 | 迁移幂等性 | 重复执行 `alembic upgrade head` | 无变化，无报错 | P1 |

### 8.4 Nginx 反向代理

| 用例 ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| DEPLOY-NGINX-001 | 前端静态资源 | `curl http://localhost/` | 返回 index.html | P0 |
| DEPLOY-NGINX-002 | API 代理 | `curl http://localhost/api/v1/health` | 代理到后端 8001，返回 200 | P0 |
| DEPLOY-NGINX-003 | WebSocket 代理 | 前端 WS 连接 | Nginx Upgrade 头透传 | P1 |
| DEPLOY-NGINX-004 | HTTPS 证书 | `curl https://localhost/` | TLS 握手成功 | P1 |

### 8.5 健康检查与回滚

| 用例 ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|---------|----------|----------|----------|--------|
| DEPLOY-HEALTH-001 | 后端健康检查 | `curl /health` | 200，{status:"healthy"} | P0 |
| DEPLOY-HEALTH-002 | 数据库连通性 | 健康检查含 DB ping | 200，含 db:"ok" | P0 |
| DEPLOY-ROLLBACK-001 | 回滚到上一版本 | `docker-compose down && docker tag clpm-backend:prev clpm-backend:latest && up` | 服务恢复到上一版本，数据完整 | P0 |

---

## 9. 测试用例汇总统计

### 9.1 按任务统计

| 任务 ID | 用例数 | P0 | P1 | 自动化目标 |
|---------|--------|----|----|------------|
| S8-TEST-001 后端单元 | 42 | 36 | 6 | 100% 自动化（pytest） |
| S8-TEST-002 前端单元 | 28 | 14 | 14 | 100% 自动化（Vitest） |
| S8-TEST-003 E2E | 22 | 18 | 4 | 100% 自动化（Playwright） |
| S8-TEST-004 性能压测 | 12 | 9 | 3 | 100% 自动化（Locust） |
| S8-TEST-005 安全扫描 | 18 | 18 | 0 | 80% 自动化 + 20% 人工 |
| S8-TEST-006 部署发布 | 14 | 12 | 2 | 70% 自动化 + 30% 人工 |
| **合计** | **136** | **107** | **29** | |

### 9.2 按模块统计

| 模块 | 单元测试 | E2E | 性能 | 安全 | 合计 |
|------|----------|-----|------|------|------|
| 认证 | 14 | 4 | 1 | 5 | 24 |
| 回路管理 | 13 | 4 | 3 | 3 | 23 |
| 性能评估 | 8 | 3 | 2 | 0 | 13 |
| 诊断中心 | 8 | 3 | 2 | 0 | 13 |
| 系统管理 | 7 | 2 | 0 | 0 | 9 |
| 回路整定 | 7 | 0 | 0 | 0 | 7 |
| 工作台 | 0 | 0 | 2 | 0 | 2 |
| 前端基础 | 28 | 0 | 2 | 0 | 30 |
| 部署运维 | 0 | 0 | 0 | 10 | 10 |
| 跨模块/异常 | 14 | 6 | 0 | 0 | 20 |
| **合计** | **99** | **22** | **12** | **18** | **151** |

### 9.3 执行顺序建议

```
S8-TEST-001（后端单元） ──┐
                          ├──→ S8-TEST-003（E2E）──→ S8-TEST-004（性能）──┐
S8-TEST-002（前端单元） ──┘                                              ├──→ S8-TEST-006（部署）
                                          S8-TEST-005（安全）──────────┘
```

1. **第一周**：S8-TEST-001 + S8-TEST-002（并行）
2. **第二周**：S8-TEST-003（依赖 001+002）
3. **第三周**：S8-TEST-004 + S8-TEST-005（并行，依赖 003）
4. **第四周**：S8-TEST-006（依赖 004+005）

### 9.4 验收门槛

| 门槛 | 标准 | 一票否决 |
|------|------|----------|
| 后端测试通过率 | 100% | ✅ |
| 前端测试通过率 | 100% | ✅ |
| E2E 通过率 | ≥95% | ✅ |
| API P95 响应 | < 500ms | ✅ |
| 安全扫描 | 0 高危 | ✅ |
| 部署健康检查 | 全部通过 | ✅ |

---

> **文档结束** — 本清单将作为 S8 阶段测试实施的唯一基准，任何用例变更需更新此文档并通知团队。
