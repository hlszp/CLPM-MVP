# CLPM 产品需求文档 (PRD)

**文档状态**: 正式版
**当前版本**: v2.2 (工业标准与算法指标增强版)
**发布日期**: 2026-06-19
**基线来源**: 业务补充需求、前期产品需求分析预研报告、轻量化产品决策

---

## 1. 文档概述

### 1.1 文档目的
本文件将前期的预研需求、系统架构及最新的业务战略补充要求收敛为“面向最终产品交付”的正式产品需求文档。本文档详细规划了 CLPM 的完整功能模块与底层算法指标边界，作为后续功能规格设计 (FDS)、系统架构设计 (ADS) 及前后端开发的首个**唯一事实来源 (Single Source of Truth)**。

### 1.2 引用标准与规范
本产品的业务逻辑、指标计算与工程实践需严格参考或符合以下行业标准及规范：
* **GB/T 44693.1-2024** 《危险化学品企业工艺平稳性 第1部分：管理导则》
* **GB/T 44693.2-2024** 《危险化学品企业工艺平稳性 第2部分：控制回路性能评估与优化技术规范》
* **GB/T 33009-2016** 《工业自动化和控制系统网络安全》
* **ISA-112** SCADA Systems Standards (控制系统生命周期管理规范参考)

### 1.3 专用术语与缩略语定义
| 术语 / 缩略语 | 英文全称 | 定义说明 |
|---|---|---|
| **CLPM** | Control Loop Performance Monitoring | 控制回路性能监测评估系统。 |
| **PV** | Process Variable | 过程变量（测量值），如实际温度、压力、流量等。 |
| **SP** | Set Point | 设定值，操作人员或上位系统期望回路达到的目标值。 |
| **OP / MV** | Output / Manipulated Variable | 控制器输出值/操纵变量，即发送给执行机构（如调节阀）的指令值。 |
| **MODE** | Control Mode | 控制模式，通常分为手动 (Manual)、自动 (Auto)、串级 (Cascade) 等。 |
| **Bad Actor** | - | 差等生回路，指在评估周期内综合绩效极差，严重影响工艺平稳性的控制回路。 |
| **Action Tracker** | - | 轻量级异常跟踪器，用于记录和追踪回路的处置状态与闭环效果。 |
| **KPI** | Key Performance Indicator | 关键绩效指标，用于量化评估回路的控制性能。 |

---

## 2. 产品定义与商业价值

### 2.1 产品定位
面向**所有具备大量自动化控制回路的流程工业**（如石油、化工、电力、冶金、造纸、医药中间体等），支撑企业安全生产与工艺平稳性的控制回路全生命周期绩效治理与优化闭环平台。

### 2.2 核心价值链
`可信数据 -> 自动性能评估 -> 异常自动定位 -> 回路诊断 -> 建议与整定 -> 轻量跟踪与验证 -> 规模化交付`

### 2.3 边界说明 (非目标)
* **不是** 单机版或离线版的手动 PID 调参软件。
* **不是** 自动下写 DCS 的黑盒自整定系统（保持绝对安全边界）。
* **不是** APC (先进过程控制)、RTO 或 SIS 的替代品。
* **不是** 繁重且包含多节点审批流的通用设备维修工单系统（EAM）。

---

## 3. 用户角色与权限模型 (RBAC)

系统采用基于角色的访问控制，确保操作合规与安全审计。

| 用户角色 | 核心关注点 / 典型场景 | 权限与职责边界 |
|---|---|---|
| **仪控工程师** | 每天查看“低效回路排行榜”，分析诊断原因，导出维修/整定建议单。 | **执行层**：配置数据映射，查看性能看板，分析波形，标记异常跟踪状态。 |
| **工艺/设备工程师**| 回路波动是否由工艺扰动或阀门设备引起？对质量有何影响？ | **协同层**：查看诊断报告，确认工艺扰动/阀门粘滞问题，协助排查。 |
| **生产技术/Sponsor**| 装置与工厂整体控制平稳率是否达标？实施后绩效是否改善？ | **查看层**：查看工厂/装置级全局看板与《定期平稳性评估报告》。 |
| **系统管理员** | 算法配置是否合理？平台运行是否健康？ | **管理层**：管理工厂模型，配置指标与算法阈值，管理用户，查看审计日志。 |
| **外部专家团队** | 疑难回路的根因分析与高级参数整定支持。 | **服务层**：诊断复核，出具高级诊断意见与整定模型样例。 |

---

## 4. 核心功能架构与模块需求

遵循“全量持续计算、自动定位差等生、轻量状态跟踪”的产品化原则，系统规划以下核心功能模块：

### 4.1 工厂模型与基础配置
* **工厂空间建模**：支持可视化的 `工厂 -> 装置 -> 单元` 多级层级结构配置。
* **回路台账管理**：支持回路与层级节点的精确绑定；管理 PV/SP/OP/MODE 的位号映射与 OPC/离线数据源对接。
* **引擎规则配置**：
  * 支持自定义回路级与装置级的评估指标公式与聚合权重。
  * 支持对故障诊断算法的敏感度、阈值等进行灵活配置。

### 4.2 自动评估与报表引擎 (Auto-Evaluation)
* **全量持续计算**：后台服务按设定节拍（如每小时、每天）自动拉取全量运行数据，持续计算性能指标，无需人工干预。
* **多维层级评估**：不仅输出**单回路**综合评分，同时自动聚合生成**单元级、装置级**的控制平稳性综合评分。
* **自动报表快照**：系统按班、日、周、月自动生成标准的《控制回路性能评估报告》并归档。

### 4.3 诊断中心与异常预警 (Diagnostics)
* **差等生自动预诊 (Bad Actor Auto-diagnosis)**：对评分跌破阈值的回路，系统自动运行诊断算法矩阵，打上预诊标签（如：疑似阀门粘滞）。
* **证据链追溯**：点击异常回路，直接呈现包含高频时序波形、特征散点图及逻辑推理过程的诊断详情。

### 4.4 轻量级异常跟踪与效果验证 (Action Tracker)
* **状态标签跟踪**：工程师对预诊结论确认后，仅需更新处理状态标签（如：待处理、处理中、已实施），不走审批流。
* **建议单导出**：一键导出包含波形与诊断结论的标准化 PDF《诊断建议书》，供外部工单系统流转。
* **时序对比验证 (A/B Test)**：标记为“已实施”后，系统自动截取实施前后的数据窗口，生成 KPI 趋势对比视图，直观证明绩效提升。

### 4.5 回路整定与仿真 (Loop Tuning) [二期规划]
* **闭环/开环模型辨识**：基于现场阶跃测试数据或日常运行数据，自动识别过程控制对象模型。
* **参数整定推荐**：应用多种工程整定算法计算推荐 PID 参数。
* **动态仿真测试**：提供直观的闭环响应曲线仿真界面，对比“当前参数”与“推荐参数”在阶跃扰动下的表现。

---

## 5. 核心算法与指标范围约束

作为技术设计的首个事实来源，本节初步界定产品必须支持的底层算法指标池范围。

### 5.1 性能评估指标池 (Performance Metrics)
系统需内置并支持以下核心 KPI 及其变体的计算与配置：
1. **可用性与质量指标**：
   * **好值率 (Good Value Rate)**：剔除通讯中断、超量程、冻结等无效数据后的时长占比。
   * **自控率 (Auto Mode Rate)**：控制器处于自动或串级模式的时长占比。
2. **平稳性与偏差指标**：
   * **绝对平均偏差 (IAE)** / **积分平方误差 (ISE)**：衡量 PV 偏离 SP 的程度。
   * **平稳率 (Steady Rate)**：PV 波动在允许公差带（死区）内的时长占比。
   * **过程变异系数 (Process Variability)**：衡量过程自身的固有波动水平。
3. **控制响应指标**：
   * **超调量 (Overshoot)** / **衰减比 (Decay Ratio)**：评估设定值改变或受扰动后的瞬态响应。

### 5.2 故障诊断指标与模式范围 (Diagnostic Features)
系统自动诊断引擎需支持对以下典型故障模式特征的提取与识别：
1. **振荡特征 (Oscillation)**：
   * **振荡率/频域分析**：通过自相关分析或 FFT 识别是否存在周期性振荡及主导频率。
   * **工厂级振荡传播**：识别同一频率的振荡在多个关联回路间的传播源。
2. **阀门/执行器健康特征**：
   * **阀门粘滞 (Stiction)**：基于 PV-OP 散点图的特定轨迹（如椭圆/平行四边形）或定量算法评估粘滞程度。
   * **输出饱和率 (Output Saturation)**：OP 长时间停留在 0% 或 100% 的时长占比（失去调节能力）。
   * **反向动作次数/行程总和**：评估阀门动作的频繁程度与磨损风险。
3. **控制策略匹配特征**：
   * **PID 调谐不佳**：过激（太灵敏导致高频震荡）或过于迟缓（响应极慢）。

### 5.3 参数整定方法支持范围 (Tuning Methods) [二期]
系统需内置以下主流的工业级模型辨识与参数整定算法，供不同场景选用：
1. **模型辨识模型**：
   * 一阶惯性加纯滞后模型 (FOPDT)
   * 二阶惯性加纯滞后模型 (SOPDT)
   * 积分加纯滞后模型 (IPDT - 针对液位等积分过程)
2. **PID 整定规则/算法**：
   * **内模控制整定法 (IMC Tuning)**：工业界最常用，平衡鲁棒性与响应速度。
   * **Lambda 整定法**：常用于纸业及对超调极其敏感的化工过程。
   * **Ziegler-Nichols (Z-N) 及 Cohen-Coon 改进法**：经典的基于阶跃响应或临界振荡的工程算法。

---

## 6. 版本发布演进路线

| 版本阶段 | 核心目标 | 功能范围界定 |
|---|---|---|
| **Phase 1: MVP (V1.0)** | 跑通“自动评估、自动诊断、轻量跟踪”闭环。 | 包含：数据接入、工厂建模、全量自动计算 6大评估指标、低效排行看板、基础故障预诊（震荡/饱和）、轻量状态跟踪、对比视图。<br>*(注：坚决不包含在线辨识、整定计算与仿真。)* |
| **Phase 2: V2.0** | 注入深度算法，实现高级整定与智能化。 | 包含：过程模型辨识、IMC/Lambda 等参数整定算法库、闭环响应仿真测试、AI 辅助波形解读与专家问答系统。 |
| **Phase 3: V3.0** | 规模化与全厂优化。 | 包含：多厂区 1200+ 回路并发优化，厂级振荡源追踪，批次 (Batch) 性能分析与经济效益折算模型。 |

---

## 7. 关键非功能性需求 (NFRs)

| 约束类别 | 要求说明 |
|---|---|
| **绝对安全边界** | **禁止自动下写 DCS 参数**。任何整定参数必须作为离线建议输出，由现场授权人员人工实施。 |
| **数据一致性** | **不掩盖数据缺失**。“数据不足/无效”是一等公民，必须通过 `Inconclusive` 状态显式反馈给用户，而非用 0 替代。 |
| **追溯与合规** | 每一条评估结果与诊断结论，必须在数据库中关联其依据的算法版本号、指标阈值版本及时间戳。 |
| **高性能架构** | 针对 V3.0 的 1200+ 回路并发目标，底层时序数据计算引擎需具备水平扩展与异步任务削峰能力。 |
