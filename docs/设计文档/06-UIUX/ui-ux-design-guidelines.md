# CLPM UI/UX 可视化设计与用户体验规范

**文档状态**: 正式版
**当前版本**: v6.2（色彩约定与工业组件执行层整改版，配套《CLPM 色彩约定表 v1.0》）
**发布日期**: 2026-07-06
**设计依据**: PRD (v6.0), FDS (v6.0), ADS (v6.0), DDS (v6.0), IDS (v6.0), 关键算法设计说明 (v2.1), 控制回路性能评估数据流程图 (v4.0), implementation-contract (v2.0), ZL 致联工业软件 UI/UX 设计套件 (2026 完整版)
**权威声明**: 本文档是视觉、交互、组件与体验规范；重构后 IA、路由、API、权限、状态机口径以 `docs/设计文档/00-BASELINE/implementation-contract.md` 为准；指标体系、数据血缘、可信度、质量策略以 v6.0 设计文档为准；工业风格设计准则是 §14 强制检查清单的事实来源，所有页面改造必须通过 §14 验收。

---

## 0. 文档变更记录

| 版本 | 日期 | 变更说明 | 作者 |
|---|---|---|---|
| v4.0 | 2026-06-20 | 产品化架构重构版，整合 PRD/FDS/ADS/DDS/IDS v3.0：①6 模块 + 门户 25 页面架构；②引入 AAS Tag 模型（7 个 OPC tag）、PV 质量码三态处理、产品化配置 UI、Tag 关联拖拽交互、配置变更确认交互；③定义设计系统（色彩/字体/间距/圆角/动效）、信息架构与导航、角色权限驱动 UI、25 页面设计规范、9 类核心组件、8 类状态机、交互模式规范、数据可视化规范、响应式与无障碍、原型实现指引。 | 系统设计团队 |
| v4.1 | 2026-06-22 | 算法对齐与算法配置界面补充（依据《关键算法设计说明》v1.0 与 IDS v3.2）：①新增 §6.7 算法配置界面设计（指标配置/公式编辑器/诊断配置/整定配置 4 类子页面）；②新增 §7.10 诊断结果展示组件（8 类诊断标签 + 融合置信度 + 证据链卡片）；③新增 §7.11 整定参数展示组件（模型参数/PID 参数/仿真结果/拟合度）；④新增 §7.12 综合评分展示组件（6 大 KPI 雷达图 + 评分构成分解）；⑤新增 §7.13 算法任务状态组件（异步任务进度/轮询/结果渲染）；⑥更新 §6.3.3 性能指标配置交互（双滑块阈值/百分比权重/单选按钮组控制类型）；⑦对齐 C1-C7 跨文档差距修正（6 大 KPI/8 类诊断标签/JSONB 阈值/calc_method/fitting_score/simpleeval 表达式引擎）。 | 系统设计团队 |
| v4.2 | 2026-06-25 | 重构后实现契约追认版：①以 implementation-contract v1.0 固化当前 Vue 前端 IA、路由、API、权限、状态机、KPI 与阶段口径；②旧 25 页面清单降为历史来源；③性能评估保留 `/metric/*`，回路管理保留 `/loop/manage` 聚合页，诊断中心承认 7 页面结构；④Action Tracker 标准枚举统一为 `IMPLEMENTED`；⑤整定接口标注为 Phase 1 实验/辅助能力，不代表 DCS 自动下写。 | 系统设计团队 |
| v5.0 | 2026-06-26 | v4.0 架构对齐版（依据 PRD/FDS/ADS/DDS/IDS v4.0 + 关键算法设计说明 v2.0）：①指标体系从 6 大 KPI 平等加权重构为 3+1+8 结构（3 核心质量指标 A/F/S 加权 + 1 投用指标 R 作折扣因子 + 8 辅助诊断指标不参与评分）；②综合评分公式更新为 P=(A·a+F·f+S·s)/(a+f+s)×R；③新增指标可信度 A/B/C/D/E 五级展示（基于 valid_rate 自动判定，E 级标记 INCONCLUSIVE 且评分留空）；④新增数据血缘下钻展示（8 字段：sampling_freq/quality_policy/tag_group/valid_rate 等）；⑤PV 质量码波形从"断线"改为"灰色虚线保留连线"+ pvValid 标记；⑥术语变更：平稳率→稳定率、跟随率→快速率；⑦指标配置页从 6 项平等权重改为 3 核心权重+1 投用折扣因子+8 辅助无权重；⑧诊断标签状态从 PENDING/IN_PROGRESS/IMPLEMENTED/IGNORED 更新为 ACTIVE/RESOLVED/SUPPRESSED + severity 四级；⑨新增任务管理页面设计（标准任务+自定义任务双轨）；⑩新增 §7.14 数据血缘展示组件 + §7.15 可信度标识组件。 | 系统设计团队 |
| v5.2-draft | 2026-06-27 | 新增 `CLPM_UIUX_工业桌面端改造方案_v1.0.md`，作为后续 UI/UX 优化基线：①基于 vue-vben-admin 5.7 主题能力定义 CLPM Industrial Light；②统一 PageToolbar / ObjectSummaryBar / KpiStrip / DataCanvas 等工业桌面端组件；③明确工作台、回路管理、性能评估、诊断中心、回路整定全模块改造方向；④将任务管理归入性能评估执行体系；⑤补充前后端接口影响评估。 | 系统设计团队 |
| v5.2 | 2026-06-27 | 第一轮工业桌面端 UI/UX 样板页与 IA 收口完成：①新增 `frontend/apps/web-antd/src/components/clpm/` 共享组件层；②完成工作台、回路详情、回路监控、性能看板、诊断详情、异常跟踪样板页改造；③性能评估“系统配置”收口为“指标配置”，新增配置 Tabs 与 `/metric/tasks` 执行记录入口；④任务口径统一为“评估任务/执行记录”；⑤整定工作台接入同风格组件，并通过 typecheck / unit tests / build 验证。 | 系统设计团队 |
| v5.3 | 2026-07-04 | FDS v5.1 / DDS v4.1 对齐版，回路管理与性能评估模块 UIUX 检讨修订（7 项变更）：①**§6.3.1 全局看板重构**——从回路级 3+1+8 聚合 KPI 改为装置级三大 KPI 卡片（综合性能/平均自控率/稳定率，来自 `unit_kpi_summary`）+ 实时自控率半圆径向仪表盘卡片（非条幅式，工业 HMI 标准可视化，每分钟刷新）+ 低效回路 Top 10 预览；②**§6.3.6 权重配置管理页面新增**——4 类控制类型权重模板（STABLE/SLOW/FAST/LOGIC）+ 5 级性能定级阈值（EXCELLENT/GOOD/FAIR/WARNING/POOR）+ 版本化保存与回滚 + 恢复国标默认值；③**§6.2.2 回路台账修订**——表格列与编辑抽屉补 `control_type` / `importance_level` / `include_in_evaluation` 三字段（对齐 DDS v4.1），新建回路弹窗同步采集，重要等级视觉编码（红/橙/灰徽章）；④**§6.3.3 性能指标配置修订**——公式编辑器标注废弃（对齐 FDS §5.3.1.2，12 项指标算法固化为独立函数模块），控制类型字段移除（迁移至 `loop_ledger.control_type`），权重编辑入口跳转 §6.3.6；⑤**§6.2.0 AAS Tag 同步状态页面新增**——同步服务状态卡片 + 手动触发同步 + Tag 列表 + 质量分布饼图；⑥**§6.3.2 低效排行补充**——明确仅展示 `include_in_evaluation=true` 回路，提供"包含不参评回路"开关；⑦**字段名清理**——`compositeScore` → `score`（§6.7.3、§6.8 任务结果渲染规则），`control_type` 字段映射从 `metric_config` 迁移至 `loop_ledger`，新增 `include_in_evaluation` 与 `grading_thresholds` 字段映射。 | 系统设计团队 |
| v6.0 | 2026-07-06 | v6.0 文档统一升级版（对齐实现契约 v2.0、PRD v6.0、FDS v6.0、DDS v6.0）：①**修复 Action Tracker 状态枚举冲突**——§6.4.4 与 §8.2.3 旧枚举 `ACTIVE/RESOLVED/SUPPRESSED` 统一为实现契约 v2.0 标准枚举 `PENDING / IN_PROGRESS / IMPLEMENTED / IGNORED`（待处理 / 处理中 / 已实施 / 已忽略），与 §7.2.2 处理状态标签保持一致；②**统一 Loop 状态机**——§8.2.1 回路就绪状态从描述性表达对齐为 `READY / PARTIAL / INACTIVE`（就绪 / 部分配置 / 已停用）；③**统一 Tuning 状态机**——§8.2.6 整定推荐状态对齐为 `DRAFT / RUNNING / COMPLETED / ROLLED_BACK`（草稿 / 运行中 / 已完成 / 已回退）；④**统一 KPI 快照状态机**——`SUCCESS / PARTIAL / INCONCLUSIVE`（§7.2.1、§8.2.2 已一致，本版校验确认）；⑤**统一 PV Quality 状态机**——`GOOD / BAD / UNCERTAIN`（§7.2.4、§8.2.5 已一致，本版校验确认）；⑥**更新引用文档版本**——PRD v3.1→v6.0、FDS v5.1→v6.0、DDS v4.1→v6.0、ADS v4.0→v6.0、IDS v3.1→v6.0、实现契约 v1.0→v2.0；⑦**设计依据版本号**——头部"以 v4.0 设计文档为准"统一修订为"以 v6.0 设计文档为准"。 | 系统设计团队 |
| v6.1 | 2026-07-06 | 工业风格设计规范整合版（融合 ZL 致联工业软件 UI/UX 设计套件 2026 完整版）：①**新增 §2.4 工业风格设计四大原则**——Calm UI / Poka-Yoke / Glanceability / 数据墨水比最大化，作为 CLPM 工业桌面端设计的核心指导原则；②**强化 §3.1.3 状态语义色严格性**——明确 Emerald/Amber/Rose/Blue 严格语义边界，禁止混用 Ant Design 预设色与局部 hex；③**新增 §3.5.1 边框优先于阴影**——使用极细边框（1px solid `--border-default`）代替厚重阴影，圆角 4-8px；④**新增 §3.7 触控目标规范**——可交互元素至少 44×44px，桌面密度 32px + 触控模式 44px 切换；⑤**新增 §7.16 工业桌面端补充组件**——ClpmNumeric（等宽数字）/ ClpmDangerConfirmModal（危险确认模态框）/ ClpmRealtimeStatus（实时状态条）/ ClpmStatusPanel（状态面板）；⑥**强化 §9.6 配置变更确认交互**——引入 typed confirmation 码确认机制，禁止高危操作使用浏览器默认 `alert()`/`Popconfirm`；⑦**新增 §9.8 高危操作屏障规范**——明确停机/删除/重算类操作的物理与逻辑双重屏障；⑧**新增 §14 工业风格强制检查清单**——18 项强制项落地，作为所有页面改造的验收判据；⑨**新增 §15 改造方案与实施计划**——4 阶段实施路径（横切组件 → P0 页面 → P1 页面 → P2 收口）；⑩**新增 §13.7 ZL 工业设计套件引用说明**——明确 ZL 套件作为工业风格细化准则，不替代 v6.0 事实来源。 | 系统设计团队 |
| v6.2 | 2026-08-08 | 色彩约定与工业组件执行层整改版（依据《CLPM UI/UX 合并审查报告》v2.0 与整改方案 Phase 0/1）：①**发布《CLPM 色彩约定表 v1.0》**（`06-UIUX/color-convention.md`）——语义五色唯一来源（ok #198754 / warning 拆深琥珀文字 #B45309+浅底 #FEF3C7 / danger #DC3545 / info=accent #0D6EFD / neutral #6C757D），ZL 原色板与 antd 色系退役，--status-* 变量为唯一实现；②**类别中性化**——回路类型/规则类型/指标类别等中性分类统一灰阶 tag（--tag-neutral-*），语义色仅服务状态/异常；③**零值中性化**——零值/无数据不着色，ClpmKpiCard 增 neutral-when-zero；④**单值仪表盘（彩虹 gauge）退役**，新增 ClpmBulletChart 子弹图组件（值条+灰阶分档区间带+目标刻线）作为单值对目标的唯一基线；⑤**工具栏收敛**——未声明工具不再占位渲染（权限不足仍灰显），页面工具图标降至 2-5 个；⑥**空态三要素强制**——为什么空/下一步动作/数据范围，ClpmEmptyState 与 ClpmDataCanvas empty-reason 为载体；⑦**弹窗标题栏统一浅色**（ClpmModal 深蓝头退役）；⑧**暗色主题**——默认浅色、用户选择可持久化（修复 init 覆盖缓存缺陷），暗色经校准后开放；⑨**ECharts 工业 preset 强制**——轴/网格/tooltip 统一走 use-echarts-preset，禁 45° 旋转轴标签（hideOverlap 抽稀）；⑩**波形图轴分离修正**——PV/SP 主轴自适应 + OP 副轴 0-100%（§7.3 执行修正）。 | 系统设计团队（AI 代理执行，用户验收） |

---

## 1. 文档概述与定位

### 1.1 文档目的

本文档将 PRD v4.0、FDS/ADS/DDS/IDS v4.0、《关键算法设计说明》v2.0 与《控制回路性能评估数据流程图》v4.0 的约束收敛为一份**面向当前实现的体验规范**，定义 CLPM 平台所有模块与页面的视觉系统、信息架构、组件标准、交互模式与状态表达。

**v5.0 核心变更摘要**（对齐 v4.0 设计文档）：
- 指标体系重构为 **3+1+8 结构**：3 核心质量指标（准确率 A / 快速率 F / 稳定率 S）参与加权评分 + 1 投用指标（有效自控率 R）作折扣因子 + 8 辅助诊断指标不参与评分
- 综合评分公式更新为 **P = (A·a + F·f + S·s)/(a+f+s) × R**（R 为折扣因子而非平等加权项）
- 新增**指标可信度 A/B/C/D/E 五级**展示，基于有效数据率（valid_rate）自动判定
- 新增**数据血缘**下钻展示（8 字段：sampling_freq / quality_policy / tag_group / valid_rate 等）
- PV 质量码波形从"断线"改为**"灰色虚线保留连线"** + pvValid 标记（对齐 KEEP_ALL_WITH_VALIDITY 策略）
- 术语变更：**平稳率 → 稳定率**、**跟随率 → 快速率**

### 1.2 产品定位

CLPM 是面向流程工业（石油、化工、电力、冶金、造纸、医药中间体等）的**控制回路全生命周期绩效治理与优化闭环平台**，是**产品化、工具化**的产品（非项目型定制化系统）。

- **核心价值链**：`可信数据 → 自动性能评估 → 异常自动定位 → 回路诊断 → 建议与整定 → 轻量跟踪与验证 → 规模化交付`
- **产品化设计原则**：模块内聚自包含（每个模块"配置 → 运行 → 分析"三态自包含）、配置驱动（指标公式/诊断算法/引擎规则均支持用户自助配置）、实体内聚（回路作为核心实体，配置态与运行态归属同一模块）。
- **安全边界**：平台不直接修改 DCS 的 P/I/D 参数，只输出建议、证据、风险与回退方案；参数由授权人员人工实施并留痕。UI 不得出现任何"下发参数""写入 DCS"类入口。
- **不是**：单机 PID 调参软件、自动下写 DCS 的黑盒自整定系统、APC/RTO/SIS 替代品、繁重审批流的通用工单系统。

### 1.3 分期范围

| 阶段 | 定位 | UI 范围 |
|---|---|---|
| **Phase 1 (MVP/V1.0)** | 跑通"自动评估、自动诊断、轻量跟踪"闭环 | 工作台门户、回路管理、性能评估、诊断中心、系统管理全部页面；回路整定模块原型页面设计完成 |
| **Phase 2 (V2.0)** | 深度算法与高级整定 | 回路整定算法实现（模型辨识、PID 参数整定、闭环仿真）、整定效果统计、AI 辅助波形解读 |
| **Phase 3 (V3.0)** | 规模化与全厂优化 | 多厂区 1200+ 回路并发、厂级振荡源追踪、批次分析、经济效益折算 |

本文档覆盖 Phase 1 当前实现契约中的菜单页、隐藏详情页、专项配置页与 Phase 2 整定模块的结构性规范。旧 25 页面清单保留为历史来源，不再作为当前菜单验收项。

### 1.4 模块结构总览（v4.2：重构后实现契约）

CLPM 当前采用 **6 大功能模块 + 1 个门户入口** 的产品化架构，页面组织以 `implementation-contract.md` §2 为准：

| 模块 | 页面数 | 核心职责 | 分期 |
|---|---|---|---|
| 工作台（门户） | 1 | 全角色日常作业入口，聚合多模块数据 | Phase 1 |
| 回路管理 | 5 | AAS tag 同步 → 回路创建 → tag 关联 → 运行监控（实体内聚） | Phase 1 |
| 性能评估 | 5 | 指标配置 → 引擎规则 → 全局看板 → 低效排行 → 统计分析（功能内聚） | Phase 1 |
| 诊断中心 | 5 | 指标配置 → 诊断列表 → 诊断详情 → 异常跟踪 → 统计分析（功能内聚） | Phase 1 |
| 回路整定 | 5 | 工作台 → 模型辨识 → 整定算法 → 闭环仿真 → 效果统计（原型先行） | Phase 2（原型 Phase 1） |
| 系统管理 | 4 | 用户角色、审计日志、安全边界、自动报表 | Phase 1 |

---

## 2. 设计基调与反 AI Slop 原则

### 2.1 视觉语气

**稳重、审计化、任务驱动。** 这是工业治理平台，不是通用 SaaS 控制台，也不是概念展示网站。

- 信息密度高，但层级清晰。
- 优先展示状态、风险、下一步动作。
- 文案以业务动作和结论为中心，避免营销化表达。
- 首屏必须先回答"现在该做什么"，再解释"为什么"。

### 2.2 反 AI Slop 禁忌（强制）

| 禁忌 | 原因 | 替代方案 |
|---|---|---|
| 大面积装饰性渐变（紫/霓虹） | AI 训练语料的"科技感"万能公式，无品牌信息 | 单一工业蓝作 accent，中性底色 |
| Emoji 作功能图标 | 训练语料里每个 bullet 配 emoji，不专业 | 语义化 SVG 图标库（Lucide） |
| 圆角卡片 + 左彩色 border accent | 2020-2024 Material/Tailwind 烂大街组合 | 诚实边框分隔，表格为主 |
| 卡片瀑布流代替工作台 | 消费级模式，不适合工业高密度场景 | 高密度表格 + 侧栏详情 + 时间线 |
| SVG 画人脸/场景/物品 | AI 画 SVG 比例诡异 | 真实数据可视化或诚实 placeholder |
| 装饰性 icon 每处都配 | icon slop，无信息量 | 仅承载差异化信息的密度元素 |
| 编造 stats/quotes 装饰 | data slop | 留白或真实数据 |

### 2.3 信息密度分型

CLPM 属于**高密度型**产品（Dashboard/Tracker/诊断类），核心卖点是"数据驱动治理"。因此：

- 每屏需至少 3 处可见的产品差异化信息（非装饰性数据、状态推断、上下文关联）。
- 装饰性 icon 仍然忌讳——加的是**有内容的**密度，不是装饰。
- 列表和工作台页面优先高密度紧凑布局，不做营销站式大留白。

### 2.4 工业风格设计四大原则（v6.1 新增，融合 ZL 致联工业设计套件）

CLPM 作为流程工业控制回路绩效治理平台，必须遵循工业软件的四大核心设计原则。这些原则是 §14 强制检查清单的设计基础，所有页面改造必须以此为准绳。

#### 2.4.1 冷静界面（Calm UI）

**定义**：杜绝毫无意义的装饰性动画与色彩。系统在 99% 的正常运行时间里应该是"安静且融入背景"的，只有在异常发生时，才通过高饱和度色彩和动态效果争夺注意力。

**CLPM 落地要求**：
- 页面背景以 `--bg-page`（#F8F9FA）中性底色为主，90% 屏幕面积使用中性色（Slate/Gray）
- 异常发生时（PV Bad / 评分低 / INCONCLUSIVE / 任务 FAILED），才使用 `--status-danger`/`--status-warning` 高饱和色争夺注意力
- 禁止大面积装饰性渐变（紫/霓虹），禁止 SVG 画人脸/场景/物品
- 装饰性动效仅用于状态反馈（Toast 300ms / 抽屉 250ms / 行 hover 150ms），不做悬浮装饰
- 图表去重阴影，仅在异常态/选中态使用轻边线或低透明度背景

**反例**：紫色渐变背景 + 霓虹色卡片 + 装饰性 icon 满屏 → 违反 Calm UI

#### 2.4.2 防呆设计（Poka-Yoke）

**定义**：假设操作员在最疲惫、环境最嘈杂的状态下使用系统。高危操作必须有物理（触控尺寸 ≥44px）和逻辑（双重确认、输入确认码）的双重屏障。

**CLPM 落地要求**：
- 高危操作（删除回路/取消任务/重算历史/配置保存/整定建议确认）禁止使用浏览器默认 `alert()` 或简单 `Popconfirm`
- 必须使用 `ClpmDangerConfirmModal` 组件（§7.16.2），包含：
  - 影响范围说明（如"将删除回路 101-FC-1023 及其 7 个 Tag 关联"）
  - 输入确认码（如"输入回路位号以确认删除"）
  - 变更原因（必填，写入审计日志）
  - 回退提示（如"删除后可通过审计日志追溯，但数据不可恢复"）
- 触控目标 ≥44×44px（§3.7），桌面密度 32px + 触控模式 44px 切换
- 整定模块全链路标识"建议输出，不下写 DCS"，参数建议卡片加入"只读建议/人工实施/需留痕"固定标识

**反例**：删除回路按钮直接 `Popconfirm("确认删除？")` → 违反 Poka-Yoke

#### 2.4.3 扫视性（Glanceability）

**定义**：操作员只有 1-2 秒的时间查看屏幕。核心数据必须通过大小、对比度、微图表（Sparklines）实现一目了然。

**CLPM 落地要求**：
- KPI 卡片主值使用 `--font-mono` 等宽字体，字号 ≥24px / 字重 700
- 表格内引入微图表（Sparklines / 进度条），低效排行、任务进度、回路监控、AAS 同步列表均需行内趋势/进度条
- 状态标签必须是"颜色 + 文本 + 语义图标"三维组合（§7.2），**绝不能仅靠颜色传递信息**
- 评分色块、PV 质量码、可信度等级必须 1-2 秒内可识别
- 首屏必须先回答"现在该做什么"（待处理异常 / 低效回路 / 失败任务），再解释"为什么"

**反例**：纯文本表格无微图、状态仅靠颜色区分 → 违反 Glanceability

#### 2.4.4 数据墨水比最大化（Maximize Data-Ink Ratio）

**定义**：移除冗余的边框、厚重的背景色、装饰性阴影，像素应该用于展示数据。

**CLPM 落地要求**：
- 使用极细边框（1px solid `--border-default`）代替厚重阴影，圆角 4-8px（§3.5.1）
- 表格优先于卡片瀑布流（§7.1），列表页禁止用卡片代替批量信息浏览
- 主操作常显，次操作 hover reveal 或"更多"菜单（降低操作列视觉噪声）
- 图表去重阴影，仅在异常态/选中态使用轻边线
- 装饰性 icon 满屏是禁忌（§2.2 反 AI Slop），icon 只承载差异化信息

**反例**：Card 瀑布流 + 重阴影 + 装饰性 icon 满屏 → 违反数据墨水比

---

## 3. 设计系统 (Design Tokens)

### 3.1 色彩系统

所有颜色必须从 CSS 变量引用，不允许页面散落写死 hex。

#### 3.1.1 基础色

| Token | 值 | 用途 |
|---|---|---|
| `--bg-page` | `#F8F9FA` | 页面背景，长时间监控眼部舒适 |
| `--bg-panel` | `#FFFFFF` | 面板背景 |
| `--bg-muted` | `#EEF1F4` | 次级背景/表头底色 |
| `--border-default` | `#E5E7EB` | 默认 1px 边框，区分模块不用重阴影 |
| `--text-primary` | `#1F2937` | 主文本 |
| `--text-secondary` | `#4B5563` | 次文本 |
| `--text-muted` | `#6C757D` | 辅助说明 |

#### 3.1.2 品牌与动作色

| Token | 值 | 用途 |
|---|---|---|
| `--accent-blue` | `#0D6EFD` | 主 CTA、当前态、链接、焦点态（单一蓝色，仅用于主操作） |
| `--accent-blue-bg` | `#E7F1FF` | 主操作浅底色（hover/selected 行） |

#### 3.1.3 风险语义色（严格遵守）

风险色优先用于标签、边线、状态点，**不大面积铺底**。

| Token | 值 | 语义 | 适用场景 |
|---|---|---|---|
| `--status-ok` | `#198754` | 正常/达标 | 评分高、好值率达标、IMPLEMENTED、Auto 模式 |
| `--status-info` | `#0D6EFD` | 信息/提示 | IN_PROGRESS、Cascade 模式 |
| `--status-warning` | `#FFC107` | 警告/需关注 | PARTIAL、评分中、需现场核实、Uncertain 质量 |
| `--status-danger` | `#DC3545` | 危险/故障 | 评分低、PENDING、Manual 模式、Bad 质量 |
| `--status-neutral` | `#6C757D` | 中性/数据不足 | INCONCLUSIVE、IGNORED、可信度 E 级 |

#### 3.1.4 评分分级配色（动态映射）

综合评分 `score`（0-100）的颜色映射，阈值由算法配置决定，UI 需支持动态读取：

| 区间（默认） | 颜色 | 语义 |
|---|---|---|
| `score >= 80` | `--status-ok` 青绿 | 优良 |
| `60 <= score < 80` | `--status-warning` 琥珀 | 关注 |
| `score < 60` | `--status-danger` 警示红 | 低效（Bad Actor） |
| `status = INCONCLUSIVE` | `--status-neutral` 冷灰 | 数据不足，**严禁显示 0 分** |

#### 3.1.5 PV 数据质量色（v5.0 更新：灰色虚线保留连线）

PV tag 质量码（Good/Bad/Uncertain）的视觉表达（对齐 v4.0 KEEP_ALL_WITH_VALIDITY 策略）：

| 质量码 | 颜色 | 波形表现 | pvValid | 标签表现 |
|---|---|---|---|---|
| `Good` | `--status-ok` 青绿 | PV 实线正常显示 | `true` | 绿色"Good"徽章 |
| `Bad` | `--status-neutral` 冷灰 | **PV 灰色虚线保留连线**（v5.0 变更：不再断线）+ 悬浮提示"PV 数据质量不可信" | `false` | 红色"Bad"徽章 |
| `Uncertain` | `--status-warning` 琥珀 | PV 琥珀色虚线保留连线 + 悬浮提示"PV 数据质量不确定" | `false` | 琥珀"Uncertain"徽章 |

> **v5.0 关键变更**：对齐 IDS v4.0 §4.4 与 FDS §5.3.1.2 KEEP_ALL_WITH_VALIDITY 策略，Bad 点不再断线，改为灰色虚线保留连线。数据点保留在时间序列中，通过 `pvValid` 标记区分有效/无效，不删除任何数据点。

#### 3.1.6 指标可信度色（v5.0 新增）

指标可信度 A/B/C/D/E 五级颜色编码（对齐 FDS §5.3.10 / 算法说明 §3.7.2）：

| 可信度等级 | valid_rate | 颜色 | 语义 | UI 处理 |
|---|---|---|---|---|
| `A` | ≥ 0.95 | `--status-ok` 青绿 | 高可信 | 正常展示 |
| `B` | 0.80 ~ 0.95 | `--status-info` 深蓝 | 较高可信 | 正常展示 + 可信度角标 |
| `C` | 0.60 ~ 0.80 | `--status-warning` 琥珀 | 中等可信 | 显著标注 + 建议复核 |
| `D` | 0.20 ~ 0.60 | `--status-danger` 警示红 | 低可信 | 警告提示 + "谨慎使用" |
| `E` | < 0.20 | `--status-neutral` 冷灰 | 极低可信 | **标记 INCONCLUSIVE**，评分留空（显示"—"） |

### 3.2 字体系统

| Token | 值 | 用途 |
|---|---|---|
| `--font-sans` | `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Noto Sans SC", sans-serif` | 通用界面文字 |
| `--font-mono` | `"Monaco", "Consolas", "SFMono-Regular", monospace` | 数字、规则 ID、tag 位号、版本号 |

**等宽强制规则**：所有 `tag_name`、`loop_tag`（回路位号）、`score`、KPI 数值、时间戳、版本号、PID 参数必须使用 `--font-mono`，确保数值对齐与快速扫视。

### 3.3 字阶与层级

页面的层级由大小、粗细、间距共同表达，不只靠颜色。

| Token | 值 | 用途 |
|---|---|---|
| `--text-h1` | `24px / 700` | 页面主标题 |
| `--text-h2` | `18px / 700` | 模块/分区标题 |
| `--text-h3` | `15px / 600` | 卡片/小节标题 |
| `--text-body` | `14px / 400` | 正文数据（高密度表格可降至 13px） |
| `--text-small` | `12px / 400` | 辅助说明/标签 |

### 3.4 间距系统

采用 4px/8px spacing scale。

| Token | 值 |
|---|---|
| `--space-1` | `4px` |
| `--space-2` | `8px` |
| `--space-3` | `12px` |
| `--space-4` | `16px` |
| `--space-6` | `24px` |
| `--space-8` | `32px` |

规则：一级区块上下间距（`--space-6` 至 `--space-8`）大于区块内元素间距（`--space-2` 至 `--space-4`）。

### 3.5 圆角与阴影

| Token | 值 | 用途 |
|---|---|---|
| `--radius-sm` | `4px` | 控件圆角（克制） |
| `--radius-md` | `8px` | 面板圆角 |
| `--radius-pill` | `999px` | 标签/徽章 |
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.05)` | 层级分离 |
| `--shadow-drawer` | `-4px 0 16px rgba(0,0,0,0.08)` | 右侧抽屉 |

**阴影只做层级分离，不做悬浮装饰。** 坚决不使用大面积重阴影。

#### 3.5.1 边框优先于阴影（v6.1 新增，对齐 ZL 工业设计套件）

工业桌面端遵循"边框优先于阴影"原则，使用极细边框分隔模块，而非厚重阴影。

**强制规则**：
- 卡片/面板分隔统一使用 `1px solid var(--border-default)`（#E5E7EB）极细边框
- 圆角控制在 4-8px（`--radius-sm` / `--radius-md`），禁止超过 12px
- 阴影仅用于层级分离（`--shadow-sm`）和抽屉（`--shadow-drawer`），禁止大面积重阴影
- 表格行分隔使用 `border-bottom: 1px solid var(--border-default)`，不使用阴影
- 选中态/异常态使用轻边线（`border-left: 2px solid var(--status-*)`）或低透明度背景（`background: rgba(0,0,0,0.02)`）

**反例**：
```css
/* 禁止 */
box-shadow: 0 8px 24px rgba(0,0,0,0.15);  /* 厚重阴影 */
border-radius: 16px;                         /* 过大圆角 */
```

**正例**：
```css
/* 推荐 */
border: 1px solid var(--border-default);
border-radius: var(--radius-md);  /* 8px */
box-shadow: var(--shadow-sm);     /* 仅层级分离 */
```

### 3.6 动效

| 场景 | 动效 | 时长 |
|---|---|---|
| 状态更新反馈 | 全局 Toast fade-in-out | 300ms |
| 抽屉滑出 | `transform: translateX` | 250ms ease-out |
| 表格行 hover | 背景色过渡 | 150ms |
| Tag 关联拖拽放置 | 槽位高亮反馈 | 150ms |
| 配置确认弹窗 | fade-in + scale | 200ms |
| 页面切换 | 无过渡（生产系统求快） | - |

### 3.7 触控目标规范（v6.1 新增，对齐 ZL 工业设计套件与 DESIGN.md）

工业现场可能使用触控平板，所有可交互元素必须满足触控目标尺寸要求。

**强制规则**：

| 场景 | 最小尺寸 | 说明 |
|---|---|---|
| 桌面密度（默认） | 32×32px | 鼠标点击场景，工具栏/图标按钮 |
| 触控模式（可切换） | 44×44px | 平板触控场景，所有可交互元素 |
| 表格行操作按钮 | 32×32px（桌面）/ 44×44px（触控） | 主操作常显，次操作 hover reveal |
| Tag 关联拖拽槽位 | 44×44px | 拖拽放置区域，必须满足触控目标 |
| 危险确认模态框按钮 | 44×44px | 高危操作，强制触控目标 |
| 状态切换开关 | 32×32px（桌面）/ 44×44px（触控） | 启用/停用开关 |

**密度切换**：
- 页面级密度切换：`紧凑 (32px) / 标准 (36px) / 触控 (44px)` 三档
- 默认紧凑密度，触控模式由用户在系统设置中开启
- 切换密度后所有可交互元素尺寸统一调整

**反例**：
```css
/* 禁止 */
button { width: 20px; height: 20px; }  /* 小于 32px，违反触控目标 */
```

**正例**：
```css
/* 推荐 */
.clpm-toolbar-btn {
  min-width: 32px;
  min-height: 32px;
}
.clpm-touch-mode .clpm-toolbar-btn {
  min-width: 44px;
  min-height: 44px;
}
```

---

## 4. 信息架构与导航

### 4.1 全局壳层

```
+----------------------------------------------------------------------------------------+
| 顶部状态栏：产品名 | 工厂模型面包屑 | 当前角色 | 全局状态摘要 | 用户菜单              |
+----------+-----------------------------------------------------------------------------+
| 左侧     |  页面主标题区                                                                |
| 主导航   | +--------------------------------------------------------------------------+ |
|          | |                                                                          | |
|          | |  页面主体区                                                              | |
|          | |                                                                          | |
|          | +--------------------------------------------------------------------------+ |
+----------+-----------------------------------------------------------------------------+
```

**壳层必含元素**：
- 左侧主导航（固定，可折叠，按角色过滤可见性）
- 顶部状态栏（工厂模型层级面包屑、当前角色、全局状态摘要、用户菜单）
- 页面主标题区（标题 + 上下文操作按钮）
- 页面主体区

### 4.2 主导航菜单结构（v4.0：6 模块 25 菜单项）

导航结构是设计基线的一部分。菜单项按 6 大模块组织，按角色过滤可见性。隐藏路由（如详情页）不在菜单中显示。

| 菜单组 | 菜单项 | 路径 | 目标角色 | 分期 |
|---|---|---|---|---|
| **工作台** | 性能总览首页 | `/` | 全角色 | Phase 1 |
| **回路管理** | 工厂层级配置 | `/loop/factory` | 系统管理员、仪控工程师 | Phase 1 |
|  | 回路台账 | `/loop/ledger` | 系统管理员、仪控工程师 | Phase 1 |
|  | Tag 关联管理 | `/loop/mapping` | 系统管理员、仪控工程师 | Phase 1 |
|  | 回路监控列表 | `/loop/monitor` | 全角色 | Phase 1 |
|  | 回路运行详情 | `/loop/monitor/:loopId` | 全角色（隐藏菜单项） | Phase 1 |
| **性能评估** | 全局看板 | `/performance` | 全角色 | Phase 1 |
|  | 低效回路排行 | `/performance/ranking` | 全角色 | Phase 1 |
|  | 性能指标配置 | `/performance/metrics` | 系统管理员 | Phase 1 |
|  | 引擎规则配置 | `/performance/rules` | 系统管理员 | Phase 1 |
|  | 性能统计报表 | `/performance/analytics` | 全角色 | Phase 1 |
| **诊断中心** | 诊断列表 | `/diagnosis` | 仪控/工艺设备/管理员/外部专家 | Phase 1 |
|  | 诊断指标配置 | `/diagnosis/metrics` | 系统管理员 | Phase 1 |
|  | 回路诊断详情 | `/diagnosis/:loopId` | 仪控/工艺设备/管理员/外部专家（隐藏菜单项） | Phase 1 |
|  | 异常跟踪 | `/diagnosis/tracker` | 仪控工程师 | Phase 1 |
|  | 诊断统计报表 | `/diagnosis/analytics` | 仪控/工艺设备/管理员/外部专家 | Phase 1 |
| **回路整定** | 整定工作台 | `/tuning` | 仪控工程师/外部专家 | Phase 2（原型 Phase 1） |
|  | 模型辨识 | `/tuning/identification` | 仪控工程师/外部专家 | Phase 2（原型 Phase 1） |
|  | 整定算法 | `/tuning/algorithm` | 仪控工程师/外部专家 | Phase 2（原型 Phase 1） |
|  | 闭环仿真 | `/tuning/simulation` | 仪控工程师/外部专家 | Phase 2（原型 Phase 1） |
|  | 整定效果统计 | `/tuning/analytics` | 仪控工程师/外部专家 | Phase 2（原型 Phase 1） |
| **系统管理** | 用户与角色 | `/system/users` | 系统管理员 | Phase 1 |
|  | 审计日志 | `/system/audit` | 系统管理员 | Phase 1 |
|  | 任务管理（v5.0 新增） | `/tasks` | 系统管理员、仪控工程师 | Phase 1 |
|  | 自动报表管理 | `/system/reports` | 系统管理员 | Phase 1 |
|  | 安全边界说明 | `/system/safety` | 全角色 | Phase 1 |

**规则**：
- 当前角色无权限的菜单项隐藏（不置灰，避免噪音）。
- 隐藏路由（详情页 `:loopId`）通过列表行点击进入，不在菜单中显示。
- Phase 2 整定模块在 Phase 1 保留原型页面（可点击查看结构与交互），算法实现标注"Phase 2 规划中"。
- Sponsor 角色默认首页为 `/performance`（全局看板），且不可见诊断中心与回路整定模块。
- 外部专家默认首页为 `/diagnosis`，仅可见诊断中心与回路整定模块。

### 4.3 页面结构模式

v4.0 定义 **4 类页面结构模式**：

| 模式 | 结构 | 适用页面 |
|---|---|---|
| **工作台页** | 三栏（左列表 + 中主区 + 右动作区）或上中下三行 | 性能总览首页、回路诊断详情、整定工作台、闭环仿真 |
| **数据表页** | 顶部筛选 + 中央表格 + 右侧详情抽屉 | 回路台账、回路监控列表、低效排行、诊断列表、异常跟踪、各统计报表、审计日志、自动报表管理 |
| **配置页** | 左侧树/列表 + 右侧表单 | 工厂层级配置、Tag 关联管理、性能指标配置、引擎规则配置、诊断指标配置、模型辨识、整定算法、用户与角色 |
| **审计页** | 顶部结论摘要 + 中部证据矩阵 + 底部风险说明 | 安全边界说明、整定效果统计 |

---

## 5. 角色与权限驱动的 UI

### 5.1 角色定义（依据 PRD v3.0 第 3 章）

| 角色 | 层级 | 默认首页 | 核心关注点 |
|---|---|---|---|
| 仪控工程师 | 执行层 | `/`（工作台） | 低效回路排行、诊断分析、导出建议单、标记处理状态、回路整定 |
| 工艺/设备工程师 | 协同层 | `/performance` | 回路波动根因、对质量影响 |
| 生产技术/Sponsor | 查看层 | `/performance` | 全厂稳定率达标、实施后绩效改善 |
| 系统管理员 | 管理层 | `/loop/factory` | 工厂模型配置、算法配置、平台健康、审计日志 |
| 外部专家团队 | 服务层 | `/diagnosis` | 疑难回路根因、高级整定支持 |

### 5.2 权限矩阵（驱动按钮可见性）

| 功能模块 / 操作 | 仪控工程师 | 工艺/设备工程师 | Sponsor | 系统管理员 | 外部专家 |
|---|:---:|:---:|:---:|:---:|:---:|
| 工厂层级配置 | ✅（查看） | 🚫 | 🚫 | ✅ | 🚫 |
| 回路台账管理（创建/编辑） | ✅ | 🚫 | 🚫 | ✅ | 🚫 |
| Tag 关联管理 | ✅ | 🚫 | 🚫 | ✅ | 🚫 |
| 性能指标配置 | 🚫 | 🚫 | 🚫 | ✅ | 🚫 |
| 引擎规则配置 | 🚫 | 🚫 | 🚫 | ✅ | 🚫 |
| 诊断指标配置 | 🚫 | 🚫 | 🚫 | ✅ | 🚫 |
| 查看全局/装置看板 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 查看回路监控列表 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 查看单回路诊断波形 | ✅ | ✅ | 🚫 | ✅ | ✅ |
| 标记异常处理状态 | ✅ | 🚫 | 🚫 | 🚫 | 🚫 |
| 导出诊断建议书 | ✅ | ✅ | 🚫 | 🚫 | ✅ |
| 回路整定操作 | ✅ | 🚫 | 🚫 | 🚫 | ✅ |
| 查看系统/审计日志 | 🚫 | 🚫 | 🚫 | ✅ | 🚫 |
| 用户与角色管理 | 🚫 | 🚫 | 🚫 | ✅ | 🚫 |

### 5.3 权限 UI 规则

- **无权限的菜单项**：隐藏（不置灰）。
- **无权限的操作按钮**：隐藏。若因上下文必须保留位置，则置灰 + tooltip 提示"无权限"。
- **越权操作拦截**：后端返回 `ERR_AUTH_403`，前端全局 Toast 提示"无权限执行此操作"。
- **Sponsor 下钻限制**：Sponsor 可见看板层级，点击回路时诊断波形入口禁用，提示"当前角色不可查看单回路诊断详情"。
- **状态变更独占**：`action_tracker.action_status` 的修改按钮仅对仪控工程师可见。
- **配置变更独占**：所有"配置页"的保存/启停操作仅对系统管理员可见（回路台账/Tag 关联除外，仪控工程师可编辑）。

---

## 6. 页面设计规范

本章按 6 模块 + 门户组织，对 25 个页面逐一规范。每个页面规范包含：路由、页面结构模式、目标角色、分期、页面内容、设计要点。

### 6.1 工作台（门户）

#### 6.1.1 性能总览首页 `/`

**页面结构模式**：工作台页（上中下三行布局 + 顶部筛选栏）
**目标角色**：全角色（仪控工程师默认首页）
**分期**：Phase 1

**页面布局**：

```
+----------------------------------------------------------------------------------------+
| 顶部筛选栏：[全厂 ▼][装置 ▼][单元 ▼]  统计粒度：(•)日 ( )周 ( )月                       |
+----------------------------------------------------------------------------------------+
| 上行（20%）：KPI 卡片区（v5.0：3+1+8 结构展示）                                          |
| [准确率A] [快速率F] [稳定率S] [有效自控率R] [综合评分P] [好值率(可信度)]                |
+----------------------------------------------+-----------------------------------------+
| 中行左（60%）：低效回路列表                  | 中行右（40%）：选中回路摘要              |
| [位号 | 评分 | 预诊标签 | 关键指标]          | 小趋势缩略图（PV/SP/OP）                |
| [位号 | 评分 | 预诊标签 | 关键指标]          | 关键状态：MODE/PID/质量码                |
| [位号 | 评分 | 预诊标签 | 关键指标]          | 快捷动作：进入诊断 / 进入异常跟踪        |
+----------------------------------------------+-----------------------------------------+
| 下行（30%）：最近月/周关键指标组合趋势                                                  |
| 自控投用率（蓝线）+ 稳定率（绿线）双轴折线  粒度切换：(•)日 ( )小时                       |
+----------------------------------------------------------------------------------------+
```

**页面内容**：
- **顶部筛选栏**：全厂/装置/单元级联下拉 + 日/周/月单选（默认日）
- **上行 KPI 卡片区**（v5.0 更新：3+1+8 结构）：6 项核心指标卡片（准确率 A / 快速率 F / 稳定率 S / 有效自控率 R / 综合评分 P / 好值率），每卡显示当前值 + 同比变化箭头 + **可信度等级角标**（A/B/C/D/E 颜色编码，对齐 §3.1.6）。综合评分卡片额外显示评分构成分解（A/F/S 加权 + R 折扣，对齐 §7.12）。好值率卡片显示可信度等级（valid_rate → A/B/C/D/E），影响综合评分可信度
- **中行左**：低效回路列表（位号/评分/预诊标签/关键指标），按评分升序排列
- **中行右**：选中回路的小趋势缩略图（PV/SP/OP 三线，PV 线根据质量码灰色虚线保留连线）+ 关键状态摘要（控制方式/PID 参数/数据质量/可信度等级）+ 快捷动作入口
- **下行**：最近月/周关键指标组合趋势（自控投用率 + 稳定率双线），支持日/小时粒度切换

**设计要点**：
- 上中下三行高度比约 20% : 40% : 30%（剩余 10% 为筛选栏与间距）
- KPI 卡片不使用渐变/彩色 border，仅用数值大小与颜色（评分色块）传递信息
- 中行左右联动：点击左侧回路行，右侧摘要即时刷新
- 下行趋势图默认显示最近 7 天，可切换至最近 30 天
- 数据来源：`kpi_snapshot_hourly`（最新一小时快照）、`action_tracker`（待办聚合）、`st_loop_data`（小趋势）

**关键交互**：

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换装置/单元筛选 | 重载 KPI 与低效列表 | 上下文保持 |
| 切换日/周/月粒度 | 重算 KPI 聚合 | 卡片数值更新 |
| 点击低效回路行 | 选中当前回路 | 中行右摘要联动刷新 |
| 点击"进入诊断" | 跳转 `/diagnosis/:loopId` | 带 `loopId` |
| 点击"进入异常跟踪" | 跳转 `/diagnosis/tracker` | 带当前回路上下文 |
| 点击 KPI 卡片 | 展开详细说明 | 展示趋势、说明和相关低性能回路 |

### 6.2 回路管理模块

#### 6.2.0 AAS Tag 同步状态 `/loop/aas-sync` [v5.3 新增]

**页面结构模式**：数据表页 + 状态卡片区
**目标角色**：系统管理员（可手动触发同步）、仪控工程师（可查看）
**分期**：Phase 1

**对齐 FDS §5.2.1 AAS Tag 同步**：本页展示 AAS Tag 同步服务的运行状态与同步结果，是回路管理模块的数据入口监控页。AAS 同步对象为 Tag 位号（非回路实体），同步结果写入 `tag_registry` 表。

**页面内容**：

- **顶部同步状态卡片区**（3 张卡片横排）：
  - **同步服务状态**：运行中（绿色脉冲点）/ 已停止（灰色）/ 异常（红色）
  - **最近同步时间**：`last_sync_at` 时间戳（等宽字体）+ 距今时长（如 "5 分钟前"）
  - **同步统计**：本次同步 Tag 总数 / 新增数 / 更新数 / 失败数（红色徽章）

- **同步操作区**（仅系统管理员可见）：
  - **手动触发同步**按钮：调用 `POST /api/v1/aas/sync`，返回任务 ID 后展示同步进度条
  - **同步周期配置**入口：跳转引擎规则配置页（§6.3.4）配置 AAS 同步周期
  - **同步日志**入口：展开抽屉显示最近 10 次同步记录（时间/状态/统计/错误信息）

- **Tag 列表区**（数据表）：
  - **筛选栏**：关键词搜索（tag 名/描述）+ tag 类型过滤（PV/SP/OP/MODE/PID_P/PID_I/PID_D/OTHER）+ 数据质量过滤（Good/Bad/Uncertain）+ 关联状态过滤（已关联/未关联）
  - **表格列**：Tag 位号（等宽）/ 描述 / 类型 / 当前值 / 数据质量徽章 / 关联回路名 / 最后同步时间 / 操作（查看关联回路）
  - **Bad 质量标签高亮**：行底色淡红，"数据质量"列红色徽章 + tooltip 显示错误码

- **质量分布饼图**（右下角悬浮卡片）：
  - 展示当前所有 Tag 的质量分布（Good 绿/Bad 红/Uncertain 琥珀）
  - 点击饼图扇区过滤 Tag 列表

**设计要点**：
- 同步服务异常时顶部全宽红色警告横幅，提示"AAS 同步服务异常，回路监控数据可能滞后"
- Bad 质量 tag 数量超过阈值（如 >10%）时，同步统计卡片闪烁告警
- 手动触发同步时按钮禁用 + 加载态，同步完成后 Toast 提示结果
- Tag 列表支持导出 CSV（权限：系统管理员）

**API 对接**：
- 同步状态：`GET /api/v1/aas/sync-status`（轮询，60 秒刷新）
- 手动触发同步：`POST /api/v1/aas/sync`（返回 task_id）
- Tag 列表：`GET /api/v1/aas/tags`（分页 + 筛选）
- 同步日志：`GET /api/v1/aas/sync-logs`（最近 N 次）

#### 6.2.1 工厂层级配置 `/loop/factory`

**页面结构模式**：配置页（左树 + 右表单）
**目标角色**：系统管理员（可编辑）、仪控工程师（可查看）
**分期**：Phase 1

**页面内容**：
- **左侧树状视图**：渲染 `工厂 → 装置 → 单元` 多级层级树（`plant_node` 表）
  - 节点图标按 `type` 差异化（工厂/装置/单元）
  - 支持新增、编辑、删除节点（系统管理员独占）
  - 同级节点命名重复时提示"该层级下已存在同名节点"
  - 大规模节点（>500）启用虚拟滚动
- **右侧表单**：选中节点时展示节点名称、类型、父节点、描述
- **右侧下方**：选中单元节点时，展示该单元下的回路台账列表（链接至 `/loop/ledger?unitId=xxx`）

**设计要点**：
- 树状视图支持拖拽调整层级（系统管理员）
- 节点删除前确认弹窗："删除后该节点下所有回路将失去归属，确认删除？"
- 配置变更需填写变更说明，记录审计日志

#### 6.2.2 回路台账 `/loop/ledger`

**页面结构模式**：数据表页（筛选 + 表格 + 抽屉）
**目标角色**：系统管理员、仪控工程师
**分期**：Phase 1

**页面内容**：

回路由用户创建（**非 AAS 同步**），AAS 同步的是 tag 位号。

**筛选栏**：关键词搜索（回路位号/描述）+ 装置/单元过滤 + 控制类型过滤（STABLE/SLOW/FAST/LOGIC）+ 重要等级过滤（1/2/3）+ 启用状态过滤 + 参评状态过滤（参评/不参评）+ Tag 关联完整性过滤

**表格列**：

| 列 | 字段 | 说明 |
|---|---|---|
| 回路位号 | `loop_tag` | 等宽字体，主标识，用户填写 |
| 描述 | `description` | 用户填写 |
| 所属单元 | `plant_node.name` | — |
| 控制类型 | `control_type` | 可编辑（STABLE/SLOW/FAST/LOGIC），下拉选择，决定权重模板 [v5.3 新增] |
| 控制方式 | `mode`（from MODE tag） | 只读展示，Manual/Auto/Cascade |
| PID 参数 | `pid_p/pid_i/pid_d`（from PID tag） | 只读展示，等宽字体 |
| Tag 关联状态 | `mapping_status` | 只读，完整/缺失（标红） |
| 重要等级 | `importance_level` | 可编辑（1/2/3），下拉选择，决定装置级聚合权重 [v5.3 对齐 DDS v4.1] |
| 参评状态 | `include_in_evaluation` | 开关控件，控制是否参与综合性能评分与装置级聚合 [v5.3 新增] |
| 启用状态 | `is_active` | 开关控件 |
| AAS 同步状态 | `last_sync_time` / `tag_count` | 只读，最后同步时间/tag 总数 |
| 操作 | — | 编辑、查看 Tag 关联、查看监控 |

**右侧抽屉（编辑回路）**：
- **基础信息（用户填写）**：回路位号、描述、所属单元
- **评估配置（用户填写）** [v5.3 新增]：
  - **控制类型**（`control_type`，单选按钮组：STABLE 稳定型 / SLOW 慢速型 / FAST 快速型 / LOGIC 逻辑型）—— 切换时弹窗提示"切换控制类型将应用对应默认权重模板，是否继续？"，确认后自动套用 §6.3.6 权重配置管理页面对应模板
  - **重要等级**（`importance_level`，单选按钮组：1 一级 / 2 二级 / 3 三级）—— 装置级聚合权重映射：一级=3、二级=2、三级=1
  - **是否参与评估**（`include_in_evaluation`，开关控件）—— 切换为"否"时弹窗提示"不参评回路仍正常计算单回路 KPI，但不进入综合性能评分、装置级聚合与低效排行，确认切换？"
- **Tag 关联状态（只读）**：7 槽位关联状态，缺失标红，提示"前往 Tag 关联管理页面编辑"
- **运行态读取（只读）**：控制方式（from MODE tag）、PID 参数（from PID tag），灰色背景标注"运行态只读"
- **扩展配置（可编辑）**：启用状态、备注
- **AAS 同步状态（只读）**：最后同步时间、tag 总数

**新建回路弹窗** [v5.3 修订]：
- **基础信息**：回路位号、描述、所属单元
- **评估配置**：控制类型（默认 STABLE）、重要等级（默认 2 二级）、是否参与评估（默认"是"）
- 创建后引导至 Tag 关联管理页面完成 7 槽位关联
- 字段缺失校验：控制类型与重要等级为必填，未选时禁用"创建"按钮

**设计要点**：
- 运行态字段（控制方式/PID 参数）明确标注"只读，从 tag 读取"，禁止编辑控件
- Tag 关联缺失时整行标红，tooltip 提示缺失项
- **控制类型切换确认弹窗**：切换将套用新权重模板，需二次确认 + 审计日志
- **参评状态切换确认弹窗**：影响装置级聚合，需二次确认 + 审计日志
- **重要等级视觉编码**：1 一级（红色徽章 · 关键回路）、2 二级（橙色徽章 · 一般回路）、3 三级（灰色徽章 · 辅助回路），辅助快速识别
- 不参评回路在台账列表中行底色淡灰，"参评状态"列开关置灰，悬浮提示"该回路不参与装置级绩效统计"

#### 6.2.3 Tag 关联管理 `/loop/mapping`

**页面结构模式**：配置页（左列表 + 右槽位）
**目标角色**：系统管理员、仪控工程师
**分期**：Phase 1

**页面布局**：

```
+----------------------------------------------+-----------------------------------------+
| 左侧（40%）：AAS 同步 Tag 列表               | 右侧（60%）：选中回路的 7 槽位关联       |
| [搜索框] [筛选：质量/关联状态]               | 回路：[选择回路 ▼]                       |
| [tag1] PV  Good  已关联回路A                 | +-------------------------------------+ |
| [tag2] SP  Good  未关联                      | | PV   (必填) [拖入或选择 tag ▼]     | |
| [tag3] OP  Good  已关联回路A                 | | SP   (必填) [拖入或选择 tag ▼]     | |
| [tag4] MODE Bad  未关联                      | | OP   (必填) [拖入或选择 tag ▼]     | |
| [tag5] PID_P Good 已关联回路A                | | MODE (必填) [拖入或选择 tag ▼]     | |
| ...                                          | | PID_P(可选) [拖入或选择 tag ▼]     | |
|                                              | | PID_I(可选) [拖入或选择 tag ▼]     | |
|                                              | | PID_D(可选) [拖入或选择 tag ▼]     | |
|                                              | +-------------------------------------+ |
|                                              | [校验状态] [保存]                       |
+----------------------------------------------+-----------------------------------------+
```

**页面内容**：
- **左侧 Tag 列表**：AAS 同步的所有 OPC tag 位号
  - 搜索框（tag 名/描述模糊查询）
  - 筛选：数据质量（Good/Bad/Uncertain）、关联状态（已关联/未关联）
  - 列：tag 名（等宽）、描述、当前值、数据质量徽章、关联回路名
- **右侧 7 槽位关联**：选中回路后展示 7 个槽位
  - **PV tag**（必填，过程测量值，含质量码）
  - **SP tag**（必填，设定值）
  - **OP tag**（必填，输出值）
  - **MODE tag**（必填，控制模式）
  - **PID_P tag**（可选，比例参数）
  - **PID_I tag**（可选，积分参数）
  - **PID_D tag**（可选，微分参数）

**设计要点**：
- 槽位支持**拖拽**（从左侧 tag 列表拖入）或**下拉选择**两种交互方式
- 必填槽位缺失时标红 + tooltip 提示"PV tag 必填"
- 已关联的 tag 在左侧列表中显示关联回路名，避免重复关联（同一 tag 可关联至多个回路，但需提示）
- 校验状态实时显示：完整（绿色）/ 缺失（红色）
- 保存前确认弹窗："确认保存 Tag 关联？变更将影响该回路的数据采集与性能评估。"

**关键交互**：

| 触发 | 行为 | 结果 |
|---|---|---|
| 拖拽 tag 至槽位 | 槽位高亮反馈（150ms） | tag 关联至槽位 |
| 下拉选择 tag | 槽位填充 | tag 关联至槽位 |
| 点击槽位清除 | 移除关联 | 槽位空缺，必填项标红 |
| 点击"校验" | 校验完整性 | 显示完整/缺失状态 |
| 点击"保存" | 弹确认弹窗 | 填写变更说明后保存，记录审计日志 |

#### 6.2.4 回路监控列表 `/loop/monitor`

**页面结构模式**：数据表页（筛选 + 表格 + 抽屉）
**目标角色**：全角色
**分期**：Phase 1

**页面内容**：
- **筛选栏**：关键词搜索（回路位号）+ 装置/单元过滤 + 控制方式过滤 + 数据质量过滤
- **视图切换**：列表视图 / 卡片视图双视图切换
- **表格列**：回路位号、名称、测量量程、单位、所属单元、PV 当前值（含质量徽章）、SP 当前值、OP 当前值、MODE（控制方式标签）、综合评分、计算状态、操作（查看详情）

**设计要点**：
- PV 当前值旁显示质量徽章（Good 绿/Bad 红/Uncertain 琥珀）
- 卡片视图：每卡显示回路位号 + PV/SP/OP 三值 + MODE 标签 + 评分色块
- 点击行/卡 → 跳转 `/loop/monitor/:loopId`

#### 6.2.5 回路运行详情 `/loop/monitor/:loopId`（隐藏路由）

**页面结构模式**：工作台页（上下分栏）
**目标角色**：全角色
**分期**：Phase 1

**页面内容**：
- **顶部**：回路位号 | 所属单元 | 控制方式 | 综合评分 | 计算状态 | 操作（进入诊断/进入异常跟踪）
- **上部**：基础信息卡片（回路位号/描述/所属单元）+ Tag 关联信息（7 槽位 tag 名 + 数据质量）+ 运行态参数（控制方式 from MODE tag / PID 参数 from PID tag，只读展示）
- **中部**：PV/SP/OP 时序趋势波形（PV 线根据质量码灰色虚线保留连线，SP/OP 线正常显示）+ MODE 背景带 + 时间窗选择器
- **下部**：近期 KPI 摘要（最近 7 天评分趋势 + 3+1+8 指标当前值 + 可信度等级）

**设计要点**：
- 运行态参数（控制方式/PID 参数）明确标注"只读，从 tag 读取"
- 波形图中 PV 线根据质量码显示灰色虚线（保留连线，v5.0 变更），SP/OP 线正常显示
- KPI 摘要按 3+1+8 结构分组展示：核心质量指标（A/F/S）+ 投用指标（R）+ 辅助诊断指标
- 时间窗最大跨度 30 天，超限提示 `ERR_TS_001`

### 6.3 性能评估模块

#### 6.3.1 全局看板 `/performance`

**页面结构模式**：仪表盘 + 数据表
**目标角色**：全角色（工艺/设备工程师、Sponsor 默认首页）
**分期**：Phase 1

**对齐 FDS §5.3.4 全局看板与低效排行** [v5.3 重构]：本页定位为**装置级绩效看板**，承载两类信息：① 装置级三大 KPI 摘要（综合性能 / 平均自控率 / 稳定率，来自 `unit_kpi_summary`）；② 实时自控率仪表盘（来自 Redis 实时键，每分钟刷新）。原 v5.0 的"回路级 3+1+8 聚合 KPI 卡片"降级为次级信息，移至低效排行页（§6.3.2）展示。

**页面布局**：

```
+------------------------------------------------------------------------+
|  [工厂选择器▼]  [装置选择器▼]  [时间窗: 今日▼]                          |
+------------------------------------------------------------------------+
|  ⚠ Partial 警告横幅（条件触发，黄色）                                    |
+------------------------------------------------------------------------+
|                                                                          |
|  ┌─ 装置级三大 KPI 卡片区（横排 3 张）─────────────────────────────────┐ |
|  │ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                │ |
|  │ │ 综合性能      │ │ 平均自控率    │ │ 稳定率        │                │ |
|  │ │   85.6        │ │   78.3%       │ │   92.1%       │                │ |
|  │ │ ↑ +2.3 环比   │ │ ↓ -1.2 环比   │ │ ↑ +0.8 环比   │                │ |
|  │ │ ▁▂▃▅▆█ 迷你   │ │ ▃▅▆▅▄▆▇ 迷你  │ │ ▅▆▇▆▅▆▇█ 迷你 │                │ |
|  │ │ 可信度 A 级   │ │ 可信度 B 级   │ │ 可信度 A 级   │                │ |
|  │ │ 参评 52 回路  │ │ 参评 52 回路  │ │ 参评 52 回路  │                │ |
|  │ └──────────────┘ └──────────────┘ └──────────────┘                │ |
|  │                                                                      │ |
|  │ ┌─ 实时自控率仪表盘卡片（与三大 KPI 同排，第 4 张）────────────────┐ │ |
|  │ │   ╭─────────────────╮                                             │ │ |
|  │ │  ╱   85.5%  ⚡ 实时  ╲   ← 半圆径向仪表盘 + 脉冲标识              │ │ |
|  │ │ │   指针 → 85.5      │      AUTO 回路 44/52                       │ │ |
|  │ │  ╲   状态: 运行中    ╱       最近 60 分钟 ▁▂▃▅▆█ 迷你 sparkline   │ │ |
|  │ │   ╰─────────────────╯       每分钟自动刷新                        │ │ |
|  │ └────────────────────────────────────────────────────────────────┘ │ |
|  └────────────────────────────────────────────────────────────────────┘ |
|                                                                          |
|  ┌─ 装置评分排名柱状图（左 60%）────────┐ ┌─ 稳定率趋势（右 40%）──────┐ |
|  │ 各装置综合评分横向柱状图             │ │ 全厂稳定率双轴折线图        │ |
|  │ 按评分排序，色块映射 5 级定级         │ │ 左轴: 装置级稳定率 Stab_unit│ |
|  │ 点击柱条 → 跳转装置详情              │ │ 右轴: 参评回路数            │ |
|  └──────────────────────────────────────┘ └────────────────────────────┘ |
|                                                                          |
|  ┌─ 低效回路 Top 10 预览（数据表）─────────────────────────────────────┐ |
|  │ 排名 | 回路位号 | 综合评分 | 稳定率 | 可信度 | 预诊 | 处理状态        │ |
|  │ ...                                                                    │ |
|  │ [查看完整排行 →] 跳转 /performance/ranking                              │ |
|  └────────────────────────────────────────────────────────────────────┘ |
+------------------------------------------------------------------------+
```

**页面内容**：

- **装置级三大 KPI 卡片区** [v5.3 重构，对齐 FDS §5.3.7.3]：
  - 数据源：`unit_kpi_summary` 表（装置级聚合快照，非回路级聚合）
  - 3 张大尺寸 KPI 卡片横排：

| KPI | 字段 | 说明 | 可信度 |
|---|---|---|---|
| **综合性能** | `avg_score` | P_composite = Σ(w_level·P_loop)/Σw_level，按重要等级加权 | 取装置内回路最低可信度 |
| **平均自控率** | `auto_mode_rate` | Auto_avg = Σ(w_level·auto_mode_rate)/Σw_level | 取装置内回路最低可信度 |
| **稳定率** | `stability_rate` | Stab_unit = Σ(w_level·steady_rate)/Σw_level | 取装置内回路最低可信度 |

  - 每张卡片显示：数值（大字 32px）+ 环比箭头（↑绿/↓红）+ 迷你 sparkline（最近 7 天）+ 可信度等级角标（A/B/C/D/E）+ 参评回路数（`evaluated_loops`）
  - 卡片底部悬浮 tooltip：显示参评回路数 `evaluated_loops`、INCONCLUSIVE 回路数 `inconclusive_loops`、不参评回路数 `excluded_loops`
  - 装置级 KPI 为 NULL（全部回路 INCONCLUSIVE 或无参评回路）时，卡片显示"--"，悬浮提示"装置内无参评回路数据"

- **实时自控率仪表盘卡片** [v5.3 新增，对齐 FDS §5.3.7.3 实时自控率]：
  - 数据源：Redis 实时键 `plant:{plant_id}:auto_rate_rt`，每分钟刷新
  - 计算公式：`Auto_rt = auto_loops_rt / evaluated_loops × 100`（当前 MODE=AUTO 的参评回路数 / 装置参评回路总数）
  - **可视化方案：半圆径向仪表盘（Radial Gauge）**，非条幅式：
    - 半圆弧度 180°，从左 0% 到右 100%
    - 弧段着色（对齐 FDS §5.3.4 颜色映射）：0-60% 红色 / 60-80% 黄色 / 80-90% 蓝色 / 90-100% 绿色
    - 指针指向当前值，指针末端有圆形锚点
    - 中心大字显示数值（36px）+ "%" + "⚡ 实时"标识（脉冲动画，每分钟刷新时数值过渡）
    - 仪表盘下方：AUTO 回路数 / 参评回路总数（如 "44/52 回路"）
    - 卡片右下角：迷你 sparkline 显示最近 60 分钟实时自控率趋势
    - 卡片左上角：状态徽章（运行中 ≥90% 绿色 / 关注 80-90% 蓝色 / 警告 60-80% 黄色 / 严重 <60% 红色）
  - 装置无参评回路时仪表盘显示"无参评回路"，指针归零
  - 刷新动画：数值变化时从旧值过渡到新值（300ms ease-out），指针同步旋转

- **装置评分排名柱状图**：
  - 各装置综合性能评分横向柱状图（ECharts）
  - 按评分降序排列，柱条颜色映射 5 级定级（EXCELLENT 绿/GOOD 蓝/FAIR 黄/WARNING 橙/POOR 红）
  - 点击柱条 → 跳转装置详情（携带 plantNodeId 参数）

- **全厂稳定率趋势双轴折线图** [v5.3 修订，对齐 FDS §5.3.4]：
  - 左轴：装置级稳定率 `Stab_unit`（0-100%）
  - 右轴：参评回路数 `evaluated_loops`
  - 支持粒度切换：日 / 小时
  - 支持时间窗切换：`today` / `yesterday` / `last_7_days`
  - 数据点缺失时断线展示，悬浮提示缺失时段

- **低效回路 Top 10 预览** [v5.3 新增]：
  - 数据表预览前 10 条低效回路（按 `score` 升序）
  - 仅展示 `include_in_evaluation=true` 的回路
  - 列：排名 / 回路位号 / 综合评分 / 稳定率 / 可信度 / 预诊标签 / 处理状态
  - 底部"查看完整排行 →"链接跳转 `/performance/ranking`

- **Partial 警告横幅** [v5.3 修订]：
  - 当批次存在 `INCONCLUSIVE` 回路或缺完整 Tag 映射时，页面顶部强制显示黄色 `Partial（部分数据缺失）` 警告横幅
  - 横幅可展开查看影响范围：INCONCLUSIVE 回路数 / Tag 缺失回路数 / 不参评回路数
  - 横幅可折叠，但不可关闭（持续提示直到问题解决）

**设计要点**：
- **卡片视觉一致性**：装置级三大 KPI 卡片与实时自控率仪表盘卡片采用统一卡片容器（圆角 8px + 1px 边框 + 阴影），但仪表盘卡片通过半圆径向仪表盘差异化体现"实时"语义
- **仪表盘工业 HMI 标准**：半圆径向仪表盘是工业 DCS/SCADA 系统的标准可视化组件，符合流程工业操作员的视觉习惯，专业感强
- **不使用条幅式**：实时自控率采用仪表盘卡片而非全宽条幅，避免占用顶部宝贵空间，与装置级三大 KPI 视觉层级平等
- **颜色映射统一**：仪表盘弧段着色与状态徽章、装置评分排名柱状图色块共用 5 级定级色板（绿/蓝/黄/橙/红）
- **数据刷新策略**：装置级三大 KPI 每小时随标准任务刷新；实时自控率每分钟刷新；趋势图与排名按用户操作刷新
- **可信度贯穿**：所有 KPI 卡片显示可信度角标，E 级回路不计入装置级聚合（已在后端聚合时剔除）
- KPI 卡片不使用渐变/彩色 border，仅用数值大小与评分色块传递信息
- 趋势图支持十字准星联动

**API 对接**：
- 装置级三大 KPI：`GET /api/v1/dashboard/board?plantNodeId={id}&timeWindow={today|yesterday|last_7_days}`（数据源 `unit_kpi_summary`）
- 实时自控率：`GET /api/v1/dashboard/auto-rate-rt?plantNodeId={id}`（数据源 Redis 实时键，60 秒轮询）
- 装置评分排名：`GET /api/v1/dashboard/board` 同接口返回
- 稳定率趋势：`GET /api/v1/dashboard/trend?metric=steady_rate&granularity={day|hour}&timeWindow={...}`
- 低效回路 Top 10：`GET /api/v1/performance/ranking?limit=10&timeWindow={...}`

#### 6.3.2 低效回路排行 `/performance/ranking`

**页面结构模式**：数据表页
**目标角色**：全角色
**分期**：Phase 1

**筛选栏**：
- 装置/单元选择器（`plantNodeId`，树形下拉）
- 时间窗选择器（`timeWindow`：`today`/`yesterday`/`last_7_days`，默认 `today`）
- 条数选择（`limit`，默认 10，可选 10/20/50）
- 预诊标签过滤

**表格列**：

| 列 | 字段 | 说明 |
|---|---|---|
| 排名 | — | 按 `score` 升序（低效优先） |
| 回路位号 | `loop_tag` | 等宽，可点击跳转诊断 |
| 所属单元 | `plant_node.name` | — |
| 综合评分 | `score` | 评分色块 + 数值，INCONCLUSIVE 显示"—" |
| 稳定率 | `steady_rate` | 核心质量指标 S |
| **可信度** | `confidence_level` | **v5.0 新增**：A/B/C/D/E 颜色角标（对齐 §3.1.6） |
| 预诊标签 | `pre_diagnosis` | 如"疑似阀门粘滞" |
| 计算状态 | `status` | SUCCESS/INCONCLUSIVE/PARTIAL 标签 |
| 处理状态 | `action_tracker.action_status` | PENDING/IN_PROGRESS/IGNORED/IMPLEMENTED |
| 操作 | — | 查看诊断、导出建议单（权限驱动） |

**行级交互**：点击行 → 右侧抽屉滑出回路摘要（小趋势图 + 关键状态 + 下一步动作入口），保留列表滚动位置与筛选条件。

**设计要点** [v5.3 修订]：
- **参评状态筛选**：本排行**仅展示 `include_in_evaluation=true` 的回路**（对齐 FDS §5.3.4）。不参评回路（`include_in_evaluation=false`，如试运行/临时停用/非关键测量回路）不进入排行，避免误判装置绩效
- 筛选栏提供"包含不参评回路"开关（默认关闭），开启后展示不参评回路并以淡灰行底色 + "不参评"标签区分
- INCONCLUSIVE 回路（可信度 E 级）默认在排行中显示"—"评分，可通过筛选栏"仅显示有效评分"开关隐藏
- 排行数据为空时展示空状态："当前筛选条件下无低效回路数据"

#### 6.3.3 性能指标配置 `/performance/metrics`

**页面结构模式**：配置页（左列表 + 右表单）
**目标角色**：系统管理员
**分期**：Phase 1

**页面内容**（v5.3 修订：3+1+8 结构指标配置，对齐 FDS §5.3.1）：
- **左侧指标列表**（按 3+1+8 分组）：
  - **核心质量指标（CORE）**：准确率 A / 快速率 F / 稳定率 S — 参与加权评分，有权重
  - **投用指标（COMMISSIONING）**：有效自控率 R — 作折扣因子，无权重（标记 `isDiscountFactor=true`）
  - **辅助诊断指标（AUXILIARY_DIAGNOSTIC）**：好值率 / 振荡率 / 饱和率 / 粘滞系数 / 输出值行程指数 / 稳态时间 / 理想稳态时间 / 自控率 — 不参与评分，权重字段固定为 null
  - 每项显示名称 + 类别标签 + 启用状态 + 当前权重（仅核心指标）
  - **顶部提示条** [v5.3 新增]：核心指标权重模板按控制类型配置，已迁移至 §6.3.6 权重配置管理页面。本页仅管理单指标的阈值、启停与算法版本
- **右侧表单**：选中指标时展示
  - **指标类别**（只读：CORE / COMMISSIONING / AUXILIARY_DIAGNOSTIC）
  - **算法公式** [v5.3 修订：标注废弃]：~~公式编辑器~~（**已废弃**，对齐 FDS §5.3.1.2 与 DDS v4.1）。12 项指标的算法公式已按 GB/T 44693.2-2024 与《关键算法设计说明 v2.1》固化为独立函数模块，不再支持用户自定义公式覆盖。表单改为**只读展示**算法公式说明（只读文本框，非编辑器），含"查看公式详情"链接跳转算法文档
  - **权重** [v5.3 修订]：仅核心质量指标可编辑，百分比数值输入，0-100%，带实时总和显示与校验；投用指标显示"折扣因子"标签，无权重输入；辅助诊断指标显示"不参与评分"，权重字段禁用
    - **注意** [R2 口径，2026-07-22]：`metric_config.weight` 为唯一用户入口，本页编辑的是指标级权重值；4 类控制类型模板（`loop_type_weight`）为出厂默认兜底，仅当 `metric_config.weight` 缺失时按回路 `control_type` 回退取值
  - **阈值**（双滑块组件，min/max/alert 三档，详见下方交互规范）
  - ~~**控制类型**（单选按钮组）~~ [v5.3 移除]：控制类型已迁移至回路台账（§6.2.2，`loop_ledger.control_type`），不再在指标配置页配置。本页仅展示当前选中控制类型的权重值（只读），编辑入口跳转 §6.3.6
  - **Metric Validity Mask**（v5.0 新增，只读展示该指标的有效性掩码表达式，如 `pv_valid && sp_valid`）
  - **tagGroup**（v5.0 新增，只读展示该指标的数据来源 tagGroup，如 `BASE` / `OP_HF`）
  - **启用状态**（开关）
  - **算法版本号**（只读，如 `KPI_CALC_v1.0`）
  - **计算方法说明**（只读）

**设计要点** [v5.3 修订]：
- **公式编辑器废弃** [v5.3 重要变更]：对齐 FDS §5.3.1.2 与 DDS v4.1，`metric_config.formula` 字段已标注废弃。12 项指标算法固化为独立函数模块，不再支持用户自定义公式覆盖。本页移除公式编辑器组件（§6.7.2 公式编辑器组件同步标注为废弃，仅保留用于历史版本兼容查看），改为只读展示算法公式说明
- **控制类型字段迁移** [v5.3 重要变更，R2 口径补充 2026-07-22]：`control_type` 字段从 `metric_config` 迁移至 `loop_ledger`（对齐 DDS v4.1）。本页不再配置控制类型；`metric_config.weight` 为唯一用户入口，4 类控制类型模板（`loop_type_weight`）作为出厂默认兜底，仅当 `metric_config.weight` 缺失时按回路 `control_type` 回退取值
- **权重约束**（v5.0 变更）：仅 3 项核心质量指标（A/F/S）权重总和须大于 0，顶部固定显示"核心指标权重总和：a+f+s"实时进度条。不再要求总和=100%（权重在公式中自动归一化：P=(A·a+F·f+S·s)/(a+f+s)×R）
- **投用指标特殊展示**：有效自控率 R 标记"折扣因子"标签，不参与权重求和，公式中作乘法折扣
- **辅助诊断指标无权重**：8 项辅助诊断指标的权重字段固定为 null，禁用编辑，标注"不参与综合评分"
- **阈值双滑块交互**（v4.1 新增，对齐 C3 JSONB 阈值对象）：
  - 单滑轨上三个把手：`min`（最小值）、`alert`（告警阈值）、`max`（最大值）
  - 拖动把手时实时显示数值 tooltip
  - `min` ≤ `alert` ≤ `max` 约束，违反时把手抖动反馈 + 红色边框
  - 滑轨分段着色：`[min, alert]` 绿色（优良区）、`[alert, max]` 琥珀（关注区）、超 `max` 红色（低效区）
  - 对于反向指标（如振荡率/饱和率，值越低越好），滑轨方向反转：`[min, alert]` 红色（低效区）、`[alert, max]` 琥珀（关注区），并在标签处标注"↓ 越低越好"
- 指标停用后，相关 KPI 显示 `INCONCLUSIVE`，不参与综合评分计算
- 配置变更需确认弹窗（§7.8），填写变更说明，记录审计日志
- **批量保存**：支持调用 `PUT /api/v1/configs/metrics` 批量保存全部指标配置（对齐 IDS v3.2 §2.8.2），事务化处理

**API 对接**：
- 加载：`GET /api/v1/configs/metrics`（批量获取，对齐 IDS v3.2 §2.8.1）
- 保存：`PUT /api/v1/configs/metrics`（批量更新，对齐 IDS v3.2 §2.8.2）或 `PUT /api/v1/performance/metrics/{metricId}`（单条更新，对齐 IDS v3.2 §2.3.4）

#### 6.3.4 引擎规则配置 `/performance/rules`

**页面结构模式**：配置页（左列表 + 右表单）
**目标角色**：系统管理员
**分期**：Phase 1

**页面内容**：
- **左侧规则列表**：引擎规则项（计算周期/数据拉取/调度参数）
- **右侧表单**：
  - 计算周期（秒/分/时单位选择，数值输入）
  - 数据拉取规则（时间窗口、采样频率、降采样策略）
  - 调度参数（并发数、超时时间、重试策略）
  - 启用状态（开关）

**设计要点**：
- 规则变更需确认弹窗，填写变更说明，记录审计日志
- 关键参数（如计算周期）变更后提示"新规则将在下一个计算周期生效"

#### 6.3.5 性能统计报表 `/performance/analytics`

**页面结构模式**：数据表页 + 图表区
**目标角色**：全角色
**分期**：Phase 1

**页面内容**：
- **筛选栏**：时间范围 + 装置/单元 + 指标选择
- **图表区**：
  - **KPI 趋势对比折线图**：多 KPI 多时间窗趋势对比
  - **装置评分排名柱状图**：各装置综合评分横向柱状图
  - **差等生分布饼图**：低效回路按预诊标签分布
- **数据表**：详细统计数据（装置/单元/回路数/平均评分/低效回路数）
- **导出**：支持导出 Excel/PDF

**设计要点**：
- 图表与数据表联动：点击图表元素过滤数据表
- 支持时间范围对比（如本月 vs 上月）

#### 6.3.6 权重配置管理 `/performance/weight-config` [v5.3 新增]

**页面结构模式**：配置页（Tab 切换 + 表格 + 版本管理）
**目标角色**：系统管理员
**分期**：Phase 1

**对齐 FDS §5.3.7.1 权重配置管理**：本页统一管理 4 类控制类型的权重模板与 5 级性能定级阈值，是回路级评分与装置级聚合的权重配置中枢。配置变更须二次确认 + 审计日志 + 版本化保存（保留历史版本，支持回滚）。

**页面布局**：

```
+------------------------------------------------------------------------+
|  权重配置管理                                                            |
|  [控制类型权重模板] [性能定级阈值] [版本历史]      [恢复国标默认值]      |
+------------------------------------------------------------------------+
|                                                                          |
|  Tab 1: 控制类型权重模板                                                 |
|  ┌──────────────────────────────────────────────────────────────────┐  |
|  │ 控制类型: [STABLE 稳定型 ▼]                                       │  |
|  │                                                                    │  |
|  │ 核心质量指标权重（a + f + s = 1.0）                                │  |
|  │ ┌────────────────┬────────────────┬────────────────┐             │  |
|  │ │ a (准确率 ③)    │ f (快速率 ④)    │ s (稳定率 ⑤)    │             │  |
|  │ │ [0.20]          │ [0.30]          │ [0.50]          │             │  |
|  │ │ 国标默认: 0.20  │ 国标默认: 0.30  │ 国标默认: 0.50  │             │  |
|  │ └────────────────┴────────────────┴────────────────┘             │  |
|  │ 权重总和: 1.00  ✓ 归一化通过                                       │  |
|  │ 适用场景: 温度控制、压力控制                                         │  |
|  │                                                                    │  |
|  │ R 折扣因子说明: 有效自控率 R 作乘法折扣，不参与权重归一              │  |
|  │ 公式: P_loop = (A·a + F·f + S·s)/(a+f+s) × (R/100)                │  |
|  │                                                                    │  |
|  │ [取消]  [保存为新版本]                                              │  |
|  └──────────────────────────────────────────────────────────────────┘  |
|                                                                          |
|  Tab 2: 性能定级阈值                                                     |
|  ┌──────────────────────────────────────────────────────────────────┐  |
|  │ 5 级性能定级（国标 GB/T 44693.2-2024 附录 D 默认值）              │  |
|  │ ┌──────────────┬───────────┬───────────┐                          │  |
|  │ │ 定级          │ 评分区间   │ 颜色       │                          │  |
|  │ ├──────────────┼───────────┼───────────┤                          │  |
|  │ │ 一级 EXCELLENT│ [90, 100] │ 绿色 ●     │ [编辑]                   │  |
|  │ │ 二级 GOOD     │ [80, 90)  │ 蓝色 ●     │ [编辑]                   │  |
|  │ │ 三级 FAIR     │ [70, 80)  │ 黄色 ●     │ [编辑]                   │  |
|  │ │ 四级 WARNING  │ [60, 70)  │ 橙色 ●     │ [编辑]                   │  |
|  │ │ 五级 POOR     │ [0, 60)   │ 红色 ●     │ [编辑]                   │  |
|  │ └──────────────┴───────────┴───────────┘                          │  |
|  │ 约束: 边界值须严格递增（90 > 80 > 70 > 60 > 0）                    │  |
|  │ [取消]  [保存为新版本]                                              │  |
|  └──────────────────────────────────────────────────────────────────┘  |
|                                                                          |
|  Tab 3: 版本历史                                                         |
|  ┌──────────────────────────────────────────────────────────────────┐  |
|  │ 版本号 | 类型       | 变更内容       | 操作人 | 时间    | 操作    │  |
|  │ v3     | 权重模板    | STABLE 调整    │ admin  | 07-04   | 回滚    │  |
|  │ v2     | 定级阈值    | EXCELLENT 90→92│ admin  | 07-03   | 回滚    │  |
|  │ v1     | 国标默认    | 初始化          │ system | 06-26   | —       │  |
|  └──────────────────────────────────────────────────────────────────┘  |
+------------------------------------------------------------------------+
```

**Tab 1: 控制类型权重模板**

- **控制类型选择器**（顶部下拉）：STABLE 稳定型 / SLOW 慢速型 / FAST 快速型 / LOGIC 逻辑型
- **核心质量指标权重表单**（3 个数值输入框）：

| 控制类型 | a (准确率 ③) | f (快速率 ④) | s (稳定率 ⑤) | 适用场景 |
|---|:---:|:---:|:---:|---|
| STABLE 稳定型 | 0.20 | 0.30 | 0.50 | 温度控制、压力控制 |
| SLOW 慢速型 | 0.30 | 0.10 | 0.60 | 缓慢调节的工艺参数 |
| FAST 快速型 | 0.20 | 0.50 | 0.30 | 副回路、速度控制 |
| LOGIC 逻辑型 | 0.00 | 0.40 | 0.60 | 安全动作、联锁保护 |

  - 每个输入框下方显示"国标默认: X.XX"，便于对比
  - 实时归一校验：a + f + s = 1.0，底部显示"权重总和: X.XX ✓ 归一化通过"或"✗ 权重总和须为 1.0"
  - 权重总和 ≠ 1.0 时禁用"保存为新版本"按钮，红色边框 + tooltip 提示

- **R 折扣因子说明区**（只读）：解释有效自控率 R 作乘法折扣，不参与权重归一，公式 `P_loop = (A·a + F·f + S·s)/(a+f+s) × (R/100)`

- **适用场景说明**（只读文本）：当前控制类型的典型应用场景

**Tab 2: 性能定级阈值**

- **5 级定级表**（双滑块组件或数值输入）：

| 定级 | 英文标识 | 评分区间（国标默认） | 颜色 |
|---|---|---|---|
| 一级 | EXCELLENT | [90, 100] | 绿色 |
| 二级 | GOOD | [80, 90) | 蓝色 |
| 三级 | FAIR | [70, 80) | 黄色 |
| 四级 | WARNING | [60, 70) | 橙色 |
| 五级 | POOR | [0, 60) | 红色 |

  - 每行可编辑评分区间下限与上限
  - 约束：边界值须严格递增（90 > 80 > 70 > 60 > 0），违反时红色边框 + 禁用保存
  - 颜色固定（不可编辑），对齐 §3.1.6 评分色板

- **双命名说明**（只读）：系统同时保留国标中文定级（一级~五级）与英文标识（EXCELLENT/GOOD/FAIR/WARNING/POOR），前端展示时以"一级 (EXCELLENT)"格式呈现

**Tab 3: 版本历史**

- **版本列表**（数据表）：
  - 列：版本号 / 变更类型（权重模板 / 定级阈值 / 国标默认初始化）/ 变更内容摘要 / 操作人 / 变更时间 / 操作（回滚到此处）
  - 按版本号降序排列（最新版本在最上方）
  - 当前生效版本以绿色"生效中"徽章标识
- **回滚操作**：点击"回滚到此处"→ 二次确认弹窗"确认回滚到 vN？当前版本将被归档，新版本号自动递增"→ 确认后创建新版本（内容为回滚目标版本的快照）

**交互规范**：
- **保存为新版本**：每次保存自动递增版本号，保留历史版本（不可物理删除）
- **二次确认**：保存时弹窗"确认保存权重配置变更？将创建新版本 vN，影响所有回路的评分计算"，需填写变更说明
- **审计日志**：所有变更记录写入 `sys_audit_log`，含操作人 / 时间 / 变更前后值 / 变更说明
- **生效时机**：保存后按 `effective_from` 在下一个计算周期原子切换，无需重启服务
- **恢复国标默认值**：右上角按钮，点击后弹窗"确认恢复所有控制类型权重与定级阈值为国标默认值？将创建新版本"，确认后创建新版本覆盖当前配置

**设计要点**：
- **版本化保存**：所有配置变更保留历史版本，支持回滚到任意历史版本
- **归一化校验**：权重总和实时校验，违反时禁用保存
- **递增校验**：定级阈值边界严格递增，违反时禁用保存
- **审计留痕**：所有变更记录审计日志，不可物理删除
- **国标默认值参照**：每个输入框显示国标默认值，便于对比与恢复
- **影响范围提示**：保存前弹窗提示"本次变更将影响 N 个回路的评分计算"，N 为受影响回路数

**API 对接**：
- 加载权重模板：`GET /api/v1/configs/weight-templates`（返回 4 类控制类型的权重模板）
- 保存权重模板：`POST /api/v1/configs/weight-templates`（创建新版本）
- 加载定级阈值：`GET /api/v1/configs/grading-thresholds`
- 保存定级阈值：`POST /api/v1/configs/grading-thresholds`（创建新版本）
- 版本历史：`GET /api/v1/configs/weight-templates/versions` + `GET /api/v1/configs/grading-thresholds/versions`
- 回滚：`POST /api/v1/configs/weight-templates/rollback` + `POST /api/v1/configs/grading-thresholds/rollback`
- 恢复国标默认值：`POST /api/v1/configs/restore-defaults`

### 6.4 诊断中心模块

#### 6.4.1 诊断列表 `/diagnosis`

**页面结构模式**：数据表页
**目标角色**：仪控/工艺设备/管理员/外部专家（Sponsor 不可见）
**分期**：Phase 1

**页面内容**：
- **分组筛选**：按预诊标签分组（`疑似粘滞`/`参数过激`/`原因不明(需人工介入)`）
- **筛选栏**：关键词搜索 + 装置/单元 + 预诊标签 + 处理状态 + **可信度等级**（v5.0 新增：A/B/C/D/E 筛选）+ 置信度范围
- **表格列**：回路位号、所属单元、诊断类型（预诊标签）、置信度、**可信度**（v5.0 新增：A/B/C/D/E 角标）、建议动作、处理状态、计算状态、操作（进入诊断详情、导出建议单）

**设计要点**：
- 置信度以进度条 + 数值展示
- 可信度等级以颜色角标展示（对齐 §3.1.6）
- 处理状态标签三维组合（颜色 + 文本 + 图标）

#### 6.4.2 诊断指标配置 `/diagnosis/metrics`

**页面结构模式**：配置页（左列表 + 右表单）
**目标角色**：系统管理员
**分期**：Phase 1

**页面内容**：
- **左侧指标列表**：8 类诊断标签对应的算法配置（对齐 C6 修正）：
  - `OSCILLATION` 振荡检测（FFT/自相关）
  - `VALVE_STICTION` 阀门粘滞检测（散点拟合）
  - `OVERAGGRESSIVE` 参数过激检测（PID 增益分析）
  - `OVERCONSERVATIVE` 参数过保守检测（PID 增益分析）
  - `EXTERNAL_DISTURBANCE` 外扰频繁检测（频谱分析）
  - `QUALITY_ABNORMAL` PV 质量异常检测（质量码统计）
  - `OUTPUT_SATURATION` 输出饱和检测（OP 范围统计）
  - `MANUAL_REVIEW` 人工复核（综合评分阈值触发）
  - 每项显示标签名 + 算法类型 + 启用状态
- **右侧表单**：
  - **诊断标签**（只读，如 `OSCILLATION` 振荡检测）
  - **算法类型**（下拉选择：FFT/ScatterFitting/PIDAnalysis/DisturbanceAnalysis/QualityCheck/SaturationCheck/Manual）
  - **计算方法**（下拉选择，对齐 C4 修正：`auto_correlation` 自相关 / `fft` 快速傅里叶变换 / `pv_op_scatter` PV-OP 散点拟合 / `pid_gain_analysis` PID 增益分析 / `spectral_analysis` 频谱分析 / `quality_code_stats` 质量码统计 / `op_range_stats` OP 范围统计 / `manual_trigger` 人工触发）
  - **算法参数**（JSON 编辑器，如 FFT 的 windowSize/overlap/minFrequency/maxFrequency）
  - **阈值**（JSONB 对象编辑器，对齐 C3 修正，如 `{amplitude: 1.5, confidence: 0.7}`）
  - **启用状态**（开关）
  - **算法版本号**（只读，如 `OSC_FFT_v1.0`）
  - **说明**（只读）

**设计要点**：
- 8 类诊断标签使用颜色编码徽章（对齐 §7.10 诊断结果展示组件）
- 阈值编辑器根据算法类型动态渲染表单字段（如 FFT 显示 amplitude/confidence 两个数值输入，粘滞检测显示 stictionIndex/fittingScore）
- 指标停用后，相关诊断标签不再生成
- 配置变更需确认弹窗（§7.8），填写变更说明，记录审计日志
- **批量保存**：支持调用 `PUT /api/v1/configs/diagnosis` 批量保存全部 8 项配置（对齐 IDS v3.2 §2.9.2），事务化处理

**API 对接**：
- 加载：`GET /api/v1/configs/diagnosis`（批量获取，对齐 IDS v3.2 §2.9.1）
- 保存：`PUT /api/v1/configs/diagnosis`（批量更新，对齐 IDS v3.2 §2.9.2）或 `PUT /api/v1/diagnosis/metrics/{diagId}`（单条更新，对齐 IDS v3.2 §2.4.4）

#### 6.4.3 回路诊断详情 `/diagnosis/:loopId`（隐藏路由）

**页面结构模式**：工作台页（上下分栏 + 右侧动作区）
**目标角色**：仪控/工艺设备/管理员/外部专家
**分期**：Phase 1

**页面布局**：

```
+----------------------------------------------------------------------------------------+
| 顶部：回路位号 | 当前判断 | 风险等级 | 处理状态 | 责任角色 | 下一步动作              |
+----------------------------------------------------------------------------------------+
| 中上：时序波形（PV/SP 主轴 + OP 副轴 + MODE 背景带）+ 时间窗选择器                      |
|       PV 线根据质量码显示灰色虚线保留连线，SP/OP 线正常显示                                |
+----------------------------------------------------------------------------------------+
| 中下左：PV-OP 散点图（拟合椭圆/平行四边形）  | 中下右：诊断结论卡片 + 规则命中解释      |
+----------------------------------------------+-----------------------------------------+
| 右侧：建议动作 | 导出建议单 | 进入异常跟踪 | （整定样例 Phase 2）                       |
+----------------------------------------------------------------------------------------+
```

**页面内容**：
- **顶部**：回路位号 | 当前判断（8 类诊断标签，对齐 C6）| 风险等级 | 处理状态 | 责任角色 | 下一步动作
- **中上**：时序波形（PV/SP 主轴 + OP 副轴 + MODE 背景带）+ 时间窗选择器
- **中下左**：PV-OP 散点图（拟合椭圆/平行四边形）
- **中下右**：诊断结论卡片（8 类诊断标签数组 + 融合置信度 + 证据链 + 建议动作，详见 §7.10）
- **右侧**：建议动作 | 导出建议单 | 进入异常跟踪 | （整定样例 Phase 2）

**波形区规范**（详见 §7.3）：
- 时间窗选择器，最大跨度 30 天（超限提示 `ERR_TS_001`）
- 默认开启 LTTB 降采样，`maxPoints=2000`
- **PV 线根据质量码显示**（v5.0 更新）：Good 实线正常 / **Bad 灰色虚线保留连线** + 悬浮提示"PV 数据质量不可信" / Uncertain 琥珀虚线保留连线 + 悬浮提示"PV 数据质量不确定"
- **SP/OP 线正常显示**，不受 PV 质量码影响
- `mode` 分段背景带：Manual 红、Auto 绿、Cascade 蓝

**诊断结论卡片**（v4.1 更新，对齐 C6/C7 + IDS v3.2 §2.4.2）：
- **诊断标签数组**（`diagnosisLabels`）：每项含 `label`（8 类枚举）/`labelName`（中文名）/`confidence`（单算法置信度 0-1，进度条 + 数值）/`evidence`（证据对象，含特征值/散点图 URL/推理过程）/`algorithm`（算法版本号）
- **融合置信度**（`fusedConfidence`）：Dempster-Shafer 证据理论融合后的置信度（0-1），醒目展示在卡片顶部
- **特征值**（`featureValues`）：振荡幅度/振荡频率/粘滞指数/输出饱和率等
- **证据链**（`evidenceChain`）：波形 URL + PV-OP 散点图数据 + 推理过程文本
- **算法版本号**（`algorithmVersion`）：如 `STICTION_CH_v1.0`
- **建议动作**：基于诊断标签生成的处置建议

#### 6.4.4 异常跟踪 `/diagnosis/tracker`

**页面结构模式**：数据表页 + 右侧抽屉
**目标角色**：仪控工程师（状态变更独占）
**分期**：Phase 1

**页面内容**：
- **筛选栏**：关键词搜索 + 装置/单元 + 处理状态 + 预诊标签 + 时间范围
- **表格列**：回路位号、预诊标签、处理状态、最后操作人、最后操作时间、PDF 下载入口、操作

**Action Tracker 状态流转**（v6.0 修订，对齐实现契约 v2.0 / §7.2.2 / §8.2.3 标准枚举）：

```
PENDING（待处理） → IN_PROGRESS（处理中） → IMPLEMENTED（已实施，触发 A/B 对比采集）
                                                  └→ IGNORED（已忽略，需注明忽略原因）
```

**状态变更交互**：
- 仅仪控工程师可见状态下拉框
- 变更时弹出确认抽屉，填写 `comment`（处理说明，必填 1-500 字符）
- 标记 IMPLEMENTED 后自动生成 A/B 对比视图
- 状态变更记录审计日志，不支持 IMPLEMENTED/IGNORED 回退至 PENDING/IN_PROGRESS
- IGNORED 需注明忽略原因（如"误报"、"已纳入整定计划"等）
- 成功后全局 Toast"状态已更新为：处理中"，列表即时变色
- 每次变更记录 `updated_by` + `updated_at`，生成处理时间线节点

**标记"已实施"后的 A/B 对比**：
- 自动截取 `[T-7天, T]` 与 `[T, T+7天]` 两窗口 KPI 数据
- 展示 Before/After 柱状对比图，提升标绿、下降标红
- 实施后数据不足 24h 时提示"评估数据采集中，请稍后查看"

**导出诊断建议书**：
- 点击"导出 PDF"调用 `POST /api/v1/tracker/{loopId}/export`（异步，返回 202 + `taskId`）
- UI 显示"生成中"状态，轮询 `checkUrl` 获取结果
- 文件名规范：`CLPM-诊断建议书-[回路位号]-[日期].pdf`
- 内容含：当前波形截图 + 诊断结论文本

#### 6.4.5 诊断统计报表 `/diagnosis/analytics`

**页面结构模式**：数据表页 + 图表区
**目标角色**：仪控/工艺设备/管理员/外部专家
**分期**：Phase 1

**页面内容**：
- **筛选栏**：时间范围 + 装置/单元 + 预诊标签 + 处理状态
- **图表区**：
  - **预诊标签分布饼图**：各预诊标签的回路数占比
  - **处理效率趋势折线**：各处理状态随时间的数量变化
  - **闭环时长分布柱状图**：从 PENDING 到 IMPLEMENTED 的时长分布
- **数据表**：详细统计数据
- **导出**：支持导出 Excel/PDF

### 6.5 回路整定模块（Phase 2，原型先行设计）

> **说明**：本模块为 Phase 2 功能，原型阶段完成所有子模块页面设计与交互演示，Phase 2 实现算法。原型页面标注"Phase 2 规划中"版本标签，参数计算结果使用 mock 数据。

#### 6.5.1 整定工作台 `/tuning`

**页面结构模式**：工作台页（三栏）
**目标角色**：仪控工程师/外部专家
**分期**：Phase 2（原型 Phase 1）

**页面布局**：

```
+----------------------------------------------+-------------------------------+
| 左侧（30%）：待整定回路列表                  | 中右侧（70%）：整定状态概览   |
| [回路位号 | 当前评分 | 推荐状态]             | +---------------------------+ |
| [回路位号 | 当前评分 | 推荐状态]             | | 选中回路：基础信息+趋势   | |
| ...                                          | | 当前评分 + 历史整定记录   | |
| 来源：诊断建议 / 手动添加                   | | 推荐状态：待辨识/已辨识/  | |
|                                              | |   已整定/已仿真           | |
|                                              | +---------------------------+ |
|                                              | 任务快捷入口：               |
|                                              | [模型辨识] [整定算法]        |
|                                              | [闭环仿真] [效果统计]        |
+----------------------------------------------+-------------------------------+
```

**页面内容**：
- **左侧待整定回路列表**：来自诊断建议或手动添加的回路（回路位号/当前评分/推荐状态）
- **中右侧整定状态概览**：选中回路的基础信息 + 当前趋势 + 历史整定记录 + 推荐状态
- **任务快捷入口**：模型辨识、整定算法、闭环仿真、效果统计

**设计要点**：
- 推荐状态流转：待辨识 → 已辨识 → 已整定 → 已仿真
- 原型阶段所有数据为 mock，标注"演示数据"

#### 6.5.2 模型辨识 `/tuning/identification`

**页面结构模式**：配置页（左参数 + 右结果）
**目标角色**：仪控工程师/外部专家
**分期**：Phase 2（原型 Phase 1）

**页面内容**：
- **左侧参数配置**：
  - 数据段选择（时间范围 + 回路）
  - 采样周期
  - 模型类型（单选按钮组：FOPDT 一阶 / SOPDT 二阶 / IPDT 积分，对齐§6.1）
  - 辨识方法（下拉选择，对齐§6.1.4：`TWO_POINT` 两点法 / `AREA` 面积法 / `COMBINED` 组合法）
  - [开始辨识] 按钮（触发 `POST /api/v1/algorithms/tuning/calculate`，异步任务，详见 §6.7.4）
- **右侧结果展示**（对齐 C5 + IDS v3.2 §2.7.4 整定计算完成结果）：
  - **模型参数表**（`modelParams`，等宽字体）：
    - FOPDT：K（增益）/ tau（时间常数）/ theta（纯滞后）
    - SOPDT：K / T1 / T2 / theta
    - IPDT：K / theta
  - **拟合度**（`fittingScore`，R² 0-1，百分比 + 评级徽章：≥0.9 优 / 0.8-0.9 良 / 0.6-0.8 中 / <0.6 差，对齐 C5）
  - **阶跃响应对比曲线**（实测 vs 模型双波形，ECharts）
  - **算法版本号**（`algorithmVersion`，只读，如 `FOPDT_ID_v1.0`）

**设计要点**：
- 辨识过程显示算法任务进度条（对齐 §7.13 算法任务状态组件），轮询 `GET /api/v1/algorithms/tasks/{task_id}`
- 结果参数表使用等宽字体，数值保留 2 位小数
- 拟合度 < 0.6 时标红 + 提示"模型拟合度不足，建议更换辨识方法或调整数据段"
- 原型阶段结果为 mock 数据

#### 6.5.3 整定算法 `/tuning/algorithm`

**页面结构模式**：配置页（左参数 + 右结果）
**目标角色**：仪控工程师/外部专家
**分期**：Phase 2（原型 Phase 1）

**页面内容**：
- **左侧参数配置**：
  - **整定方法**（单选按钮组，对齐§6.3-§6.7：`IMC` 内模控制 / `LAMBDA` Lambda 整定 / `ZIEGLER_NICHOLS` ZN 法 / `COHEN_COON` Cohen-Coon 法 / `SIMC` SIMC 法）
  - 模型来源（从模型辨识结果导入 / 手动输入）
  - 算法参数（根据方法动态渲染）：
    - IMC：闭环时间常数 lambda（数值输入）
    - LAMBDA：期望响应时间 lambda（数值输入）
    - ZN/CC/SIMC：无额外参数（只读模型参数）
  - [计算推荐参数] 按钮（触发 `POST /api/v1/algorithms/tuning/calculate`，异步任务）
- **右侧结果展示**（对齐 IDS v3.2 §2.7.4 整定计算完成结果）：
  - **推荐 PID 参数表**（`pidParams`，P/I/D 三组对比：当前/推荐/差异，等宽字体）：
    - Kp（比例增益）/ Ti（积分时间）/ Td（微分时间）
    - 差异列高亮：推荐值标绿，差异百分比标注
  - **模型参数**（`modelParams`，只读，来自辨识结果）
  - **拟合度**（`fittingScore`，只读，来自辨识结果）
  - **算法版本号**（`algorithmVersion`，只读，如 `IMC_TUNE_v1.0`）

**设计要点**：
- 参数对比表高亮差异（推荐值标绿，差异 > 50% 标橙提示"参数变化较大，请谨慎实施"）
- **安全边界强调**：推荐参数仅作离线建议，UI 明确标注"参数需由授权人员人工实施，平台不下写 DCS"
- 计算过程显示算法任务进度条（对齐 §7.13）
- 原型阶段结果为 mock 数据

#### 6.5.4 闭环仿真 `/tuning/simulation`

**页面结构模式**：工作台页（上下分栏）
**目标角色**：仪控工程师/外部专家
**分期**：Phase 2（原型 Phase 1）

**页面内容**：
- **上部参数配置**：
  - PID 参数切换（单选：当前 PID vs 推荐 PID）
  - 扰动类型（下拉：阶跃 step / 脉冲 impulse / 正弦 sine，对齐§6.8.2）
  - 扰动幅值、仿真时长
  - [开始仿真] 按钮（触发 `POST /api/v1/algorithms/tuning/calculate`，`enableSimulation=true`，异步任务）
- **下部仿真结果**（对齐 IDS v3.2 §2.7.4 + §6.8.2）：
  - **阶跃响应双波形对比**（当前 PID 蓝线 vs 推荐 PID 绿线，ECharts）
  - **性能指标对比表**（`simulationResult`，等宽字体）：
    - riseTime（上升时间）/ overshoot（超调）/ settlingTime（settling time）/ itae（ITAE 误差积分）
    - 改善项标绿，恶化项标红，delta 数值标注

**设计要点**：
- 双波形对比使用不同颜色 + 图例
- 性能指标对比表高亮改善项（标绿）与恶化项（标红）
- 仿真过程显示算法任务进度条（对齐 §7.13）
- 原型阶段仿真结果为 mock 数据

#### 6.5.5 整定效果统计 `/tuning/analytics`

**页面结构模式**：审计页（结论摘要 + 证据矩阵 + 风险说明）
**目标角色**：仪控工程师/外部专家
**分期**：Phase 2（原型 Phase 1）

**页面内容**：
- **顶部结论摘要**：整定效果总体评价（改善/持平/恶化）
- **中部证据矩阵**：
  - 整定前后 KPI 对比柱状图（评分/稳定率/振荡率等）
  - 整定效果趋势折线（整定前后时间窗 KPI 变化）
- **底部风险说明**：整定建议的安全边界说明、回退方案

**设计要点**：
- 整定前后对比使用 Before/After 双柱
- 风险说明明确"参数需人工实施，平台不下写 DCS"

### 6.6 系统管理模块

#### 6.6.1 用户与角色 `/system/users`

**页面结构模式**：配置页（左列表 + 右表单）
**目标角色**：系统管理员
**分期**：Phase 1

**页面内容**：
- **左侧用户列表**：用户名/姓名/角色/启用状态
- **右侧表单**：用户基本信息 + 角色分配 + 权限矩阵展示

**设计要点**：
- 角色分配下拉框限定为 5 种角色
- 用户创建/编辑需确认弹窗，记录审计日志

#### 6.6.2 审计日志 `/system/audit`

**页面结构模式**：数据表页
**目标角色**：系统管理员
**分期**：Phase 1

**页面内容**：
- **筛选栏**：操作人 + 操作类型 + 时间范围 + 模块
- **表格列**：时间戳、操作人、操作类型、模块、操作详情、IP 地址
- **不可物理删除**：仅支持查询，无删除按钮

**设计要点**：
- 时间戳使用等宽字体
- 操作类型支持颜色编码（创建绿/修改蓝/删除红）

#### 6.6.3 自动报表管理 `/system/reports`

**页面结构模式**：数据表页
**目标角色**：系统管理员
**分期**：Phase 1

**页面内容**：
- **报表周期配置**：班/日/周/月报表的生成时间配置
- **报表记录表格**：周期、生成时间、状态（生成中/已生成/生成异常）、下载入口
- **异常处理**：截图服务失败标记"生成异常"，支持手动重试

**设计要点**：
- 报表生成状态实时更新（轮询）
- 下载文件名规范：`CLPM-报表-[周期]-[日期].pdf`

#### 6.6.4 安全边界说明 `/system/safety`

**页面结构模式**：审计页（结论摘要 + 证据矩阵 + 风险说明）
**目标角色**：全角色
**分期**：Phase 1

**页面内容**：
- **顶部结论摘要**：CLPM 安全边界总述
- **中部证据矩阵**：
  - **能做**：只读 DCS 数据、输出建议、证据留痕、回退方案
  - **不能做**：下写 DCS 参数、自动整定、替代 APC/SIS
- **底部风险说明**：网络分区、数据隔离、权限控制说明

**设计要点**：
- "能做/不能做"使用绿色对勾/红色叉号清晰区分
- 全角色可见，无操作按钮

### 6.7 算法配置界面（v4.1 新增）

> **说明**：本节定义算法配置相关的界面设计规范，对齐《关键算法设计说明》v1.0 与 IDS v3.2 §2.7-§2.9。算法配置界面分散在各模块中（性能指标配置 §6.3.3 / 诊断指标配置 §6.4.2 / 整定参数配置 §6.5），本节定义跨模块共用的配置组件与交互模式。

#### 6.7.1 算法配置总览 `/config/algorithms`（导航入口）

**页面结构模式**：导航卡片页
**目标角色**：系统管理员
**分期**：Phase 1

**页面内容**：
- **配置分类卡片**（3 类，网格布局）：
  - **指标配置**（跳转 `/performance/metrics`）：3+1+8 指标体系（3 核心质量指标公式/权重/阈值 + 1 投用指标折扣因子 + 8 辅助诊断指标配置）
  - **诊断配置**（跳转 `/diagnosis/metrics`）：8 类诊断标签算法/参数/阈值
  - **整定配置**（跳转 `/tuning`，Phase 2）：模型辨识/整定算法/仿真参数
- **算法版本信息**：每类配置卡片底部显示当前算法版本号（如 `KPI_CALC_v1.0`）
- **最近变更记录**：每类配置卡片显示最近 3 条审计日志摘要

**设计要点**：
- 卡片采用极简风格，白底 + 灰边 + 左侧色条（指标蓝/诊断橙/整定紫）
- 仅系统管理员可见"编辑"入口，其他角色只读查看

#### 6.7.2 公式编辑器组件（Formula Editor） ~~已废弃~~ [v5.3 标注废弃]

> ⚠️ **废弃说明** [v5.3 重要变更]：对齐 FDS §5.3.1.2 与 DDS v4.1，`metric_config.formula` 字段已标注废弃。12 项指标算法已按 GB/T 44693.2-2024 与《关键算法设计说明 v2.1》固化为独立函数模块，不再支持用户自定义公式覆盖。**本组件仅保留用于历史版本兼容查看，新建/编辑场景不再使用**。性能指标配置页（§6.3.3）已改为只读展示算法公式说明，公式编辑入口移除。下方规格仅作历史参考。

**适用页面**：~~性能指标配置 §6.3.3~~（已废弃，§6.3.3 改为只读算法公式说明展示）

**组件结构**：
```
+----------------------------------------------------------+
| 公式编辑器                                                |
| +------------------------------------------------------+ |
| | sum(quality==Good) / count(*) * 100                  | |
| |                                                      | |
| |                                                      | |
| +------------------------------------------------------+ |
| 变量插入：[pv] [sp] [op] [mode] [pv_quality] [timestamps] [pv_range] [n] |
| 函数插入：[sum] [mean] [std] [count] [count_if] [abs] [sqrt] [min] [max] [duration] |
| 语法校验：✓ 公式有效 / ✗ 错误：[错误详情]                 |
| 字符数：23/500  |  超时限制：5s  |  引擎：simpleeval       |
+----------------------------------------------------------+
```

**交互规范**：
- **变量/函数插入**：点击按钮在光标位置插入对应变量/函数名，自动补全括号
- **实时语法校验**：输入时调用 simpleeval 安全沙箱校验（对齐 C7），错误标红 + tooltip 显示错误详情
- **字符数限制**：500 字符，超限时标红 + 禁止继续输入
- **可用变量**：pv/sp/op/mode/pv_quality/timestamps/pv_range/n
- **可用函数**：sum/mean/std/count/count_if/abs/sqrt/min/max/duration
- **禁用操作**：import/exec/eval/属性访问（安全沙箱拦截）
- **超时限制**：5 秒，超时标红 + 提示"公式执行超时，请优化"

**视觉规范**：
- 编辑器使用等宽字体（JetBrains Mono / Fira Code）
- 变量高亮：蓝色；函数高亮：紫色；运算符：默认色
- 校验通过：绿色对勾 + "公式有效"；校验失败：红色叉号 + 错误详情

#### 6.7.3 诊断算法配置组件（Diagnosis Algorithm Config）

**适用页面**：诊断指标配置 §6.4.2

**组件结构**：
- **诊断标签徽章**（只读，8 类颜色编码，对齐 §7.10）
- **算法类型下拉**（FFT/ScatterFitting/PIDAnalysis/DisturbanceAnalysis/QualityCheck/SaturationCheck/Manual）
- **计算方法下拉**（对齐 C4：auto_correlation/fft/pv_op_scatter/pid_gain_analysis/spectral_analysis/quality_code_stats/op_range_stats/manual_trigger）
- **算法参数 JSON 编辑器**（根据算法类型动态渲染表单字段）
- **阈值 JSONB 编辑器**（对齐 C3，根据算法类型动态渲染字段）

**动态表单规则**：
| 算法类型 | 算法参数字段 | 阈值字段 |
|---|---|---|
| FFT | windowSize/overlap/minFrequency/maxFrequency | amplitude/confidence |
| ScatterFitting | fittingType/minPoints | stictionIndex/fittingScore |
| PIDAnalysis | gainMarginThreshold/phaseMarginThreshold | gainDeviation/oscillationIndex |
| DisturbanceAnalysis | windowSize/frequencyBand | disturbanceIndex/frequencyConcentration |
| QualityCheck | windowSize | badRate/uncertainRate |
| SaturationCheck | highThreshold/lowThreshold/minDuration | saturationRate |
| Manual | （无） | score |

#### 6.7.4 整定参数配置组件（Tuning Params Config）

**适用页面**：模型辨识 §6.5.2 / 整定算法 §6.5.3 / 闭环仿真 §6.5.4

**组件结构**：
- **模型类型单选按钮组**（FOPDT/SOPDT/IPDT）
- **辨识方法下拉**（TWO_POINT/AREA/COMBINED）
- **整定方法单选按钮组**（IMC/LAMBDA/ZIEGLER_NICHOLS/COHEN_COON/SIMC）
- **算法参数动态表单**（根据整定方法渲染：IMC/LAMBDA 显示 lambda 输入，其他方法无额外参数）
- **仿真参数表单**（扰动类型/幅值/仿真时长）

**算法任务触发交互**（对齐 §7.13）：
- 点击"开始辨识/计算/仿真"按钮 → 调用 `POST /api/v1/algorithms/tuning/calculate`
- 返回 202 + `taskId` → 显示算法任务进度条组件
- 前端轮询 `GET /api/v1/algorithms/tasks/{task_id}`（间隔 3 秒）
- 任务完成后渲染结果（模型参数/PID 参数/仿真结果）
- 任务失败时显示错误信息 + 重试按钮

### 6.8 任务管理页面（v5.0 新增，标准任务+自定义任务双轨）

> **说明**：本节定义任务管理界面设计规范，对齐 FDS §6 任务管理双轨（标准任务+自定义任务）与 IDS v4.0 任务管理接口。任务管理页统一管理 KPI 评估任务的调度、执行、结果查看。

#### 6.8.1 任务管理总览 `/tasks`

**页面结构模式**：数据表页（顶部筛选 + Tab 切换 + 中央表格 + 右侧详情抽屉）
**目标角色**：系统管理员、仪控工程师
**分期**：Phase 1

**页面布局**：
```
+----------------------------------------------------------+
| 任务管理                                                  |
| [标准任务] [自定义任务]                  [+ 新建自定义任务] |
+----------------------------------------------------------+
| 筛选：状态[全部▼] 类型[全部▼] 时间窗[今日] [搜索]        |
+----------------------------------------------------------+
| 任务ID | 类型 | 回路范围 | 状态 | 可信度 | 开始时间 | 操作 |
| ...                                                        |
+----------------------------------------------------------+
```

**Tab 切换**（双轨）：
- **标准任务 Tab**：展示每小时定时执行的标准任务列表，只读（不可手动创建/取消），支持查看执行详情
- **自定义任务 Tab**：展示用户手动触发的自定义任务列表，支持新建/取消/查看

#### 6.8.2 标准任务列表 `/tasks/standard`

**页面内容**：
- **任务列表**（高密度表格，对齐 §7.1）：
  - 列：任务ID / 任务类型（KPI_CALCULATION）/ 回路范围（全厂/装置/单元）/ 状态 / 可信度 / 开始时间 / 耗时 / 操作
  - 状态列：对齐 §7.13 任务状态枚举
  - 可信度列：对齐 §7.15 可信度角标组件
- **顶部统计摘要**：
  - 今日执行次数 / 成功率 / 平均耗时 / 平均可信度等级
- **自动刷新**：每 60 秒自动刷新列表（标准任务每小时产生新记录）
- **筛选**：按状态、装置、时间窗筛选
- **行点击**：展开右侧抽屉展示任务详情

**标准任务详情抽屉**：
- 任务基本信息（ID、触发时间、耗时、写入 `kpi_snapshot_hourly` 的记录数）
- 评估结果摘要（3+1+8 指标体系摘要，对齐 §7.12）
- 数据血缘（对齐 §7.14，展示 8 字段）
- 失败回路清单（如有）：回路ID / 错误码 / 错误信息
- 参与聚合说明："本任务结果已参与装置级聚合"

#### 6.8.3 自定义任务列表 `/tasks/custom`

**页面内容**：
- **新建任务按钮**（右上角，仅仪控工程师/系统管理员可见）：打开新建任务抽屉
- **任务列表**（高密度表格）：
  - 列：任务ID / 任务类型 / 回路范围 / 时间窗 / 状态 / 可信度 / 创建人 / 创建时间 / 操作
  - 操作列：查看详情 / 取消任务（仅 PENDING/PROCESSING 状态）/ 删除记录（仅 SUCCESS/FAILED/CANCELLED 状态）
- **筛选**：按状态、类型、创建人、时间窗筛选

**新建自定义任务抽屉**：
```
+----------------------------------------------------------+
| 新建自定义评估任务                              [关闭 ✖]   |
+----------------------------------------------------------+
| 任务类型：[KPI 计算 ▼]                                    |
| 回路范围：[选择回路/装置/单元]  （多选树形组件）            |
| 时间窗：  开始 [2026-06-26 00:00] ~ 结束 [2026-06-26 01:00]|
| 优先级：  [普通 ▼]                                        |
| 备注：    [文本框，可选]                                   |
|                                                           |
| ⚠ 自定义任务结果不参与装置级聚合，仅独立展示              |
|                                                           |
|                            [取消]  [提交任务]              |
+----------------------------------------------------------+
```

**交互规范**：
- **新建任务**：调用 `POST /api/v1/tasks` → 返回 202 + `taskId` → 列表新增一行（状态 `PENDING`）
- **任务轮询**：新建后自动轮询该任务状态（对齐 §7.13），完成后刷新可信度列
- **取消任务**：调用 `DELETE /api/v1/tasks/{taskId}` → 状态变更为 `CANCELLED`
- **结果查看**：点击"查看详情"跳转到任务结果页（§6.8.4）
- **时间窗校验**：结束时间需晚于开始时间，时间跨度 ≤ 30 天，否则标红提示
- **回路范围校验**：至少选择 1 个回路，否则禁用"提交任务"按钮

#### 6.8.4 任务结果详情 `/tasks/:taskId`

**页面结构模式**：工作台页（上中下三行）
**目标角色**：系统管理员、仪控工程师
**分期**：Phase 1

**页面内容**：
- **顶部任务摘要**：任务ID / 类型 / 状态徽章 / 可信度角标 / 时间窗 / 耗时
- **中部结果展示**（按任务类型动态渲染）：
  - `KPI_CALCULATION`：3+1+8 指标体系卡片（对齐 §7.12 综合评分展示组件）
  - `DIAGNOSIS_ANALYSIS`：诊断标签数组 + 融合置信度（对齐 §7.10）
  - `TUNING_CALCULATION`：模型参数 + PID 参数 + 仿真结果（对齐 §7.11）
- **底部数据血缘**：§7.14 数据血缘展示组件（展开态）
- **结果存储说明**：
  - 标准任务：结果写入 `kpi_snapshot_hourly`，参与装置级聚合
  - 自定义任务：结果写入 `kpi_snapshot_custom`，独立存储，不参与聚合

**交互规范**：
- 任务进行中时显示进度条（对齐 §7.13）
- 任务失败时显示错误信息卡片 + 重试按钮
- 自定义任务结果页提供"导出报告"按钮（PDF/Excel）

#### 6.8.5 任务管理设计要点

- **双轨隔离**：标准任务与自定义任务在列表、存储、聚合逻辑上严格隔离，UI 通过 Tab 明确区分
- **可信度贯穿**：所有任务结果展示均带可信度角标（对齐 §7.15）；有效自控率 R 为 E 级/缺失的结果标注 INCONCLUSIVE，个别指标（非 R）为 E 级的结果正常展示并附灰色"可信度 E"角标
- **血缘可追溯**：每个任务结果页展示数据血缘（对齐 §7.14），支持审计化追溯
- **权限控制**：
  - 查看标准任务：全角色
  - 查看自定义任务：创建人 + 系统管理员
  - 新建自定义任务：仪控工程师、系统管理员
  - 取消/删除自定义任务：创建人 + 系统管理员
- **性能考量**：标准任务列表默认展示最近 24 小时（约 24 条/回路），自定义任务列表分页展示（每页 20 条）

---

## 7. 核心组件规范

### 7.1 高密度表格 (Data Table)

工业场景下，所有台账、清单、排行优先使用紧凑型表格，**不允许用卡片代替批量信息浏览**。

**必须具备**：
- 多维联合筛选（顶部筛选栏）
- 列头排序
- 固定表头与首列（回路位号列固定）
- 状态颜色高亮（行级或单元格级）
- 行级快捷动作（如一键导出）
- 批量选择
- 空状态
- Partial 状态提示

**性能要求**：单页 100 条以上回路表格 FPS ≥ 40。超 500 条启用虚拟滚动。

**筛选栏统一布局**（所有列表页）：
```
[关键词搜索] [装置/单元过滤] [状态过滤] [风险过滤] [排序入口] [批量操作]
```

### 7.2 状态标签 (Status Badge)

统一标签体系，必须是"**颜色 + 文本 + 语义图标**"三维组合，**绝不能仅靠颜色传递信息**。

#### 7.2.1 计算状态标签（`kpi_snapshot_hourly.status`）

| 值 | 颜色 | 文本 | 图标 |
|---|---|---|---|
| `SUCCESS` | `--status-ok` 青绿 | 正常 | ✓ |
| `INCONCLUSIVE` | `--status-neutral` 冷灰 | 数据不足 | ? |
| `PARTIAL` | `--status-warning` 琥珀 | 部分计算 | ⚠ |

#### 7.2.2 处理状态标签（`action_tracker.action_status`）

| 值 | 颜色 | 文本 | 图标 |
|---|---|---|---|
| `PENDING` | `--status-danger` 警示红 | 待处理 | ⏳ |
| `IN_PROGRESS` | `--status-info` 深蓝 | 处理中 | ⟳ |
| `IGNORED` | `--status-neutral` 冷灰 | 已忽略 | — |
| `IMPLEMENTED` | `--status-ok` 青绿 | 已实施 | ✓ |

#### 7.2.3 控制模式标签（from MODE tag）

| 值 | 颜色 | 文本 | 波形表现 |
|---|---|---|---|
| `0` Manual | `--status-danger` 红 | 手动 | 红色背景带 |
| `1` Auto | `--status-ok` 绿 | 自动 | 绿色背景带 |
| `2` Cascade | `--status-info` 蓝 | 串级 | 蓝色背景带 |

#### 7.2.4 PV 数据质量标签（v4.0 新增）

| 值 | 颜色 | 文本 | 图标 |
|---|---|---|---|
| `Good` | `--status-ok` 青绿 | Good | ✓ |
| `Bad` | `--status-danger` 警示红 | Bad | ✖ |
| `Uncertain` | `--status-warning` 琥珀 | Uncertain | ? |

#### 7.2.5 诊断标签（`diagnosis_label`，8 类枚举，v4.1 更新对齐 C6）

| 标签值 | 中文名 | 颜色 | 图标 |
|---|---|---|---|
| `OSCILLATION` | 振荡 | `--status-warning` 琥珀 | 〰 |
| `VALVE_STICTION` | 阀门粘滞 | `--status-danger` 警示红 | ⚠ |
| `OVERAGGRESSIVE` | 参数过激 | `--status-danger` 警示红 | ⚡ |
| `OVERCONSERVATIVE` | 参数过保守 | `--status-info` 深蓝 | 🐢 |
| `EXTERNAL_DISTURBANCE` | 外扰频繁 | `--status-warning` 琥珀 | ↯ |
| `QUALITY_ABNORMAL` | PV 质量异常 | `--status-neutral` 冷灰 | ? |
| `OUTPUT_SATURATION` | 输出饱和 | `--status-warning` 琥珀 | ⛔ |
| `MANUAL_REVIEW` | 人工复核 | `--status-neutral` 冷灰 | ✋ |

*说明：v4.1 对齐《关键算法设计说明》§5 C6 修正，诊断标签从自由文本改为 8 类固定枚举，颜色编码详见 §7.10。*

#### 7.2.6 指标可信度标签（v5.0 新增，`confidence_level`）

对齐 FDS §5.3.10 / 算法说明 §3.7.2，基于有效数据率（valid_rate）自动判定五级，颜色编码详见 §3.1.6：

| 值 | 颜色 | 文本 | 图标 | 触发条件 | UI 处理 |
|---|---|---|---|---|---|
| `A` | `--status-ok` 青绿 | 高可信 | ✓ | valid_rate ≥ 0.95 | 正常展示，无额外提示 |
| `B` | `--status-info` 深蓝 | 较高可信 | ✓ | 0.80 ≤ valid_rate < 0.95 | 正常展示 + 可信度角标 |
| `C` | `--status-warning` 琥珀 | 中等可信 | ⚠ | 0.60 ≤ valid_rate < 0.80 | 显著标注 + tooltip "建议复核" |
| `D` | `--status-danger` 警示红 | 低可信 | ⚠ | 0.20 ≤ valid_rate < 0.60 | 警告提示 + tooltip "数据有效率低，谨慎使用" |
| `E` | `--status-neutral` 冷灰 | 极低可信 | ? | valid_rate < 0.20 | 如实标注"可信度 E"（灰标签）；**仅当 E 级发生在有效自控率 R 上时**才标记 INCONCLUSIVE、评分显示"—"、禁止参与聚合 |

**展示规则**：
- 角标形式：`[评分值] [可信度等级徽章]`，如 `78.5 [B]`
- 有效自控率 R 为 E 级时：评分区域显示 `INCONCLUSIVE —`，评分数值不展示
- 综合评分可信度 = min(A/F/S/R 可信度)，取核心指标 + R 中的最低等级
- 悬浮提示展示 `valid_rate` 具体数值（如 "有效数据率 87.3%"）
- 与计算状态标签（§7.2.1）联动（2026-07-20 按当前代码实现修订）：个别指标（非 R）为 E 级时 status 仍为 SUCCESS（score 有值、参与聚合，灰标签提示）；仅 R 为 E 级/缺失时强制 `status=INCONCLUSIVE`

### 7.3 时序波形 (Time-Series Waveform)

**FDS 强制约束**：诊断详情页与回路运行详情页使用上下分栏，上部为时序波形。

**技术要求**：
- 图表库：ECharts，Canvas/WebGL 渲染
- **降采样**：必须开启 LTTB 降采样，`maxPoints` 默认 2000；超 10 万点强制降采样（`ERR_TS_DOWNSAMPLE_REQ`）
- **多轴联动**：PV、SP 同一 Y 轴（主轴）；OP 独立副 Y 轴（0-100%）
- **MODE 背景带**：按 `mode` 值分段着色（Manual 红/Auto 绿/Cascade 蓝）
- **PV 质量码表达**（v5.0 更新，对齐 KEEP_ALL_WITH_VALIDITY 策略）：
  - `Good`：PV 实线正常显示，`pvValid=true`
  - `Bad`：**PV 灰色虚线保留连线**（v5.0 变更：不再断线），`pvValid=false`，悬浮提示"PV 数据质量不可信"，**绝不使用 0 掩盖**
  - `Uncertain`：PV 琥珀色虚线保留连线，`pvValid=false`，悬浮提示"PV 数据质量不确定"
  - **SP/OP 线正常显示**，不受 PV 质量码影响
- **交互**：鼠标框选放大 (Zoom)、平移 (Pan)、十字准星联动 (Crosshair Sync)
- **时间窗**：最大跨度 30 天，超限提示 `ERR_TS_001`

### 7.4 PV-OP 散点图

诊断详情页下部，展示 PV-OP 二维散点分布与拟合曲线：
- 散点颜色按 PV `quality` 区分（Good 实心 / Bad 半透明 / Uncertain 半透明琥珀）
- 拟合图形：椭圆（粘滞特征）/ 平行四边形（振荡特征）
- 命中规则时高亮拟合区域

### 7.5 树形视图

工厂层级配置页使用。要求：
- 支持 `工厂 → 装置 → 单元` 多级展开/折叠
- 节点图标按 `type` 差异化
- 选中节点高亮，右侧表单联动
- 支持拖拽调整层级（系统管理员）
- 大规模节点（>500）启用虚拟滚动

### 7.6 侧边抽屉 (Drawer)

异常跟踪与低效排行详情使用右侧抽屉。

- 滑出动画：`transform: translateX`，250ms ease-out
- 宽度：Desktop 480px，Tablet 全宽
- 关闭后保留列表滚动位置与筛选条件
- 遮罩层点击关闭

### 7.7 Tag 关联选择器（v4.0 新增）

Tag 关联管理页面使用的核心组件。

**结构**：
- 左侧：AAS 同步 tag 列表（搜索/筛选/数据质量徽章/关联状态）
- 右侧：7 槽位关联区（PV/SP/OP/MODE/PID_P/PID_I/PID_D）

**交互**：
- **拖拽**：从左侧 tag 列表拖拽至右侧槽位，槽位高亮反馈（150ms）
- **下拉选择**：点击槽位下拉框，从 tag 列表选择
- **清除**：点击槽位清除按钮移除关联
- **校验**：必填槽位（PV/SP/OP/MODE）缺失时标红 + tooltip

**视觉**：
- 槽位标签：必填项标注红色 `*`，可选项标注灰色 `(可选)`
- 已关联 tag：等宽字体显示 tag 名 + 数据质量徽章
- 缺失槽位：红色虚线边框 + "未关联"占位文本

### 7.8 配置变更确认弹窗（v4.0 新增）

所有配置页（性能指标/引擎规则/诊断指标/工厂层级/Tag 关联）的保存操作使用统一确认弹窗。

**结构**：
- 标题：确认 [配置名称] 变更
- 变更摘要：列出本次变更的字段与新旧值对比
- 变更说明输入框（必填）：填写变更原因/说明
- 操作按钮：[取消] [确认变更]

**行为**：
- 确认后调用对应 API，记录审计日志（操作人/操作类型/变更内容/变更说明/时间戳）
- 成功后全局 Toast"配置已保存，变更已记录审计日志"

### 7.9 空状态与异常状态

**空状态 (Empty)**：必须说明原因 + 提供动作入口。
- "暂无计算数据，请检查数据接入配置"
- "当前时间窗无低效回路"

**Partial 状态**：明确"缺什么、还能继续做什么、下一步去哪"。
- 批次 Partial：黄色警告横幅，列出 Inconclusive 回路数量与影响

**Inconclusive 状态**（v5.0 更新：对齐可信度 E 级，数据不足是一等公民，不是错误）：
- 灰色标签"数据不足" + 可信度角标"E"
- 波形区灰色虚线保留连线（pvValid=false 的点）
- 评分区显示"—"而非 0（**严禁以 0 分掩盖数据缺失**）
- tooltip"可信度 E 级（有效数据率 < 20%），无法计算"
- INCONCLUSIVE 快照在排行/聚合统计中单独计数展示，不参与装置级评分聚合

**Error 状态**：
- 计算超时/内存溢出：中断批次，记录日志，触发管理员告警
- 越权操作：全局 Toast"无权限"
- 网络异常：标准错误态 + 重试按钮

### 7.10 诊断结果展示组件（v4.1 新增）

诊断详情页（§6.4.3）与诊断列表（§6.4.1）使用的核心组件，对齐 C6（8 类诊断标签）+ IDS v3.2 §2.4.2。

**8 类诊断标签颜色编码**：

| 标签 | 中文名 | 颜色 | 图标 |
|---|---|---|---|
| `OSCILLATION` | 振荡 | `--status-warning` 琥珀 | 〰 |
| `VALVE_STICTION` | 阀门粘滞 | `--status-danger` 警示红 | ⚠ |
| `OVERAGGRESSIVE` | 参数过激 | `--status-danger` 警示红 | ⚡ |
| `OVERCONSERVATIVE` | 参数过保守 | `--status-info` 深蓝 | 🐢 |
| `EXTERNAL_DISTURBANCE` | 外扰频繁 | `--status-warning` 琥珀 | ↯ |
| `QUALITY_ABNORMAL` | PV 质量异常 | `--status-neutral` 冷灰 | ? |
| `OUTPUT_SATURATION` | 输出饱和 | `--status-warning` 琥珀 | ⛔ |
| `MANUAL_REVIEW` | 人工复核 | `--status-neutral` 冷灰 | ✋ |

**诊断结论卡片结构**：
```
+----------------------------------------------------------+
| 融合置信度：82%  ████████████░░░░░░  (fusedConfidence)    |
+----------------------------------------------------------+
| 诊断标签：                                                |
| [⚠ 阀门粘滞 85%]  [〰 振荡 72%]  (diagnosisLabels 数组)   |
+----------------------------------------------------------+
| 证据链：                                                  |
| • 粘滞指数：0.78 (≥0.6 阈值命中)                          |
| • 拟合度：0.92 (椭圆拟合)                                 |
| • PV-OP 散点图：[查看]                                    |
| • 推理：PV-OP 散点图呈现椭圆轨迹，拟合度 0.92             |
+----------------------------------------------------------+
| 算法版本：STICTION_CH_v1.0  |  诊断时间：2026-06-20 08:00 |
+----------------------------------------------------------+
```

**组件字段映射**（对齐 IDS v3.2 §2.4.2）：
- `fusedConfidence`：融合置信度（0-1），醒目展示在卡片顶部，进度条 + 百分比
- `diagnosisLabels[]`：诊断标签数组，每项含：
  - `label`：8 类枚举值，渲染为颜色编码徽章
  - `labelName`：中文名
  - `confidence`：单算法置信度（0-1），进度条 + 数值
  - `evidence`：证据对象（特征值/散点图 URL/推理过程），可展开查看
  - `algorithm`：算法版本号
- `featureValues`：特征值对象（oscillation_amplitude/oscillation_frequency/stiction_index/output_saturation_rate）
- `evidenceChain`：证据链（waveformUrl/scatterPlot/reasoning）
- `algorithmVersion`：算法版本号
- `diagnosedAt`：诊断时间

**交互**：
- 点击诊断标签徽章 → 展开该标签的证据详情（特征值/散点图/推理过程）
- 点击"查看散点图" → 跳转或弹窗显示 PV-OP 散点图
- 融合置信度 < 0.5 时标橙提示"置信度较低，建议人工复核"

### 7.11 整定参数展示组件（v4.1 新增）

整定模块（§6.5）使用的核心组件，对齐 C5（fitting_score）+ IDS v3.2 §2.5.1/§2.7.4。

**模型参数表**（`modelParams`，等宽字体）：

| 模型类型 | 参数字段 | 示例值 |
|---|---|---|
| FOPDT | K / tau / theta | 1.20 / 30.50 / 5.00 |
| SOPDT | K / T1 / T2 / theta | 1.20 / 20.00 / 15.00 / 5.00 |
| IPDT | K / theta | 0.05 / 5.00 |

**拟合度展示**（`fittingScore`，R² 0-1，对齐 C5）：
- 进度条 + 百分比数值
- 评级徽章：≥0.9 优（绿）/ 0.8-0.9 良（蓝）/ 0.6-0.8 中（琥珀）/ <0.6 差（红）
- <0.6 时附加提示"模型拟合度不足，建议更换辨识方法或调整数据段"

**PID 参数对比表**（`pidParams`）：

| 参数 | 当前值 | 推荐值 | 差异 |
|---|---|---|---|
| Kp | 1.20 | 1.50 | +25% ↑ |
| Ti | 50.00 | 33.00 | -34% ↓ |
| Td | 0.00 | 2.27 | +2.27 ↑ |

- 推荐值标绿，差异 > 50% 标橙提示"参数变化较大，请谨慎实施"
- 差异百分比标注（↑ 增加 / ↓ 减少）

**仿真结果对比表**（`simulationResult`）：

| 指标 | 当前 PID | 推荐 PID | 改善 |
|---|---|---|---|
| 上升时间 (s) | 18.5 | 12.5 | -32% ↑ 改善 |
| 超调 (%) | 15.2 | 8.3 | -45% ↑ 改善 |
| Settling time (s) | 65.0 | 45.0 | -31% ↑ 改善 |
| ITAE | 2100.0 | 1250.5 | -40% ↑ 改善 |

- 改善项标绿，恶化项标红，delta 数值标注

**算法版本号**（`algorithmVersion`）：只读展示，如 `IMC_TUNE_v1.0` / `FOPDT_ID_v1.0`

### 7.12 综合评分展示组件（v5.0 更新：3+1+8 结构）

工作台（§6.1.1）/ 全局看板（§6.3.1）/ 低效排行（§6.3.2）使用的核心组件，对齐 v4.0 指标体系（3 核心 + 1 投用）+ 算法说明 §4.10 综合评分。

**3 核心指标雷达图**（v5.0 变更：从 6 维改为 3 维，ECharts）：
- 3 个维度：准确率 A / 快速率 F / 稳定率 S
- 均为正向指标（值越高越好），直接显示实际值
- 支持双图层叠加：当前值（实线）vs 目标值（虚线）
- 悬浮显示具体数值 + 可信度等级

**投用指标独立展示**（v5.0 新增）：
- 有效自控率 R 以独立进度条展示，标注"折扣因子"
- R=100% 时标注"全投用"，R=0% 时标注"未投用（评分=0）"

**评分构成分解**（v5.0 变更：3 核心加权 × R 折扣）：
```
综合评分：78.5  [GOOD]  可信度：B
├── 基础评分 = (A·a + F·f + S·s) / (a+f+s)
│   ├── 准确率 A (a=0.25): 82.1 × 0.25 = 20.53  ████████░░
│   ├── 快速率 F (f=0.20): 75.0 × 0.20 = 15.00  ██████░░░░
│   └── 稳定率 S (s=0.55): 80.0 × 0.55 = 44.00  █████████████████
│   └── 基础评分 = (20.53+15.00+44.00) / 1.00 = 79.53
├── × 折扣因子 R: 98.8% (有效自控率)
└── 综合评分 P = 79.53 × 0.988 = 78.5
```

**可信度展示**（v5.0 新增，2026-07-20 按当前代码实现修订）：
- 综合评分旁显示可信度角标（A/B/C/D/E，对齐 §3.1.6）
- 综合评分可信度 = min(A/F/S/R 可信度)，取核心指标 + R 中的最低等级
- 有效自控率 R 为 E 级/缺失时评分显示"—" + 标签"INCONCLUSIVE"；个别指标（非 R）为 E 级时评分正常显示、附灰色"可信度 E"角标

**辅助诊断指标摘要**（v5.0 新增，折叠区域）：
- 好值率：95.2%（影响可信度）
- 振荡率：12.3%（反向，稳定率修正因子）
- 饱和率：5.4%（反向）
- 粘滞系数：8.2%（诊断辅助）
- 输出值行程指数：0.05（诊断辅助）
- 点击展开查看完整 8 项辅助诊断指标

**状态标签**（`status`，对齐 IDS v3.2）：
| 状态 | 颜色 | 文本 | 图标 | 条件 |
|---|---|---|---|---|
| `GOOD` | `--status-ok` 青绿 | 优良 | ✓ | score ≥ 80 |
| `WARNING` | `--status-warning` 琥珀 | 关注 | ⚠ | 60 ≤ score < 80 |
| `POOR` | `--status-danger` 警示红 | 低效 | ✖ | score < 60 |
| `INCONCLUSIVE` | `--status-neutral` 冷灰 | 数据不足 | ? | 数据不足，显示"—" |

**算法版本号**（`algorithmVersion`）：只读展示，如 `KPI_CALC_v1.0`

### 7.13 算法任务状态组件（v5.0 更新：标准/自定义任务双轨）

整定模块（§6.5）与算法配置界面（§6.7）与任务管理页面（§6.8）使用的核心组件，对齐 IDS v4.0 §2.7 算法服务接口与 FDS §6 任务管理。

**任务状态枚举**（对齐 `app.schemas.task.TaskStatus`）：
| 状态 | 颜色 | 文本 | 图标 |
|---|---|---|---|
| `PENDING` | `--status-neutral` 冷灰 | 排队中 | ⏳ |
| `RUNNING` | `--status-info` 深蓝 | 处理中 | ⟳ |
| `SUCCESS` | `--status-ok` 青绿 | 成功 | ✓ |
| `FAILED` | `--status-danger` 警示红 | 失败 | ✖ |
| `CANCELLED` | `--status-neutral` 冷灰 | 已取消 | ✖ |

**任务类型枚举**（`taskType`，对齐 `app.schemas.task.TaskType`，单一枚举不再拆分 taskCategory）：
- `STANDARD`：标准任务（每小时定时触发，参与装置级聚合，写入 `kpi_snapshot_hourly`）
- `CUSTOM`：自定义任务（用户按需触发，不参与聚合，写入 `kpi_snapshot_custom`）

**进度条组件**：
```
+----------------------------------------------------------+
| 整定计算任务  [标准]                  状态：处理中 ⟳       |
| 任务ID：task-uuid-xxx                                     |
| 进度：██████████░░░░░░░░░░  45%                          |
| 预计剩余：30s    开始时间：2026-06-20 10:00:00            |
+----------------------------------------------------------+
```

**交互规范**：
- **任务提交**：调用 `POST /api/v1/algorithms/{type}` → 返回 202 + `taskId`
- **任务轮询**：前端每 3 秒调用 `GET /api/v1/algorithms/tasks/{task_id}` 获取状态
- **进度更新**：`progress` 字段（0-100）实时更新进度条
- **任务完成**：`status=SUCCESS` 时停止轮询，渲染 `result` 字段（按 `taskType` 不同结构）
- **任务失败**：`status=FAILED` 时停止轮询，显示 `error` 字段（errorCode/message/details）+ 重试按钮
- **任务超时**：轮询超过 15 分钟未完成，提示"任务执行超时，请稍后查看任务历史"
- **任务取消**：支持"取消"按钮（仅 `PROCESSING`/`PENDING` 状态可用），调用取消接口

**结果渲染规则**（按 `taskType`）：
- `KPI_CALCULATION`：渲染 3+1+8 指标体系 + score + status + confidence_level（对齐 §7.12）
- `DIAGNOSIS_ANALYSIS`：渲染诊断标签数组 + fusedConfidence（对齐 §7.10）
- `TUNING_CALCULATION`：渲染 modelParams + fittingScore + pidParams + simulationResult（对齐 §7.11）

**标准任务与自定义任务差异**（v5.0 新增）：
| 维度 | 标准任务 `STANDARD` | 自定义任务 `CUSTOM` |
|---|---|---|
| 触发方式 | 每小时定时自动触发 | 用户手动触发（指定回路/时间窗） |
| 参与聚合 | 是，写入 `kpi_snapshot_hourly` 并参与装置级聚合 | 否，写入 `kpi_snapshot_custom`，独立展示 |
| 取消能力 | 不可取消（系统调度） | 可取消（仅 PENDING/PROCESSING 状态） |
| UI 标识 | 蓝色"标准"徽章 | 灰色"自定义"徽章 |
| 列表入口 | 任务管理页 → 标准任务 Tab | 任务管理页 → 自定义任务 Tab |

**错误处理**：
| 错误码 | UI 响应 |
|---|---|
| `ERR_TASK_NOT_FOUND` | Toast"任务不存在或已过期" + 返回上一页 |
| `ERR_ALGORITHM_DATA_INSUFFICIENT` | 提示"数据点不足（50/100），请扩大时间窗" |
| `ERR_ALGORITHM_TIMEOUT` | 提示"算法执行超时，请重试或联系管理员" |
| `ERR_ALGORITHM_INVALID_PARAMS` | 标红对应参数字段 + tooltip 错误详情 |
| `ERR_FORMULA_INVALID` | 公式编辑器标红 + 错误详情 |
| `ERR_LABEL_INVALID` | 标签字段标红 + 提示"诊断标签不在 8 类枚举内" |
| `ERR_CONTROL_TYPE_INVALID` | 控制类型字段标红 + 提示"控制类型不在 STABLE/SLOW/FAST/LOGIC 枚举内" |

### 7.14 数据血缘展示组件（v5.0 新增）

回路运行详情（§6.2.5）/ 诊断详情（§6.4.3）/ 全局看板（§6.3.1）使用的核心组件，对齐 FDS §5.4 数据血缘与 DDS `kpi_snapshot_hourly` 7 个血缘字段。

**用途**：在指标卡片或波形区域旁展示该次评估结果的数据血缘信息，支持用户下钻查看数据来源与处理策略，满足审计化追溯需求。

**血缘字段**（8 项，对齐 DDS `kpi_snapshot_hourly` 新增字段 + 算法版本）：

| 字段 | 中文名 | 类型 | 说明 |
|---|---|---|---|
| `sampling_freq` | 采样频率 | string | 数据采样频率（如 "1Hz"），对齐统一秒级采样 |
| `quality_policy` | 质量策略 | string | 数据质量策略标识，固定为 `KEEP_ALL_WITH_VALIDITY` |
| `tag_group` | Tag 分组 | string | 数据分组标签（BASE/OP_HF/PVOP_HF/MODE_HF/QUALITY_HF），对齐 DataPlanner tagGroup |
| `valid_rate` | 有效数据率 | float | 有效数据点占比（0.0~1.0），驱动可信度等级判定 |
| `data_block_ids` | DataBlock ID | string[] | 本次计算消费的 DataBlock 缓存 ID 列表 |
| `aggregation_policy` | 聚合策略 | string | 数据聚合策略（如 "1s_original" / "downsample_5s"），对齐按需降采样 |
| `data_policy_version` | 数据策略版本 | string | 数据处理策略版本号（如 "DATA_v4.0"） |
| `algorithm_version` | 算法版本 | string | 指标计算算法版本号（如 "KPI_CALC_v2.0"） |

**组件结构**：
```
+----------------------------------------------------------+
| 📊 数据血缘                              [展开 ▼]          |
+----------------------------------------------------------+
| 采样频率: 1Hz              质量策略: KEEP_ALL_WITH_VALIDITY|
| Tag 分组: PVOP_HF          有效数据率: 0.873 [B 可信度]     |
| 聚合策略: 1s_original      数据策略版本: DATA_v4.0          |
| 算法版本: KPI_CALC_v2.0                                   |
| DataBlock: [db_001, db_002, db_003]  [查看详情]            |
+----------------------------------------------------------+
```

**交互规范**：
- **默认折叠**：组件默认折叠为单行摘要（`采样频率 + 有效数据率 + 可信度等级`），点击展开查看完整 8 字段
- **可信度联动**：`valid_rate` 字段旁显示对应可信度等级徽章（对齐 §3.1.6 / §7.15）
- **DataBlock 下钻**：点击 `data_block_ids` 中的 ID 可弹出抽屉，展示该 DataBlock 的缓存元数据（创建时间、TTL、压缩方式、命中的 tag 列表）
- **版本号对比**：鼠标悬浮版本号字段时 tooltip 展示当前版本与上一版本的差异摘要
- **复制血缘**：组件右上角提供"复制血缘 JSON"按钮，方便审计留痕

**视觉规范**：
- 字段名使用 `--text-muted` + 12px 字号
- 字段值使用 `--font-mono` + 13px 字号（数值/ID 类必须等宽）
- 有效数据率 `valid_rate` 数值按可信度等级着色（A 绿/B 蓝/C 琥珀/D 红/E 灰）
- 折叠/展开动画：高度过渡 200ms

**展示位置**：
- 回路运行详情页：波形组件下方，与 KPI 摘要并列
- 诊断详情页：诊断结论卡片底部，作为证据链一部分
- 全局看板：回路行点击展开抽屉中，紧随评分展示

### 7.15 可信度标识组件（v5.0 新增）

通用角标组件，用于所有展示指标评分/数值的场景，对齐 FDS §5.3.10 / 算法说明 §3.7.2 可信度五级定义。

**用途**：在任何展示评分、KPI 数值、指标结果的卡片/表格/详情旁，附加可信度角标，直观传达数据可信程度。

**角标形态**（3 种）：

| 形态 | 适用场景 | 示例 |
|---|---|---|
| **紧凑角标** | 表格单元格、卡片标题旁 | `78.5 [B]` |
| **徽章形态** | 详情页标题、抽屉头部 | `[可信度: B 较高可信]` |
| **完整卡片** | 评分展示区、血缘组件 | 含 valid_rate 数值 + 等级 + 提示文案 |

**紧凑角标规范**（最常用）：
- 尺寸：与评分数值同行，右间距 `--space-2`
- 背景色：对应等级颜色（对齐 §3.1.6），透明度 15%
- 文字色：对应等级颜色（满色）
- 字号：11px / 600 字重
- 圆角：`--radius-pill`
- 内容：仅显示等级字母（A/B/C/D/E）

**E 级特殊处理**：
- 当可信度为 E 级时，评分数值不展示，替换为 `—`
- 角标文字扩展为 `INCONCLUSIVE`
- 背景色为 `--status-neutral` 透明度 20%
- 悬浮 tooltip："有效数据率过低（valid_rate < 0.20），结果不可信，已标记为 INCONCLUSIVE"

**颜色映射**（对齐 §3.1.6）：

| 等级 | 背景色（15% 透明） | 文字色 | 图标 |
|---|---|---|---|
| `A` | `--status-ok` 15% | `--status-ok` | ✓ |
| `B` | `--status-info` 15% | `--status-info` | ✓ |
| `C` | `--status-warning` 15% | `--status-warning` | ⚠ |
| `D` | `--status-danger` 15% | `--status-danger` | ⚠ |
| `E` | `--status-neutral` 20% | `--status-neutral` | ? |

**交互规范**：
- 鼠标悬浮角标显示 tooltip，内容：`{等级} 可信度 · 有效数据率 {valid_rate} · {说明文案}`
- C/D 级 tooltip 追加建议："建议复核数据源" / "数据有效率低，谨慎使用"
- E 级 tooltip 追加："评分已置空，不参与装置级聚合"
- 点击角标跳转数据血缘组件（§7.14）展开视图

**使用位置**（强制）：
- §6.1.1 工作台 KPI 卡片评分旁
- §6.3.1 全局看板 KPI 摘要列
- §6.3.2 低效排行表格评分列
- §6.4.1 诊断列表诊断结果列
- §7.12 综合评分展示组件
- §7.14 数据血缘组件 `valid_rate` 字段旁

### 7.16 工业桌面端补充组件（v6.1 新增，对齐 ZL 工业设计套件）

为落实 §2.4 工业风格设计四大原则，本节定义 4 个补充组件，作为现有 §7.1-§7.15 组件体系的工业风格强化层。

#### 7.16.1 ClpmNumeric（等宽数字组件）

**用途**：统一承载所有动态刷新的数值、位号、时间戳、PID 参数，强制等宽防抖。

**Props**：

| Prop | 类型 | 默认 | 说明 |
|---|---|---|---|
| `value` | `string \| number` | - | 显示值 |
| `unit` | `string` | - | 单位（kPa / °C / m³/h 等） |
| `delta` | `number` | - | 偏离度（正负百分比，显示为 +X% / -X%） |
| `deltaSeverity` | `'ok' \| 'warning' \| 'danger' \| 'neutral'` | `'neutral'` | 偏离度语义色 |
| `mono` | `boolean` | `true` | 是否等宽字体 |
| `tabularNums` | `boolean` | `true` | 是否启用 tabular-nums 防抖 |

**渲染规则**：
- 主值使用 `--font-mono` + `tabular-nums`，字号 ≥16px
- 单位使用 `--font-sans`，字号 10-12px，颜色 `--text-muted`
- 偏离度按 `deltaSeverity` 着色，背景为低透明度色块（如 `bg-emerald-50`）
- 偏离度绝对值超阈值（默认 5%）时自动升级语义色

**使用位置**（强制）：
- §7.1 高密度表格所有数值列（score / valid_rate / PV / SP / OP / PID / 时间戳）
- §7.3 时序波形数值显示区
- §7.10 诊断结果置信度数值
- §7.11 整定参数表
- §7.12 综合评分展示

**反例**：`<span>78.5</span>` 普通字体 → 违反等宽防抖

#### 7.16.2 ClpmDangerConfirmModal（危险确认模态框）

**用途**：高危操作的物理与逻辑双重屏障（Poka-Yoke），禁止使用浏览器 `alert()` 或简单 `Popconfirm`。

**Props**：

| Prop | 类型 | 默认 | 说明 |
|---|---|---|---|
| `visible` | `boolean` | `false` | 是否显示 |
| `title` | `string` | - | 操作标题（如"危险操作验证"） |
| `severity` | `'danger' \| 'warning'` | `'danger'` | 严重等级，决定 Header 高饱和色 |
| `impactSummary` | `string` | - | 影响范围说明（如"将删除回路 101-FC-1023 及其 7 个 Tag 关联"） |
| `irreversible` | `boolean` | `true` | 是否不可逆 |
| `confirmCode` | `string` | - | 需输入的确认码（如回路位号 / "999" / "CONFIRM"） |
| `confirmCodeHint` | `string` | - | 确认码提示（如"输入回路位号以确认删除"） |
| `requireReason` | `boolean` | `true` | 是否要求填写变更原因 |
| `reasonPlaceholder` | `string` | - | 变更原因 placeholder |
| `rollbackHint` | `string` | - | 回退提示（如"删除后可通过审计日志追溯，但数据不可恢复"） |

**渲染规则**：
- Header 高饱和色（danger=rose / warning=amber），含闪烁 alert-triangle 图标
- Body 包含：
  - 影响范围说明（加粗关键词）
  - 不可逆警告（如"数据不可逆转"）
  - 确认码输入区（居中、等宽、tracking-widest）
  - 变更原因输入框（必填，写入审计日志）
- Footer：
  - "取消操作"按钮（中性色）
  - "执行"按钮（danger=rose / warning=amber），初始禁用，确认码匹配后解禁
- 触控目标 ≥44×44px（§3.7）

**使用位置**（强制替代所有 `Popconfirm` / `Modal.confirm` 用于高危操作）：
- 回路删除（`/loop/manage`）
- Tag 解绑/删除（`/tag/list`）
- 任务取消/重算（`/tasks/list`、`/metric/recompute`）
- 配置保存（`/diagnosis/config`、`/tuning/algorithm`）
- 整定建议确认（`/tuning/*`）

**反例**：`<a-popconfirm title="确认删除？">删除</a-popconfirm>` → 违反 Poka-Yoke

#### 7.16.3 ClpmRealtimeStatus（实时状态条）

**用途**：统一表达数据延迟、自动刷新、接口失败、在线/离线状态，替代分散在页面逻辑中的状态判断。

**Props**：

| Prop | 类型 | 默认 | 说明 |
|---|---|---|---|
| `status` | `'online' \| 'delayed' \| 'offline' \| 'error'` | - | 实时状态 |
| `lastUpdate` | `Date` | - | 最后更新时间 |
| `latencyMs` | `number` | - | 延迟毫秒数 |
| `autoRefresh` | `boolean` | `true` | 是否自动刷新 |
| `refreshIntervalMs` | `number` | `60000` | 刷新间隔（默认 1 分钟） |

**渲染规则**：
- 状态点（pulse 动画）：
  - `online`：emerald 绿点 + pulse
  - `delayed`（latency > 5s）：amber 黄点
  - `offline`：slate 灰点
  - `error`：rose 红点 + pulse
- 文本：`ENV: PRODUCTION` + 最后更新时间（等宽）
- 自动刷新开关（触控目标 44px）

**使用位置**：
- 工作台顶部状态栏（`/dashboard/workbench`）
- 性能看板顶部（`/metric/dashboard`）
- 回路监控列表（`/loop/monitor`）
- AAS 同步状态（`/loop/aas-sync`）

#### 7.16.4 ClpmStatusPanel（状态面板）

**用途**：统一状态卡视觉，替代 AAS 页面混用 Card 与 ClpmDataCanvas 的不一致。

**Props**：

| Prop | 类型 | 默认 | 说明 |
|---|---|---|---|
| `title` | `string` | - | 面板标题 |
| `status` | `string` | - | 状态枚举（如 `SYNCING` / `SUCCESS` / `FAILED`） |
| `metrics` | `Array<{label, value, unit, severity}>` | - | 关键指标列表 |
| `actions` | `Array<{label, icon, onClick}>` | - | 主操作按钮列表 |

**渲染规则**：
- 使用 `1px solid var(--border-default)` 极细边框（§3.5.1）
- 圆角 `--radius-md`（8px）
- Header：标题 + 状态标签（§7.2）
- Body：指标列表（每项使用 ClpmNumeric）
- Footer：主操作按钮（触控目标 44px）

**使用位置**：
- AAS 同步状态卡片（`/loop/aas-sync`）
- 任务详情状态区（`/tasks/:taskId`）
- 整定工作台状态区（`/tuning/workbench`）

---

## 8. 状态机与状态表达规范

### 8.1 页面状态矩阵

所有列表/详情页统一遵循以下状态表达：

| 页面 | Loading | Empty | Error | Success | Partial/Inconclusive |
|---|---|---|---|---|---|
| 性能总览首页 | 正在加载当前 KPI 与待办 | 当前没有待处理回路 | 当前快照无法读取 | 展示低效清单和动作区 | 部分回路 Inconclusive，只展示可判定部分 |
| 工厂层级配置 | 正在加载层级树 | 当前无工厂节点 | 加载失败 | 展示树与表单 | 部分回路缺映射字段，标红 |
| 回路台账 | 正在加载回路列表 | 当前无回路 | 读取失败 | 展示台账表格 | 部分回路 Tag 关联缺失，标红 |
| Tag 关联管理 | 正在加载 tag 列表 | 当前无同步 tag | 读取失败 | 展示 tag 列表与槽位 | 部分槽位缺失，标红 |
| 回路监控列表 | 正在加载回路状态 | 当前无回路 | 读取失败 | 展示回路列表 | 部分回路 PV 质量码 Bad，标红 |
| 回路运行详情 | 正在加载趋势与参数 | 当前无足够数据 | 趋势读取失败 | 展示波形与参数 | PV 质量码 Bad，灰色虚线保留连线 |
| 全局看板 | 正在计算 KPI 聚合 | 当前无可算回路 | 口径未固化或结果失效 | 展示 KPI 摘要 | 仅部分 KPI 可算，顶部 Partial 横幅 |
| 低效排行 | 正在生成排序 | 当前时间窗无低效回路 | 排序失败 | 展示排行和动作 | 仅部分回路可排序 |
| 性能指标配置 | 正在加载指标配置 | 当前无指标配置 | 读取失败 | 展示指标列表与表单 | — |
| 引擎规则配置 | 正在加载规则配置 | 当前无规则配置 | 读取失败 | 展示规则表单 | — |
| 性能统计报表 | 正在加载统计数据 | 当前无统计数据 | 读取失败 | 展示图表与数据表 | 部分数据缺失，标注 |
| 诊断列表 | 正在生成诊断 | 当前无可诊断回路 | 诊断读取失败 | 展示诊断分组与清单 | 部分诊断缺证据，标记待核实 |
| 诊断指标配置 | 正在加载指标配置 | 当前无指标配置 | 读取失败 | 展示指标列表与表单 | — |
| 回路诊断详情 | 正在加载趋势与散点 | 当前无足够证据窗口 | 趋势或事件读取失败 | 展示波形、散点、结论、动作 | PV 质量码 Bad，灰色虚线保留连线 |
| 异常跟踪 | 正在加载追踪记录 | 当前无追踪记录 | 读取失败 | 展示处理轴与状态 | 缺 A/B 对比数据（实施不足 24h） |
| 诊断统计报表 | 正在加载统计数据 | 当前无统计数据 | 读取失败 | 展示图表与数据表 | 部分数据缺失，标注 |
| 整定工作台 | 正在加载待整定回路 | 当前无待整定回路 | 读取失败 | 展示回路列表与概览 | — |
| 模型辨识 | 正在加载配置 | 当前无模型配置 | 读取失败 | 展示参数与结果 | 辨识失败，提示重试 |
| 整定算法 | 正在加载配置 | 当前无算法配置 | 读取失败 | 展示参数与结果 | 计算失败，提示重试 |
| 闭环仿真 | 正在加载配置 | 当前无仿真配置 | 读取失败 | 展示参数与结果 | 仿真失败，提示重试 |
| 整定效果统计 | 正在加载统计数据 | 当前无统计数据 | 读取失败 | 展示图表与数据表 | 部分数据缺失，标注 |
| 用户与角色 | 正在加载用户列表 | 当前无用户 | 读取失败 | 展示用户列表与表单 | — |
| 审计日志 | 正在加载日志 | 当前无日志记录 | 读取失败 | 展示日志列表 | — |
| 自动报表管理 | 正在加载报表列表 | 当前无报表 | 读取失败 | 展示报表记录 | 生成异常，支持重试 |
| 任务管理（v5.0） | 正在加载任务列表 | 当前无任务 | 读取失败 | 展示标准/自定义任务列表 | 部分任务 FAILED，标红支持重试 |
| 安全边界说明 | 正在加载内容 | — | 读取失败 | 展示安全边界说明 | — |

### 8.2 核心状态机

#### 8.2.1 回路就绪状态（v6.0 修订：对齐实现契约 v2.0 标准枚举）
```
READY（就绪，PV/SP/OP/MODE 完整校验通过，参与性能评估与装置级聚合）
PARTIAL（部分配置，Tag 关联字段缺失，标红，不参与性能评估）
INACTIVE（已停用，loop_ledger.is_active=false，不参与任何计算与展示）
```
- 状态枚举与实现契约 v2.0、DDS v6.0 `loop_ledger.status` 字段保持一致
- 历史命名 `ACTIVE/PAUSED/DECOMMISSIONED` 已于 v6.0 废弃，统一视为旧命名

#### 8.2.2 回路评估状态（`kpi_snapshot_hourly.status`，v6.1 更新：可信度联动对齐当前代码实现）
```
正常计算 → SUCCESS（输出评分，标注可信度 A/B/C/D/E 等级；个别指标 E 级不熔断）
无数据（空 Bundle / 取数失败） → INCONCLUSIVE（评分 NULL、不参与聚合）
有效自控率 R 缺失或可信度 E 级 → INCONCLUSIVE（综合评分 None → 评分 NULL、不参与聚合）
0.20 ≤ valid_rate < 0.60（可信度 D 级） → SUCCESS + 警告"数据有效率低，谨慎使用"
必需指标（好值率/自控率/平稳率）任一缺失 → PARTIAL（黄色横幅）
```
- 可信度等级由指标级 `valid_rate` 自动判定（对齐 §3.1.6 / §7.2.6 / §7.15）
- 仅 R 为 E 级/缺失时强制 `status=INCONCLUSIVE`，评分数值留空显示"—"；个别指标（非 R）为 E 级时 status 仍为 SUCCESS 并参与聚合
- 可信度 C/D 级评分正常展示，但附加角标提示

#### 8.2.3 Action Tracker 处理状态（`action_tracker.action_status`，v6.0 修订：状态枚举统一为实现契约 v2.0 标准枚举）
```
PENDING（待处理） → IN_PROGRESS（处理中） → IMPLEMENTED（已实施，触发 A/B 对比采集）
                                                  └→ IGNORED（已忽略，需注明忽略原因）
```
- 每次变更记录 `updated_by` + `updated_at` + comment（处理说明，必填）
- 标记 IMPLEMENTED 后截取 `[T-7天,T]` vs `[T,T+7天]` 窗口
- 状态枚举与 §7.2.2 处理状态标签、§6.4.4 Action Tracker 状态流转保持一致
- 历史命名 `ACTIVE/RESOLVED/SUPPRESSED` 已于 v6.0 废弃，统一视为旧命名

#### 8.2.4 诊断标签（v4.1 更新，8 类枚举对齐 C6）
- `OSCILLATION` 振荡（FFT/自相关检测，振荡周期/频率特征命中）
- `VALVE_STICTION` 阀门粘滞（PV-OP 散点拟合命中椭圆/平行四边形）
- `OVERAGGRESSIVE` 参数过激（PID 增益分析，增益偏差/振荡指数超阈值）
- `OVERCONSERVATIVE` 参数过保守（PID 增益分析，响应时间/settling time 超阈值）
- `EXTERNAL_DISTURBANCE` 外扰频繁（频谱分析，扰动指数/频率集中度超阈值）
- `QUALITY_ABNORMAL` PV 质量异常（质量码统计，Bad/Uncertain 占比超阈值）
- `OUTPUT_SATURATION` 输出饱和（OP 范围统计，饱和时长占比超阈值）
- `MANUAL_REVIEW` 人工复核（综合评分 < 40，算法置信度低，需人工介入）

*说明：v4.1 对齐《关键算法设计说明》§5 C6 修正，诊断标签从自由文本改为 8 类固定枚举。多标签可共存（如同时存在振荡+阀门粘滞），通过 Dempster-Shafer 证据理论融合置信度（`fusedConfidence`）。*

#### 8.2.5 PV 数据质量状态（v5.0 更新：灰色虚线保留连线）
```
PV tag 质量码：
  Good → PV 实线正常显示，pvValid=true，参与 KPI 计算
  Bad → PV 灰色虚线保留连线（v5.0 变更：不再断线），pvValid=false，悬浮提示"PV 数据质量不可信"，不计入好值率
  Uncertain → PV 琥珀色虚线保留连线，pvValid=false，悬浮提示"PV 数据质量不确定"，按权重折算
SP/OP 线不受 PV 质量码影响，始终正常显示
```
- 对齐 KEEP_ALL_WITH_VALIDITY 策略：Bad/Uncertain 数据点保留在时间序列中，不删除
- 各指标通过 Metric Validity Mask 决定有效点（对齐算法说明 §3.5）

#### 8.2.6 整定推荐状态（v6.0 修订：对齐实现契约 v2.0 标准枚举，Phase 2）
```
DRAFT（草稿，新建整定任务） → RUNNING（运行中，辨识/整定/仿真计算中） → COMPLETED（已完成，结果可应用）
                                                                  └→ ROLLED_BACK（已回退，参数未实施或回滚到上一版本）
```
- 状态枚举与实现契约 v2.0、DDS v6.0 `tuning_record.status` 字段保持一致
- 历史命名 `PENDING/IN_PROGRESS/FAILED` 已于 v6.0 废弃，统一视为旧命名

#### 8.2.7 报表生成状态
```
生成中 → 已生成（可下载）
生成异常（截图服务失败）→ 支持手动重试
```

#### 8.2.8 配置变更状态（v4.0 新增）
```
编辑中 → 弹确认弹窗（填写变更说明） → 确认变更 → 记录审计日志 → 生效
```

#### 8.2.9 任务状态（v5.0 新增，对齐 §7.13）
```
PENDING（排队） → PROCESSING（处理中） → SUCCESS（成功，写入快照表）
                                    └→ FAILED（失败，可重试）
PENDING/PROCESSING → CANCELLED（仅自定义任务可取消）
```
- 标准任务（STANDARD）：系统每小时定时触发，不可取消，结果写入 `kpi_snapshot_hourly` 参与聚合
- 自定义任务（CUSTOM）：用户按需触发，可取消，结果写入 `kpi_snapshot_custom` 不参与聚合
- 任务结果均带可信度角标（对齐 §7.15）；有效自控率 R 为 E 级/缺失的结果标注 INCONCLUSIVE，个别指标（非 R）为 E 级的结果正常展示并附灰色"可信度 E"角标

---

## 9. 交互模式规范

### 9.1 全局交互原则

- 任何页面优先服务当前用户要完成的动作，而非展示所有信息。
- `数据不足 / Inconclusive` 是一等状态，不是错误。
- 所有跳转保持当前装置/单元上下文。
- Phase 2 整定模块原型页面允许查看结构与交互演示，算法结果为 mock 数据。

### 9.2 动作反馈

- 任何主级按钮点击必须触发明确的状态流转提示。
- 采用轻量级全局 Toast（fade-in-out，300ms），不阻断操作。
- 异步任务（如 PDF 导出）显示"生成中"状态，轮询结果，不阻塞 UI。

### 9.3 上下文保持

- 列表页点击行 → 右侧抽屉，关闭后保留列表滚动位置与筛选条件。
- 跨页跳转携带 `loopId`、`plantNodeId`、`timeWindow` 等上下文参数。

### 9.4 筛选/排序/分页

| 控件 | 默认值 | 说明 |
|---|---|---|
| 排行榜 `limit` | 10 | 可选 10/20/50 |
| 排行榜 `timeWindow` | `today` | 枚举 today/yesterday/last_7_days |
| 工作台统计粒度 | `day` | 枚举 day/week/month |
| 工作台趋势粒度 | `day` | 枚举 day/hour |
| 波形 `downsample` | `true` | LTTB 算法 |
| 波形 `maxPoints` | `2000` | 前端最大点数 |
| 波形时间窗上限 | 30 天 | 超限 `ERR_TS_001` |
| 表格排序 | `score` 升序 | 低效优先 |
| 表格分页 | 默认 10/页 | 超 500 条虚拟滚动 |

### 9.5 Tag 关联拖拽交互（v4.0 新增）

| 触发 | 行为 | 结果 |
|---|---|---|
| 从左侧 tag 列表拖拽 | 拖拽时显示拖拽预览 | 槽位高亮提示可放置 |
| 拖拽至槽位上方 | 槽位高亮反馈（150ms） | 提示可放置 |
| 释放至槽位 | tag 关联至槽位 | 槽位填充 tag 名 + 质量徽章 |
| 点击槽位下拉 | 展开 tag 列表 | 可搜索选择 |
| 点击槽位清除按钮 | 移除关联 | 槽位空缺，必填项标红 |
| 重复关联同一 tag | 提示"该 tag 已关联至其他回路，确认重复关联？" | 确认后允许多关联 |

### 9.6 配置变更确认交互（v4.0 新增）

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击"保存"按钮 | 弹出确认弹窗 | 显示变更摘要 + 变更说明输入框 |
| 填写变更说明后确认 | 调用 API | 记录审计日志，Toast"配置已保存" |
| 未填写变更说明 | 确认按钮置灰 | 提示"请填写变更说明" |
| 点击"取消" | 关闭弹窗 | 不保存变更 |

### 9.7 关键页面交互明细

#### 性能总览首页 `/`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换装置/单元筛选 | 重载 KPI 与低效列表 | 上下文保持 |
| 切换日/周/月粒度 | 重算 KPI 聚合 | 卡片数值更新 |
| 点击低效回路行 | 选中当前回路 | 中行右摘要联动刷新 |
| 点击"进入诊断" | 跳转 `/diagnosis/:loopId` | 带 `loopId` |
| 点击"进入异常跟踪" | 跳转 `/diagnosis/tracker` | 带当前回路上下文 |
| 点击 KPI 卡片 | 展开详细说明 | 展示趋势、说明和相关低性能回路 |
| 切换下行趋势粒度 | 日/小时切换 | 重绘趋势图 |

#### 回路台账 `/loop/ledger`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击"新建回路" | 弹出新建弹窗 | 填写基础信息后创建 |
| 点击行"编辑" | 右侧抽屉滑出 | 展示基础信息/Tag 关联/运行态/扩展配置 |
| 点击"查看 Tag 关联" | 跳转 `/loop/mapping?loopId=xxx` | 带回路上下文 |
| 点击"查看监控" | 跳转 `/loop/monitor/:loopId` | 带回路上下文 |
| 切换启用状态开关 | 弹确认弹窗 | 填写变更说明后保存 |

#### Tag 关联管理 `/loop/mapping`

| 触发 | 行为 | 结果 |
|---|---|---|
| 选择回路 | 右侧槽位加载该回路当前关联 | 7 槽位填充 |
| 拖拽 tag 至槽位 | 槽位高亮反馈 | tag 关联至槽位 |
| 下拉选择 tag | 槽位填充 | tag 关联至槽位 |
| 点击"校验" | 校验完整性 | 显示完整/缺失状态 |
| 点击"保存" | 弹确认弹窗 | 填写变更说明后保存，记录审计日志 |

#### 回路诊断详情 `/diagnosis/:loopId`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换时间窗 | 刷新波形和散点 | 显示当前窗口变化 |
| 悬浮 PV 灰色虚线区域 | 显示 tooltip | "PV 数据质量不可信" |
| 点击规则命中 | 展开解释 | 显示规则、阈值和影响 |
| 点击"导出建议单" | 触发异步导出 | Toast"生成中" |
| 点击"进入异常跟踪" | 跳转 `/diagnosis/tracker` | 带当前回路 |
| 框选波形区域 | 放大该区域 | 联动散点图高亮 |

#### 异常跟踪 `/diagnosis/tracker`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某追踪记录 | 右侧抽屉展示处理轴 | 显示时间线、状态、操作人 |
| 切换处理状态下拉 | 弹出确认抽屉 | 填写 comment 后确认 |
| 确认状态变更 | 调用 PATCH 接口 | Toast"状态已更新"，列表变色 |
| 点击"导出 PDF" | 触发异步导出 | 显示"生成中"，轮询结果 |
| 标记 IMPLEMENTED 后 | 自动展示 A/B 对比 | Before/After 柱状图 |

### 9.8 高危操作屏障规范（v6.1 新增）

依据 ZL 致联工业软件 UI/UX 设计套件《Poka-Yoke 防呆设计》原则，CLPM 平台所有高危操作必须采用**物理屏障 + 逻辑屏障**双重防御，禁止使用浏览器默认 `alert()`、`window.confirm()` 或 Ant Design `Popconfirm`、`Modal.confirm` 承载。

#### 9.8.1 操作风险分级

| 风险等级 | 定义 | 典型场景 | 屏障强度 |
|---|---|---|---|
| **高（DANGER）** | 不可逆、影响历史数据或装置级运行 | 删除回路、取消评估任务、历史重算、批量解绑 Tag、用户禁用、参数配置回滚 | 强制 `ClpmDangerConfirmModal` + typed confirmation + 审计备注 |
| **中（WARNING）** | 可逆但影响范围广、需留痕 | 配置保存（指标/权重/引擎规则）、整定建议确认、状态机变更（如 IMPLEMENTED）、批量启用/停用 | `ClpmDangerConfirmModal` 简化模式 + 变更摘要 + 备注 |
| **低（INFO）** | 可逆、单对象、无副作用 | 单条编辑保存、视图筛选、列设置、导出 PDF | 普通 `Modal` + 变更说明（可选） |

#### 9.8.2 物理屏障（触控与可达性）

1. **触控目标**：危险按钮自身满足 §3.7 桌面密度 32×32px，触控模式自动切换至 44×44px。
2. **位置隔离**：危险按钮与常规操作按钮分组放置，禁止紧邻主操作（如"保存"与"删除"不可相邻）。
3. **色相警示**：DANGER 级按钮使用 `--rose-600` 实心填充；WARNING 级使用 `--amber-500` 描边；不可仅靠颜色区分，必须叠加图标与文本。
4. **模态遮罩**：`ClpmDangerConfirmModal` 触发后，背景遮罩 `rgba(15, 23, 42, 0.6)` + `blur(2px)`，禁止背景交互。
5. **二次点击距离**：危险按钮触发后弹出模态框，确认按钮在模态框右下角，与触发按钮距离 ≥ 200px，避免连续误点。

#### 9.8.3 逻辑屏障（信息与确认）

`ClpmDangerConfirmModal`（详见 §7.16.2）必须按以下顺序呈现信息：

1. **操作摘要**：动词 + 对象（如"删除回路 `41LIC201117-PIDA`"），动词使用红色加粗。
2. **影响范围**：列出受影响的对象数量与下游影响（如"将级联解绑 7 个 Tag、影响 22 条历史快照、不可恢复"）。
3. **回退提示**：明确说明是否可回退、回退方式、回退代价。
4. **变更原因**：必填文本域（min 10 字符），如"装置停工检修，回路永久退役"。
5. **typed confirmation 码**：在输入框提示"请输入回路 tag/任务 ID/确认码以确认"，用户必须精确输入目标对象的标识符方可激活确认按钮。
6. **审计备注**：可选文本域，记录操作上下文（如工单号、审批单号）。
7. **二次确认按钮**：默认 disabled，仅在 typed confirmation 码匹配 + 变更原因填写后激活；按钮文案使用完整动词（如"确认删除"，不可使用"确定"或"OK"）。

#### 9.8.4 强制使用场景

| 场景 | 当前实现（不符合） | v6.1 强制改造为 |
|---|---|---|
| 删除回路 / 删除 Tag / 解绑 Tag | `Popconfirm` | `ClpmDangerConfirmModal`（输入 loop_tag 或 tag 位号） |
| 取消评估任务 / 重跑任务 / 历史重算 | `Popconfirm` 或 `Modal.confirm` | `ClpmDangerConfirmModal`（输入 task_id 短码，显示影响回路数与时间窗口） |
| 配置保存（指标/权重/引擎规则） | `Modal.confirm` 或直接保存 | `ClpmDangerConfirmModal` 简化模式：变更摘要 + 旧值/新值 diff + 影响范围 + 备注 |
| 状态机变更（如标记 IMPLEMENTED） | `Modal.confirm` 或下拉直接切换 | `ClpmDangerConfirmModal` 简化模式：状态流转摘要 + 备注 |
| 整定参数建议确认 | `Modal.confirm` | `ClpmDangerConfirmModal`：建议值 + 证据 + 风险 + 回退方案 + "需授权人员人工实施"标识 |
| 用户禁用 / 角色调整 / 报表删除 | `Popconfirm` | `ClpmDangerConfirmModal`（输入用户名或报表 ID） |

#### 9.8.5 整定安全边界（红线）

依据 CLPM 安全边界原则（§1.2），整定模块全链路禁止出现任何"下发参数""写入 DCS""自动实施"语义入口：

1. 所有参数建议卡片必须显示固定标识：`只读建议 · 人工实施 · 需留痕`。
2. 禁止出现"下发""写入""应用""执行"等动词按钮，统一改为"导出建议单""生成回退方案""标记已人工实施"。
3. PID 参数区在 UI 上明确标示"经济区间 / 警告区间 / 过载区间"三段，参数调节滑块仅在建议值范围内可拖动。
4. 整定结果展示组件（§7.11）顶部固定横幅：`本平台不直接修改 DCS 的 P/I/D 参数，参数由授权人员人工实施并留痕`。

#### 9.8.6 审计字段留痕

所有高危操作确认后，必须写入审计日志（`audit_log` 表），字段包括：

| 字段 | 来源 | 示例 |
|---|---|---|
| `operator` | 当前登录用户 | `admin` |
| `action` | 操作类型枚举 | `LOOP_DELETE` / `TASK_CANCEL` / `CONFIG_SAVE` / `TUNING_CONFIRM` |
| `target_type` | 对象类型 | `loop` / `task` / `metric_config` / `tuning_recommendation` |
| `target_id` | 对象标识 | `41LIC201117-PIDA` 或 `task_id` 短码 |
| `reason` | 用户填写的变更原因 | `装置停工检修` |
| `change_summary` | 自动生成的变更摘要 JSON | `{"before": {...}, "after": {...}, "affected": 22}` |
| `audit_note` | 用户填写的审计备注 | `工单 WO-2026-07-06-001` |
| `performed_at` | 操作时间戳（UTC+8） | `2026-07-06 14:23:11+08:00` |

---

## 10. 数据可视化规范

### 10.1 评分颜色映射

综合评分 `score`（0-100）动态映射，阈值由算法配置决定：

| 区间（默认） | 颜色 | 表现 | status 枚举（v4.1） |
|---|---|---|---|
| `>= 80` | 青绿 `--status-ok` | 优良 | `GOOD` |
| `60-79` | 琥珀 `--status-warning` | 关注 | `WARNING` |
| `< 60` | 警示红 `--status-danger` | 低效 | `POOR` |
| 数据不足 | 冷灰 `--status-neutral` | 显示"—"非 0 | `INCONCLUSIVE` |

*说明：v4.1 对齐 IDS v3.2，评分状态从仅 `SUCCESS/INCONCLUSIVE/PARTIAL` 扩展为 `GOOD/WARNING/POOR/INCONCLUSIVE` 四态，对齐§7.12 综合评分展示组件。*

### 10.2 趋势折线图

- 全厂稳定率/自控投用率趋势，ECharts 实现
- 支持时间窗切换
- 数据量大时降采样
- 十字准星联动
- 工作台首页组合趋势：自控投用率（蓝线）+ 稳定率（绿线）双轴

### 10.3 A/B 对比柱状图

- Before/After 双柱对比
- 提升标绿、下降标红
- 标注 delta 数值

### 10.4 散点图

- PV-OP 二维分布
- 散点颜色按 PV `quality` 区分（Good 实心 / Bad 半透明 / Uncertain 半透明琥珀）
- 拟合曲线叠加（椭圆/平行四边形）
- 命中规则高亮

### 10.5 PV 质量码在波形图中的处理（v4.0 关键）

**核心规则**：数据质量主要针对 PV 值，PV tag 携带质量码（Good/Bad/Uncertain）。

| 质量码 | PV 线表现 | SP/OP 线表现 | 悬浮提示 |
|---|---|---|---|
| `Good` | 实线正常显示（青绿或主色） | 正常显示 | 无 |
| `Bad` | 灰色虚线保留连线 | 正常显示 | "PV 数据质量不可信" |
| `Uncertain` | 琥珀色虚线保留连线 | 正常显示 | "PV 数据质量不确定" |

**实现要点**：
- PV 线根据质量码分段渲染：Good 段实线、Bad 段灰色虚线保留连线、Uncertain 段琥珀虚线保留连线
- SP/OP 线始终正常显示，不受 PV 质量码影响
- 质量码图例固定显示在波形区右上角
- KPI 好值率基于 PV 质量码统计（Good 时长 / 总时长）

### 10.6 统计图表（v4.0 分散到各模块）

| 图表 | 所属模块 | 用途 |
|---|---|---|
| KPI 趋势对比折线图 | 性能统计报表 | 多 KPI 多时间窗趋势对比 |
| 装置评分排名柱状图 | 性能统计报表 | 各装置综合评分横向柱状图 |
| 差等生分布饼图 | 性能统计报表 | 低效回路按预诊标签分布 |
| 预诊标签分布饼图 | 诊断统计报表 | 各预诊标签的回路数占比 |
| 处理效率趋势折线 | 诊断统计报表 | 各处理状态随时间的数量变化 |
| 闭环时长分布柱状图 | 诊断统计报表 | PENDING 到 IMPLEMENTED 的时长分布 |
| 整定前后 KPI 对比柱状图 | 整定效果统计 | 整定前后 KPI 对比 |
| 整定效果趋势折线 | 整定效果统计 | 整定前后时间窗 KPI 变化 |

---

## 11. 响应式与无障碍

### 11.1 响应式断点

| 视口 | 断点 | 规则 |
|---|---|---|
| Desktop | `>= 1440px` | 完整三栏/双栏工作台，高密度多栏 |
| Tablet/HMI | `1024px - 1439px` | 保留两栏，次级详情抽屉化，侧边栏可折叠 |
| Mobile | `< 768px` | 步骤式/抽屉式，仅关键数据查看，不建议波形诊断 |

### 11.2 页面级响应式规则

| 页面 | Desktop | Tablet | Mobile |
|---|---|---|---|
| 性能总览首页 | 上中下三行工作台 | 上行 KPI + 中行列表 + 下行趋势 | 卡片顺序化，保留主次 |
| 工厂层级配置 | 左树 + 右表单 | 树在上，表单在下 | 树折叠，表单全宽 |
| 回路台账 | 大表格 + 抽屉 | 表格优先，抽屉全宽 | 列表 + 全宽抽屉 |
| Tag 关联管理 | 左 tag 列表 + 右槽位 | 上下分栏 | 步骤式：先选回路，再关联 |
| 回路监控列表 | 大表格 + 抽屉 | 表格优先 | 列表化摘要 |
| 回路运行详情 | 上下分栏 | 趋势在上，参数在下 | 趋势摘要 + 可展开参数区 |
| 全局看板 | KPI 卡片 + 趋势 + 表格 | 表格优先，趋势折叠 | 摘要优先 |
| 低效排行 | 大表格 + 侧栏 | 表格优先，侧栏抽屉化 | 列表化摘要 |
| 性能指标配置 | 左列表 + 右表单 | 上下分栏 | 步骤式 |
| 诊断详情 | 趋势 + 散点 + 动作侧栏 | 趋势在上，散点/动作在下 | 趋势摘要 + 可展开证据区 |
| 异常跟踪 | 表格 + 抽屉 | 表格优先，抽屉全宽 | 列表 + 全宽抽屉 |
| 整定工作台 | 三栏工作台 | 左列表 + 右概览 | 步骤式 |
| 闭环仿真 | 上下分栏 | 参数在上，结果在下 | 步骤式 |

### 11.3 无障碍

| 领域 | 规则 |
|---|---|
| 键盘 | 状态变更、导出、Tag 关联拖拽等操作必须可 Tab 完整操作（拖拽需提供下拉选择替代方案） |
| 触达 | 可点击元素最小 44px |
| 色彩 | 状态标签必须颜色+文本+图标，绝不仅靠颜色 |
| 对比度 | 满足 WCAG AA 标准 |
| 等宽 | 数字、回路位号、tag 名、ID 等宽字体确保对齐 |

### 11.4 浏览器兼容

Chrome (v90+), Edge (v90+), Safari (v14+)。

---

## 12. 原型实现指引

### 12.1 当前正式前端技术栈

- **框架**：Vue 3 + Vite + TypeScript + vue-vben-admin
- **组件**：Ant Design Vue + Vben 组件体系
- **路由**：Vue Router，真实路由以 `frontend/apps/web-antd/src/router/routes/modules/` 为准
- **图表**：ECharts（LTTB 降采样、Canvas/WebGL）
- **图标**：Lucide / Iconify 语义图标
- **状态**：Pinia / Vben stores 承载登录、权限、偏好与业务状态
- **数据**：正式前端通过 `/api/v1/*` 接入后端；原型 mock 仅作历史参考

### 12.2 当前路由配置

当前路由、页面清单与旧路径兼容策略以 `docs/设计文档/00-BASELINE/implementation-contract.md` §2-§3 为准。旧 v4.0 25 页面 React 路由示例不再作为验收依据。

### 12.3 huashu-design 技能对接

后续原型开发使用 `huashu-design` 技能，遵循其设计哲学：

| huashu-design 原则 | CLPM 应用 |
|---|---|
| 反 AI Slop | §2.2 禁忌清单已对齐：禁渐变/Emoji/卡片瀑布/SVG 画物 |
| 高密度信息分型 | CLPM 属高密度型，每屏 ≥3 处差异化信息 |
| 资产 > 规范 | 工业图标用 Lucide 语义库，不手画 SVG |
| Junior Designer 模式 | 先写 assumptions + placeholder，尽早 show |
| 诚实 placeholder | 缺数据留灰色方块+标签，不画烂 SVG |
| 系统优先不填充 | 每元素必须 earn its place，不加 data slop |

### 12.4 原型角色切换模拟

第一阶段不做真实鉴权，用顶部角色切换器模拟：
- 切换影响菜单可见项、默认首页、主 CTA、操作权限
- 不刷新页面数据，但重新计算可见区域和禁用态
- 角色集：仪控工程师、工艺/设备工程师、Sponsor、系统管理员、外部专家
- 角色默认首页：仪控工程师 `/`、工艺/设备工程师 `/performance`、Sponsor `/performance`、系统管理员 `/loop/factory`、外部专家 `/diagnosis`

### 12.5 原型验收标准（Phase 1）

- 25 个页面可运行（含回路整定模块 5 个原型页面）
- 角色切换器驱动菜单与页面可见态
- 至少 1 条闭环链路可跑通（工作台→低效排行→诊断详情→异常跟踪标记已实施→A/B 对比）
- Tag 关联管理支持拖拽与下拉两种交互
- 配置页保存弹确认弹窗，填写变更说明
- 波形图正确处理 PV 质量码（Good 实线/Bad 灰色虚线保留连线/Uncertain 琥珀虚线保留连线）
- 所有主按钮有真实结果
- 所有列表页支持筛选、排序
- `npm run dev` 与 `npm run build` 通过

---

## 13. 附录

### 13.1 数据字段与 UI 映射速查

| 数据字段 | 来源表 | UI 展示位置 | 展示形式 |
|---|---|---|---|
| `loop_tag` | `loop_ledger` | 全局表格主列 | 等宽字体，可点击 |
| `description` | `loop_ledger` | 台账表格 | 普通文本 |
| `score` | `kpi_snapshot_hourly` | 排行/看板/诊断 | 评分色块 + 数值 |
| `good_value_rate` | `kpi_snapshot_hourly` | 看板 KPI 卡/雷达图 | 百分比 |
| `auto_mode_rate` | `kpi_snapshot_hourly` | 看板 KPI 卡/雷达图 | 百分比 |
| `steady_rate` | `kpi_snapshot_hourly` | 看板 KPI 卡/排行/雷达图 | 百分比 |
| `accuracy_rate` | `kpi_snapshot_hourly`（v4.1 新增，对齐 C1/C2） | 看板 KPI 卡/雷达图 | 百分比 |
| `oscillation_rate` | `kpi_snapshot_hourly` | 诊断详情/雷达图 | 百分比（反向指标） |
| `saturation_rate` | `kpi_snapshot_hourly`（v4.1 新增，对齐 C1/C2） | 诊断详情/雷达图 | 百分比（反向指标） |
| `status` | `kpi_snapshot_hourly` | 全局状态标签 | GOOD/WARNING/POOR/INCONCLUSIVE 四维标签（v4.1 更新） |
| `algorithm_version` | `kpi_snapshot_hourly`（v4.1 新增） | 看板/配置页 | 只读等宽字体（如 `KPI_CALC_v1.0`） |
| `diagnosis_label` | `diagnosis_result`（v4.1：8 类枚举） | 诊断列表/详情 | 8 类颜色编码徽章（对齐 §7.10） |
| `confidence` | `diagnosis_result`（v4.1 新增） | 诊断详情 | 单算法置信度进度条 + 数值（0-1） |
| `fused_confidence` | `diagnosis_result`（v4.1 新增，对齐 C6） | 诊断详情卡片顶部 | 融合置信度进度条 + 百分比 |
| `evidence` | `diagnosis_result`（v4.1 新增） | 诊断详情证据链 | 可展开证据对象（特征值/散点图 URL/推理过程） |
| `algorithm` | `diagnosis_result`（v4.1 新增） | 诊断详情 | 算法版本号只读标签 |
| `action_status` | `action_tracker` | Tracker/排行 | PENDING/IN_PROGRESS/IGNORED/IMPLEMENTED 标签 |
| `updated_by` | `action_tracker` | Tracker 时间线 | 操作人 |
| `updated_at` | `action_tracker` | Tracker 时间线 | 时间戳（等宽） |
| `evidence_url` | `action_tracker` | Tracker 下载入口 | PDF 下载按钮 |
| `pv/sp/op` | `st_loop_data` | 诊断波形/监控列表 | 三曲线（PV/SP 主轴，OP 副轴） |
| `mode` | `st_loop_data`（from MODE tag） | 诊断波形/台账/监控 | 背景带（Manual红/Auto绿/Cascade蓝）+ 标签 |
| `pv_quality` | `st_loop_data`（PV tag 质量码） | 诊断波形/监控列表 | Good 实线/Bad 灰色虚线保留连线/Uncertain 琥珀虚线保留连线 |
| `pid_p/pid_i/pid_d` | `st_loop_data`（from PID tag） | 台账/运行详情 | 只读等宽字体 |
| `is_active` | `loop_ledger` | 台账表格 | 启用开关 |
| `importance_level` | `loop_ledger` | 台账表格 | 可编辑数值 |
| `plant_node.type` | `plant_node` | 模型树 | 图标差异化（FACTORY/UNIT/EQUIPMENT） |
| `tag_name` | `tag_registry` | Tag 关联管理左侧列表 | 等宽字体 |
| `quality` | `tag_registry` | Tag 关联管理左侧列表 | Good/Bad/Uncertain 徽章 |
| `last_sync_at` | `tag_registry` | 台账/Tag 关联 | 时间戳（等宽） |
| `model_params` | `tuning_record`（v4.1 新增） | 整定结果展示 | 等宽字体参数表（K/tau/theta 等） |
| `pid_params` | `tuning_record`（v4.1 新增） | 整定结果展示 | P/I/D 三组对比表 |
| `fitting_score` | `tuning_record`（v4.1 新增，对齐 C5） | 整定结果展示 | R² 拟合度进度条 + 评级徽章 |
| `simulation_result` | `tuning_record`（v4.1 新增） | 闭环仿真结果 | 性能指标对比表 + 双波形图 |
| `method` | `tuning_record`（v4.1 新增） | 整定算法配置 | 单选按钮组（IMC/LAMBDA/ZN/CC/SIMC） |
| `calc_method` | `diagnosis_config`（v4.1 新增，对齐 C4） | 诊断指标配置 | 下拉选择 |
| `control_type` | `loop_ledger`（v5.3 从 `metric_config` 迁移） | 回路台账表格/编辑抽屉 | 单选按钮组（STABLE/SLOW/FAST/LOGIC），决定权重模板 |
| `include_in_evaluation` | `loop_ledger`（v5.3 新增） | 回路台账表格/编辑抽屉 | 开关控件，控制是否参评 |
| `grading_thresholds` | `metric_config`（v5.3 新增） | 权重配置管理页 | 5 级阈值双滑块组件（EXCELLENT/GOOD/FAIR/WARNING/POOR） |
| `threshold` | `metric_config`/`diagnosis_config`（v4.1：JSONB，对齐 C3） | 配置页 | 双滑块/动态表单字段 |

### 13.2 API 接口与 UI 对接速查

| 接口 | 方法 | 路径 | UI 触发点 | 权限 |
|---|---|---|---|---|
| 工厂层级树 | GET | `/api/v1/plant-nodes` | 工厂层级配置加载 | 系统管理员/仪控工程师 |
| 创建工厂节点 | POST | `/api/v1/plant-nodes` | 工厂层级配置新增 | 系统管理员 |
| 更新工厂节点 | PATCH | `/api/v1/plant-nodes/{id}` | 工厂层级配置编辑 | 系统管理员 |
| 回路台账列表 | GET | `/api/v1/loops` | 回路台账加载 | 系统管理员/仪控工程师 |
| 创建回路 | POST | `/api/v1/loops` | 回路台账新建 | 系统管理员/仪控工程师 |
| 更新回路 | PUT | `/api/v1/loops/{loopId}` | 回路台账编辑 | 系统管理员/仪控工程师 |
| AAS Tag 列表 | GET | `/api/v1/aas/tags` | Tag 关联管理加载 | 系统管理员/仪控工程师 |
| Tag 关联保存 | PUT | `/api/v1/loops/{loopId}/tags` | Tag 关联管理保存 | 系统管理员/仪控工程师 |
| 回路监控列表 | GET | `/api/v1/loops/monitor` | 回路监控列表加载 | 全角色 |
| 回路运行详情 | GET | `/api/v1/loops/{loopId}/monitor` | 回路运行详情加载 | 全角色 |
| KPI 快照 | GET | `/api/v1/dashboard/overview` | 工作台/全局看板加载 | 全角色 |
| 低效排行 | GET | `/api/v1/performance/ranking` | 排行页加载 | 查看层+ |
| 性能指标配置（单条） | GET/PUT | `/api/v1/performance/metrics` / `/{metricId}` | 性能指标配置页 | 系统管理员 |
| 性能指标配置（批量，v4.1） | GET/PUT | `/api/v1/configs/metrics` | 性能指标配置页批量保存 | 系统管理员 |
| 引擎规则配置 | GET/PUT | `/api/v1/performance/rules` | 引擎规则配置页 | 系统管理员 |
| 性能统计报表 | GET | `/api/v1/performance/analytics` | 性能统计报表页 | 全角色 |
| 诊断列表 | GET | `/api/v1/diagnosis/list` | 诊断列表加载 | 执行层+ |
| 诊断指标配置（单条） | GET/PUT | `/api/v1/diagnosis/metrics` / `/{diagId}` | 诊断指标配置页 | 系统管理员 |
| 诊断指标配置（批量，v4.1） | GET/PUT | `/api/v1/configs/diagnosis` | 诊断指标配置页批量保存 | 系统管理员 |
| 波形数据 | GET | `/api/v1/timeseries/{loopId}/waveform` | 诊断详情/运行详情加载 | 执行层+ |
| 更新状态 | PATCH | `/api/v1/tracker/{loopId}/status` | Tracker 状态变更 | 仅仪控工程师 |
| 导出建议书 | POST | `/api/v1/tracker/{loopId}/export` | 导出按钮 | 执行层+ |
| 诊断统计报表 | GET | `/api/v1/diagnosis/analytics` | 诊断统计报表页 | 执行层+ |
| 触发 KPI 计算（v4.1） | POST | `/api/v1/algorithms/kpi/calculate` | 算法配置页/手动重算 | 系统管理员 |
| 触发诊断分析（v4.1） | POST | `/api/v1/algorithms/diagnosis/analyze` | 算法配置页/手动重算 | 系统管理员 |
| 触发整定计算（v4.1） | POST | `/api/v1/algorithms/tuning/calculate` | 整定模块各页面 | 执行层+ |
| 查询算法任务状态（v4.1） | GET | `/api/v1/algorithms/tasks/{task_id}` | 算法任务进度轮询 | 触发者角色+ |
| 用户列表 | GET | `/api/v1/system/users` | 用户与角色页 | 系统管理员 |
| 审计日志 | GET | `/api/v1/system/audit` | 审计日志页 | 系统管理员 |
| 报表记录 | GET | `/api/v1/system/reports` | 自动报表管理页 | 系统管理员 |

### 13.3 错误码与 UI 响应

| 错误码 | 含义 | UI 响应 |
|---|---|---|
| `ERR_AUTH_401` | Token 失效 | 跳转登录 |
| `ERR_AUTH_403` | 越权 | Toast"无权限执行此操作" |
| `ERR_DATA_INCONCLUSIVE` | 数据不足 | 灰色 Inconclusive 标签，不显示 0 分 |
| `ERR_TS_DOWNSAMPLE_REQ` | 超量需降采样 | 自动开启降采样重试 |
| `ERR_TS_001` | 时间窗超 30 天 | 提示"请缩小查询时间范围" |
| `ERR_TAG_NOT_FOUND` | Tag 未找到 | Toast"tagId 在 tag_registry 中不存在，请先触发 AAS 同步" |
| `ERR_LOOP_TAG_REQUIRED` | 回路必填 tag 缺失 | 回路标红 + tooltip 提示"PV/SP/OP/MODE 必填 tag 缺失" |
| `ERR_METRIC_WEIGHT_SUM` | 指标权重总和不为 100% | 弹窗提示"权重总和须为 100%，当前为 X%" |
| `ERR_CONFIG_FORBIDDEN` | 越权修改配置 | Toast"仅系统管理员可修改配置" |
| `ERR_TASK_NOT_FOUND`（v4.1） | 算法任务未找到 | Toast"任务不存在或已过期" + 返回上一页 |
| `ERR_ALGORITHM_DATA_INSUFFICIENT`（v4.1） | 算法数据不足 | 提示"数据点不足（50/100），请扩大时间窗" |
| `ERR_ALGORITHM_TIMEOUT`（v4.1） | 算法执行超时 | 提示"算法执行超时，请重试或联系管理员" |
| `ERR_ALGORITHM_INVALID_PARAMS`（v4.1） | 算法参数无效 | 标红对应参数字段 + tooltip 错误详情 |
| `ERR_FORMULA_INVALID`（v4.1） | 表达式语法错误 | 公式编辑器标红 + 错误详情 |
| `ERR_LABEL_INVALID`（v4.1） | 诊断标签枚举无效 | 标签字段标红 + 提示"诊断标签不在 8 类枚举内" |
| `ERR_CONTROL_TYPE_INVALID`（v4.1） | 控制类型枚举无效 | 控制类型字段标红 + 提示"控制类型不在 STABLE/SLOW/FAST/LOGIC 枚举内" |

### 13.4 文档引用关系

| 文档 | 对本文档的约束 |
|---|---|
| PRD v6.0 | 产品定位、角色模型、6 模块 25 页面划分、AAS Tag 模型、安全边界、6 大 KPI、产品化设计原则 |
| FDS v6.0 | 各模块 UI 形态强制约束、业务流程、状态流转、异常分支、配置变更确认流程 |
| ADS v6.0 | 技术选型（Vue 3 / vue-vben-admin / ECharts）、性能边界、部署架构 |
| DDS v6.0 | 数据字段、枚举值、状态定义、PV 质量码阈值、AAS Tag 模型 |
| IDS v6.0（v4.1 更新） | 接口路径、参数、分页/降采样默认值、错误码、算法服务接口（§2.7）、批量配置接口（§2.8/§2.9） |
| 关键算法设计说明 v2.1（v4.1 新增） | 6 大 KPI 算法（§4）、8 类诊断标签（§5）、整定算法（§6）、综合评分（§4.7）、表达式引擎（§4.9）、C1-C7 跨文档差距修正 |
| 实现契约 v2.0 | IA、路由、API 路径、权限矩阵、状态机标准枚举、KPI 契约、阶段口径 |
| DESIGN.md | 原型设计基线、菜单结构声明、状态机原则 |

### 13.5 与历史版本的关系

| 版本 | 关系说明 |
|---|---|
| v0.1 草稿（2026-06-16） | 历史 `prototype-*-v0.1-2026-06-16.md` 文件基于 approved 产品化架构的 P0 治理闭环验证包，与 PRD/FDS 正式版存在根本性概念冲突。v0.1 文件已于 2026-06-20 清理删除。 |
| v3.0（2026-06-20） | 基于 PRD v2.2/FDS v2.0 等，5 模块 14 页面架构。本文档 v4.0 已吸收 v3.0 中有价值的页面级交互明细与状态矩阵，映射到 v4.0 的 6 模块 25 页面体系。 |
| v4.0（2026-06-20） | 基于 PRD v3.0/FDS v3.0/ADS v3.0/DDS v3.0/IDS v3.0，6 模块 + 门户 25 页面架构，引入 AAS Tag 模型、PV 质量码处理、产品化配置 UI、Tag 关联拖拽交互、配置变更确认交互。 |
| v4.1（2026-06-22） | 基于《关键算法设计说明》v1.0 与 IDS v3.2，新增算法配置界面（§6.7）、诊断结果展示组件（§7.10）、整定参数展示组件（§7.11）、综合评分展示组件（§7.12）、算法任务状态组件（§7.13），更新性能指标配置交互（双滑块/百分比/单选按钮组），对齐 C1-C7 跨文档差距修正。 |
| v5.0-v5.3（2026-06-26 ~ 2026-07-04） | 指标体系重构为 3+1+8 结构、引入可信度 A/B/C/D/E 五级、新增数据血缘展示、PV 质量码改为灰色虚线保留连线、新增任务管理页面与权重配置管理页面。 |
| **v6.0（2026-07-06）** | **当前权威版本**。v6.0 文档统一升级版：修复 Action Tracker 状态枚举冲突（统一为 PENDING/IN_PROGRESS/IMPLEMENTED/IGNORED），统一 5 类状态机（Loop / Action Tracker / KPI 快照 / Tuning / PV Quality），对齐实现契约 v2.0 与 PRD/FDS/DDS v6.0。本文档为唯一 UI/UX 事实来源。 |

### 13.6 v4.1 核心变更摘要

| 变更项 | v4.0 | v4.1 |
|---|---|---|
| 6 大 KPI 清单 | 部分缺失（accuracy_rate/saturation_rate） | 完整 6 项（对齐 C1/C2） |
| 诊断标签 | 自由文本（3 类示例） | 8 类固定枚举 + 融合置信度（对齐 C6） |
| 阈值类型 | DECIMAL 数值 | JSONB 对象 `{min, max, alert}`（对齐 C3） |
| 诊断配置 calc_method | 未明确 | 新增 calc_method 字段（对齐 C4） |
| 整定 fitting_score | 未明确 | 新增 fitting_score R² 拟合度（对齐 C5） |
| 表达式引擎 | 未明确 | simpleeval 安全沙箱（对齐 C7） |
| 评分状态枚举 | SUCCESS/INCONCLUSIVE/PARTIAL | GOOD/WARNING/POOR/INCONCLUSIVE 四态 |
| 算法服务接口 | 无 | 新增 4 个异步 API（KPI/诊断/整定/任务查询） |
| 批量配置接口 | 仅单条 GET/PUT | 新增批量 GET/PUT（/api/v1/configs/metrics、/api/v1/configs/diagnosis） |
| 算法配置界面 | 无 | 新增 §6.7（总览/公式编辑器/诊断配置/整定配置） |
| 诊断结果展示 | 简单卡片 | 新增 §7.10 组件（8 类标签 + 融合置信度 + 证据链） |
| 整定参数展示 | 简单表格 | 新增 §7.11 组件（模型参数/PID 对比/拟合度/仿真结果） |
| 综合评分展示 | 数值 + 色块 | 新增 §7.12 组件（6 大 KPI 雷达图 + 评分构成分解） |
| 算法任务状态 | 无 | 新增 §7.13 组件（异步任务进度/轮询/结果渲染） |
| 性能指标配置交互 | 数值输入 | 双滑块阈值 + 百分比权重 + 单选按钮组控制类型 |
| 整定方法枚举 | IMC/Lambda/ZN/CC | IMC/LAMBDA/ZIEGLER_NICHOLS/COHEN_COON/SIMC（5 类） |
| 错误码 | 9 类 | 新增 7 类算法相关错误码（共 16 类） |

### 13.7 ZL 工业设计套件引用说明（v6.1 新增）

本节明确 ZL 致联工业软件 UI/UX 设计套件在 CLPM UI/UX v6.1 中的引用关系与适用范围。

#### 13.7.1 套件文件清单

| 文件 | 路径 | 内容 | 在 v6.1 中的作用 |
|---|---|---|---|
| `IndustrialDesignSystem.md` | `ZL-MES-UI-Design-Kit/` | ZL 工业设计系统规范指南（2026 完整版）— 6 章核心原则、色彩、字体、组件、交互 | §2.4 四大原则、§3.5.1 边框优先、§3.7 触控目标的事实来源 |
| `Industrial_UI_Agent_Skill.md` | `ZL-MES-UI-Design-Kit/` | AI 智能体 skill 描述 — Calm UI / 色彩语义严格 / 等宽数字 / 边框优先 | §2.4、§3、§9.8 的 AI 生成代码自检清单 |
| `IndustrialDesignReference.html` | `ZL-MES-UI-Design-Kit/` | 完整设计参考页面 — 语义色、表格微图、Poka-Yoke 模态框、高密度数据网格 | §7.16 工业桌面端补充组件的视觉参考实现 |
| `clpm_dashboard.html` | `ZL-MES-UI-Design-Kit/` | CLPM 仪表盘 HTML — F 型布局、KPI 卡、低效列表 | §6.1 工作台门户的视觉参考实现 |

#### 13.7.2 适用范围与优先级

| 来源 | 角色 | 优先级 |
|---|---|---|
| CLPM UI/UX v6.1（本文档） | CLPM 平台 UI/UX 事实来源 | **最高** |
| 实现契约 v2.0 | IA / 路由 / API / 权限 / 状态机 / KPI 事实来源 | **最高** |
| ZL 工业设计套件 | 工业风格细化准则（Calm UI / Poka-Yoke / Glanceability / 数据墨水比） | 次之，作为 §14 强制检查清单的事实来源 |
| DESIGN.md v3.0 | 反 AI Slop / 表格优先 / token 化 / 配置确认留痕 | 次之，作为视觉风格治理准则 |

**关键约束**：ZL 套件中的 Teal/Emerald 状态色必须映射到 CLPM `--status-ok` token；ZL 主操作色以 CLPM 工业蓝 `--primary` 为准；ZL 大屏模式（无侧边栏）仅可作为独立 HMI/大屏参考，不替代 CLPM 全局壳层（左导航 + 顶部状态栏 + 标题区 + 主体区 + 全局摘要）。

#### 13.7.3 AI 智能体生成工业 UI 的自检清单

依据 `Industrial_UI_Agent_Skill.md`，AI 智能体（Trae / Claude Code）生成 CLPM 前端代码时必须自检以下 4 项：

1. **Calm UI**：90% 屏幕使用中性色（Slate/Gray）；仅在异常发生时使用高饱和度色彩。
2. **色彩语义严格**：Emerald 仅用于 Running/Success/Online；Amber 仅用于 Idle/Warning/Paused；Rose 仅用于 Error/Critical/Stopped/Alarm；Indigo/Blue 用于主操作。
3. **等宽数字**：所有动态数字必须使用 `--font-mono` 或 Tailwind `tabular-nums` 类。
4. **边框优先于阴影**：使用 1px solid `--border-default`（如 `#E2E8F0`）分隔卡片；圆角 4-8px；禁止使用厚重阴影。

#### 13.7.4 引用更新机制

ZL 套件版本升级时，按以下流程同步到 UIUX v6.x：

1. 在 `ZL-MES-UI-Design-Kit/` 内更新文件。
2. 在 §0 文档变更记录新增条目，标注影响的 UIUX 章节。
3. 更新 §14 强制检查清单（如有新增强制项）。
4. 通知 zp 机器（Claude Code）按 §15 改造方案分阶段实施。

---

## 14. 工业风格强制检查清单（v6.1 新增）

本章是 ZL 致联工业软件 UI/UX 设计套件在 CLPM 平台的**落地验收判据**，所有页面改造必须通过本章 18 项强制项验收。zp 机器（Claude Code）执行页面改造时，每完成一个页面组必须按本章逐项核对，并在 PR Body 中标注通过项与未通过项的原因。

### 14.1 产品定位与任务优先（2 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **F-01** | 页面必须体现工业治理平台定位 | 稳重、审计化、任务驱动；首屏优先回答"现在该做什么"；禁止娱乐化插画、装饰性动画、营销文案 | 首屏截图审查；菜单与卡片标题审查 | §2.1 §2.4.1 |
| **F-02** | 不允许空点击 | 主按钮或主链接必须触发状态变化、上下文变化或路由变化；空态按钮必须置灰并提示原因 | 逐按钮点击测试 | §9.1 |

### 14.2 信息架构与全局壳层（3 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **A-01** | 信息架构以 v6.0 与实现契约为准 | 旧 25 页面清单仅作历史来源；正式路由为当前页面事实；禁止复活已废弃路由 | 路由表 diff；菜单对照实现契约 §2 | §4 §1.4 |
| **A-02** | 全局壳层完整保留 | 左导航 + 顶部状态栏 + 标题区 + 主体区 + 全局摘要；ZL 无侧栏大屏模式仅作为独立 HMI/大屏参考 | 所有页面布局审查 | §4.1 |
| **A-03** | 角色权限驱动菜单与按钮 | 无权限菜单隐藏；无权限按钮隐藏或置灰并 tooltip 提示原因；禁止前端硬编码角色字符串 | 多角色账号登录测试（admin/engineer/viewer） | §5 |

### 14.3 色彩与数字排版（3 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **C-01** | 所有颜色来自 token/CSS 变量 | 不在业务页面散落 hex；ZL 色板只映射到 CLPM token（`--status-ok` / `--status-warning` / `--status-error` / `--primary` / `--text-*` / `--border-*`） | `grep -R "#[0-9a-fA-F]{6}" frontend/apps/web-antd/src/views` 人工白名单审计 | §3.1 §3.5 |
| **C-02** | 状态色严格语义化 | 正常=Emerald、信息=Blue、警告=Amber、危险=Rose、中性=Slate；不得只靠颜色区分，必须叠加文本或图标 | SUCCESS/PARTIAL/INCONCLUSIVE、PENDING/IN_PROGRESS/IMPLEMENTED/IGNORED、GOOD/BAD/UNCERTAIN 快照检查 | §3.1.3 §8 |
| **C-03** | 动态数字、位号、Tag、版本号、时间戳、PID 参数等宽 | 使用 `--font-mono`、`tabular-nums` 或 `ClpmNumeric`（§7.16.1）组件；评分、KPI、PV/SP/OP、PID、时间戳强制覆盖 | 视觉检查 + class 检查 + 实时刷新跳动测试 | §3.3 §7.16.1 |

### 14.4 表格与扫视性（3 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **G-01** | 台账、排行、任务、审计以高密度表格为主 | 支持筛选、排序、固定列、批量、空态、Partial 提示；禁止用卡片流替代表格 | 18 页面组逐页核对页面类型 | §6 §2.4.3 §2.4.4 |
| **G-02** | 表格内微图与 hover reveal | 低效排行、任务进度、回路监控、AAS 同步列表加入行内趋势/进度条；主操作常显，次操作 hover reveal 或"更多"菜单 | 表格行交互测试 | §7.16 §2.4.3 |
| **G-03** | 状态标签必须有颜色、文本、图标或语义前缀 | 状态枚举使用 v6.0 大写标准（PENDING/IN_PROGRESS/IMPLEMENTED/IGNORED、GOOD/BAD/UNCERTAIN、SUCCESS/PARTIAL/INCONCLUSIVE） | 标签视觉审查 | §7.2 §8 |

### 14.5 KPI 与可信度（3 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **K-01** | INCONCLUSIVE 不得显示 0 分 | 可信度 E 级显示 `—`，不参与聚合；评分卡必须显示可信度徽章（A/B/C/D/E） | 构造 valid_rate<20% 数据测试 | §7.15 §8.2.2 |
| **K-02** | 可信度徽章 tooltip 显示 valid_rate | 评分、KPI、任务结果显示 A/B/C/D/E；tooltip 显示 valid_rate 与采样策略；可展开或跳转数据血缘 | tooltip 测试 | §7.15 §6.3 |
| **K-03** | PV 质量码 Bad/Uncertain 不断线、不置 0 | Bad 灰色虚线保留连线；Uncertain 琥珀虚线保留连线；tooltip 显示质量码与时间戳 | 构造 Bad/Uncertain 数据测试 | §7.2.4 §8.2.5 |

### 14.6 数据血缘与配置变更（2 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **D-01** | 指标、诊断、任务结果可追溯 | 展示采样频率、质量策略、tag_group、valid_rate、策略版本、算法版本；可下钻至原始数据 | 详情页血缘面板测试 | §7.14 §6 |
| **D-02** | 配置变更保存必须二次确认并留痕 | 弹窗展示变更摘要、新旧值、影响范围、变更说明；写入 `audit_log` 表（§9.8.6） | 配置保存流程测试 + audit_log 表记录检查 | §9.6 §9.8 |

### 14.7 高危操作与安全边界（2 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **P-01** | 高危操作不用浏览器默认确认承载 | 禁止 `alert()` / `window.confirm()` / `Popconfirm` / `Modal.confirm` 承载高危操作；统一使用 `ClpmDangerConfirmModal`（§7.16.2）+ typed confirmation + 审计备注 | `grep -R "Popconfirm\|Modal.confirm\|alert(" frontend/apps/web-antd/src/views` 逐项分类豁免 | §9.8 §7.16.2 |
| **P-02** | UI 不得出现"下发参数""写入 DCS""自动实施"入口 | 整定只输出建议、证据、风险与回退方案；整定页顶部固定横幅"只读建议 · 人工实施 · 需留痕" | 全文搜索"下发/写入 DCS/自动实施" | §1.2 §9.8.5 §6.5 |

### 14.8 异常状态与响应式无障碍（3 项）

| 编号 | 强制项 | 落地要求 | 验证方式 | 影响章节 |
|---|---|---|---|---|
| **E-01** | 空/异常/Partial 是一等状态 | 说明原因、可用能力、下一步动作和恢复入口；禁止空表格无提示 | 构造空数据/接口失败/Partial 数据测试 | §7.3 §8 |
| **E-02** | 响应式不简单堆成细长卡片流 | Desktop 保留三栏/双栏；Tablet 折叠次级区；Mobile 步骤式或抽屉式 | 三种断点视觉测试（1440/768/375） | §11 |
| **E-03** | 触达、键盘、对比度合格 | 点击目标 ≥44px（触控模式）；拖拽必须有下拉替代；状态不能只靠颜色；键盘 Tab 顺序合理；对比度 WCAG AA | 键盘导航测试 + 对比度审计 + 触控尺寸审查 | §3.7 §11 |

### 14.9 验收判定口径

| 等级 | 判定 |
|---|---|
| **通过** | 18 项强制项全部满足；页面类型结构正确；状态、权限、数据质量、可信度、空态完整；无明显 v6.0 冲突 |
| **有条件通过** | 不影响业务安全和状态正确性，但存在建议项缺失、局部密度、层级或响应式不足；可上线但需在下个迭代补齐 |
| **不通过** | 出现任一严重问题：空点击、权限错误、状态枚举错误、INCONCLUSIVE 显示 0、PV Bad 断线/置 0、无审计确认、危险操作用浏览器默认确认、暗示下写 DCS、用卡片替代表格等 |
| **需融合设计** | 视觉来自 ZL 工业规范但未映射到 CLPM token、状态机、路由、权限或技术栈；需要先做规范融合后再验收 |

---

## 15. 改造方案与实施计划（v6.1 新增）

本章是 UIUX v6.1 落地的执行路径，作为 zp 机器（Claude Code）页面改造的输入。所有改造任务必须按本章 4 阶段顺序推进，每阶段交付后由 mb 机器（Trae）按 §14 强制检查清单验收。

### 15.1 改造目标

| 目标 | 验收标准 | 影响章节 |
|---|---|---|
| **冷静统一** | 页面背景、面板、状态色全部来自 token；无业务页面散落 hex | §3 §14 C-01 |
| **一眼可读** | KPI、评分、PV/SP/OP、PID、时间戳等全部等宽；关键表格引入微图/进度条 | §7.16 §14 C-03 G-02 |
| **防误操作** | 高影响操作统一 typed confirmation；配置变更展示影响范围和审计备注 | §9.8 §14 P-01 D-02 |
| **高密度工业桌面** | 台账页表格优先，次要操作隐藏，详情用抽屉/侧栏/时间线 | §6 §14 G-01 G-02 |
| **安全边界明确** | 整定模块全链路标识"建议输出，不下写 DCS" | §9.8.5 §14 P-02 |

### 15.2 横切组件任务（阶段 1）

阶段 1 不改业务页面，只交付组件/工具层，作为后续页面改造的基础设施。

| ID | 任务 | 范围 | 优先级 | 输出 |
|---|---|---|---|---|
| **UI-01** | 建立工业语义色映射工具 | `constants/industrial-status.ts` 或 `composables/useIndustrialStatus.ts`；替换页面散落色值；映射到 `--status-ok` / `--status-warning` / `--status-error` / `--primary` token | P0 | 共享色映射模块 + 替换文档 |
| **UI-02** | 新增 `ClpmNumeric` 与全局 `.clpm-num` utility | 数值、时间戳、位号、PID、版本号统一组件；通过 prop 控制精度、单位、趋势箭头 | P0 | `components/clpm/numeric.vue` + 全局 CSS utility |
| **UI-03** | 新增 `ClpmDangerConfirmModal`（§7.16.2） | 删除、取消、重算、配置保存、整定建议确认统一组件；支持 typed confirmation + 变更摘要 + 审计备注 | P0 | `components/clpm/danger-confirm-modal.vue` |
| **UI-04** | 新增 `ClpmRealtimeStatus`（§7.16.3） | 数据延迟、自动刷新、接口失败、在线/离线统一表达 | P1 | `components/clpm/realtime-status.vue` |
| **UI-05** | 增强 `ClpmTable` 或表格规范封装 | hover reveal、行内进度、列设置、密度切换（32px/44px） | P1 | `components/clpm/table.vue` 增强 或表格规范文档 |
| **UI-06** | 建立图表主题 preset | ECharts grid/axis/tooltip/shadow/状态色 preset；禁用 `shadowBlur`（tooltip/选中态除外） | P1 | `composables/useEChartsPreset.ts` |

**阶段 1 验收**：UI-01 ~ UI-03 必须先完成；UI-04 ~ UI-06 可与阶段 2 并行。所有组件通过 `pnpm run check:type` + 单元测试。

### 15.3 页面级改造任务（阶段 2-4）

#### 阶段 2：P0 页面组（安全与防呆优先）

| ID | 页面组 | 路由 | 任务 | 验收重点 |
|---|---|---|---|---|
| **P0-1** | 回路管理台账 + Tag 清单 | `/loop/manage` `/tag/list` | 替换删除/解绑 Popconfirm 为 `ClpmDangerConfirmModal`；统一状态色（UI-01）；数值/位号等宽（UI-02）；操作列主次分层，次操作 hover reveal | §14 P-01 C-01 C-03 G-02 |
| **P0-2** | 任务列表 + 历史重算 | `/tasks/list` `/metric/recompute` | 取消、重跑、重算统一 `ClpmDangerConfirmModal`（输入 task_id 短码，显示影响回路数与时间窗口）；进度列内联 progress；任务 ID/时间 mono | §14 P-01 C-03 G-02 D-02 |
| **P0-3** | 整定模块全链路 | `/tuning/*` | 全页面加入安全边界条（顶部固定横幅）；参数建议区标记经济/警告/过载区间；禁止出现"下发/写入 DCS"语义；建议确认走 `ClpmDangerConfirmModal` | §14 P-02 §9.8.5 |

#### 阶段 3：P1 页面组（核心业务页面对齐）

| ID | 页面组 | 路由 | 任务 | 验收重点 |
|---|---|---|---|---|
| **P1-1** | 工作台 + 性能看板 | `/dashboard/workbench` `/metric/dashboard` | 统一 `ClpmRealtimeStatus`；KPI 卡数值等宽（UI-02）；低效 Top10 加行内趋势/可信度徽章；减少装饰性 Card，改为 `ClpmDataCanvas` | §14 C-03 K-01 K-02 G-02 |
| **P1-2** | 回路监控 + AAS 同步 | `/loop/monitor` `/loop/aas-sync` | PV/SP/OP/MODE 行内高密度展示；质量码线型/颜色统一（Good 实线/Bad 灰虚/Uncertain 琥珀虚）；同步状态面板组件化（`ClpmStatusPanel` §7.16.4） | §14 K-03 G-01 G-02 |
| **P1-3** | 诊断列表 + 异常跟踪 | `/diagnosis/list` `/diagnosis/tracker` | 状态流色彩重映射（UI-01）；A/B 对比入口主次分层；状态更新使用 `ClpmDangerConfirmModal` 简化模式 + 审计 | §14 C-02 G-03 D-02 |

#### 阶段 4：P2 收口（统计、系统管理、旧页清理）

| ID | 页面组 | 路由 | 任务 | 验收重点 |
|---|---|---|---|---|
| **P2-1** | 统计类页面 | `/metric/statistics` `/diagnosis/statistics` `/tuning/stats` | 去图表阴影（UI-06 preset）；统一 ECharts preset；图例与语义色一致；增加空/partial/error 状态 | §14 C-01 E-01 |
| **P2-2** | 系统管理 | `/system/users` `/system/audit` `/system/permissions` `/system/reports` | 用户禁用/角色调整/报表删除加入 typed confirmation；审计字段等宽；权限矩阵密度优化 | §14 P-01 C-03 |
| **P2-3** | 旧示例页清理 | `_core/*` `dashboard/analytics/*` `dashboard/workspace/*` | 隔离或删除业务无关 dashboard 示例页；避免样式污染；保留时改为开发示例并隔离路由 | §14 A-01 |

### 15.4 实施顺序与产出

| 阶段 | 内容 | 产出 | 验收方 |
|---|---|---|---|
| **阶段 1** | 横切 token、数字、危险确认、图表主题 | 组件/工具层 PR，最小页面改动验证 | mb 机器 type-check 通过 + 组件 demo 截图 |
| **阶段 2** | P0 页面组改造（回路管理 + 任务 + 整定） | 3 个 PR，安全边界与防呆达标 | mb 机器按 §14 强制项验收 |
| **阶段 3** | P1 页面组改造（看板 + 监控 + 诊断） | 3 个 PR，核心业务页面对齐 | mb 机器按 §14 强制项验收 |
| **阶段 4** | 统计 + 系统管理 + 旧页清理 | 3 个 PR，视觉债务收口 | mb 机器按 §14 强制项验收 + 长期验收脚本 |

### 15.5 验收清单（每阶段必过）

| 检查项 | 验收方式 | 工具/命令 |
|---|---|---|
| 无业务页面新增散落 hex | `grep -R "#[0-9a-fA-F]\{6\}" frontend/apps/web-antd/src/views` 人工白名单 | grep + 人工白名单 |
| 高风险操作不用简单 Popconfirm | 搜索 `Popconfirm`、`Modal.confirm`、`alert(`，逐项分类豁免 | grep + 人工核对 |
| 数值等宽覆盖 | 评分、KPI、PV/SP/OP、PID、时间戳视觉检查 + class 检查 | 视觉审查 + grep `tabular-nums\|font-mono` |
| 图表无装饰阴影 | 搜索 `shadowBlur`，仅 tooltip/选中态允许 | grep `shadowBlur` |
| 状态语义一致 | SUCCESS/PARTIAL/INCONCLUSIVE、PENDING/IN_PROGRESS/IMPLEMENTED/IGNORED、GOOD/BAD/UNCERTAIN 快照检查 | 视觉审查 |
| 整定安全边界 | 所有整定页面可见"不下写 DCS/人工实施/留痕"提示 | 全文搜索 + 视觉审查 |
| 质量验证 | `cd frontend && pnpm run check:type` + 关键页面截图审查 + 必要 E2E 补充 | CI + Playwright |

### 15.6 双机协作约定

- **mb 机器（Trae）**：负责 UIUX v6.1 设计文档维护、§14 强制检查清单更新、阶段验收、视觉审查；不直接改业务页面代码。
- **zp 机器（Claude Code）**：负责按 §15.2 ~ §15.3 任务清单实施页面改造；每个 PR Body 必须标注通过的 §14 强制项编号（如 `通过 F-01 F-02 A-01 A-02 A-03 C-01 C-02 C-03 G-01 G-02 G-03 K-01 K-02 K-03 D-02 P-01 P-02 E-01`）。
- **PR 流程**：阶段 1 横切组件 PR 由 zp 机器推送到 `zp/feat-ui-crosscutting`；阶段 2-4 页面组 PR 推送到 `zp/refactor-page-p0-1`、`zp/refactor-page-p0-2` 等分支；mb 机器 review 后合并。
- **冲突预防**：阶段 1 完成前，阶段 2 不开始；阶段 2 内部三个 P0 任务可并行，但需协调 `ClpmDangerConfirmModal` 接口稳定性。
- **进度同步**：每阶段完成后，在 `docs/过程文档/` 新建 `uiux-v6.1-stage-N-summary.md`，记录通过项、未通过项、下一步计划。
