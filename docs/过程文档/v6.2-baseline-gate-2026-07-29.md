# v6.2 基线门禁结果（2026-07-29）

> 在切出阶段子分支前，对不可变基线 `main@e7ca7749` 跑完整门禁并保存结果，
> 作为后续各阶段回归对照（v6.2 开发规约：开分支前运行后端 pytest、ruff、alembic、
> 前端类型检查及关键 E2E，保存基线结果）。

## 基线锚点

| 项 | 值 |
|---|---|
| 基线分支 | `main` |
| 基线 commit | `e7ca7749`（Merge pull request 'fix(diagnosis): 修复跟踪页 SPA 导航白屏' #11） |
| 等价代码状态 | `codex/v6.2-integration@90431984`（baseline 提交仅含 4 份文档，无业务代码改动） |
| 运行时间 | 2026-07-29 |

## 后端门禁

| 检查 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `uv run pytest -q --no-header` | **3409 passed, 1 skipped, 15 deselected, 33 xfailed**（229.46s） |
| 静态检查 | `uv run ruff check .` | All checks passed |
| 格式检查 | `uv run ruff format --check .` | 453 files already formatted |
| schema 漂移 | `uv run alembic check` | 退出码 0，无新 upgrade 操作 |

## 前端门禁

| 检查 | 命令 | 结果 |
|---|---|---|
| 类型检查 | `pnpm run check:type` | 退出码 0，通过 |

## 回归对照口径

- Phase 0 完成后预期后端用例数 **+47**（3409 → 3456），均为新增门禁/回归测试，原有用例不得转红。
- 现有 3+1+8 KPI、诊断中心、Action Tracker、回路整定旧流程不得回归。
- 前端 vitest 基线当时未单独记录；Phase 0 新增 3 份前端测试（tuning-algorithm-source / tuning-model / tuning-workbench），需在 Phase 0 收尾时全绿。

## 结论

不可变基线 `main@e7ca7749` 门禁全绿，可作为 v6.2 各阶段分支的回归对照起点。
