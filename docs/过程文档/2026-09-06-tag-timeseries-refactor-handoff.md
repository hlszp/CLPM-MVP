# 测点子表重构：智能体交接与阶段提示词

日期：2026-09-06。用途：用户分派 zcode/其他实施智能体时复制使用；本文件不代表已启动实施。

## 1. 工作区与共同约定

- 工作区：`/Users/zhangping/DEV/CLPM-MVP-worktrees/tag-timeseries-refactor`。
- 分支：`codex/tag-timeseries-refactor`，当前基线 `9ee40210e133288830ac8a3ee25103ee6be8bb8d`。目前仅本地，三份方案文档尚未提交；跨机不能假定 GitHub 已有此分支/文档。
- 已纳入另一任务提交到 main 的 WS/token 修复；主工作区 `/Users/zhangping/DEV/CLPM-MVP` 收尾核验为干净。在独立 worktree 实施，不覆盖既有修复，不切换、stash 或收编主工作区后续改动。
- worktree 不提供运行时隔离；P0 先落实专属测试数据库、Redis/Celery、容器/端口/卷，再跑服务和故障实验，避免干扰主开发实例。
- 目标已经确定：**每测点一张 TDengine 子表，统一适配层给原算法提供 1 秒逻辑宽表；性能评估、诊断、整定算法主体与取数接口保持。**
- 保持本地计算、远端历史仅 data_import、gap skip/默认关闭/600s；不换 SignalR 库，不删旧表或诊断/整定文件，不自行推送/合并/部署。
- 总体分派授权覆盖哪些阶段，就在这些阶段内持续推进；阶段门槛未通过不能写成完成，未授权的生产操作不因开发通过自动执行。

本文中的“设计”“计划”分别指：

- [设计：测点子表与逻辑宽表](/Users/zhangping/DEV/CLPM-MVP-worktrees/tag-timeseries-refactor/docs/设计文档/2026-09-06-tag-timeseries-logical-wide-refactor.md)
- [计划：任务、验收与合并门槛](/Users/zhangping/DEV/CLPM-MVP-worktrees/tag-timeseries-refactor/docs/过程文档/2026-09-06-tag-timeseries-refactor-plan.md)

共同必读顺序：本工作区 AGENTS.md → docs/过程文档/stale-docs.md → docs/过程文档/handovers/2026-09-06-codex-data-pipeline-review-handover.md → 设计 → 计划及 §8 最新状态。原移交/旧整改报告仅作问题线索，先核对当前代码，不能把已修项重新列为现行缺陷。

## 2. 可直接交给 zcode 的总提示词

> 在上述独立 worktree 和 codex/tag-timeseries-refactor 分支执行测点子表重构。先按共同必读顺序读文件，遵守设计冻结边界，再依计划 P0～P4 实施；每阶段使用下列五节提示词，逐项验收并更新计划 §8。测点事实按独立源时间/质量存本地 TDengine，由唯一 builder 按历史绑定和有效覆盖生成逻辑宽表；保持现有 Provider、RawTimeSeries、DataBlock、DataPlanner 与评估/诊断/整定算法契约。正常 COV 常值可保持，缺口、坏质量、无初值不能填成正常；旧历史保留 legacy 读取。优先复用上一轮已修复的采集可靠性措施。交付可复现测试、真实 TD/PG/Redis 证据、受保护文件差异及迁移/回退说明；未验明确登记。提交/推送按用户实际授权办理，不自行合 main。全部完成后交给主审 Codex 检查最终版本，由主审按用户授权合并。

## 3. P0 阶段提示词：基线与契约

### 1. 必读清单

按共同顺序阅读；重点设计 §2/§5/§6，计划 §2/§4-P0/§5；核对已有整改的最新状态。

### 2. 阶段范围

完成 P0-1～P0-6：活跃入口、签名/DTO/时间/质量/计数、受保护文件、独立密集参考与 AAS 字段合同、性能基线。Owner 按计划 §2；本阶段仅建立文档/测试基线，不改业务和运行数据库。

### 3. 执行纪律

独立工作区；不复用旧报告测试数字当新证据；未知 AAS 行为记录，继续不依赖它的基线工作；更新计划 §8。

### 4. 完成度核验问题

逐项回答计划 §6 五个复选项：验收证据、适用门禁、越界、状态同步、阻断 bug；确认 P0 晋级条件全部成立。

### 5. 人工决策点

将具体接口/质量/来源合同矛盾交主审；现有目标不重复确认。只有需改变用户目标或外部契约时，提供具体影响后等决策。

## 4. P1 阶段提示词：存储与元数据

### 1. 必读清单

共同必读＋P0 基线产物与计划 §8 最新行；重点设计 §4/§7，计划 §4-P1、V07～V12/V14。

### 2. 阶段范围

P1-1～P1-5：TD DDL、PG 迁移、点身份/绑定历史、覆盖/批次/冲突/锚点、仓储和布局配置。依赖 P0 契约通过；Owner A，D 验证；不切生产读写。

### 3. 执行纪律

迁移与 ORM 同批、单迁移 Owner；旧表保留；在隔离数据库验证目标 TD 版本与恢复，不以 fake 代替真实同 ts/部分写行为；更新 §8。

### 4. 完成度核验问题

回答计划 §6 五项；P1 晋级条件、真实升级/重复执行/无漂移、默认 legacy 与失败恢复是否有证据？

### 5. 人工决策点

交主审核对具体迁移和职责映射；普通类型/索引选择按设计自主实施。需要删数据、变更保留期或外部字段时才提实质决策。

## 5. P2 阶段提示词：实时与导入写点

### 1. 必读清单

共同必读＋P1 接口/迁移和 §8 最新行；重点设计 §4.3/§6.1/§7，计划 §4-P2、V05～V07/V11～V13。

### 2. 阶段范围

P2-1～P2-5：独立事件缓冲与 PointHistoryWriter、迟到/重复/冲突、批次恢复、点级导入与计数、隔离 shadow。Owner B，底层接口由 A 修改；不改 WS/token 或算法。

### 3. 执行纪律

沿用已有租约/故障隔离；同一事件流影子写，不新建第二订阅者；gap skip 与 now−5min 保持；记录崩溃未确认边界，禁止默填；更新 §8。

### 4. 完成度核验问题

回答计划 §6 五项；原始事实对账、共享点、同 tick 多事件、质量变化、部分成功/取消/重试与进度是否逐项通过？

### 5. 人工决策点

发现 AAS 合同缺项给出可复现样例，交主审判断对应验收边界；不把“接了库”作为可靠性完成，不自行扩大真实源端负载。

## 6. P3 阶段提示词：逻辑宽表兼容

### 1. 必读清单

共同必读＋P0 契约与 P1/P2 交付；重点设计 §2/§5/§6，计划 §4-P3、V01～V10/V13～V16。

### 2. 阶段范围

P3-1～P3-5：唯一 builder、Provider/布局路由、旧活跃取数桥接、逻辑完整性与趋势、统一缓存版本。纯 builder 可按 P0 样本先开发，集成依赖 P2；Owner C。

### 3. 执行纪律

冻结函数签名/返回结构/算法与门禁；同窗角色并集只组装一次；不把 SQL 放进算法，不对缺口盲目 ffill，不修改前端；更新 §8。

### 4. 完成度核验问题

回答计划 §6 五项；密集→COV→重建逐层等价、未知掩码、历史改绑/切换边界、全部活跃入口和缓存竞争是否通过？

### 5. 人工决策点

确有必要修改受保护区时，先给具体取数桥接 diff 与底层无法兼容的证据，交主审处理；不先改算法再要求追认。

## 7. P4 阶段提示词：联合验收

### 1. 必读清单

共同必读＋P1～P3 阶段结果；重点设计 §7/§8，计划 §4-P4/§5/§6/§7。

### 2. 阶段范围

P4-1～P4-5：完整差分矩阵、真实 PG/TD/Redis 故障、隔离容量、迁移/回退与既有前端冒烟。Owner D，各 Owner 修复失败；不擅自生产压测/切换。

### 3. 执行纪律

测试最终实际版本；独立参考；等价用例与纠偏用例分开；环境阻塞如实登记；修复实现不放宽断言；更新 §8 并形成验收报告。

### 4. 完成度核验问题

回答计划 §6 五项；V01～V16、性能目标、真实数据库及两种旧写状态下回退是否有证据？有未验时不能宣称可切 point。

### 5. 人工决策点

容量不足或业务结果差异提供数据和解释交主审；不以改算法/降采样/少算点通过。验收通过后交主审，实施者不自合 main。

## 8. P5 阶段提示词：交回 Codex 检查合并

### 1. 必读清单

共同必读＋各阶段报告、最终 diff、精确 HEAD 与验证证据；重点计划 §4-P5/§7 和设计冻结/迁移边界。

### 2. 阶段范围

由主审执行 P5-1～P5-5：复核逻辑零改动、读写/质量/覆盖/缓存与旧入口，整合 main 已提交变更，测试最终版本，通过后合并。

### 3. 执行纪律

保护 main 其他任务改动；无自动 stash/reset；用户已授权检查后合并，不另设无依据确认；合并不等于推送/部署或切生产配置。

### 4. 完成度核验问题

回答计划 §6 五项；最终测试对象与合并对象相同、迁移同批、阻断项清零、旧历史可读、回退覆盖已证实？

### 5. 人工决策点

有实质范围/契约变化或不可自行仲裁的冲突，先提交具体差异给用户；否则按已有授权完成审查合并，并报告提交与实际验收状态。
