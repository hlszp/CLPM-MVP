# CLPM v6.2 手工试用与检查计划

> **生成日期**：2026-07-31
> **前端地址**：http://localhost:5666
> **后端 API**：http://localhost:7101
> **预计耗时**：2-3 小时
> **适用范围**：v6.2 Phase 0-3 全模块功能验证

---

## 试用准备

### 账号清单（密码统一 `admin123`）

| 账号 | 角色 | 可访问模块 | 试用重点 |
|---|---|---|---|
| `admin` | 系统管理员 | 全部模块 | 全模块 + 高级参数 + 系统管理 |
| `ic_engineer` | 仪控工程师 | 工作台/回路/诊断/整定 | 整定流程 + 诊断闭环（无高级参数） |
| `pe_engineer` | 工艺工程师 | 工作台/回路/性能评估 | 性能评估 + KPI |
| `sponsor` | 项目发起人 | 工作台/回路 | 工作台待办门户（无整定入口） |
| `expert` | 外部专家 | 工作台/回路/整定 | 整定高级参数 |

### 环境确认

```bash
# 确认服务运行
curl -s http://localhost:7101/api/v1/health | jq .status      # 预期 ok
curl -s http://localhost:7101/api/v1/health/ready | jq .status # 预期 ok
curl -s -o /dev/null -w "%{http_code}" http://localhost:5666/  # 预期 200
```

### 试用记录模板

每个检查项记录：`✅ 通过` / `❌ 失败（现象描述）` / `⚠️ 部分通过（说明）`

---

## 模块一：登录与权限基线（15min）

### 1.1 登录流程

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| L1 | 浏览器打开 `http://localhost:5666/auth/login` | 显示登录页，含用户名/密码输入框 | |
| L2 | 输入 `admin` / `admin123`，点击登录 | 跳转到工作台 `/dashboard/workbench` | |
| L3 | 查看右上角用户信息 | 显示"系统管理员" | |
| L4 | 退出登录，重复 L1-L2 用 `ic_engineer` 登录 | 跳转工作台，显示"仪控工程师" | |

### 1.2 角色权限边界（v6.2 P1-022 重点）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| P1 | `sponsor` 登录，查看左侧菜单 | **不显示**"回路整定"菜单项 | |
| P2 | `sponsor` 手动访问 `http://localhost:5666/tuning/workbench` | 跳转 403 或重定向回工作台 | |
| P3 | `ic_engineer` 登录，进入整定流程高级参数区域 | **不显示**高级参数折叠面板（仅 ADMIN/EXPERT 可见） | |
| P4 | `expert` 登录，进入整定流程 | **显示**高级参数折叠面板（候选阶次/纯滞后预估） | |
| P5 | `admin` 登录，进入整定流程 | **显示**高级参数折叠面板 | |

---

## 模块二：工作台——跨模块待办门户（10min）

> v6.2 P1-017 重构：工作台变为跨模块待办门户，取消与装置性能的重复心智入口。

### 2.1 待办卡片（admin 登录）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| W1 | 进入 `/dashboard/workbench` | 顶部显示 KPI 摘要条 + 多张待办卡片 | |
| W2 | 查看待办卡片内容 | 至少包含：诊断待处理 / 异常跟踪待办 / 评估待执行 / 整定任务 四类计数 | |
| W3 | 检查计数数据来源 | 计数非 0 且非伪数据（点击可跳转对应模块） | |
| W4 | 点击"诊断待处理"卡片 | 跳转到 `/diagnosis/tasks` | |
| W5 | 点击"整定任务"卡片 | 跳转到 `/tuning/workbench` | |
| W6 | 检查是否有装置性能完整看板 | 工作台仅保留入口卡，完整看板归属 `/metric/pid-dashboard` | |

### 2.2 角色条件渲染

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| W7 | `sponsor` 登录查看工作台 | **不显示**整定卡片（SPONSOR 无整定权限） | |
| W8 | `pe_engineer` 登录查看工作台 | **不显示**整定卡片 | |

---

## 模块三：回路管理（20min）

> 遵循"配置→运行→分析"三态自包含原则。

### 3.1 回路台账与列表

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| LP1 | 进入 `/loop/ledger` | 显示回路台账列表（至少 2 条种子回路） | |
| LP2 | 查看列表列 | 含回路名/Tag名/装置/控制类型/状态/重要性等级 | |
| LP3 | 点击某回路行 | 跳转到 `/loop/detail/:id` 详情页 | |

### 3.2 回路详情（稳定元素根防白屏——v6.2 P1-021）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| LP4 | 在详情页查看回路上下文头 | 显示回路选择器 + 时间范围选择器 + 返回按钮 | |
| LP5 | 切换时间范围 | 数据图表随之刷新 | |
| LP6 | **按 F5 硬刷新详情页** | 页面不白屏，URL 保持，内容恢复（P1-021 稳定元素根） | |
| LP7 | 浏览器后退再前进 | 页面正常，不白屏 | |

### 3.3 回路监控与数据

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| LP8 | 进入 `/loop/monitor` | 显示回路监控视图（实时/最近状态） | |
| LP9 | 进入 `/loop/data` | 显示历史数据查询/导入入口 | |
| LP10 | 尝试历史数据导入（如有可用数据） | 导入任务可提交，进度可查看 | |

### 3.4 AAS 同步与 Tag 管理

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| LP11 | 进入 `/loop/aas-sync` | 显示 AAS 同步页面 | |
| LP12 | 进入 `/tag/list` | 显示 Tag 位号列表 | |
| LP13 | 查看 Tag 列表 | 每个 Tag 有质量码标识（Good/Bad/Uncertain） | |

---

## 模块四：性能评估（20min）

### 4.1 PID 仪表盘

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| M1 | 进入 `/metric/pid-dashboard` | 显示装置级 PID 性能仪表盘 | |
| M2 | 查看 KPI 指标卡片 | 含 3 核心 + 1 综合 + 8 辅助指标 | |
| M3 | 查看趋势图 | 趋势图加载正常，支持时间范围切换 | |
| M4 | 检查 3+1+8 评分公式 | 综合评分 = 3 核心 + 1 综合 + 8 辅助加权（公式不变） | |

### 4.2 回路性能与置信度（v6.2 Phase 0 重点）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| M5 | 进入 `/metric/loop-performance` | 显示回路性能列表 | |
| M6 | 查看置信度列 | 每回路有 A/B/C/D/E 等级标识 | |
| M7 | 查看数据不完整的回路 | 显示 INCONCLUSIVE 提示，非"伪成功" | |
| M8 | 检查有效数据率 | valid_rate 阈值 95/80/60/20% 对应 A/B/C/D/E | |

### 4.3 评估任务

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| M9 | 进入 `/metric/tasks` | 显示评估任务列表 | |
| M10 | 查看任务状态 | 任务有 PENDING/RUNNING/SUCCESS/FAILED 状态 | |
| M11 | 如有 RUNNING 任务，查看进度 | 进度按小时窗口平滑更新（非按回路粗粒度） | |

### 4.4 指标配置与报表

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| M12 | 进入 `/metric/config` | 显示指标配置（ADMIN 可编辑，其他只读） | |
| M13 | 进入 `/metric/kpi-report` | 显示 KPI 报表/可生成报表 | |

---

## 模块五：诊断中心（25min）

> v6.2 P1-018：tasks/records 合并为进行中/历史 Tabs。

### 5.1 诊断总览

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| D1 | 进入 `/diagnosis/overview` | 显示诊断总览（回路状态分布/异常计数） | |
| D2 | 查看状态分布图 | 饼图/柱图正常渲染（无引线，悬浮框显示） | |
| D3 | 点击某异常回路 | 跳转到诊断详情页 | |

### 5.2 任务中心——Tabs 合并（v6.2 P1-018 重点）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| D4 | 进入 `/diagnosis/tasks` | 显示 Tabs：进行中 / 历史 | |
| D5 | 默认在"进行中"Tab | 显示进行中的诊断任务列表 | |
| D6 | 点击"历史"Tab | 切换到历史诊断记录列表 | |
| D7 | 验证 URL query 同步 | 切换 Tab 时 URL 含 `?tab=history` 或 `?tab=active` | |
| D8 | **复制历史 Tab 的 URL，新标签页打开** | 直接进入历史 Tab（URL 驱动） | |
| D9 | **按 F5 硬刷新** | Tab 状态保持，不回退到默认 | |

### 5.3 旧路由兼容（v6.2 P0-037 重点）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| D10 | 浏览器直接访问 `http://localhost:5666/diagnosis/records` | 自动 redirect 到 `/diagnosis/tasks?tab=history` | |
| D11 | redirect 后检查页面不白屏 | 历史记录列表正常显示 | |

### 5.4 诊断详情与回路分析

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| D12 | 进入 `/diagnosis/loop-analysis` | 显示回路级诊断分析 | |
| D13 | 从总览点击某回路进入 `/diagnosis/detail/:loopId` | 显示该回路诊断详情（诊断标签/证据链/置信度） | |
| D14 | 查看诊断标签 | 标签非误报（v6.2 诊断标签修复） | |
| D15 | **按 F5 硬刷新详情页** | 不白屏（稳定元素根） | |

### 5.5 Action Tracker（诊断闭环）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| D16 | 进入 `/diagnosis/tracker` | 显示 Action Tracker 列表 | |
| D17 | 查看状态机 | 状态含：待处理 / 处理中 / 已实施 / 已忽略 | |
| D18 | 检查 v6.2 P3-008 新增字段 | 列表含"负责人"和"计划执行时间"列 | |
| D19 | 尝试修改某条 Tracker 状态 | 状态可流转（待处理→处理中→已实施） | |
| D20 | 查看已实施记录 | 有"效果验证"字段 | |

### 5.6 诊断配置

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| D21 | 进入 `/diagnosis/config` | 显示诊断规则/阈值配置 | |
| D22 | `ic_engineer` 登录查看配置 | 可查看（ADMIN 可编辑） | |

---

## 模块六：回路整定（40min）——核心验证

> v6.2 P1-019：三页合并为可恢复 stepper（flow.vue）。
> v6.2 Phase 0/2/3：安全门禁 + 可信辨识 + 模型生命周期。

### 6.1 整定工作台入口

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T1 | `admin` 登录，进入 `/tuning/workbench` | 显示整定入口卡片（新建整定 + 效果统计） | |
| T2 | 查看未终态任务 | 如有 RUNNING/IDENTIFIED 任务，显示"继续"入口 | |
| T3 | 点击"新建整定" | 跳转到 `/tuning/flow/model`（stepper 步骤 0） | |

### 6.2 整定流程 Stepper（v6.2 P1-019 重点）

#### 步骤 0：模型辨识

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T4 | 在 `/tuning/flow/model` 查看 Stepper | 顶部显示 3 步：模型辨识 → PID 整定 → 仿真对比，当前在步骤 0 | |
| T5 | 查看回路上下文头 | 显示当前回路 + 时间范围（P1-021 统一上下文头） | |
| T6 | 选择一个回路和时间范围 | 回路下拉列表可选，时间范围可设 | |
| T7 | 查看辨识策略选项 | 含 AUTO / HISTORY_ONLY / STEP_ONLY | |
| T8 | 检查 IPDT 选项 | 历史 IPDT 选项明确标注"尚未实现"或不可选（P0-013） | |
| T9 | `ic_engineer` 登录查看高级参数 | **不显示**高级参数折叠面板（P1-022 角色门禁） | |
| T10 | `admin` 登录查看高级参数 | **显示**高级参数折叠面板（候选阶次/纯滞后预估 θ） | |
| T11 | 选择 AUTO 策略，提交辨识 | 提交后显示异步任务进度（按小时窗口更新） | |
| T12 | 等待辨识完成 | 返回辨识结果：模型类型/参数/可信度等级/激励评分 | |
| T13 | 检查可信度展示 | 可信度 A/B/C/D/E 等级清晰，含 reason code | |

#### 步骤 1：PID 整定

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T14 | 辨识通过后进入步骤 1 | Stepper 高亮步骤 1，显示整定算法选择 | |
| T15 | 查看算法选项 | 含 IMC / LAMBDA / SIMC / ZN / IDENTIFICATION_ONLY（P3-006） | |
| T16 | 选择算法并执行整定 | 返回推荐 PID 参数 + 风险评估 + 回退方案（P3-007） | |
| T17 | 查看人工实施清单 | 含当前值/建议值/风险/回退值/单位转换（P3-007） | |
| T18 | **确认无 DCS 下写按钮** | 只有"建议/证据/风险/回退"，无"自动实施"或"下写 DCS"按钮（安全红线） | |

#### 步骤 2：仿真对比

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T19 | 进入步骤 2 仿真对比 | Stepper 高亮步骤 2，显示 PID 对比仿真界面 | |
| T20 | 查看候选 PID 列表 | 至少有当前 PID + 推荐 PID 两组 | |
| T21 | 添加第三组候选 PID | 可添加并标注 label | |
| T22 | 执行仿真对比 | 返回每组 PID 的响应曲线 + 性能指标（上升时间/超调/稳定时间/ITAE） | |
| T23 | 检查曲线渲染 | Echarts 正常渲染，多组曲线颜色区分 | |

### 6.3 安全门禁验证（v6.2 Phase 0 核心）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T24 | 构造 D/E 级可信度模型尝试整定 | **禁止**整定，显示"可信度不足，禁止整定"（P0-014~018） | |
| T25 | 构造 C 级可信度模型 | 需**显式人工确认**风险后方可继续整定 | |
| T26 | 构造无阶跃数据 AUTO 辨识 | 返回 **INCONCLUSIVE** + reason code，不盲成功（P0-001~005） | |
| T27 | 查看整定工作台风险卡片 | 无数据时显示"暂不可用"，**不显示 0**（P0-024~026） | |

### 6.4 /compare 独立 schema（v6.2 P0-030）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T28 | 在仿真对比页只选 1 组候选 PID 尝试对比 | 前端阻止或后端返回 422（min_length=2 校验） | |
| T29 | 不选 currentPid 直接对比 | 可正常执行（currentPid 可选） | |

### 6.5 旧路由兼容（v6.2 P0-037 重点）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T30 | 直接访问 `http://localhost:5666/tuning/model` | redirect 到 `/tuning/flow/model` | |
| T31 | 直接访问 `http://localhost:5666/tuning/algorithm` | redirect 到 `/tuning/flow/algorithm` | |
| T32 | 直接访问 `http://localhost:5666/tuning/simulation` | redirect 到 `/tuning/flow/simulation` | |
| T33 | redirect 后检查页面不白屏 | Stepper 正常显示 | |
| T34 | 在旧路由上按 F5 硬刷新 | 保持新路由，不白屏 | |

### 6.6 状态覆盖组件（v6.2 P1-023）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T35 | 触发辨识错误（如断开后端） | 显示 error 状态覆盖层 + 重试按钮 | |
| T36 | 无辨识结果时查看 | 显示 empty 状态覆盖层 | |
| T37 | 辨识加载中查看 | 显示 loading 状态覆盖层 | |
| T38 | 点击重试按钮 | 重新触发请求 | |

### 6.7 流程持久化与恢复（v6.2 P1-019）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| T39 | 在步骤 1 执行 `location.reload()` | 页面恢复到步骤 1（sessionStorage 持久化） | |
| T40 | 新标签页打开 `/tuning/flow/model` | 从 sessionStorage 恢复状态 | |

---

## 模块七：系统管理（15min）

### 7.1 用户管理

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| SY1 | `admin` 进入 `/system/users` | 显示 5 个种子用户 | |
| SY2 | 尝试新增用户 | 表单可提交，用户创建成功 | |
| SY3 | 尝试编辑用户角色 | 角色可修改 | |
| SY4 | 尝试停用用户 | is_active 可切换 | |

### 7.2 权限管理

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| SY5 | 进入 `/system/permissions` | 显示角色权限矩阵 | |
| SY6 | 查看整定权限 | ADMIN/IC_ENGINEER/EXPERT 有整定权限 | |
| SY7 | 确认页面合并不扩大权限 | 与 v6.2 前权限一致（C6 不变量） | |

### 7.3 审计日志

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| SY8 | 进入 `/system/audit` | 显示审计日志列表 | |
| SY9 | 查看刚才的操作记录 | 含用户/操作类型/目标/时间/前后值 | |
| SY10 | 检查整定操作日志 | 整定任务创建/状态变更已记录 | |

---

## 模块八：全局交叉验证（15min）

### 8.1 实时数据与 SignalR

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| G1 | 打开回路监控页，观察实时数据 | 数据通过 WebSocket 实时更新（非轮询） | |
| G2 | 检查浏览器 Network 面板 | 有 SignalR WebSocket 连接 | |
| G3 | 等待 1 分钟，确认数据持续更新 | 无断连 | |

### 8.2 导航与白屏防护

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| G4 | 在各模块间快速切换 | 无白屏闪烁 | |
| G5 | 在回路详情页前进/后退 | 历史栈正常，不白屏 | |
| G6 | 深链直接访问 `/diagnosis/detail/:loopId` | 正常加载（非白屏） | |

### 8.3 数据架构验证（v6.2 不变量）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| G7 | 触发 KPI 计算任务 | 计算走本地 TDengine（不降级远端 API） | |
| G8 | 尝试历史数据导入 | 导入走远端 AAS 接口（仅 data_import 可调用远端） | |

### 8.4 安全红线终检（v6.2 核心价值）

| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| G9 | 全局搜索"下写 DCS"或"自动实施"入口 | **不存在**任何 DCS 参数下写端点或自动实施按钮 | |
| G10 | 整定结果确认只输出建议 | 含建议值/证据/风险/回退方案，无直接下写 | |

---

## 试用结果汇总

### 统计

| 模块 | 检查项数 | 通过 | 失败 | 部分通过 |
|---|---|---|---|---|
| 登录与权限 | 9 | | | |
| 工作台 | 8 | | | |
| 回路管理 | 13 | | | |
| 性能评估 | 13 | | | |
| 诊断中心 | 22 | | | |
| 回路整定 | 40 | | | |
| 系统管理 | 10 | | | |
| 全局交叉 | 10 | | | |
| **合计** | **125** | | | |

### v6.2 关键验证点检查汇总

| 验证点 | 对应检查项 | 通过 |
|---|---|---|
| P1-017 工作台跨模块待办 | W1-W8 | ⬜ |
| P1-018 诊断 Tabs 合并 | D4-D9 | ⬜ |
| P1-019 整定 stepper | T4-T22, T39-T40 | ⬜ |
| P1-021 统一上下文头 + 防白屏 | LP4-LP7, T5 | ⬜ |
| P1-022 高级参数角色门禁 | P3-P5, T9-T10 | ⬜ |
| P1-023 状态覆盖组件 | T35-T38 | ⬜ |
| P0-014~018 可信度放行门禁 | T24-T25 | ⬜ |
| P0-001~005 AUTO fallback 安全 | T26 | ⬜ |
| P0-024~026 未知风险不显示 0 | T27 | ⬜ |
| P0-030 /compare 独立 schema | T28-T29 | ⬜ |
| P0-037 旧路由兼容 | D10-D11, T30-T34 | ⬜ |
| P0-039 安全边界无 DCS 下写 | T18, G9-G10 | ⬜ |
| P3-006 IDENTIFICATION_ONLY | T15 | ⬜ |
| P3-007 人工实施清单 | T17 | ⬜ |
| P3-008 Tracker 闭环字段 | D18 | ⬜ |

### 失败项跟踪表

| 编号 | 检查项 | 现象描述 | 严重级别 | 状态 |
|---|---|---|---|---|
| | | | | |

---

> **试用完成后**：将本文档检查结果填入，失败项录入跟踪表，作为 v6.2 生产部署实弹验证的输入依据。
