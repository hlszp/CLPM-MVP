node_modules/
dist/
coverage/
.DS_Store
.playwright/
.playwright-mcp/
.claude/*
!.claude/commands/
.gstack/
.omc/
.backups/
.trae/documents/
test-results/
playwright-report/
*.tsbuildinfo
.env
.env.*
!.env.example
!.env.prod.example
!**/.env.development
!**/.env.production

# E2E 调试脚本（临时工具，不入库）
e2e/check_*.ts
e2e/debug_*.ts

# Docker
.env.prod
.td-password-changed

# codebuddy 分析输出（不入库）
docs/codebuddyCheck/

# 预研文档中的大体积 PDF（OCR 国标等，不入库）
docs/预研文档/*.pdf

# Celery Beat 运行时临时文件
celerybeat-schedule*
celerybeat.pid

# Docker 镜像构建产物（大文件不入库，manifest.json 和 README.md 入库）
releases/images/*.tar.gz
releases/images/*.tar
releases/clpm-delivery-*.tar.gz
releases/delivery-staging/
releases/clpm-deploy-repo/

# graphify 技能自动生成目录（不入库）
graphify-out/

# 性能实测与诊断产物（不入库）
logs-frontend-dev.log
logs-mvp-uvicorn.log
output/
.playwright-cli/
__pycache__/

# Turbo 缓存
.turbo/
logs/

# 本地 pnpm 缓存（不应入库）
.pnpm-store/

# CLPM-engine 引擎目录（独立管理，不入库）
CLPM-engine/

# Qoder IDE 本地产物（repowiki 知识文档 .md/.yaml 除外；仅索引/缓存/元数据不入库）
.qoder/better-harness/
# IDE 自动索引：_index.yaml / metadata.json 每次打开自动变化，不入库
.qoder/repowiki/**/_index.yaml
.qoder/repowiki/**/repowiki-metadata.json
.qoder/repowiki/**/.cache/

# E2E 截图与视觉检查产物（不入库，本地保留即可）
e2e/screenshots/
e2e/visual-inspection/
e2e/verify-fixes-*.png

# superpowers brainstorm 可视化线框（临时设计产物，不入库）
.superpowers/

# 交付包二进制不入库（2026-09-05）
releases/*.tar.gz
