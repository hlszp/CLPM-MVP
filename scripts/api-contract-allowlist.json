{
  "generatedAt": "2026-09-24",
  "note": "API 契约对账白名单。check_api_contract.py 会对白名单内的端点放行（WARN 可见、不阻塞 CI）；不在白名单且前端从不调用的端点会被提示登记。登记不等于永久保留：清理周期需逐条确认后从本文件移除并删除实现。",
  "registryDoc": "docs/过程文档/dead-code-registry-2026-09-24.md",
  "backendUnused": [
    "DELETE /algorithms/dataplanner/cache/*",
    "GET /algorithms/dataplanner/cache/stats",
    "GET /algorithms/tasks/*",
    "GET /auth/rbac-test",
    "GET /configs/metrics",
    "GET /health",
    "GET /health/db-connections",
    "GET /health/ready",
    "GET /performance/board",
    "GET /realtime",
    "GET /reports/export-download/*",
    "GET /tasks/*/download",
    "GET /tasks/active",
    "GET /timeseries/*/waveform",
    "GET /tuning/knowledge-base",
    "GET /tuning/knowledge-base/*",
    "GET /tuning/knowledge-base/similar",
    "GET /workbench/flags",
    "GET /workbench/lane-more",
    "GET /workbench/staff-load",
    "POST /algorithms/dataplanner/bundle",
    "POST /algorithms/dataplanner/plan",
    "POST /algorithms/kpi/calculate",
    "POST /algorithms/tuning/calculate",
    "POST /timeseries/batch/waveform",
    "POST /tuning/identify/segments",
    "POST /tuning/simulate",
    "PUT /configs/metrics"
  ],
  "backendUnusedReasons": {
    "DELETE /algorithms/dataplanner/cache/*": "算法直调端点：计算已全部收敛到 Celery 任务链路，前端不再直调（待清理评估）",
    "GET /algorithms/dataplanner/cache/stats": "算法直调端点：计算已全部收敛到 Celery 任务链路，前端不再直调（待清理评估）",
    "GET /algorithms/tasks/*": "算法直调端点：计算已全部收敛到 Celery 任务链路，前端不再直调（待清理评估）",
    "GET /auth/rbac-test": "仅测试用端点（已 ADMIN 保护），保留用于权限回归排查",
    "GET /configs/metrics": "被同域新端点取代（历史口径），无消费方（待清理评估）",
    "GET /health": "k8s/部署探针与健康检查（deploy/deploy-on-server.sh 等 curl 消费），非前端调用",
    "GET /health/db-connections": "k8s/部署探针与健康检查（deploy/deploy-on-server.sh 等 curl 消费），非前端调用",
    "GET /health/ready": "k8s/部署探针与健康检查（deploy/deploy-on-server.sh 等 curl 消费），非前端调用",
    "GET /performance/board": "被同域新端点取代（历史口径），无消费方（待清理评估）",
    "GET /realtime": "被同域新端点取代（历史口径），无消费方（待清理评估）",
    "GET /reports/export-download/*": "文件直链（前端用字符串拼 URL 交给浏览器下载，不经 requestClient）",
    "GET /tasks/*/download": "文件直链（前端用字符串拼 URL 交给浏览器下载，不经 requestClient）",
    "GET /tasks/active": "被同域新端点取代（历史口径），无消费方（待清理评估）",
    "GET /timeseries/*/waveform": "波形查询：前端统一走 /loop-data 链路，本组端点无消费方（待清理评估）",
    "GET /tuning/knowledge-base": "整定知识库：P0 未接入前端（设计保留，待 M2 评估）",
    "GET /tuning/knowledge-base/*": "整定知识库：P0 未接入前端（设计保留，待 M2 评估）",
    "GET /tuning/knowledge-base/similar": "整定知识库：P0 未接入前端（设计保留，待 M2 评估）",
    "GET /workbench/flags": "被 /workbench/aggregate 取代（BFF 合并），子端点无消费方（待清理评估）",
    "GET /workbench/lane-more": "被 /workbench/aggregate 取代（BFF 合并），子端点无消费方（待清理评估）",
    "GET /workbench/staff-load": "被 /workbench/aggregate 取代（BFF 合并），子端点无消费方（待清理评估）",
    "POST /algorithms/dataplanner/bundle": "算法直调端点：计算已全部收敛到 Celery 任务链路，前端不再直调（待清理评估）",
    "POST /algorithms/dataplanner/plan": "算法直调端点：计算已全部收敛到 Celery 任务链路，前端不再直调（待清理评估）",
    "POST /algorithms/kpi/calculate": "算法直调端点：计算已全部收敛到 Celery 任务链路，前端不再直调（待清理评估）",
    "POST /algorithms/tuning/calculate": "算法直调端点：计算已全部收敛到 Celery 任务链路，前端不再直调（待清理评估）",
    "POST /timeseries/batch/waveform": "波形查询：前端统一走 /loop-data 链路，本组端点无消费方（待清理评估）",
    "POST /tuning/identify/segments": "无人调用（待清理评估）",
    "POST /tuning/simulate": "无人调用（待清理评估）",
    "PUT /configs/metrics": "被同域新端点取代（历史口径），无消费方（待清理评估）"
  },
  "frontendCallsWithoutBackend": [
    "/menu/all"
  ],
  "frontendCallsWithoutBackendReasons": {
    "/menu/all": "vben 框架 accessMode=backend 的菜单接口预留：后端未实现 /menu/all。当前 accessMode=frontend 故不触发；若切后端菜单模式需先实现该端点或删除该封装。"
  },
  "frontendDeadFunctions": []
}
