import type { RouteRecordRaw } from 'vue-router';

/**
 * 诊断中心路由模块 —— MVP 精简版：已屏蔽
 *
 * 原模块包含 诊断总览 / 诊断任务 / 回路分析 / 诊断详情 / 异常跟踪 等页面。
 * MVP 版本按范围要求屏蔽整个诊断模块，
 * 保留文件结构便于后续恢复，路由导出空数组。
 */
const routes: RouteRecordRaw[] = [];

export default routes;
