# CLPM Prototype Interaction Detail

日期：2026-06-16
版本：v0.1
用途：补齐 P0 深做页面的点击联动、筛选、状态切换和关键交互逻辑，避免前端实现时靠猜。

## 1. 全局交互原则

- 任何页面都优先服务当前用户要完成的动作，而不是展示所有信息。
- `数据不足 / 需现场核实 / 不可判定` 是一等状态，不是错误。
- 所有跳转都应保持当前样本批次上下文。
- P1/P2/P3 页在原型中只允许“查看结构”和“返回”，不做深交互。

## 2. 工程首页 `/`

### 关键交互

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击低性能回路 | 选中当前回路 | 中间证据摘要与右侧动作区联动刷新 |
| 点击数据雷达某状态 | 过滤左侧清单 | 只显示该状态回路 |
| 点击“进入证据链” | 跳转单回路证据页 | 带 `loopId` |
| 点击“进入闭环治理” | 跳转闭环页 | 带当前回路上下文 |
| 点击“进入样本验证” | 跳转样本验证相关页 | 保持当前样本批次 |

## 3. 样本批次 `/samples`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某样本批次 | 查看批次详情 | 右侧信息区更新 |
| 点击“就绪校验” | 跳转 `/samples/readiness` | 带当前批次 |
| 点击“冻结样本” | 展示冻结说明 | 原型中只做确认态，不做真实写入 |

## 4. 数据导入 `/samples/import`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换导入方式 | 更换说明文案和字段示例 | 更新右侧说明 |
| 点击导入任务 | 查看任务详情 | 展示解析结果、字段映射、事件可用性 |
| 点击“进入就绪校验” | 跳转 `/samples/readiness` | 聚焦当前批次 |

## 5. 就绪校验 `/samples/readiness`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某风险项 | 高亮受影响回路或字段 | 跳转候选回路列表或映射页 |
| 点击“进入数据雷达” | 跳转 `/samples/radar` | 聚焦风险相关状态 |
| 点击“进入台账校核” | 跳转 `/loops/verification` | 带过滤条件 |

## 6. 数据雷达 `/samples/radar`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击状态分组 | 更新中间回路列表 | 仅显示该组回路 |
| 点击某回路 | 展示解释区详情 | 显示缺失字段、好值率、自动时长、建议动作 |
| 点击建议动作 | 跳转相关页面 | 如“进入证据链”“进入台账校核”“进入现场核实” |

## 7. 回路清单 `/loops`

| 触发 | 行为 | 结果 |
|---|---|---|
| 调整筛选条件 | 刷新表格 | 支持装置、类型、状态筛选 |
| 点击某回路 | 右侧摘要区更新 | 展示当前版本、映射状态、下一步入口 |
| 点击“查看证据” | 跳转单回路证据页 | 带 `loopId` |

## 8. 点位映射 `/loops/mapping`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某回路 | 显示该回路映射矩阵 | 高亮缺失点位 |
| 点击缺失字段 | 展示原因说明 | 例如“历史数据无 MODE 点位” |

## 9. 台账校核 `/loops/verification`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击待校核问题 | 打开问题详情 | 显示异常原因和建议修正 |
| 点击“标记已校核” | 切换状态 | 仅更新原型状态，不做真实提交 |

## 10. 排除管理 `/loops/exclusions`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击排除记录 | 展示审批详情 | 包含原因、有效期、影响指标 |
| 点击“查看影响” | 跳转指标溯源 | 查看该排除对 KPI 的影响 |

## 11. 版本管理 `/loops/versions`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某版本 | 展示引用对象 | 显示哪些 KPI / 诊断 / 证据包引用了它 |
| 点击引用对象 | 跳转对应页面 | 如证据包、回路证据 |

## 12. 指标总览 `/performance`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某 KPI 卡片 | 展开详细说明 | 展示趋势、说明和相关低性能回路 |
| 点击“查看排行” | 跳转 `/performance/ranking` | 带当前排序类型 |
| 点击“查看溯源” | 跳转 `/performance/lineage` | 带当前指标上下文 |

## 13. 低效排行 `/performance/ranking`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换排序维度 | 重排列表 | 变更高亮字段 |
| 点击某行 | 跳转回路证据页 | 进入 `/diagnosis/loop/:loopId` |
| 点击建议动作 | 跳转闭环治理 | 带当前回路上下文 |

## 14. 诊断清单 `/diagnosis`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击诊断分组 | 过滤列表 | 只显示对应类型 |
| 点击某条诊断 | 跳转证据链页 | 带 `loopId` |
| 点击置信度标签 | 展示说明 | 解释为什么是该置信度 |

## 15. 单回路证据链 `/diagnosis/loop/:loopId`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换时间窗 | 刷新趋势和事件线 | 显示当前窗口变化 |
| 点击规则命中 | 展开解释 | 显示规则、阈值和影响 |
| 点击建议动作 | 跳转闭环治理或整定样例 | 根据可整定状态决定去向 |
| 点击事件点 | 高亮趋势区 | 对应时间位置联动 |

## 16. 建议审核 `/closure/review`

| 触发 | 行为 | 结果 |
|---|---|---|
| 选择审核结论 | 更新右侧总结 | 展示通过/驳回/需补证据影响 |
| 点击“查看证据” | 返回当前回路证据链 | 保持上下文 |
| 点击“进入实施记录” | 跳转 `/closure/implementation` | 若状态允许 |

## 17. 多方审核 `/closure/multi-review`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换某角色意见 | 展示角色说明 | 更新会签状态 |
| 发现冲突 | 高亮冲突提示 | 显示需协调项 |

## 18. 实施记录 `/closure/implementation`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某实施记录 | 展示详情 | 显示人工实施说明、窗口、备注 |
| 点击“进入复评” | 跳转 `/closure/reevaluation` | 带对应记录 |

## 19. 风险回退 `/closure/rollback`

| 触发 | 行为 | 结果 |
|---|---|---|
| 查看回退条件 | 展示条件说明 | 明确何时恢复原参数/动作 |
| 点击“查看原始参数” | 展示原参数卡片 | 用于说明只读边界与人工实施 |

## 20. 观察复评 `/closure/reevaluation`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换前后窗口 | 刷新对比数据 | KPI、报警、操作频次同步变化 |
| 点击趋势对比点 | 高亮对应时段 | 联动文字摘要 |

## 21. 管理首页 `/sponsor`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击代表性样例 | 切换右侧展示内容 | 更换为对应回路摘要 |
| 点击“查看完整证据包” | 跳转 `/evidence` | 保持当前包上下文 |
| 点击风险项 | 展示补充说明 | 告知当前不能证明的事项 |

## 22. 证据包 `/evidence`

| 触发 | 行为 | 结果 |
|---|---|---|
| 切换摘要 / manifest | 切换视图 | 摘要视图和 manifest 视图互切 |
| 点击某版本引用 | 跳转版本管理或关联页面 | 如 `loops/versions` |
| 点击导出按钮 | 展示原型态导出反馈 | 不生成真实文件，只展示确认状态 |

## 23. 样本报告 `/evidence/sample-report`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击某代表性样例 | 跳转对应证据链或 Sponsor 样例区 | 保持样本上下文 |

## 24. 安全边界 `/system/safety`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击“能做 / 不能做”项 | 展开详细说明 | 明确只读 DCS、人工实施、审计、回退 |
| 点击网络分区说明 | 展示架构小图或文字 | 说明控制网 / 管理网 / DMZ |

## 25. 整定样例 `/tuning/sample`

| 触发 | 行为 | 结果 |
|---|---|---|
| 点击当前/建议参数切换 | 高亮参数差异 | 同步变化仿真摘要 |
| 点击可信度分解 | 展示各维度得分 | 数据质量、激励指数、模型质量、风险 |
| 点击回退说明 | 展示回退条件和原参数 | 强化安全边界 |

## 26. P1/P2/P3 结构展示页通用交互

| 触发 | 行为 | 结果 |
|---|---|---|
| 查看“规划能力” | 展示未来会包含什么 | 保持版本标签显著 |
| 点击“返回 P0 主链” | 返回上一级业务页 | 不让用户误以为可深做 |
